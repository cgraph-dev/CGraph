---
phase: 01-shell-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/landing/src/components/liquid-glass/LiquidGlassLayout.tsx
  - apps/landing/src/components/liquid-glass/index.ts
autonomous: true

must_haves:
  truths:
    - 'LiquidGlassLayout wraps children with Navigation + Footer + pearl bg'
    - 'LiquidGlassLayout is exported from the liquid-glass barrel'
    - 'Layout is ready for secondary pages to import'
  artifacts:
    - path: 'apps/landing/src/components/liquid-glass/LiquidGlassLayout.tsx'
      provides: 'Shared page shell for secondary pages'
      exports: ['LiquidGlassLayout']
    - path: 'apps/landing/src/components/liquid-glass/index.ts'
      provides: 'Updated barrel export'
      contains: 'LiquidGlassLayout'
  key_links:
    - from: 'LiquidGlassLayout.tsx'
      to: 'Navigation.tsx'
      via: 'import { Navigation }'
      pattern: 'import.*Navigation'
    - from: 'LiquidGlassLayout.tsx'
      to: 'Footer.tsx'
      via: 'import { Footer }'
      pattern: 'import.*Footer'
---

<objective>
Create a `LiquidGlassLayout` wrapper component that secondary pages (legal, company, resources)
can import instead of the old `MarketingLayout`. This provides the shared Navigation, Footer,
and pearl-white background without each page needing to wire them up individually.

Purpose: Single shared layout for all non-landing pages, enabling incremental migration in Phase 2.
Output: LiquidGlassLayout.tsx component + updated barrel export. </objective>

<execution_context> ../skills/execute-plan/SKILL.md </execution_context>

<context>
@apps/landing/.gsd/PROJECT.md
@apps/landing/src/components/liquid-glass/Navigation.tsx
@apps/landing/src/components/liquid-glass/Footer.tsx
@apps/landing/src/components/liquid-glass/shared.ts
@apps/landing/src/components/marketing/MarketingLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LiquidGlassLayout component</name>
  <files>
    apps/landing/src/components/liquid-glass/LiquidGlassLayout.tsx
  </files>
  <action>
  Create a layout wrapper component that:
  - Accepts `children: React.ReactNode` and optional `title?: string` prop
  - Renders `<div className="min-h-screen bg-pearl">` as outer wrapper
  - Renders `<Navigation />` at top
  - Renders `<main>` with `className="mx-auto max-w-4xl px-6 py-24 lg:px-12"` wrapping children
  - Optionally renders an `<h1>` with the title (glass-styled) if `title` is provided
  - Renders `<Footer />` at bottom
  - Uses the `cn()` helper from `./shared`
  - Does NOT import SEO — each page handles its own SEO

Style the page content area with glass-surface card for the main content block:

- Content wrapper: `glass-surface rounded-3xl p-8 sm:p-12 shadow-glass`
- Title: `text-3xl font-bold text-slate-900 mb-8`
- Prose content: `prose prose-slate max-w-none` (if user has @tailwindcss/typography) or provide
  basic text styling via Tailwind utilities

Follow existing patterns from Navigation.tsx and Footer.tsx for imports and structure. </action>
<verify> `npx tsc --noEmit --pretty false 2>&1 | grep 'LiquidGlassLayout'` — no errors. </verify>
<done> LiquidGlassLayout.tsx exists, exports the component, TypeScript passes. </done> </task>

<task type="auto">
  <name>Task 2: Update barrel export</name>
  <files>
    apps/landing/src/components/liquid-glass/index.ts
  </files>
  <action>
  Add `export { LiquidGlassLayout } from './LiquidGlassLayout';` to the barrel file.
  Keep all existing exports unchanged.
  </action>
  <verify>
  `grep 'LiquidGlassLayout' apps/landing/src/components/liquid-glass/index.ts` — matches.
  </verify>
  <done>
  LiquidGlassLayout is importable from `@/components/liquid-glass`.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no new errors
- LiquidGlassLayout renders Nav + content + Footer on pearl bg
</verification>

<success_criteria>

- LiquidGlassLayout component created with Nav + Footer + glass content area
- Exported from barrel
- TypeScript passes </success_criteria>

<output>
After completion, create `apps/landing/.gsd/phases/01-shell-polish/01-02-SUMMARY.md`
</output>
