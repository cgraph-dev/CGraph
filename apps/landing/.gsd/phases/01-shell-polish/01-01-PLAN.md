---
phase: 01-shell-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/landing/src/main.tsx
  - apps/landing/src/index.css
  - apps/landing/vite.config.ts
  - apps/landing/package.json
autonomous: true

must_haves:
  truths:
    - 'Page loading spinner renders on pearl-white background with purple accent'
    - 'No orphaned CSS rules or dark-theme colors remain in index.css'
    - 'Vite build completes with no warnings about empty gsap chunk'
  artifacts:
    - path: 'apps/landing/src/main.tsx'
      provides: 'Pearl-white PageLoader with Liquid Glass spinner'
      contains: 'bg-pearl'
    - path: 'apps/landing/src/index.css'
      provides: 'Clean global styles with no orphaned rules'
    - path: 'apps/landing/vite.config.ts'
      provides: 'Updated manualChunks without gsap'
  key_links:
    - from: 'apps/landing/src/main.tsx'
      to: 'apps/landing/src/index.css'
      via: "import './index.css'"
      pattern: "import.*index\\.css"
---

<objective>
Fix the app shell to be fully Liquid Glass. The PageLoader in main.tsx still uses dark
cosmic colors (`bg-[#0a0a0f]`, emerald spinner). index.css has orphaned CSS at the bottom.
Vite config has a dead `gsap` manual chunk since LandingPage no longer imports gsap.

Purpose: Eliminate dark-theme artifacts from shared infrastructure so every page load feels
on-brand. Output: Clean main.tsx, index.css, and vite.config.ts — zero dark-theme remnants in shared
files. </objective>

<execution_context> ../skills/execute-plan/SKILL.md </execution_context>

<context>
@apps/landing/.gsd/PROJECT.md
@apps/landing/.gsd/ROADMAP.md
@apps/landing/src/main.tsx
@apps/landing/src/index.css
@apps/landing/vite.config.ts
@apps/landing/tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PageLoader and clean index.css</name>
  <files>
    apps/landing/src/main.tsx
    apps/landing/src/index.css
  </files>
  <action>
  In `main.tsx`, update the `PageLoader` component (around L44-49):
  - Change `bg-[#0a0a0f]` → `bg-pearl` (pearl-white: rgb(250,250,252))
  - Change `border-emerald-500/30 border-t-emerald-500` → `border-glow-purple/30 border-t-glow-purple`
  - Change `text-gray-400` → `text-slate-400`
  - Keep the same spinner structure (h-12 w-12 animate-spin rounded-full)

In `index.css`, remove the orphaned CSS lines at the bottom of the file (after the closing brace of
`@layer utilities`). There are stray lines like `transition: opacity 0.3s;` and `z-index: -1;` and a
closing `}` that are outside any valid selector. Delete them entirely.

Optionally remove the legacy @font-face declarations for Zentry, General, and Robert fonts (lines
~10-44) since the Liquid Glass theme uses Inter from Google Fonts CDN. If removing, verify no
secondary page still references these fonts directly. </action> <verify>
`grep -n 'bg-\[#0a0a0f\]\|emerald-500' apps/landing/src/main.tsx` returns empty.
`tail -5 apps/landing/src/index.css` shows clean end-of-file (no orphaned rules). </verify> <done>
PageLoader displays pearl-white bg with purple spinner. No orphaned CSS after @layer utilities
block. </done> </task>

<task type="auto">
  <name>Task 2: Remove gsap from Vite chunks</name>
  <files>
    apps/landing/vite.config.ts
  </files>
  <action>
  In `vite.config.ts`, remove the `gsap: ['gsap']` line from the `manualChunks` config
  inside `rollupOptions.output`. Keep the `three`, `framer-motion`, and `react-vendor` chunks.

Do NOT remove gsap from package.json yet — secondary pages may still import it through old marketing
components. That cleanup happens in Phase 3. </action> <verify>
`grep 'gsap' apps/landing/vite.config.ts` returns empty.
`cd apps/landing && npx tsc --noEmit --pretty false 2>&1 | wc -l` — no new errors introduced.
</verify> <done> Vite config has no gsap chunk. TypeScript passes with no regressions. </done>
</task>

</tasks>

<verification>
- `cd apps/landing && npx vite build 2>&1 | tail -10` — build succeeds, no gsap chunk warning
- `grep -rn '#0a0a0f\|emerald-500' apps/landing/src/main.tsx` — zero matches
- `tail -3 apps/landing/src/index.css` — clean EOF
</verification>

<success_criteria>

- PageLoader uses pearl bg + purple accent spinner
- index.css has no orphaned CSS
- Vite config has no gsap chunk
- Build still succeeds </success_criteria>

<output>
After completion, create `apps/landing/.gsd/phases/01-shell-polish/01-01-SUMMARY.md`
</output>
