---
phase: 03-cleanup-testing
plan: 01
type: execute
wave: 1
depends_on: ['02-01', '02-02', '02-03']
files_modified:
  - apps/landing/src/components/marketing/ # DELETE entire directory
  - apps/landing/src/styles/ # DELETE entire directory
  - apps/landing/package.json # remove gsap dependency
  - apps/landing/src/__tests__/App.test.tsx
  - apps/landing/src/__tests__/CTA.test.tsx
  - apps/landing/src/__tests__/Features.test.tsx
  - apps/landing/src/__tests__/Footer.test.tsx
  - apps/landing/src/__tests__/Hero.test.tsx
  - apps/landing/src/__tests__/Navigation.test.tsx
  - apps/landing/src/__tests__/Security.test.tsx
  - apps/landing/src/__tests__/NotFound.test.tsx
autonomous: true

must_haves:
  truths:
    - 'components/marketing/ directory no longer exists'
    - 'styles/ directory (16 CSS files) no longer exists'
    - 'gsap is not in package.json dependencies'
    - 'Old test stubs that import from marketing tree are deleted'
    - 'No file in src/ imports from @/components/marketing or @/styles/'
    - 'TypeScript and build pass clean'
  artifacts:
    - path: 'apps/landing/package.json'
      provides: 'Clean dependency list without gsap'
      contains: '"dependencies"'
  key_links:
    - from: 'apps/landing/src/**'
      to: '@/components/marketing'
      via: 'ZERO imports'
      pattern: 'MUST NOT MATCH: import.*@/components/marketing'
---

<objective>
Delete the entire old dark-theme marketing component tree (22 files), all 16 legacy
CSS style files, and remove the gsap dependency. Also delete old test stubs that
import from deleted paths.

This is safe ONLY after all pages have been migrated in Phase 02.

Purpose: Eliminate ~5,000 lines of dead code and the gsap runtime dependency. Output: Clean src/
tree with only liquid-glass components. </objective>

<execution_context> ../skills/execute-plan/SKILL.md </execution_context>

<context>
@apps/landing/.gsd/PROJECT.md
@apps/landing/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old marketing tree and styles</name>
  <files>
    apps/landing/src/components/marketing/
    apps/landing/src/styles/
  </files>
  <action>
  1. Pre-flight: Verify no file outside of components/marketing/ imports from marketing:
     `grep -rn '@/components/marketing\|../marketing' apps/landing/src/ --include='*.tsx' --include='*.ts' | grep -v 'components/marketing/'`
     This MUST return empty. If not, STOP and fix remaining imports first.

2. Delete the entire marketing directory: `rm -rf apps/landing/src/components/marketing/` This
   removes 22 files:
   - MarketingLayout.tsx, index.ts, marketing-pages.css
   - effects/ (2 files), layout/ (3 files), sections/ (10 files), ui/ (7 files)

3. Delete the entire styles directory: `rm -rf apps/landing/src/styles/` This removes 16 CSS files
   (all dark-theme utility styles).

4. Check for any remaining imports of deleted paths:
   `grep -rn '@/styles/\|@/components/marketing' apps/landing/src/` Must return empty. </action>
   <verify>
   `test ! -d apps/landing/src/components/marketing && echo "DELETED" || echo "STILL EXISTS"` —
   prints DELETED. `test ! -d apps/landing/src/styles && echo "DELETED" || echo "STILL EXISTS"` —
   prints DELETED. `grep -rn '@/components/marketing\|@/styles/' apps/landing/src/` — zero matches.
   </verify> <done> 22 marketing component files and 16 CSS files deleted. No broken imports.
   </done> </task>

<task type="auto">
  <name>Task 2: Remove gsap dependency and clean old tests</name>
  <files>
    apps/landing/package.json
    apps/landing/src/__tests__/App.test.tsx
    apps/landing/src/__tests__/CTA.test.tsx
    apps/landing/src/__tests__/Features.test.tsx
    apps/landing/src/__tests__/Footer.test.tsx
    apps/landing/src/__tests__/Hero.test.tsx
    apps/landing/src/__tests__/Navigation.test.tsx
    apps/landing/src/__tests__/Security.test.tsx
    apps/landing/src/__tests__/NotFound.test.tsx
  </files>
  <action>
  1. Remove gsap from package.json dependencies:
     Edit `apps/landing/package.json` — remove the `"gsap": "^3.14.2"` line.

2. Delete old test files that test deleted marketing components:
   - Delete: App.test.tsx (mocks gsap), CTA.test.tsx, Features.test.tsx, Footer.test.tsx,
     Hero.test.tsx, Navigation.test.tsx, Security.test.tsx
   - Delete: NotFound.test.tsx (tests old MarketingLayout wrapper)
   - KEEP: ErrorBoundary.test.tsx, Logo.test.tsx, SEO.test.tsx, setup.ts (these test utilities that
     still exist)

3. Run pnpm install to update lockfile: `cd apps/landing && pnpm install`

4. Verify build works: `cd apps/landing && npx tsc --noEmit` </action> <verify>
   `grep 'gsap' apps/landing/package.json` — zero matches. `ls apps/landing/src/__tests__/` —
   contains only ErrorBoundary.test.tsx, Logo.test.tsx, SEO.test.tsx, setup.ts. `npx tsc --noEmit` —
   exits 0. </verify> <done> gsap removed from dependencies. 8 old test stubs deleted. 4 utility
   tests preserved. TypeScript and pnpm install pass. ~5,000 lines of dead code eliminated. </done>
   </task>

</tasks>

<verification>
- `find apps/landing/src/components/marketing apps/landing/src/styles 2>&1` — "No such file or directory"
- `grep -c 'gsap' apps/landing/package.json` — 0
- `npx tsc --noEmit` — exits 0
- `ls apps/landing/src/__tests__/` — only ErrorBoundary, Logo, SEO, setup
</verification>

<success_criteria>

- Marketing tree (22 files) deleted
- Styles directory (16 files) deleted
- gsap removed from package.json
- 8 outdated test stubs deleted
- TypeScript and build pass </success_criteria>

<output>
After completion, create `apps/landing/.gsd/phases/03-cleanup-testing/03-01-SUMMARY.md`
</output>
