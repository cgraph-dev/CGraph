/**
 * Mobile Modules — Feature-based architecture barrel exports.
 *
 * Each module contains its own screens, components, hooks, store, and types.
 * This mirrors the web's module architecture for cross-platform parity.
 *
 * @module modules
 */

// Module barrel exports will be added as screens are migrated.
// Import pattern: import { LoginScreen } from '@/modules/auth';

export {};
