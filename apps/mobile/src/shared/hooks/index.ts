/**
 * Shared Hooks — utility hooks re-exported for cross-module use.
 *
 * @module shared/hooks
 */

// Re-export hooks that are used across multiple modules
// Hooks will be moved here incrementally from the legacy hooks/ directory
export {};
