/**
 * Shared UI Components — advanced UI primitives.
 * Re-exports from legacy components/ui/ during migration.
 *
 * @module shared/components/ui
 */

export { default as BottomSheet } from '../../components/ui/BottomSheet';
export { default as MarkdownRenderer } from '../../components/ui/MarkdownRenderer';
export { default as Carousel } from '../../components/ui/Carousel';
