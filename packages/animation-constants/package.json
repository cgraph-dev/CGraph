{
  "name": "@cgraph/animation-constants",
  "version": "0.9.26",
  "private": true,
  "description": "Platform-agnostic animation timing, spring, and easing constants shared across web and mobile",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
