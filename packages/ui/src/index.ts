/**
 * @cgraph/ui — Shared UI primitives for web and mobile.
 *
 * Platform-agnostic component contracts and utilities that
 * can be re-exported with platform-specific implementations.
 *
 * @module @cgraph/ui
 */

export { ErrorBoundary } from './error-boundary';
export { VisuallyHidden } from './visually-hidden';
export { Portal } from './portal';
export type { ErrorBoundaryProps, FallbackProps } from './error-boundary';
