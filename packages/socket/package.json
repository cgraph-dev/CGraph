{
  "name": "@cgraph/socket",
  "version": "1.0.0",
  "description": "Shared Phoenix channel client for CGraph platforms",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phoenix": "^1.7.0"
  },
  "devDependencies": {
    "@types/phoenix": "^1.6.0",
    "tsup": "^8.0.0",
    "typescript": "^5.8.0",
    "vitest": "^1.6.0"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0"
  }
}
