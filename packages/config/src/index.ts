/**
 * @cgraph/config — Shared configuration constants for CGraph applications.
 *
 * Platform-agnostic configuration values used across web, mobile, and packages.
 * @module @cgraph/config
 */

export { APP_CONFIG } from './app';
export { LIMITS } from './limits';
export { API_CONFIG } from './api';
export { FEATURE_FLAGS } from './features';
