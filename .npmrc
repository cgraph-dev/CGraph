# Suppress warnings about ignored build scripts
# These are for native modules that use prebuilt binaries
ignore-scripts=false
strict-peer-dependencies=false

# Approve trusted build scripts
# These are well-known packages that require build scripts
pnpm.onlyBuiltDependencies=@parcel/watcher @swc/core esbuild bufferutil utf-8-validate wrtc keccak
