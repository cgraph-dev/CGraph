---
phase: 06-message-features-sync
plan: '04'
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - apps/web/src/modules/chat/components/message-bubble/edit-history-viewer.tsx
  - apps/web/src/modules/chat/components/message-bubble/message-bubble.tsx
  - apps/web/src/modules/chat/store/chatStore.message-ops.ts
  - apps/web/src/lib/socket/conversationChannel.ts
  - apps/mobile/src/screens/messages/conversation-screen/components/message-edit-form.tsx
  - apps/mobile/src/screens/messages/conversation-screen/components/edit-history-viewer.tsx
  - apps/mobile/src/screens/messages/conversation-screen/components/message-bubble.tsx
  - apps/mobile/src/stores/chatStore.ts
autonomous: true
must_haves:
  - Web "(edited)" indicator is clickable and opens edit history showing all previous versions
  - When one user edits a message, the other user sees the updated content and "(edited)" indicator
    in real-time without refresh
  - Mobile has an inline edit form accessible from the message action menu
  - Mobile "(edited)" indicator is tappable and opens edit history showing all previous versions
---

<objective>
Build edit history viewer on web and mobile, and add the missing mobile inline edit form, so users can see previous message versions and mobile users can edit their own messages.
Purpose: Completes SC-1 — user edits a message and recipient sees edit history. Depends on 06-01 which wires the backend to actually record edit history.
Output: Clickable/tappable "(edited)" labels that open a history panel on both platforms; functional edit form on mobile.
</objective>

<context>
**Depends on 06-01 outputs:**
- Backend now inserts MessageEdit records on every edit with `previous_content`, `edit_number`, `edited_by_id`
- `EditHistory` interface exists in shared-types: `{ id, messageId, previousContent, editNumber, editedById, createdAt }`
- `Message` interface has optional `edits?: EditHistory[]`

**What already exists:**

- Web: `MessageEditForm` component at
  apps/web/src/modules/chat/components/message-bubble/message-edit-form.tsx (inline edit form works)
- Web: "(edited)" indicator in message-bubble.tsx L206 — currently plain text, not interactive
- Web: `editMessage` store action in chatStore.message-ops.ts L31-50
- Web: `message_updated` socket handler in conversationChannel.ts L150-155 — updates message but
  does not include edits
- Mobile: `editMessage` store action in chatStore.ts L239
- Mobile: `message_updated` event handler in chatStore.ts L703-709
- Mobile: NO inline edit form component exists

**Key references:**

- Web message bubble: apps/web/src/modules/chat/components/message-bubble/message-bubble.tsx L206
  (edited indicator)
- Web edit form: apps/web/src/modules/chat/components/message-bubble/message-edit-form.tsx
- Mobile message bubble:
  apps/mobile/src/screens/messages/conversation-screen/components/message-bubble.tsx
- Backend conversation channel: apps/backend/lib/cgraph_web/channels/conversation_channel.ex
  (message_updated broadcast) </context>

<tasks>
<task name="web_edit_history_viewer" type="auto">
  <files>apps/web/src/modules/chat/components/message-bubble/edit-history-viewer.tsx, apps/web/src/modules/chat/components/message-bubble/message-bubble.tsx, apps/web/src/lib/socket/conversationChannel.ts, apps/web/src/modules/chat/store/chatStore.message-ops.ts</files>
  <action>
    1. Create `edit-history-viewer.tsx` component:
       - Props: `edits: EditHistory[]`, `currentContent: string`, `isOpen: boolean`, `onClose: () => void`
       - Render a popover/dropdown listing all previous versions in reverse chronological order
       - Each entry shows: edit number, previous content, timestamp
       - Current version shown at top as "Current"
       - Style with existing Tailwind design tokens (muted text for old versions)

    2. In `message-bubble.tsx`, make the "(edited)" indicator interactive:
       - Wrap in a clickable element that toggles the EditHistoryViewer
       - Pass `message.edits` to the viewer component
       - Only render viewer if `message.edits?.length > 0`

    3. In `conversationChannel.ts`, update the `message_updated` handler:
       - Ensure the updated message payload includes the `edits` array
       - Map backend `edits` data to `EditHistory[]` format (snake_case → camelCase)

    4. In `chatStore.message-ops.ts`, update the `editMessage` action:
       - After successful edit, update the local message's `edits` array optimistically
       - Add the previous content as a new EditHistory entry before the server confirms

  </action>
  <verify>
    cd apps/web && pnpm tsc --noEmit 2>&1 | tail -10
  </verify>
  <done>Web "(edited)" label opens a history viewer showing all previous versions; edit history updates in real-time via socket</done>
</task>

<task name="mobile_edit_form_and_history" type="auto">
  <files>apps/mobile/src/screens/messages/conversation-screen/components/message-edit-form.tsx, apps/mobile/src/screens/messages/conversation-screen/components/edit-history-viewer.tsx, apps/mobile/src/screens/messages/conversation-screen/components/message-bubble.tsx, apps/mobile/src/stores/chatStore.ts</files>
  <action>
    1. Create mobile `message-edit-form.tsx`:
       - Inline form that replaces the message content area when editing
       - Pre-populated with current message content
       - Save button calls `editMessage` store action
       - Cancel button restores original view
       - Triggered from message action menu "Edit" option
       - Follow React Native styling patterns from existing mobile components

    2. Create mobile `edit-history-viewer.tsx`:
       - Bottom sheet or modal listing previous versions
       - Each entry: edit number, previous content, timestamp (formatted)
       - Current version at top
       - Triggered by tapping "(edited)" indicator

    3. In mobile `message-bubble.tsx`:
       - Add "(edited)" indicator for messages where `isEdited` is true (if not already present)
       - Make it tappable to open the edit history viewer
       - When in edit mode, render the `MessageEditForm` inline

    4. In `chatStore.ts`, update `message_updated` event handler:
       - Map incoming `edits` data from socket to `EditHistory[]` format
       - Store edits on the message object

  </action>
  <verify>
    cd apps/mobile && pnpm tsc --noEmit 2>&1 | tail -10
  </verify>
  <done>Mobile has inline edit form from action menu; "(edited)" label opens history viewer with all previous versions</done>
</task>
</tasks>

<verification>
1. Web type check: `cd apps/web && pnpm tsc --noEmit`
2. Mobile type check: `cd apps/mobile && pnpm tsc --noEmit`
3. Functional web: edit a message → recipient sees "(edited)" → click shows previous version(s)
4. Functional mobile: long-press message → "Edit" → inline form → save → "(edited)" appears → tap shows history
5. Real-time: edit on web → mobile shows "(edited)" with correct history via socket
</verification>

<success_criteria>

- SC-1: User edits a message and recipient sees edit history — clickable "(edited)" opens viewer
  with all previous versions
- Edit form available on both platforms (web already has it; mobile now has it)
- Edit history updates in real-time via socket broadcast for both platforms </success_criteria>
