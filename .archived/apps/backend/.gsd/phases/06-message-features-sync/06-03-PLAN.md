---
phase: 06-message-features-sync
plan: '03'
type: verify
wave: 1
depends_on: []
files_modified: []
autonomous: true
must_haves:
  - Reply flow works end-to-end on web — reply_to_id sent, ReplyPreview renders, thread context
    visible
  - Reply flow works end-to-end on mobile — reply_to_id sent, reply preview renders
  - Reaction flow works end-to-end on web — add/remove reaction, optimistic update, real-time
    broadcast
  - Reaction flow works end-to-end on mobile — add/remove reaction, picker modal, animated bubble
---

<objective>
Verify that MSG-07 (Reply/Quote) and MSG-09 (React with Emoji) are fully functional end-to-end since discovery found complete implementations on all platforms.
Purpose: Confirm SC-3 (reply with visible thread context) and SC-4 (emoji reactions in real-time) are met without code changes.
Output: Verification report confirming both features work or identifying any gaps that were missed in discovery.
</objective>

<context>
**MSG-07 (Reply/Quote) — Discovery says FULLY IMPLEMENTED:**
- Backend: `reply_to_id` in new_message handler, `belongs_to :reply_to` on Message
- Web: `sendMessage` with `replyToId`, `ReplyPreview` component, `threadStore.ts`
- Mobile: `sendMessage` with `replyToId`, reply in actions menu, reply preview bar

**MSG-09 (React with Emoji) — Discovery says FULLY IMPLEMENTED:**

- Backend: `add_reaction`/`remove_reaction` handlers, `ReactionActions` module, `Reaction` schema
- Web: `addReaction`/`removeReaction` with optimistic updates, socket handlers,
  `AnimatedReactionBubble`, `ReactionPicker`, `EmojiPicker`
- Mobile: `addReaction`/`removeReaction`, `useMessageReactions` hook, `reaction-picker-modal.tsx`,
  `animated-reaction-bubble.tsx`

**Key file references:**

- Backend channel: apps/backend/lib/cgraph_web/channels/conversation_channel.ex
- Web reply preview: apps/web/src/modules/chat/components/message-input/reply-preview.tsx
- Web thread store: apps/web/src/modules/chat/store/threadStore.ts
- Web reaction picker:
  apps/web/src/modules/chat/components/animatedReactionBubble/reaction-picker.tsx
- Mobile reaction modal:
  apps/mobile/src/screens/messages/conversation-screen/components/reaction-picker-modal.tsx
  </context>

<tasks>
<task name="verify_reply_backend_and_types" type="auto">
  <files>apps/backend/lib/cgraph_web/channels/conversation_channel.ex, apps/backend/lib/cgraph/messaging/message.ex, packages/shared-types/src/models.ts</files>
  <action>
    1. Verify `handle_in("new_message")` accepts and passes `reply_to_id` to message creation
    2. Verify `Message` schema has `belongs_to :reply_to, Message` and `reply_to_id` field
    3. Verify new_message broadcast includes `reply_to_id` in the payload
    4. Verify shared-types `Message` interface has `replyToId?: string` field
    5. Run existing backend tests related to messaging: `cd apps/backend && mix test test/cgraph/messaging/ --trace 2>&1 | grep -E "reply|Reply" | head -20`
  </action>
  <verify>
    cd apps/backend && mix test test/cgraph/messaging/ 2>&1 | tail -5
  </verify>
  <done>Reply backend pipeline confirmed: reply_to_id flows from channel → message creation → broadcast</done>
</task>

<task name="verify_reply_and_reaction_web" type="auto">
  <files>apps/web/src/modules/chat/store/chatStore.message-ops.ts, apps/web/src/modules/chat/components/message-input/reply-preview.tsx, apps/web/src/modules/chat/components/animatedReactionBubble/reaction-picker.tsx, apps/web/src/modules/chat/components/animated-reaction-bubble.tsx</files>
  <action>
    1. Verify web `sendMessage` passes `replyToId` through socket
    2. Verify `ReplyPreview` component renders quoted message content and sender
    3. Verify `addReaction`/`removeReaction` store actions exist with optimistic updates
    4. Verify socket handlers for `reaction_added` and `reaction_removed` events exist in conversationChannel.ts
    5. Verify `ReactionPicker` and `AnimatedReactionBubble` components render
    6. Type check: `cd apps/web && pnpm tsc --noEmit 2>&1 | tail -5`
  </action>
  <verify>
    cd apps/web && pnpm tsc --noEmit 2>&1 | tail -5
  </verify>
  <done>Web reply and reaction UI confirmed: components exist, types compile, store actions wire through socket</done>
</task>

<task name="verify_reply_and_reaction_mobile" type="auto">
  <files>apps/mobile/src/stores/chatStore.ts, apps/mobile/src/screens/messages/conversation-screen/components/reaction-picker-modal.tsx, apps/mobile/src/screens/messages/conversation-screen/components/animated-reaction-bubble.tsx</files>
  <action>
    1. Verify mobile `sendMessage` passes `replyToId` through socket
    2. Verify reply option exists in message action menu
    3. Verify reply preview bar renders above input when replying
    4. Verify `addReaction`/`removeReaction` store actions exist
    5. Verify `reaction-picker-modal.tsx` opens and sends reaction through socket
    6. Verify `animated-reaction-bubble.tsx` renders reaction display
    7. Verify `useMessageReactions` hook aggregates reactions correctly
    8. Type check: `cd apps/mobile && pnpm tsc --noEmit 2>&1 | tail -5`
  </action>
  <verify>
    cd apps/mobile && pnpm tsc --noEmit 2>&1 | tail -5
  </verify>
  <done>Mobile reply and reaction flows confirmed: actions wire through socket, UI components exist, types compile</done>
</task>
</tasks>

<verification>
1. Backend tests pass: `cd apps/backend && mix test test/cgraph/messaging/ --trace`
2. Web type check passes: `cd apps/web && pnpm tsc --noEmit`
3. Mobile type check passes: `cd apps/mobile && pnpm tsc --noEmit`
4. Code inspection confirms all wiring (no dead code or disconnected components)
</verification>

<success_criteria>

- SC-3: Reply/quote flow is complete — user can reply to a specific message with visible thread
  context on both platforms
- SC-4: Reaction flow is complete — user reacts with emoji and it appears in real-time for both
  parties on both platforms
- If any gap is found, document it for a follow-up execute plan </success_criteria>
