---
phase: 08-social-profiles
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/modules/social/components/contacts-presence-list.tsx
  - apps/web/src/modules/social/components/index.ts
  - apps/web/src/modules/social/hooks/usePresence.ts
  - apps/mobile/src/screens/social/contacts-screen.tsx
  - apps/mobile/src/screens/social/index.ts
  - apps/mobile/src/hooks/useContactsPresence.ts
autonomous: true

must_haves:
  truths:
    - 'User sees green dot on contacts who are online in the contacts/friends list'
    - 'When a contact goes offline, their dot disappears within 10 seconds'
    - 'Presence updates are real-time via WebSocket, not polling'
    - "Only friends see each other's presence (non-friends cannot)"
  artifacts:
    - path: 'apps/web/src/modules/social/components/contacts-presence-list.tsx'
      provides: 'Contacts list with online/offline indicators'
      min_lines: 40
    - path: 'apps/mobile/src/screens/social/contacts-screen.tsx'
      provides: 'Mobile contacts screen with presence dots'
      min_lines: 50
    - path: 'apps/mobile/src/hooks/useContactsPresence.ts'
      provides: 'Mobile hook for global friend presence tracking'
      min_lines: 25
  key_links:
    - from: 'apps/web/src/modules/social/hooks/usePresence.ts'
      to: 'presence:lobby channel'
      via: 'socketManager.joinPresenceLobby()'
      pattern: 'joinPresence|presenceLobby'
    - from: 'apps/web/src/modules/social/components/contacts-presence-list.tsx'
      to: 'apps/web/src/modules/social/hooks/usePresence.ts'
      via: 'usePresence hook providing onlineFriends set'
      pattern: 'usePresence|isUserOnline'
---

<objective>
Wire presence indicators into contacts/friends lists on web and mobile.

Purpose: NOTIF-05 — users see online/offline status for their contacts in real-time. The presence
system is fully built (`CGraph.Presence` with Tracker, Queries, Store + Redis + Cachex), the
`PresenceChannel` already broadcasts `friend_online`/`friend_offline`/`friend_status_changed` events
filtered by friend_ids, and the web `usePresence` hook already tracks online friends. This plan
creates the UI components that display presence dots in contact lists. Output: Web
contacts-presence-list component, mobile contacts screen with presence, mobile presence hook.
</objective>

<execution_context> @.gsd/skills/execute-plan/SKILL.md @.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md

@apps/backend/lib/cgraph_web/channels/presence_channel.ex @apps/backend/lib/cgraph/presence.ex
@apps/backend/lib/cgraph/presence/tracker.ex @apps/backend/lib/cgraph/presence/queries.ex
@apps/web/src/modules/social/hooks/usePresence.ts
@apps/web/src/modules/social/store/friendStore.impl.ts
@apps/mobile/src/screens/auth/onboarding/onboarding-screen.tsx </context>

<tasks>

<task type="auto">
  <name>Task 1: Build web contacts list with presence indicators</name>
  <files>apps/web/src/modules/social/components/contacts-presence-list.tsx</files>
  <action>
1. Create `contacts-presence-list.tsx` in `apps/web/src/modules/social/components/`:
   - Import and use the existing `usePresence()` hook which returns `{ isConnected, onlineFriends: Set<string>, isUserOnline: (id) => boolean, onlineCount }`
   - Import friends list from the existing friendStore (check `friendStore.impl.ts` for the friends array / fetch function)
   - Render a list of all friends/contacts sorted: online first, then offline
   - Each contact row: avatar (32px circle), display_name, @username, and a presence dot:
     - Green dot (8px, `bg-green-500`) with subtle pulse animation for online
     - Gray dot (8px, `bg-gray-400`) for offline
     - Optional: show `last_seen` timestamp for offline users if available from the presence store
   - Include a header: "Contacts" with online count badge (e.g., "3 online")
   - Include status_message text below username if set (muted, truncated to 1 line)
   - Empty state when no friends: "Add friends to see them here"
   - Export from `apps/web/src/modules/social/components/index.ts`

2. If `usePresence` hook doesn't include `statusMessages` map, enhance it minimally:
   - Add a `statusMessages: Map<string, string>` state
   - Populate from `friend_status_changed` event's `status_message` field (already broadcast by
     PresenceChannel)
   - Export `getStatusMessage(userId)` helper </action> <verify>Run
     `cd apps/web && npx tsc --noEmit` — no type errors</verify> <done>Web contacts list renders
     friends with green/gray presence dots, sorted online-first, with status messages and online
     count</done> </task>

<task type="auto">
  <name>Task 2: Build mobile contacts screen with presence</name>
  <files>apps/mobile/src/hooks/useContactsPresence.ts, apps/mobile/src/screens/social/contacts-screen.tsx</files>
  <action>
1. Create `useContactsPresence.ts` in `apps/mobile/src/hooks/`:
   - Connect to the presence channel (check existing mobile socket patterns in `apps/mobile/src/services/` or `apps/mobile/src/lib/`)
   - Track online friends as a Set<string>
   - Listen for `friend_online`, `friend_offline`, `friend_status_changed` events
   - Return `{ onlineFriends: Set<string>, isOnline: (id: string) => boolean, statusMessages: Map<string, string> }`
   - Handle app state changes (foreground/background) — the PresenceChannel already handles `set_app_state`

2. Create `contacts-screen.tsx` in `apps/mobile/src/screens/social/`:
   - Fetch friends list from API (`GET /api/v1/friends`) on mount
   - Use `useContactsPresence` for real-time presence
   - SectionList with two sections: "Online" and "Offline"
   - Each row: avatar (Image, 40px circle), display_name, @username, presence dot (View, 10px
     circle, green/gray)
   - Absolute-positioned presence dot on bottom-right of avatar
   - Show status_message below username if set (numberOfLines={1})
   - Pull-to-refresh to reload friends list
   - Export from `apps/mobile/src/screens/social/index.ts` </action> <verify>Run
     `cd apps/mobile && npx tsc --noEmit` — no type errors</verify> <done>Mobile contacts screen
     shows friends in Online/Offline sections with real-time presence dots and status
     messages</done> </task>

</tasks>

<verification>
- [ ] `cd apps/web && npx tsc --noEmit` passes
- [ ] `cd apps/mobile && npx tsc --noEmit` passes
- [ ] Web contacts list uses existing usePresence hook for real-time updates
- [ ] Mobile contacts screen connects to presence channel
- [ ] Both platforms show green/gray dots and status messages
</verification>

<success_criteria>

- Contacts lists on both platforms show real-time online/offline presence
- Presence updates are instant via WebSocket (no polling)
- Status messages visible beneath usernames
- Online contacts sorted to top </success_criteria>

<output>
After completion, create `.gsd/phases/08-social-profiles/08-02-SUMMARY.md`
</output>
