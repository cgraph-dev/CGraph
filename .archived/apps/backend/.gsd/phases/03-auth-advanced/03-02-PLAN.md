---
phase: 03-auth-advanced
plan: 02
type: execute
wave: 2
depends_on: ['03-01']
files_modified:
  - apps/web/src/pages/auth/login/useLoginForm.ts
  - apps/web/src/pages/auth/login/two-factor-form.tsx
  - apps/web/src/modules/auth/store/auth-actions.ts
  - apps/mobile/src/stores/authStore.ts
  - apps/mobile/src/screens/auth/two-factor-verify-screen.tsx
  - apps/mobile/src/navigation/auth-navigator.tsx
autonomous: true

must_haves:
  truths:
    - 'Web login shows TOTP entry form when backend returns 2fa_required'
    - 'Web TOTP form submits to /login/2fa and receives tokens on valid code'
    - 'Web TOTP form shows error on invalid code without losing entered digits'
    - 'Mobile login navigates to TwoFactorVerifyScreen when backend returns 2fa_required'
    - 'Mobile TOTP screen submits to /login/2fa and logs user in on valid code'
    - 'Both platforms support backup code entry as fallback'
  artifacts:
    - path: 'apps/web/src/pages/auth/login/two-factor-form.tsx'
      provides: 'Web 2FA verification form for login'
      min_lines: 40
    - path: 'apps/mobile/src/screens/auth/two-factor-verify-screen.tsx'
      provides: 'Mobile 2FA verification screen for login'
      min_lines: 60
  key_links:
    - from: 'useLoginForm.ts'
      to: 'two-factor-form.tsx'
      via: 'conditional render when loginState is 2fa_required'
    - from: 'auth-actions.ts login()'
      to: '/api/v1/auth/login/2fa'
      via: 'verifyLoginTwoFactor action'
    - from: 'authStore.ts login()'
      to: 'TwoFactorVerifyScreen'
      via: '2fa_required response triggers navigation'
---

<objective>
Add 2FA verification UI to web and mobile login flows.

Purpose: Backend now returns 2fa_required for TOTP-enabled users (Plan 03-01). Both frontends need
UI to collect the TOTP code and complete authentication. Output: Web 2FA form component + mobile 2FA
verify screen. </objective>

<execution_context> @.github/skills/execute-plan/SKILL.md @.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03-auth-advanced/03-01-SUMMARY.md

@apps/web/src/pages/auth/login/useLoginForm.ts @apps/web/src/pages/auth/login.tsx
@apps/web/src/modules/auth/store/auth-actions.ts @apps/mobile/src/screens/auth/login-screen.tsx
@apps/mobile/src/stores/authStore.ts @apps/mobile/src/navigation/auth-navigator.tsx </context>

<tasks>

<task type="auto">
  <name>Task 1: Web 2FA login form + auth action</name>
  <files>apps/web/src/pages/auth/login/two-factor-form.tsx, apps/web/src/pages/auth/login/useLoginForm.ts, apps/web/src/modules/auth/store/auth-actions.ts</files>
  <action>
**1. Create `two-factor-form.tsx`:**
- 6-digit code input (auto-focus, numeric only, auto-submit on 6 chars)
- "Use backup code" toggle that switches to a text input for backup codes
- Error display for invalid codes
- "Back to login" link that resets to password form
- Loading state during verification
- Props: `twoFactorToken: string`, `onVerified: () => void`, `onBack: () => void`
- On submit: call `verifyLoginTwoFactor(twoFactorToken, code)` from auth store

**2. Modify `useLoginForm.ts` (or login page):**

- Add state: `loginStep: 'credentials' | '2fa'` and `twoFactorToken: string | null`
- On login response: if response has `status === '2fa_required'`, set `loginStep = '2fa'` and store
  `twoFactorToken`
- Conditionally render `TwoFactorForm` when `loginStep === '2fa'`

**3. Add `verifyLoginTwoFactor` to `auth-actions.ts`:**

- POST `/api/v1/auth/login/2fa` with `{ two_factor_token, code }`
- On success: store tokens + user, set `isAuthenticated: true` (same as regular login)
- On error: throw with error message (form displays it)

Match existing login page styling. Use the same input components used elsewhere in auth pages.
</action> <verify>pnpm --filter web typecheck passes. Visually: login page renders normally; after
entering valid credentials for a 2FA user, TOTP form appears.</verify> <done>Web login flow handles
2fa_required response, shows TOTP form, completes auth on valid code.</done> </task>

<task type="auto">
  <name>Task 2: Mobile 2FA login screen + auth store</name>
  <files>apps/mobile/src/screens/auth/two-factor-verify-screen.tsx, apps/mobile/src/stores/authStore.ts, apps/mobile/src/navigation/auth-navigator.tsx</files>
  <action>
**1. Create `two-factor-verify-screen.tsx`:**
- Screen matching existing auth screen styling (use same layout patterns as login-screen.tsx)
- 6-digit code input with auto-focus and numeric keyboard
- "Use backup code" option
- Error display for invalid codes
- Loading state during verification
- Route params: `{ twoFactorToken: string }`
- On submit: call `verifyLoginTwoFactor(twoFactorToken, code)` from authStore
- On success: authStore handles navigation (sets `isAuthenticated: true`)
- Back button returns to login screen

**2. Modify `authStore.ts`:**

- In `login()` action: detect `2fa_required` response. Instead of storing tokens, navigate to
  `TwoFactorVerify` screen with `twoFactorToken` param. Do NOT set `isAuthenticated: true`.
- Add `verifyLoginTwoFactor(twoFactorToken: string, code: string)` action:
  - POST `/api/v1/auth/login/2fa` with `{ two_factor_token: twoFactorToken, code }`
  - On success: store tokens + user via `saveAuth()`, set `isAuthenticated: true`, connect socket
  - On error: throw for screen to catch and display

**3. Register screen in `auth-navigator.tsx`:**

- Add `<Stack.Screen name="TwoFactorVerify" component={TwoFactorVerifyScreen} />` to auth stack
- Add `TwoFactorVerify: { twoFactorToken: string }` to `AuthStackParamList` if not present

**4. Register deep link (optional but consistent):**

- No deep link needed for 2FA verify — it's only reachable via login flow. </action>
  <verify>TypeScript compiles without errors. Mobile auth flow navigates to 2FA screen on mock
  2fa_required response.</verify> <done>Mobile login flow handles 2fa_required, navigates to TOTP
  screen, completes auth on valid code.</done> </task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter web typecheck` passes
- [ ] Web login page renders credential form by default
- [ ] Web transitions to TOTP form on 2fa_required response
- [ ] Web TOTP form has 6-digit input + backup code toggle
- [ ] Mobile `TwoFactorVerifyScreen` registered in auth navigator
- [ ] Mobile authStore detects 2fa_required and navigates to TOTP screen
- [ ] Mobile TOTP screen has 6-digit input + backup code option
- [ ] Both platforms call POST `/api/v1/auth/login/2fa` with correct payload
</verification>

<success_criteria>

- Web and mobile both handle the 2FA login gate seamlessly
- No regression — users without 2FA still log in normally
- Backup codes work as fallback on both platforms
- UI matches existing auth page styling </success_criteria>

<output>
After completion, create `.gsd/phases/03-auth-advanced/03-02-SUMMARY.md`
</output>
