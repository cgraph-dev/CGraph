---
phase: 21
plan: 02
title: 'Web Admin Dashboard & Mock Data Audit — Remove All Remaining Mocks'
scope:
  'Fix admin dashboard mock fallback, audit entire web codebase for any remaining MOCK_ constants or
  hardcoded fake data, and remove them all.'
wave: 1
autonomous: true
depends_on: ['21-01']
files_modified:
  - apps/web/src/modules/admin/stores/adminStore.mockData.ts
  - apps/web/src/modules/admin/stores/adminStore.ts
must_haves:
  truths:
    - 'Admin dashboard shows "Data unavailable" or error state when API calls fail, not fake
      statistics (12847 users, $2847.50 revenue, etc.)'
    - '`adminStore.mockData.ts` is deleted or its mock data removed'
    - 'Full codebase grep for `MOCK_|mock-data|mockData|getMock` returns zero results in production
      code (test files excluded)'
    - 'All TODO comments containing `@todo(api)` are resolved or converted to GitHub issues'
  tests:
    - 'grep -r "MOCK_" apps/web/src/ --include="*.ts" --include="*.tsx" returns only test files'
---

## Context

After 21-01 removes the major mock data areas, this plan does a final sweep to catch any remaining
mock data patterns across the entire web codebase.

Known remaining:

- `adminStore.mockData.ts` — fallback data for admin dashboard (fake user counts, revenue)

## Tasks

1. **Fix admin dashboard error handling**
   - Remove mock data fallback from admin store
   - Show "Unable to load" with retry button when admin API calls fail
   - Delete `adminStore.mockData.ts` file

2. **Full mock data audit**
   - Run:
     `grep -rn "MOCK_\|getMock\|mock-data\|mockData" apps/web/src/ --include="*.ts" --include="*.tsx"`
   - Exclude test files and Storybook files
   - Fix every remaining instance

3. **Resolve @todo(api) comments**
   - Search: `grep -rn "@todo(api)" apps/web/src/`
   - For each: either wire to the API (if backend endpoint exists) or create a GitHub issue
