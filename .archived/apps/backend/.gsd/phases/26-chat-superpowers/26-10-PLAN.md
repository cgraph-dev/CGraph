---
phase: 26-chat-superpowers
plan: 10
type: execute
wave: 5
depends_on: ['26-07', '26-08']
files_modified:
  - test/cgraph/messaging/secret_chat_test.exs
  - test/cgraph/messaging/stickers_test.exs
  - test/cgraph/messaging/file_transfer_test.exs
  - test/cgraph_web/channels/secret_chat_channel_test.exs
  - test/cgraph_web/controllers/api/v1/secret_chat_controller_test.exs
  - test/cgraph_web/controllers/api/v1/sticker_controller_test.exs
  - test/cgraph_web/controllers/api/v1/file_transfer_controller_test.exs
  - test/cgraph/integration/e2ee_flow_test.exs
  - test/cgraph/integration/chat_features_test.exs
autonomous: true

must_haves:
  truths:
    - 'Secret chat full lifecycle test passes (create → send → self-destruct → verify deleted)'
    - 'Sticker system test passes (browse store → add pack → send sticker in message)'
    - 'File transfer test passes (initiate → upload chunks → complete → download)'
    - 'E2EE integration test passes (bootstrap → exchange bundles → send encrypted message)'
    - 'Existing 2372 tests still pass (no regressions)'
    - 'All new controllers return proper error codes (400, 401, 403, 404)'
  artifacts:
    - path: 'test/cgraph/integration/e2ee_flow_test.exs'
      provides: 'End-to-end E2EE flow integration test'
    - path: 'test/cgraph/integration/chat_features_test.exs'
      provides: 'Cross-feature integration tests'
    - path: 'test/cgraph/messaging/secret_chat_test.exs'
      provides: 'Secret chat context tests'
    - path: 'test/cgraph/messaging/stickers_test.exs'
      provides: 'Sticker system tests'
    - path: 'test/cgraph/messaging/file_transfer_test.exs'
      provides: 'File transfer context tests'
  key_links:
    - from: 'test/cgraph/integration/e2ee_flow_test.exs'
      to: 'lib/cgraph/crypto/e2ee.ex + conversation_channel.ex'
      via: 'full E2EE handshake simulation'
      pattern: "E2EE\\.|ConversationChannel"
---

<objective>
Comprehensive integration testing and regression fixing for all Phase 26 features.

Purpose: Each prior plan adds features independently. This plan verifies they work together, writes
integration tests for cross-feature flows, fixes any regressions in the existing 2372 test suite,
and ensures proper error handling across all new endpoints.

Output: Integration tests, regression fixes, and confidence that the full system works. </objective>

<execution_context> ../skills/execute-plan/SKILL.md @.gsd/templates/summary.md </execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/phases/26-chat-superpowers/26-01-SUMMARY.md
@.gsd/phases/26-chat-superpowers/26-02-SUMMARY.md
@.gsd/phases/26-chat-superpowers/26-03-SUMMARY.md
@.gsd/phases/26-chat-superpowers/26-04-SUMMARY.md
@.gsd/phases/26-chat-superpowers/26-05-SUMMARY.md
@.gsd/phases/26-chat-superpowers/26-06-SUMMARY.md
@.gsd/phases/26-chat-superpowers/26-07-SUMMARY.md
@.gsd/phases/26-chat-superpowers/26-08-SUMMARY.md
@.gsd/phases/26-chat-superpowers/26-09-SUMMARY.md
@test/support/factory.ex
@test/support/conn_case.ex
@test/support/channel_case.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature-level context tests for new systems</name>
  <files>
    test/cgraph/messaging/secret_chat_test.exs
    test/cgraph/messaging/stickers_test.exs
    test/cgraph/messaging/file_transfer_test.exs
    test/cgraph_web/controllers/api/v1/secret_chat_controller_test.exs
    test/cgraph_web/controllers/api/v1/sticker_controller_test.exs
    test/cgraph_web/controllers/api/v1/file_transfer_controller_test.exs
  </files>
  <action>
  Write comprehensive tests for each new system. Use existing test infrastructure (ex_machina
  factories, ConnCase, ChannelCase). Create factories for new schemas as needed.

**`test/cgraph/messaging/secret_chat_test.exs`:**

- Test create_secret_conversation between two users → creates with device binding
- Test create_secret_conversation with blocked user → error
- Test create_secret_conversation with self → error
- Test send_secret_message → creates message with required ciphertext
- Test send_secret_message to non-member → error
- Test set_self_destruct_timer → timer applies to future messages
- Test destroy_secret_conversation → hard-deletes all messages and conversation
- Test list_secret_conversations → returns only user's secret convos
- Test secret message cleanup worker deletes expired messages
- Test secret messages are hard-deleted (not soft-deleted)

**`test/cgraph/messaging/stickers_test.exs`:**

- Test list_store_packs → returns available packs
- Test search_stickers("cat") → returns cat-themed stickers
- Test add_pack (free) → user gets pack
- Test add_pack (premium, sufficient coins) → user gets pack, coins deducted
- Test add_pack (premium, insufficient coins) → error
- Test remove_pack → removes from user's collection
- Test list_user_packs → returns user's added packs
- Test recently_used → returns user's recent sticker usage

**`test/cgraph/messaging/file_transfer_test.exs`:**

- Test initiate_upload with valid params → creates file_transfer record
- Test initiate_upload exceeding tier limit → error with limit info
- Test initiate_upload with blocked file type (.exe) → error
- Test upload_chunk → succeeds for valid chunk
- Test complete_upload → assembles file, returns URL
- Test generate_download_url → returns signed URL with expiry
- Test cancel_upload → cleans up partial upload
- Test file cleanup worker removes abandoned uploads (>24h old)

**Controller tests (3 files):** For each controller, test:

- Authenticated access works (200/201)
- Unauthenticated access returns 401
- Invalid params return 400 with error details
- Not-found IDs return 404
- Permission violations return 403 Follow existing ConnCase patterns in the codebase. </action>
  <verify> All new feature tests pass:
  `mix test test/cgraph/messaging/secret_chat_test.exs   test/cgraph/messaging/stickers_test.exs test/cgraph/messaging/file_transfer_test.exs   test/cgraph_web/controllers/api/v1/secret_chat_controller_test.exs   test/cgraph_web/controllers/api/v1/sticker_controller_test.exs   test/cgraph_web/controllers/api/v1/file_transfer_controller_test.exs`
  — 40+ tests pass. </verify> <done> Comprehensive test coverage for all new Phase 26 systems:
  secret chat, stickers, and file transfer. Both context-level and controller-level tests. Proper
  error handling verified. </done> </task>

<task type="auto">
  <name>Task 2: Integration tests + regression fix pass</name>
  <files>
    test/cgraph/integration/e2ee_flow_test.exs
    test/cgraph/integration/chat_features_test.exs
    test/cgraph_web/channels/secret_chat_channel_test.exs
  </files>
  <action>
  **Create `test/cgraph/integration/` directory if needed.**

**`test/cgraph/integration/e2ee_flow_test.exs`:** Full E2EE lifecycle integration test:

1. User A registers → checks bootstrap status (should be `no_identity_key`)
2. User A uploads identity key + 50 pre-keys → bootstrap status becomes `ready`
3. User B registers → uploads identity key + 50 pre-keys
4. User A requests User B's pre-key bundle → gets bundle, B's pre-key count decreases by 1
5. User A opens conversation with B → joins ConversationChannel
6. User A sends encrypted message with ratchet_header → broadcast includes ratchet_header
7. User B receives message with ciphertext and ratchet_header intact
8. Verify server never accessed plaintext (message.content is base64 ciphertext)
9. Safety number computed for A↔B session matches when computed from either side
10. Pre-key exhaustion: deplete all of B's pre-keys → exhaustion notification fires

**`test/cgraph/integration/chat_features_test.exs`:** Cross-feature integration tests:

1. Send sticker as message in conversation → message.content_type is "sticker"
2. Send file attachment in conversation → file_url populated
3. Schedule message → wait for Oban to fire → message appears in conversation
4. Create poll in conversation → vote → real-time tallies update
5. Send message with reply_to → reply preview included in broadcast
6. Send message with URL → link preview job enqueued
7. Forward message from conversation A to conversation B → forwarded_from set

**`test/cgraph_web/channels/secret_chat_channel_test.exs`:**

- Test join with valid device → success
- Test join from different device → error
- Test send encrypted message → broadcast with ciphertext to peer
- Test typing indicator → broadcast
- Test message_read triggers self-destruct timer
- Test screenshot_detected → alert sent to peer

**Regression pass:** Run `mix test` on the full suite. Fix any failures introduced by Phase 26
changes:

- Check for import/alias conflicts from new modules
- Check for migration ordering issues
- Check for factory conflicts with new schemas
- Document any pre-existing failures (5 known) vs new failures </action> <verify> `mix test` — full
  suite passes. New integration tests pass. Pre-existing failure count does NOT increase (≤5 known
  pre-existing failures). </verify> <done> Integration tests verify cross-feature flows work. E2EE
  lifecycle tested end-to-end. Full regression pass confirms no new failures introduced. System is
  stable. </done> </task>

</tasks>

<verification>
- `mix test` passes (all 2372+ existing tests + 60+ new tests)
- Integration tests cover: E2EE lifecycle, sticker in message, file in message, scheduled
  delivery, poll voting, reply preview, link preview, message forward
- No new regression failures introduced
</verification>

<success_criteria> All Phase 26 features verified through integration tests. Full test suite passes
with no new regressions. Error handling validated across all new endpoints. System is
production-ready. </success_criteria>

<output>
After completion, create `.gsd/phases/26-chat-superpowers/26-10-SUMMARY.md`
</output>
