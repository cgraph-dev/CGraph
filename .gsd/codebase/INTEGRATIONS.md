# CGraph External Integrations

> Auto-generated by codebase mapper. Source of truth: `mix.exs`, `package.json` files,
> `config/runtime.exs`, `.env.example` files, and infrastructure definitions.

---

## 1. Database & ORM

### PostgreSQL (Primary Database)

- **Driver**: `postgrex ~> 0.21` ([`apps/backend/mix.exs`](apps/backend/mix.exs))
- **ORM**: `ecto_sql ~> 3.13` (Ecto) — migrations, changesets, query builder
- **ID Strategy**: ULIDs via `ecto_ulid ~> 0.3` with `binary_id: true`
- **Managed hosting**: **Supabase** (env vars: `SUPABASE_URL`, `SUPABASE_ANON_KEY`,
  `SUPABASE_SERVICE_ROLE_KEY` in [`apps/backend/.env.example`](apps/backend/.env.example))
- **Connection pooling**: **PgBouncer** sidecar in Docker container
  ([`infrastructure/pgbouncer/pgbouncer.ini`](infrastructure/pgbouncer/pgbouncer.ini))
- **Read replica** support: `CGraph.ReadRepo` configured via `READ_REPLICA_DATABASE_URL`
  ([`apps/backend/config/runtime.exs`](apps/backend/config/runtime.exs))
- **Dev version**: PostgreSQL 16 Alpine ([`docker-compose.dev.yml`](docker-compose.dev.yml))

### Redis

- **Client**: `redix ~> 1.5` ([`apps/backend/mix.exs`](apps/backend/mix.exs))
- **In-memory cache**: `cachex ~> 4.1` (Elixir-side fallback)
- **Uses**: Caching, rate limiting, PubSub, presence tracking, distributed rate limiter
- **Config**: `REDIS_URL` env var
  ([`apps/backend/config/runtime.exs`](apps/backend/config/runtime.exs#L310-L340))
- **Dev version**: Redis 7 Alpine ([`docker-compose.dev.yml`](docker-compose.dev.yml))

### MeiliSearch (Full-Text Search)

- **Version**: `v1.12` ([`docker-compose.dev.yml`](docker-compose.dev.yml))
- **Integration**: `CGraph.Search.Engine` module
  ([`apps/backend/lib/cgraph/search/`](apps/backend/lib/cgraph/search/))
- **Config**: `MEILISEARCH_URL`, `MEILISEARCH_API_KEY`
  ([`apps/backend/config/runtime.exs`](apps/backend/config/runtime.exs))
- **Fallback**: Gracefully degrades to PostgreSQL `ILIKE` queries when not configured
- **Indexes**: Messages, Users, Posts, Groups (via `CGraph.Search.Indexer`)

### WatermelonDB (Mobile Offline Database)

- **Package**: `@nozbe/watermelondb ^0.27.1`
  ([`apps/mobile/package.json`](apps/mobile/package.json))
- **Purpose**: Offline-first local SQLite database for mobile app

---

## 2. Authentication & Authorization

### JWT Authentication

- **Library**: `guardian ~> 2.4` + `guardian_phoenix ~> 2.0`
  ([`apps/backend/mix.exs`](apps/backend/mix.exs))
- **JWT lib**: `jose ~> 1.11`
- **Config**: `JWT_SECRET` env var, issuer: `"cgraph"`
  ([`apps/backend/config/runtime.exs`](apps/backend/config/runtime.exs#L170-L178))
- **Token TTL**: Access 15 min (`JWT_ACCESS_TOKEN_TTL`), Refresh 7 days (`JWT_REFRESH_TOKEN_TTL`)
  > **Note**: `.env.example` uses `ACCESS_TOKEN_EXPIRY`/`REFRESH_TOKEN_EXPIRY` (different names).

### Password Hashing

- **Library**: `argon2_elixir ~> 4.1` ([`apps/backend/mix.exs`](apps/backend/mix.exs))

### TOTP / 2FA

- **Library**: `nimble_totp ~> 1.0` ([`apps/backend/mix.exs`](apps/backend/mix.exs))

### OAuth 2.0 Providers

- **Library**: `assent ~> 0.2` ([`apps/backend/mix.exs`](apps/backend/mix.exs))
- **Implementation**: [`apps/backend/lib/cgraph/oauth/`](apps/backend/lib/cgraph/oauth/) (providers,
  apple, config, user_manager)
- **Providers configured**
  ([`apps/backend/config/runtime.exs`](apps/backend/config/runtime.exs#L285-L320)):
  - **Google** — `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`
  - **Apple** — `APPLE_CLIENT_ID`, `APPLE_TEAM_ID`, `APPLE_KEY_ID`, `APPLE_PRIVATE_KEY`
  - **Facebook** — `FACEBOOK_CLIENT_ID`, `FACEBOOK_CLIENT_SECRET` (runtime.exs only, not in
    `.env.example`)
  - **TikTok** — `TIKTOK_CLIENT_KEY`, `TIKTOK_CLIENT_SECRET` (runtime.exs only, not in
    `.env.example`)
  - **(Discord)** — `DISCORD_CLIENT_ID`, `DISCORD_CLIENT_SECRET` (in `.env.example`)

### Web3 / Ethereum Wallet Authentication

- **Libraries**: `ex_keccak ~> 0.7`, `ex_secp256k1 ~> 0.7`
  ([`apps/backend/mix.exs`](apps/backend/mix.exs))
- **Purpose**: Ethereum wallet signature verification for wallet-based login
- **Web frontend**: `wagmi 3.5.0`, `viem ^2.46.3`, `@wagmi/connectors 7.2.1`,
  `@walletconnect/modal ^2.7.0` ([`apps/web/package.json`](apps/web/package.json))

### Apple Sign In (Mobile)

- **Package**: `expo-apple-authentication ~8.0.0`
  ([`apps/mobile/package.json`](apps/mobile/package.json))

### Biometric Auth (Mobile)

- **Package**: `expo-local-authentication ~17.0.0`
  ([`apps/mobile/package.json`](apps/mobile/package.json))

---

## 3. Payment Processing — Stripe

- **Backend library**: `stripity_stripe ~> 3.2` ([`apps/backend/mix.exs`](apps/backend/mix.exs))
- **Frontend SDKs**: `@stripe/react-stripe-js ^3.5.0`, `@stripe/stripe-js ^5.5.0`
  ([`apps/web/package.json`](apps/web/package.json))
- **Config file**: [`apps/backend/config/stripe.exs`](apps/backend/config/stripe.exs)
- **Runtime config**: [`apps/backend/config/runtime.exs`](apps/backend/config/runtime.exs#L200-L230)
- **Subscription module**:
  [`apps/backend/lib/cgraph/subscriptions/`](apps/backend/lib/cgraph/subscriptions/)
- **Tiers**: Free, Premium, Enterprise (with `tier_feature.ex`, `tier_limit.ex`, `tier_limits/`)
- **Env vars**: `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PUBLISHABLE_KEY`,
  `STRIPE_PRICE_PREMIUM`, `STRIPE_PRICE_ENTERPRISE`
- **Webhook endpoint**: Incoming Stripe webhook for payment events
- **Checkout**: Stripe Checkout with `success_url`, `cancel_url`
- **Portal**: Stripe Customer Portal (`portal_return_url`)

---

## 4. Email / Notification Services

### Email — Resend (via Swoosh)

- **Library**: `swoosh ~> 1.20` ([`apps/backend/mix.exs`](apps/backend/mix.exs))
- **Adapter**: `Swoosh.Adapters.Resend` (production)
- **HTTP client**: `finch ~> 0.20` (Swoosh API client)
- **Env var**: `RESEND_API_KEY`
  ([`apps/backend/config/runtime.exs`](apps/backend/config/runtime.exs#L188-L198))
  > **Note**: `.env.example` has generic SMTP settings (`SMTP_HOST`, etc.) instead of
  > `RESEND_API_KEY`.
- **Templates**:
  [`apps/backend/lib/cgraph/mailer/templates/`](apps/backend/lib/cgraph/mailer/templates/)
- **Background jobs**: `CGraph.Workers.EmailDigestWorker` (daily at 8 AM UTC)

### Push Notifications — Expo Push

- **Env var**: `EXPO_ACCESS_TOKEN`
  ([`apps/backend/config/runtime.exs`](apps/backend/config/runtime.exs#L350-L355))
  > **Note**: Not in `.env.example`.
- **Module**:
  [`apps/backend/lib/cgraph/notifications/push_service/`](apps/backend/lib/cgraph/notifications/push_service/)
- **Mobile integration**: `expo-notifications ~0.32.16`
  ([`apps/mobile/package.json`](apps/mobile/package.json))

### In-App Notifications

- **Module**: [`apps/backend/lib/cgraph/notifications/`](apps/backend/lib/cgraph/notifications/)
  (delivery, queries, push_tokens)
- **Real-time delivery**: Phoenix Channels (see WebSocket section)

---

## 5. Cloud Storage

### Cloudflare R2 (S3-compatible Object Storage)

- **Libraries**: `ex_aws ~> 2.5`, `ex_aws_s3 ~> 2.5`
  ([`apps/backend/mix.exs`](apps/backend/mix.exs))
- **Bucket**: `cgraph-uploads`
  ([`infrastructure/terraform/pages.tf`](infrastructure/terraform/pages.tf))
- **Config**: `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_ACCOUNT_ID`
  ([`apps/backend/config/runtime.exs`](apps/backend/config/runtime.exs#L260-L275))
- **Host pattern**: `{account_id}.r2.cloudflarestorage.com`
- **File upload lib**: `waffle ~> 1.1`, `waffle_ecto ~> 0.0.12`

### Storage Providers

- **Module**: [`apps/backend/lib/cgraph/storage/`](apps/backend/lib/cgraph/storage/)
- **Backends**: `local.ex`, `r2.ex`, `s3.ex` — configurable via `STORAGE_PROVIDER` (local, s3, r2).
  Note: `.env.example` lists "supabase" as an option but no `supabase.ex` backend exists.
- **Uploads module**: [`apps/backend/lib/cgraph/uploads/`](apps/backend/lib/cgraph/uploads/)

### Mobile File Access

- `expo-file-system ~19.0.0`, `expo-image-picker ~17.0.0`, `expo-document-picker ^14.0.8`,
  `expo-media-library ~18.2.1` ([`apps/mobile/package.json`](apps/mobile/package.json))

---

## 6. WebSocket / Real-Time

### Phoenix Channels (Primary Real-Time Transport)

- **Library**: `phoenix_pubsub ~> 2.1` ([`apps/backend/mix.exs`](apps/backend/mix.exs))
- **Client (TS)**: `phoenix ^1.8.0` ([`apps/web/package.json`](apps/web/package.json),
  [`apps/mobile/package.json`](apps/mobile/package.json))
- **Shared package**: `@cgraph/socket` wraps Phoenix channel client
  ([`packages/socket/`](packages/socket/))

### Channel Types ([`apps/backend/lib/cgraph_web/channels/`](apps/backend/lib/cgraph_web/channels/))

| Channel                   | Purpose                          |
| ------------------------- | -------------------------------- |
| `user_socket.ex`          | Main WebSocket entry point       |
| `conversation_channel.ex` | 1:1 and group messaging          |
| `group_channel.ex`        | Group features                   |
| `forum_channel.ex`        | Forum discussions                |
| `thread_channel.ex`       | Threaded conversations           |
| `board_channel.ex`        | Board/kanban features            |
| `call_channel.ex`         | Voice/video call signaling       |
| `webrtc_lobby_channel.ex` | WebRTC call lobby                |
| `voice_state_channel.ex`  | Voice state management           |
| `secret_chat_channel.ex`  | Secret/ephemeral chat            |
| `document_channel.ex`     | Real-time document collaboration |
| `presence_channel.ex`     | User presence                    |
| `ai_channel.ex`           | AI feature channel               |
| `user_channel.ex`         | User-specific events             |
| `qr_auth_channel.ex`      | QR code login authentication     |
| `backpressure.ex`         | Channel backpressure management  |
| `socket_security.ex`      | WebSocket security module        |

### Collaborative Editing (Yjs)

- **Web packages**: `yjs ^13.6.0`, `y-protocols ^1.0.6`
  ([`apps/web/package.json`](apps/web/package.json))
- **Backend**: `CGraph.Collaboration.DocumentServer`
  ([`apps/backend/lib/cgraph/collaboration/`](apps/backend/lib/cgraph/collaboration/))

### WebRTC (Voice/Video Calls)

- **Module**: [`apps/backend/lib/cgraph/webrtc/`](apps/backend/lib/cgraph/webrtc/) (signaling, room,
  calls, participants)
- **Config**: STUN/TURN servers, SFU support
  ([`apps/backend/config/runtime.exs`](apps/backend/config/runtime.exs#L360-L380))
- **Env vars**: `WEBRTC_STUN_SERVERS`, `WEBRTC_TURN_URL`, `WEBRTC_TURN_USERNAME`,
  `WEBRTC_TURN_CREDENTIAL`, `WEBRTC_SFU_URL`
- **Default STUN**: `stun:stun.l.google.com:19302`

### LiveKit SFU (Group Calls)

- **Backend config**: `CGraph.WebRTC.LiveKit` module
  ([`apps/backend/config/runtime.exs`](apps/backend/config/runtime.exs))
- **Web client**: `livekit-client ^2.9.0` ([`apps/web/package.json`](apps/web/package.json))
- **Mobile client**: `@livekit/react-native ^2.4.1`, `@livekit/react-native-webrtc ^125.0.7`
  ([`apps/mobile/package.json`](apps/mobile/package.json))
- **Dev server**: `livekit/livekit-server:latest`
  ([`infrastructure/docker-compose.livekit.yml`](infrastructure/docker-compose.livekit.yml))
- **Env vars**: `LIVEKIT_API_KEY`, `LIVEKIT_API_SECRET`, `LIVEKIT_URL`
- **Default dev**: `ws://localhost:7880` (key: `devkey`, secret: `secret`)
- **Purpose**: SFU for group calls with 3+ participants (peer-to-peer for 1:1)

### Presence

- **Module**: [`apps/backend/lib/cgraph/presence/`](apps/backend/lib/cgraph/presence/)
- **Sampled Presence** for large channels (tiers: 100→1K→100K→∞ with decreasing sample rates)

---

## 7. End-to-End Encryption (E2EE)

### Crypto Package (`@cgraph/crypto`)

- **Source**: [`packages/crypto/`](packages/crypto/)
- **Dependencies**: `@noble/hashes ^1.8.0`, `@noble/post-quantum ^0.2.1`
- **Protocols implemented**:
  - **PQXDH** — Post-Quantum Extended Diffie-Hellman key agreement
  - **Double Ratchet** — Signal Protocol message ratchet
  - **Triple Ratchet** — ECDH + ML-KEM-768 (post-quantum KEM)
  - **AES-256-GCM** — Symmetric encryption
  - **X3DH** — Extended Triple Diffie-Hellman
- **Exports**: `.` (root), `aes`, `utils`, `errors`, `stores`, `types-portable`, `kem`, `x3dh`,
  `pqxdh`, `doubleRatchet`, `spqr`, `tripleRatchet`

### Mobile Crypto

- `react-native-quick-crypto ^0.7.0` — Native crypto acceleration
  ([`apps/mobile/package.json`](apps/mobile/package.json))
- `expo-crypto ~15.0.0` — Expo crypto module
- `react-native-get-random-values ^1.11.0` — Secure random

### Server-Side Encryption

- **Module**: [`apps/backend/lib/cgraph/encryption/`](apps/backend/lib/cgraph/encryption/),
  [`apps/backend/lib/cgraph/crypto/`](apps/backend/lib/cgraph/crypto/)
- **Env var**: `ENCRYPTION_KEY` — for encrypting TOTP secrets, sensitive data at rest

---

## 8. Error Tracking & Monitoring

### Sentry

- **Backend**: `sentry ~> 11.0` ([`apps/backend/mix.exs`](apps/backend/mix.exs))
- **Web**: `@sentry/react ^10.38.0` ([`apps/web/package.json`](apps/web/package.json))
- **Mobile**: `@sentry/react-native ^7.12.0`
  ([`apps/mobile/package.json`](apps/mobile/package.json))
- **Config**: `SENTRY_DSN` env var, `VITE_SENTRY_DSN` for frontend

### OpenTelemetry (Distributed Tracing)

- **Core**: `opentelemetry ~> 1.5`, `opentelemetry_api ~> 1.4`
  ([`apps/backend/mix.exs`](apps/backend/mix.exs))
- **Exporter**: `opentelemetry_exporter ~> 1.8` (OTLP HTTP protobuf)
- **Instrumentations**: `opentelemetry_phoenix ~> 1.2`, `opentelemetry_ecto ~> 1.2`,
  `opentelemetry_oban ~> 1.1`
- **Backend**: Grafana Tempo ([`infrastructure/tempo/tempo.yml`](infrastructure/tempo/tempo.yml))
- **Config**: `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_SAMPLE_RATE` (default 10%)

### Prometheus Metrics

- **Library**: `telemetry_metrics_prometheus_core ~> 1.2`
  ([`apps/backend/mix.exs`](apps/backend/mix.exs))
- **Telemetry**: `telemetry_metrics ~> 1.1`, `telemetry_poller ~> 1.1`
- **Endpoint**: `/metrics` on Phoenix backend
- **Scraper**: Prometheus `v2.53.0`
  ([`infrastructure/prometheus/prometheus.yml`](infrastructure/prometheus/prometheus.yml))
- **SLO rules**: [`infrastructure/prometheus/rules/`](infrastructure/prometheus/rules/)

### Grafana (Dashboards & Alerting)

- **Local**: Grafana `11.1.0`
  ([`infrastructure/docker-compose.observability.yml`](infrastructure/docker-compose.observability.yml))
- **Production**: Grafana Cloud with remote write
  ([`infrastructure/grafana/grafana-cloud-remote-write.yml`](infrastructure/grafana/grafana-cloud-remote-write.yml))
- **Alloy sidecar**: Grafana Alloy `v1.13.1` in backend Docker image for metrics shipping
- **Custom dashboards**: [`infrastructure/grafana/dashboards/`](infrastructure/grafana/dashboards/)
- **Alerts**: [`infrastructure/grafana/alerts/`](infrastructure/grafana/alerts/)

### Log Aggregation

- **Loki** — log storage ([`infrastructure/loki/`](infrastructure/loki/))
- **Promtail** — log shipping ([`infrastructure/promtail/`](infrastructure/promtail/))
- **Structured JSON logging** in production (`CGraph.Telemetry.JsonFormatter`)

### Alerting

- **Alertmanager** `v0.27.0` — alert routing
- **Channels**: **Slack** (webhook), **PagerDuty** (service key)
- **Config**: `SLACK_WEBHOOK_URL`, `PAGERDUTY_SERVICE_KEY`

---

## 9. Analytics & Performance

### Web Vitals

- **Landing**: `web-vitals ^5.1.0` ([`apps/landing/package.json`](apps/landing/package.json))
- **Vercel**: `@vercel/speed-insights ^1.3.1`
  ([`apps/landing/package.json`](apps/landing/package.json))

### Charts/Visualization

- **Web**: `recharts ^3.7.0` ([`apps/web/package.json`](apps/web/package.json))

### Load Testing

- **Tool**: k6 ([`infrastructure/load-tests/k6/`](infrastructure/load-tests/k6/))
- **CI workflow**: [`load-test.yml`](.github/workflows/load-test.yml)

---

## 10. Webhooks

### Incoming Webhooks

| Source     | Purpose                              | Module                                                                                                      |
| ---------- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------- |
| **Stripe** | Payment events, subscription changes | [`apps/backend/lib/cgraph_web/controllers/`](apps/backend/lib/cgraph_web/controllers/) (webhook controller) |

### Outgoing Webhooks

- **Module**: [`apps/backend/lib/cgraph/webhooks/`](apps/backend/lib/cgraph/webhooks/)
- **Files**: `endpoint.ex`, `endpoints.ex`, `deliveries.ex`, `delivery.ex`, `signature.ex`
- **Purpose**: User-configurable webhook endpoints for external integrations
- **Features**: Delivery tracking, signature verification, retry logic

---

## 11. Third-Party SDKs & Libraries

### HTTP Clients (Backend)

| Library           | Version                      | Purpose             |
| ----------------- | ---------------------------- | ------------------- |
| `tesla ~> 1.15`   | HTTP client with middleware  | General API calls   |
| `finch ~> 0.20`   | HTTP client (Swoosh adapter) | Email sending       |
| `hackney ~> 1.20` | HTTP client                  | ExAws/S3 operations |
| `req ~> 0.5`      | Modern HTTP client           | AI/LLM client       |

### HTTP Client (Frontend)

| Library              | Version     | Purpose                                |
| -------------------- | ----------- | -------------------------------------- |
| `axios ^1.9.0`       | HTTP client | API calls from web & mobile            |
| `@cgraph/api-client` | workspace   | Resilient wrapper with circuit breaker |

### Background Jobs

- **Oban** `~> 2.20` — PostgreSQL-backed job queue
- **Workers (cron-scheduled)**:
  - `ScheduledMessageWorker` — Process scheduled messages (every minute)
  - `StatusExpiryWorker` — Clear expired custom statuses (every minute)
  - `EmailDigestWorker` — Send email digests (daily 8 AM UTC)
  - `MessageArchivalWorker` — Archive old messages (daily 3 AM UTC)
  - `CleanupLinkPreviewCache` — Clean expired link preview cache (daily 4 AM UTC)
  - `RankingUpdateWorker` — Update forum rankings (hourly) + weekly reset (Monday 00:00 UTC)
  - `DocumentCompactionWorker` — CRDT document compaction check (every 15 min)
  - `DeleteExpiredSecretMessages` — Delete expired secret messages (every minute)
  - `ExpireSecretConversations` — Expire secret conversations past their TTL (every minute)
  - `FileCleanupWorker` — Clean up abandoned/expired file transfers (hourly)
  - `PulseDecayWorker` — Pulse reputation decay (daily 2 AM UTC)
- **Plugins**: Pruner (7-day retention), Lifeline (rescue stalled jobs after 30 min), Cron scheduler
- **Queues** (22 total): `default`, `mailers`, `notifications`, `events`, `cleanup`,
  `notification_retry`, `webhooks`, `exports`, `external_api`, `dead_letter`, `maintenance`,
  `backups`, `push_notifications`, `email_notifications`, `archival`, `search`, `critical`,
  `emails`, `media`, `sync`, `link_previews`, `rankings`

### Circuit Breaker

- **Backend**: `fuse ~> 2.5` ([`apps/backend/mix.exs`](apps/backend/mix.exs)),
  `CGraph.CircuitBreaker` module
- **Frontend**: Built into `@cgraph/api-client` package

### Internationalization

- **Web**: `i18next ^25.8.11`, `react-i18next ^16.5.4`
  ([`apps/web/package.json`](apps/web/package.json))
- **Language detection**: `i18next-browser-languagedetector ^8.2.1`
- **Backend loading**: `i18next-http-backend ^3.0.2`

### Content Sanitization

- **Web**: `dompurify ^3.3.2` ([`apps/web/package.json`](apps/web/package.json))
- **Backend**: `html_sanitize_ex ~> 1.4` ([`apps/backend/mix.exs`](apps/backend/mix.exs))

### Date/Time

- **Frontend**: `date-fns ^4.1.0` (web), `^4.1.0` (mobile)
- **Backend**: `timex ~> 3.7` ([`apps/backend/mix.exs`](apps/backend/mix.exs))

### Validation

- **Frontend**: `zod ^3.24.0` ([`apps/web/package.json`](apps/web/package.json),
  [`packages/utils/package.json`](packages/utils/package.json))

### Markdown

- **Web**: `react-markdown ^10.0.0`, `remark-gfm ^4.0.1`
  ([`apps/web/package.json`](apps/web/package.json))

### PDF Generation

- **Web**: `jspdf ^4.0.0` ([`apps/web/package.json`](apps/web/package.json))

### QR Codes

- **Web**: `qrcode.react ^4.2.0` ([`apps/web/package.json`](apps/web/package.json))
- **Mobile**: `react-native-qrcode-svg ^6.3.21`
  ([`apps/mobile/package.json`](apps/mobile/package.json))

### Drag & Drop

- **Web**: `@dnd-kit/core ^6.3.1`, `@dnd-kit/sortable ^10.0.0`
  ([`apps/web/package.json`](apps/web/package.json))

### 3D Graphics (Dev/Visual)

- **Web devDeps**: `three ^0.182.0`, `@react-three/fiber ^9.5.0`, `@react-three/drei ^10.7.7`,
  `@react-three/postprocessing ^3.0.4`

### Animations

- **Web**: `motion ^12.0.0` (Framer Motion), `gsap ^3.14.2`, `lottie-web ^5.13.0` (Lottie avatar
  borders)
- **Mobile**: `react-native-reanimated ~4.1.1`, `react-native-gesture-handler ~2.28.0`,
  `lottie-react-native ^7.3.6`
- **Shared**: `@cgraph/animation-constants` workspace package

### Confetti

- **Web**: `canvas-confetti ^1.9.4` ([`apps/web/package.json`](apps/web/package.json))

---

## 12. Security Infrastructure

### CORS

- **Library**: `corsica ~> 2.1` ([`apps/backend/mix.exs`](apps/backend/mix.exs))

### Rate Limiting

- **Backend modules**:
  [`apps/backend/lib/cgraph/rate_limiter/`](apps/backend/lib/cgraph/rate_limiter/)
- **Distributed**: Redis-backed (`CGraph.RateLimiter.Distributed`)
- **Local fallback**: Cachex-based when Redis unavailable
- **CDN-level**: Cloudflare rate limiting
  ([`infrastructure/terraform/rate_limiting.tf`](infrastructure/terraform/rate_limiting.tf))

### WAF (Web Application Firewall)

- **Config**: [`infrastructure/terraform/waf.tf`](infrastructure/terraform/waf.tf) via Cloudflare

### Security Headers

- **Config**: [`infrastructure/terraform/headers.tf`](infrastructure/terraform/headers.tf) via
  Cloudflare
- **Docker**: X-Content-Type-Options, X-Frame-Options, Referrer-Policy in web Dockerfile

### Idempotency

- **Module**: [`apps/backend/lib/cgraph/idempotency/`](apps/backend/lib/cgraph/idempotency/)

### Audit Logging

- **Module**: [`apps/backend/lib/cgraph/audit/`](apps/backend/lib/cgraph/audit/)

### Feature Flags

- **Module**: [`apps/backend/lib/cgraph/feature_flags/`](apps/backend/lib/cgraph/feature_flags/)

---

## 13. AI Integration (Feature-Flagged — Disabled)

- **Status**: Modules implemented with heuristic fallbacks; LLM integration feature-flagged off
  ([`apps/backend/config/runtime.exs`](apps/backend/config/runtime.exs))
- **Provider**: Anthropic (Claude)
- **Model**: `claude-4-opus` (configured but `enabled: false`)
- **HTTP client**: `req ~> 0.5` (LLM client in `CGraph.AI.LLMClient`)
- **Implemented modules** ([`apps/backend/lib/cgraph/ai/`](apps/backend/lib/cgraph/ai/)):
  - `LLMClient` — HTTP client for OpenAI, Anthropic, Ollama APIs
  - `Moderation` — Content moderation with keyword fallback
  - `SmartReplies` — Reply suggestion with heuristic fallback
  - `Sentiment` — Sentiment analysis with heuristic fallback
  - `Summarizer` — Conversation summarization with heuristic fallback
- **Features** (all disabled via feature flag): forum moderation, chat suggestions, content
  moderation, smart search
- **Channel**: `ai_channel.ex` in
  [`apps/backend/lib/cgraph_web/channels/`](apps/backend/lib/cgraph_web/channels/)

---

## 14. CDN & DNS

### Cloudflare

- **Services**: DNS, CDN, Pages (web hosting), R2 (storage), WAF, rate limiting, SSL/TLS
- **Terraform managed**: [`infrastructure/terraform/`](infrastructure/terraform/)
- **CDN config**:
  [`infrastructure/cloudflare/cdn_configuration.yaml`](infrastructure/cloudflare/cdn_configuration.yaml)
- **DNS records**:
  - `@` / `www` → Vercel (landing)
  - `web` / `app` → Cloudflare Pages (web app)
    > **Note**: `apps/web/vercel.json` also exists with Vercel deployment config. Terraform DNS is
    > authoritative (points to `cgraph-web.pages.dev`).
  - Backend → Fly.io

---

## 15. Mobile-Specific Integrations

| Package                                     | Version    | Purpose                  |
| ------------------------------------------- | ---------- | ------------------------ |
| `expo-notifications`                        | `~0.32.16` | Push notifications       |
| `expo-secure-store`                         | `~15.0.0`  | Secure key-value storage |
| `expo-camera`                               | `~17.0.0`  | Camera access            |
| `expo-contacts`                             | `^15.0.11` | Contact access           |
| `expo-haptics`                              | `~15.0.0`  | Haptic feedback          |
| `expo-audio`                                | `~1.1.1`   | Audio playback           |
| `expo-video`                                | `~3.0.15`  | Video playback           |
| `expo-clipboard`                            | `~8.0.0`   | Clipboard access         |
| `expo-linking`                              | `~8.0.0`   | Deep linking             |
| `expo-web-browser`                          | `~15.0.0`  | In-app browser           |
| `expo-blur`                                 | `~15.0.8`  | Blur effects             |
| `expo-linear-gradient`                      | `~15.0.8`  | Gradient views           |
| `expo-splash-screen`                        | `~31.0.0`  | Splash screen            |
| `expo-device`                               | `~8.0.0`   | Device info              |
| `expo-constants`                            | `~18.0.13` | Expo constants           |
| `expo-auth-session`                         | `~7.0.0`   | OAuth session handling   |
| `expo-asset`                                | `~12.0.12` | Asset management         |
| `expo-font`                                 | `~14.0.0`  | Custom font loading      |
| `expo-status-bar`                           | `~3.0.0`   | Status bar control       |
| `@react-native-community/netinfo`           | `^11.3.1`  | Network status           |
| `@react-native-community/datetimepicker`    | `8.4.4`    | Date/time picker         |
| `@react-native-community/slider`            | `5.0.1`    | Slider component         |
| `@react-native-masked-view/masked-view`     | `0.3.2`    | Masked views             |
| `@react-native-async-storage/async-storage` | `2.2.0`    | Persistent storage       |
| `react-native-iap`                          | `^14.7.12` | In-app purchases         |

Source: [`apps/mobile/package.json`](apps/mobile/package.json)

---

## 16. Integration Summary Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                         │
├──────────┬──────────┬──────────┬──────────┬────────────────┤
│ Stripe   │ Resend   │ Sentry   │ Google   │ Cloudflare     │
│ (billing)│ (email)  │ (errors) │ (OAuth)  │ (CDN/DNS/R2)   │
├──────────┼──────────┼──────────┼──────────┼────────────────┤
│ Apple    │ Facebook │ TikTok   │ Discord  │ Grafana Cloud  │
│ (OAuth)  │ (OAuth)  │ (OAuth)  │ (OAuth)  │ (monitoring)   │
├──────────┴──────────┼──────────┼──────────┼────────────────┤
│ Expo Push           │ Supabase │ Vercel   │ EAS Build      │
│ (notifications)     │ (DB)     │ (landing)│ (mobile CI)    │
└─────────────────────┴──────────┴──────────┴────────────────┘
                              │
                    ┌─────────▼─────────┐
                    │  Phoenix/Elixir   │  ← Fly.io
                    │  Backend API      │
                    ├───────────────────┤
                    │ PostgreSQL 16     │  ← Supabase / PgBouncer
                    │ Redis 7           │  ← Rate limit, cache, PubSub
                    │ MeiliSearch v1.12 │  ← Full-text search
                    └───────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
    │ React 19     │  │ React Native │  │ React 19     │
    │ Vite 6       │  │ Expo 54      │  │ Vite 6       │
    │ Web App      │  │ Mobile App   │  │ Landing Page │
    │ (CF Pages)   │  │ (iOS/Android)│  │ (Vercel)     │
    └──────────────┘  └──────────────┘  └──────────────┘
```
