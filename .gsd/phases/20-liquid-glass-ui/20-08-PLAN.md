---
phase: 20-liquid-glass-ui
plan: 08
type: execute
wave: 3
depends_on: ['20-01']
files_modified:
  - apps/web/src/pages/auth/**/*.tsx
  - apps/web/src/pages/settings/**/*.tsx
  - apps/web/src/pages/customize/**/*.tsx
  - apps/web/src/pages/security/**/*.tsx
  - apps/web/src/pages/profile/**/*.tsx
  - apps/web/src/pages/social/**/*.tsx
autonomous: true

must_haves:
  truths:
    - 'Auth pages (login, register, onboarding, forgot/reset password) use glass card layouts'
    - 'Settings pages (theme customization, badges, blocked users, two-factor, data export, etc.)
      use glass surfaces'
    - 'Customize pages use glass panel styling'
    - 'Security pages (E2EE verification, key verification) use glass surfaces'
    - 'All auth, settings, and profile stores remain untouched'
  artifacts:
    - path: 'apps/web/src/pages/auth/login.tsx'
      provides: 'Glass auth card layout'
      contains: 'glassSurface'
    - path: 'apps/web/src/pages/settings/settings.tsx'
      provides: 'Glass settings page'
      contains: 'glassSurface'
  key_links:
    - from: 'pages/auth/'
      to: 'useAuthStore'
      via: 'store hook unchanged'
      pattern: 'useAuthStore'
---

<objective>
Upgrade all auth, settings, customize, security, profile, and social page-level components (~68 files) from legacy dark styling to liquid-glass surfaces.

Purpose: Page-level components wrap module-level components. Upgrading them ensures the entire page
background, card containers, and page layouts use glass — not just the interior widgets.

Output: ~68 upgraded page files across auth/, settings/, customize/, security/, profile/, and
social/ subdirectories of pages/. </objective>

<execution_context> ../skills/execute-plan/SKILL.md @.gsd/templates/summary.md </execution_context>

<context>
@.gsd/phases/20-liquid-glass-ui/PLAN.md
@apps/web/src/components/liquid-glass/shared.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade auth pages and settings pages</name>
  <files>
    apps/web/src/pages/auth/ (all .tsx files with bg-dark/bg-gray, ~15 files)
    apps/web/src/pages/settings/ (all .tsx files with bg-dark/bg-gray, ~33 files)
  </files>
  <action>
Find ALL files with `bg-dark-` or `bg-gray-` in `pages/auth/` and `pages/settings/` using:
```bash
grep -rl "bg-dark-\|bg-gray-" pages/auth/ pages/settings/
```

Apply glass recipes to each:

**pages/auth/ pattern (~15 files)**:

- `login.tsx`, `register/`, `forgot-password.tsx`, `reset-password/` → Page card containers should
  use `glassSurface` centered on the page. Form inputs should already inherit glass styling from
  upgraded input.tsx (Plan 20-01).
- `onboarding/` (7 step files + progress bar) → Step cards → `glassSurface`. Progress bar track →
  `bg-white/[0.06]`. Step content areas → `bg-white/[0.04]`.
- `o-auth-callback.tsx` → Loading state → `bg-white/[0.04]`.
- Main containers: `bg-dark-900` → `bg-transparent` (let app-layout handle background). Inner cards:
  `bg-dark-800` → `glassSurface`.

**pages/settings/ pattern (~33 files)**:

- `settings.tsx` → Main settings page shell → `glassSurface` or transparent (let layout handle).
- `theme-customization/` (6 files) → Tab content panels → `bg-white/[0.04]`. Preview panels →
  `bg-white/[0.04] border border-white/[0.06]`. Theme/avatar/chat/effects tabs → glass card
  sections.
- `badge-selection/` (5 files) → Badge cards → `bg-white/[0.04] hover:bg-white/[0.08]`. Selected →
  `bg-white/[0.12] ring-1 ring-white/[0.16]`. Preview modal → `glassSurfaceElevated`. Filter bar →
  glass pills.
- `two-factor-setup/` (5 files) → Step cards → `glassSurface`. QR code area → `bg-white/[0.04]`.
  Progress indicator → glass dots.
- `blocked-users/` (4 files) → User cards → `bg-white/[0.04]`. Confirm modal →
  `glassSurfaceElevated`. Search bar → glass input.
- `custom-emoji/` (2 files) → Emoji list → `bg-white/[0.04]` items. Upload modal →
  `glassSurfaceElevated`.
- `rss-feeds/` (2 files) → Feed cards → `bg-white/[0.04]`. Page container → `glassSurface`.
- `title-selection/` (3 files) → Title cards → `bg-white/[0.04] hover:bg-white/[0.08]`. Preview
  modal → `glassSurfaceElevated`.
- Standalone: `app-theme-settings.tsx`, `connected-accounts.tsx`, `data-export.tsx`,
  `delete-account.tsx`, `storage-management.tsx` → All use `glassSurface` for card containers.

MUST NOT change: Auth flow logic, form validation, 2FA verification, settings save handlers,
TanStack Query hooks. </action> <verify> Run
`grep -rn "bg-dark-\|bg-gray-" apps/web/src/pages/auth/ apps/web/src/pages/settings/ | grep -v __tests__ | grep -v stories | wc -l`
— zero matches. Run
`cd apps/web && npx tsc --noEmit --pretty false 2>&1 | grep -E "pages/(auth|settings)/" | head -10`
— zero type errors. </verify> <done>All auth and settings pages use glass surfaces.</done> </task>

<task type="auto">
  <name>Task 2: Upgrade customize, security, profile, and social pages</name>
  <files>
    apps/web/src/pages/customize/ (all .tsx files with bg-dark/bg-gray, ~12 files)
    apps/web/src/pages/security/ (all .tsx files with bg-dark/bg-gray, ~2 files)
    apps/web/src/pages/profile/ (all .tsx files with bg-dark/bg-gray, ~3 files)
    apps/web/src/pages/social/ (all .tsx files with bg-dark/bg-gray, ~3 files)
  </files>
  <action>
Find ALL files using:
```bash
grep -rl "bg-dark-\|bg-gray-" pages/customize/ pages/security/ pages/profile/ pages/social/
```

**pages/customize/ (~12 files)**: These are customization pages (avatar, theme, effects, etc.).
Apply:

- Page containers → `glassSurface` or `bg-transparent`.
- Card sections → `bg-white/[0.04] rounded-xl`.
- Picker grids → `bg-white/[0.04]` items, selected → `bg-white/[0.12]`.
- Preview areas → `bg-white/[0.04] border border-white/[0.06]`.
- Buttons → preserve brand colors for primary, glass for secondary.

**pages/security/ (2 files)**:

- `e2-ee-verification-page.tsx` → Glass verification card `glassSurface`. Safety number display →
  `bg-white/[0.04] font-mono`.
- `key-verification-page.tsx` → Glass verification UI. Fingerprint display → `bg-white/[0.04]`.

**pages/profile/ (~3 files)**: Profile display pages → `glassSurface` for profile card. Stats →
`bg-white/[0.04]`.

**pages/social/ (~3 files)**: Social pages (friends-tab, discover-tab, social.tsx) → Glass
containers. List items → `bg-white/[0.04] hover:bg-white/[0.08]`. Discovery cards → `glassSurface`.

MUST NOT change: E2EE verification logic, profile data fetching, social friend logic, customization
store hooks. </action> <verify> Run
`grep -rn "bg-dark-\|bg-gray-" apps/web/src/pages/customize/ apps/web/src/pages/security/ apps/web/src/pages/profile/ apps/web/src/pages/social/ | grep -v __tests__ | grep -v stories | wc -l`
— zero matches. Run
`cd apps/web && npx tsc --noEmit --pretty false 2>&1 | grep -E "pages/(customize|security|profile|social)/" | head -10`
— zero type errors. </verify> <done>All customize, security, profile, and social pages use glass
surfaces.</done> </task>

</tasks>

<verification>
1. `cd apps/web && npx tsc --noEmit` — zero TypeScript errors
2. `grep -rn "bg-dark-\|bg-gray-" apps/web/src/pages/auth/ apps/web/src/pages/settings/ apps/web/src/pages/customize/ apps/web/src/pages/security/ apps/web/src/pages/profile/ apps/web/src/pages/social/ | grep -v __tests__ | grep -v stories | wc -l` — zero
</verification>

<success_criteria>

- Zero `bg-dark-*` / `bg-gray-*` in auth, settings, customize, security, profile, social page files
- Auth pages use glassSurface for card layouts
- Settings customization tabs use glass surfaces
- Security verification pages use glass
- TypeScript compiles with zero errors </success_criteria>

<output>
After completion, create `.gsd/phases/20-liquid-glass-ui/20-08-SUMMARY.md`
</output>
