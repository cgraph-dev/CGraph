---
phase: 20-liquid-glass-ui
plan: 09
type: execute
wave: 3
depends_on: ['20-01']
files_modified:
  - apps/web/src/pages/forums/**/*.tsx
  - apps/web/src/pages/groups/**/*.tsx
  - apps/web/src/pages/messages/**/*.tsx
autonomous: true

must_haves:
  truths:
    - 'Forum board view, post view, admin panels, and moderation queue pages use glass surfaces'
    - 'Group channel pages and group component pages use glass styling'
    - 'Message pages use glass surfaces'
    - 'All forum, group, and chat stores remain untouched'
  artifacts:
    - path: 'apps/web/src/pages/forums/'
      provides: 'Glass forum page shells'
    - path: 'apps/web/src/pages/groups/'
      provides: 'Glass group page shells'
  key_links:
    - from: 'pages/forums/'
      to: 'useForumStore'
      via: 'store hook unchanged'
      pattern: 'useForumStore'
---

<objective>
Upgrade all forums, groups, and messages page-level components (~81 files) from legacy dark styling to liquid-glass surfaces.

Purpose: Forums pages alone have 60 files across board views, post views, admin panels, creation
forms, leaderboards, and moderation queues. Combined with groups and messages pages, this completes
the most content-heavy page areas.

Output: ~81 upgraded page files. </objective>

<execution_context> ../skills/execute-plan/SKILL.md @.gsd/templates/summary.md </execution_context>

<context>
@.gsd/phases/20-liquid-glass-ui/PLAN.md
@apps/web/src/components/liquid-glass/shared.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade forums pages (~60 files)</name>
  <files>
    apps/web/src/pages/forums/ (all .tsx files with bg-dark/bg-gray)
  </files>
  <action>
Find ALL files using:
```bash
grep -rl "bg-dark-\|bg-gray-" pages/forums/
```

This directory has ~60 files across these subdirectories:

- `forum-board-view/` (10 files) — Board listing pages, category views
- `forum-admin/panels/` (9 files) — Admin configuration panels
- `forum-post/` (8 files) — Individual post view, replies
- `moderation-queue/` (6 files) — Moderation queue pages
- `create-forum/` (6 files) — Forum creation wizard
- `forums/` (5 files) — Forum index pages
- `forum-leaderboard/` (5 files) — Leaderboard pages
- Root-level forum files (5 files) — Forum shell pages

Apply these patterns consistently to ALL files:

**Page shells / containers**: `bg-dark-900/950` → `bg-transparent` (let app-layout background show
through). Inner content areas → `bg-white/[0.02]`.

**Card containers**: `bg-dark-800` → `glassSurface rounded-xl`. `bg-dark-700` →
`bg-white/[0.04] rounded-lg`.

**List items / rows**: `bg-dark-700 hover:bg-dark-600` →
`bg-white/[0.04] hover:bg-white/[0.08] transition-colors`.

**Form inputs**: `bg-dark-700 border-dark-600` →
`bg-white/[0.06] border-white/[0.08] focus:border-white/[0.16]`.

**Modals / overlays**: `bg-dark-800` → `glassSurfaceElevated`.

**Dividers / borders**: `border-dark-600/700` → `border-white/[0.06]`.

**Sidebar nav items**: `bg-dark-700` → `bg-white/[0.04]`, active → `bg-white/[0.12]`.

**Tab content areas**: `bg-dark-800` → `bg-white/[0.04]`.

**Stat cards / metrics**: `bg-dark-700` → `bg-white/[0.04] border border-white/[0.06]`.

Import `glassSurface` and/or `glassSurfaceElevated` from `@/components/liquid-glass/shared` in files
that use card-level surfaces.

MUST NOT change: Forum store hooks, forum admin settings logic, moderation actions, post CRUD
operations, leaderboard query logic. </action> <verify> Run
`grep -rn "bg-dark-\|bg-gray-" apps/web/src/pages/forums/ | grep -v __tests__ | grep -v stories | wc -l`
— zero or near-zero. Run
`cd apps/web && npx tsc --noEmit --pretty false 2>&1 | grep "pages/forums/" | head -10` — zero type
errors. </verify> <done>All forum pages use glass surfaces. Zero bg-dark/bg-gray in
pages/forums/.</done> </task>

<task type="auto">
  <name>Task 2: Upgrade groups and messages pages (~21 files)</name>
  <files>
    apps/web/src/pages/groups/ (all .tsx files with bg-dark/bg-gray, ~14 files)
    apps/web/src/pages/messages/ (all .tsx files with bg-dark/bg-gray, ~7 files)
  </files>
  <action>
Find ALL files using:
```bash
grep -rl "bg-dark-\|bg-gray-" pages/groups/ pages/messages/
```

**pages/groups/ (~14 files)**:

- `group-channel/` (8 files) — Channel view pages, message areas, channel headers
- `components/` (5 files) — Group page-level components (group info, member sidebar, etc.)
- Root files (~1) — Group shell page

Apply:

- Channel page containers → `bg-transparent` or `bg-white/[0.02]`.
- Channel content areas → matching chat glass styling (consistent with Plan 20-02/20-03).
- Group info sidebars → `glassSurface`.
- Member lists → `bg-white/[0.04]` items with `hover:bg-white/[0.08]`.
- Channel headers → glass bar `bg-[rgb(30,32,40)]/[0.72] backdrop-blur-[16px]`.

**pages/messages/ (~7 files)**: Message pages (likely inbox, DM views). Apply same chat-consistent
glass patterns:

- Message list containers → `bg-transparent`.
- Message item cards → `bg-white/[0.04] hover:bg-white/[0.08]`.
- Input areas → inherit from glass input primitives.

MUST NOT change: Group channel logic, message fetch/send handlers, group socket connections,
`useGroupStore`, `useChatStore`. </action> <verify> Run
`grep -rn "bg-dark-\|bg-gray-" apps/web/src/pages/groups/ apps/web/src/pages/messages/ | grep -v __tests__ | grep -v stories | wc -l`
— zero. Run
`cd apps/web && npx tsc --noEmit --pretty false 2>&1 | grep -E "pages/(groups|messages)/" | head -10`
— zero type errors. </verify> <done>All group and message pages use glass surfaces.</done> </task>

</tasks>

<verification>
1. `cd apps/web && npx tsc --noEmit` — zero TypeScript errors
2. `grep -rn "bg-dark-\|bg-gray-" apps/web/src/pages/forums/ apps/web/src/pages/groups/ apps/web/src/pages/messages/ | grep -v __tests__ | grep -v stories | wc -l` — zero
</verification>

<success_criteria>

- Zero `bg-dark-*` / `bg-gray-*` in pages/forums/, pages/groups/, pages/messages/ source files
- Forum pages use glassSurface consistently across all views
- Group channel pages match chat glass styling
- TypeScript compiles with zero errors </success_criteria>

<output>
After completion, create `.gsd/phases/20-liquid-glass-ui/20-09-SUMMARY.md`
</output>
