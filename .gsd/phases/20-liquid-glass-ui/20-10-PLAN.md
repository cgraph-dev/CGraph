---
phase: 20-liquid-glass-ui
plan: 10
type: execute
wave: 3
depends_on: ['20-01']
files_modified:
  - apps/web/src/pages/admin/**/*.tsx
  - apps/web/src/pages/calls/**/*.tsx
  - apps/web/src/pages/community/**/*.tsx
  - apps/web/src/pages/creator/**/*.tsx
  - apps/web/src/pages/explore/**/*.tsx
  - apps/web/src/pages/friends/**/*.tsx
  - apps/web/src/pages/gamification/**/*.tsx
  - apps/web/src/pages/leaderboard/**/*.tsx
  - apps/web/src/pages/notifications/**/*.tsx
  - apps/web/src/pages/premium/**/*.tsx
  - apps/web/src/pages/search/**/*.tsx
  - apps/web/src/pages/not-found.tsx
autonomous: true

must_haves:
  truths:
    - 'Admin dashboard tabs use glass card surfaces'
    - 'Premium pricing, coin shop, and creator pages use glass styling'
    - 'Leaderboard, gamification, and community pages use glass surfaces'
    - 'Friends, calls, notifications, search, and explore pages use glass'
    - 'The 404 page uses glass styling'
    - 'All admin, premium, and gamification stores remain untouched'
  artifacts:
    - path: 'apps/web/src/pages/admin/'
      provides: 'Glass admin dashboard'
    - path: 'apps/web/src/pages/premium/'
      provides: 'Glass premium pages'
  key_links:
    - from: 'pages/admin/'
      to: 'TanStack Query hooks'
      via: 'query hooks unchanged'
      pattern: 'useQuery|useMutation'
---

<objective>
Upgrade all remaining page-level components (~61 files) — admin, calls, community, creator, explore, friends, gamification, leaderboard, notifications, premium, search, and the 404 page.

Purpose: This final plan completes the page-level glass upgrade across every remaining page
directory, ensuring 100% glass surface coverage.

Output: ~61 upgraded page files across 12 page subdirectories + not-found.tsx. </objective>

<execution_context> ../skills/execute-plan/SKILL.md @.gsd/templates/summary.md </execution_context>

<context>
@.gsd/phases/20-liquid-glass-ui/PLAN.md
@apps/web/src/components/liquid-glass/shared.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade admin, premium, creator, gamification, and leaderboard pages</name>
  <files>
    apps/web/src/pages/admin/ (~9 files)
    apps/web/src/pages/premium/ (~7 files)
    apps/web/src/pages/creator/ (~4 files)
    apps/web/src/pages/gamification/ (~7 files)
    apps/web/src/pages/leaderboard/ (~8 files)
  </files>
  <action>
Find ALL files using:
```bash
grep -rl "bg-dark-\|bg-gray-" pages/admin/ pages/premium/ pages/creator/ pages/gamification/ pages/leaderboard/
```

**pages/admin/ (~9 files)**: `admin-dashboard.tsx` → `glassSurface` dashboard container. Tab pages
(audit, reports, settings, users, overview) → glass card sections. `ban-user-modal.tsx` →
`glassSurfaceElevated`. User management (user-row, user-search-bar, users-pagination) →
`bg-white/[0.04]` rows, glass search input, glass pagination buttons.

**pages/premium/ (~7 files)**: Pricing cards → `glassSurface` with accent borders for popular plan.
`coin-shop/` → shop item cards `bg-white/[0.04] hover:bg-white/[0.08]`. Feature comparison → glass
table. Plan selector → glass pills.

**pages/creator/ (~4 files)**: `creator-dashboard.tsx` → `glassSurface` dashboard.
`analytics-page.tsx` → glass chart containers. `earnings-page.tsx` → glass stat cards.
`payout-page.tsx` → glass payout cards.

**pages/gamification/ (~7 files)**: Achievement pages, quest boards → `glassSurface` containers.
Badge display → `bg-white/[0.04]` cards. Progress sections → glass bars.

**pages/leaderboard/ (~8 files)**: `sections/` (5 files) → glass section cards. Podium → glass with
gradient accent for top 3. User rows → `bg-white/[0.04] hover:bg-white/[0.08]`. Main leaderboard →
`glassSurface`.

MUST NOT change: Admin RBAC, TanStack Query hooks (admin config, audit log, user list, reports),
Stripe integration, gamification logic, leaderboard queries. </action> <verify> Run
`grep -rn "bg-dark-\|bg-gray-" apps/web/src/pages/admin/ apps/web/src/pages/premium/ apps/web/src/pages/creator/ apps/web/src/pages/gamification/ apps/web/src/pages/leaderboard/ | grep -v __tests__ | grep -v stories | wc -l`
— zero. Run
`cd apps/web && npx tsc --noEmit --pretty false 2>&1 | grep -E "pages/(admin|premium|creator|gamification|leaderboard)/" | head -10`
— zero type errors. </verify> <done>Admin, premium, creator, gamification, and leaderboard pages all
use glass surfaces.</done> </task>

<task type="auto">
  <name>Task 2: Upgrade calls, community, friends, notifications, search, explore, and not-found pages</name>
  <files>
    apps/web/src/pages/calls/ (~6 files)
    apps/web/src/pages/community/ (~6 files)
    apps/web/src/pages/friends/ (~6 files)
    apps/web/src/pages/notifications/ (~3 files)
    apps/web/src/pages/search/ (~2 files)
    apps/web/src/pages/explore/ (~2 files)
    apps/web/src/pages/not-found.tsx
  </files>
  <action>
Find ALL files using:
```bash
grep -rl "bg-dark-\|bg-gray-" pages/calls/ pages/community/ pages/friends/ pages/notifications/ pages/search/ pages/explore/ pages/not-found.tsx
```

**pages/calls/ (~6 files)**: Call history → `bg-white/[0.04]` list items. Call screen → glass
controls overlay. Video tiles → `bg-white/[0.04] rounded-xl`. Call headers → glass bar.

**pages/community/ (~6 files)**: `user-leaderboard/` (6 files) → Top 3 spotlight → glass cards with
accent. User cards → `bg-white/[0.04] hover:bg-white/[0.08]`. Loading skeleton →
`bg-white/[0.06] animate-pulse`. Pagination → glass buttons.

**pages/friends/ (~6 files)**: Friends list → `bg-white/[0.04]` items with presence dots. Friend
request cards → `glassSurface`. Action buttons → glass secondary buttons.

**pages/notifications/ (~3 files)**: Notification items → `bg-white/[0.04] hover:bg-white/[0.08]`.
Unread → `bg-white/[0.06]`. Page container → transparent.

**pages/search/ (~2 files)**: Search header → glass search bar. Result items →
`bg-white/[0.04] hover:bg-white/[0.08]`.

**pages/explore/ (~2 files)**: Explore cards → `glassSurface`. Category indicators → glass pills.

**pages/not-found.tsx**: Glass 404 card → `glassSurface` centered with `rounded-xl`. Error text →
keep light text. Home link → `LGButton` or glass button `bg-white/[0.06] hover:bg-white/[0.10]`.

MUST NOT change: Call logic, friend request handlers, notification mark-as-read, search query logic.
</action> <verify> Run
`grep -rn "bg-dark-\|bg-gray-" apps/web/src/pages/calls/ apps/web/src/pages/community/ apps/web/src/pages/friends/ apps/web/src/pages/notifications/ apps/web/src/pages/search/ apps/web/src/pages/explore/ apps/web/src/pages/not-found.tsx | grep -v __tests__ | grep -v stories | wc -l`
— zero. Run
`cd apps/web && npx tsc --noEmit --pretty false 2>&1 | grep -E "pages/(calls|community|friends|notifications|search|explore)/" | head -10`
— zero type errors. </verify> <done>All remaining pages use glass surfaces. Zero bg-dark/bg-gray
across all page directories.</done> </task>

</tasks>

<verification>
1. `cd apps/web && npx tsc --noEmit` — zero TypeScript errors
2. `grep -rn "bg-dark-\|bg-gray-" apps/web/src/pages/ | grep -v __tests__ | grep -v stories | wc -l` — zero
3. Full page coverage confirmed across all 20+ page subdirectories
</verification>

<success_criteria>

- Zero `bg-dark-*` / `bg-gray-*` in any pages/ source file (excluding tests/stories)
- Admin dashboard, premium pricing, and creator pages use glass
- All community, friends, call, and notification pages use glass
- 404 page uses glass styling
- TypeScript compiles with zero errors
- **PHASE MILESTONE: With this plan complete, zero bg-dark/bg-gray should remain in the entire
  apps/web/src/ directory (excluding tests/stories)** </success_criteria>

<output>
After completion, create `.gsd/phases/20-liquid-glass-ui/20-10-SUMMARY.md`
</output>
