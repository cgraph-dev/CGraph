---
phase: 20-liquid-glass-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/ui/card.tsx
  - apps/web/src/components/ui/modal.tsx
  - apps/web/src/components/ui/button.tsx
  - apps/web/src/components/ui/input.tsx
  - apps/web/src/components/ui/text-area.tsx
  - apps/web/src/components/ui/select.tsx
  - apps/web/src/components/ui/badge.tsx
  - apps/web/src/components/ui/tooltip.tsx
  - apps/web/src/components/ui/popover.stories.tsx
  - apps/web/src/components/ui/skeleton.tsx
  - apps/web/src/components/ui/skeletons/admin-skeleton.tsx
  - apps/web/src/components/ui/skeletons/channel-list-skeleton.tsx
  - apps/web/src/components/ui/skeletons/conversation-skeleton.tsx
  - apps/web/src/components/ui/skeletons/explore-skeleton.tsx
  - apps/web/src/components/ui/skeletons/forum-skeleton.tsx
  - apps/web/src/components/ui/skeletons/message-skeleton.tsx
  - apps/web/src/components/ui/skeletons/settings-skeleton.tsx
  - apps/web/src/components/ui/skeletons/user-card-skeleton.tsx
  - apps/web/src/components/ui/empty-state.tsx
  - apps/web/src/components/ui/glass-card.tsx
  - apps/web/src/components/ui/animated-avatar/animated-avatar.tsx
  - apps/web/src/components/ui/animated-avatar/avatar-style-picker.tsx
  - apps/web/src/components/ui/avatar.tsx
  - apps/web/src/components/ui/profile-photo-viewer.tsx
autonomous: true

must_haves:
  truths:
    - 'Every shared UI primitive renders with frosted-glass styling instead of opaque
      bg-dark-*/bg-gray-* backgrounds'
    - 'Existing CVA variant APIs and component prop interfaces remain unchanged'
    - 'All skeleton loaders use translucent bg-white/[0.06] animate-pulse instead of bg-dark-600/700'
    - 'Modal and popover surfaces use glassSurfaceElevated for proper depth'
  artifacts:
    - path: 'apps/web/src/components/ui/card.tsx'
      provides: 'Glass surface card primitive'
      contains: 'glassSurface'
    - path: 'apps/web/src/components/ui/modal.tsx'
      provides: 'Glass elevated modal'
      contains: 'glassSurfaceElevated'
    - path: 'apps/web/src/components/ui/skeleton.tsx'
      provides: 'Translucent skeleton loader'
      contains: 'bg-white/[0.06]'
  key_links:
    - from: 'apps/web/src/components/ui/card.tsx'
      to: 'components/liquid-glass/shared.ts'
      via: 'import glassSurface'
      pattern: 'import.*glassSurface.*from.*liquid-glass/shared'
    - from: 'apps/web/src/components/ui/modal.tsx'
      to: 'components/liquid-glass/shared.ts'
      via: 'import glassSurfaceElevated'
      pattern: 'import.*glassSurfaceElevated.*from.*liquid-glass/shared'
---

<objective>
Replace all legacy `bg-dark-*` / `bg-gray-*` styling in `components/ui/` with liquid-glass design system surfaces.

Purpose: These shared UI primitives (card, modal, button, input, select, badge, tooltip, popover,
skeleton, empty-state, avatar) are consumed by every feature module. Upgrading them first propagates
glass styling everywhere they're rendered.

Output: ~24 upgraded components in `components/ui/` using `glassSurface`, `glassSurfaceElevated`,
and inline glass Tailwind recipes from `components/liquid-glass/shared.ts`. </objective>

<execution_context> ../skills/execute-plan/SKILL.md @.gsd/templates/summary.md </execution_context>

<context>
@.gsd/phases/20-liquid-glass-ui/PLAN.md
@apps/web/src/components/liquid-glass/shared.ts
@apps/web/src/components/liquid-glass/tokens.css
@apps/web/src/components/ui/card.tsx
@apps/web/src/components/ui/modal.tsx
@apps/web/src/components/ui/input.tsx
@apps/web/src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade core interactive primitives (card, modal, button, input, select, badge)</name>
  <files>
    apps/web/src/components/ui/card.tsx
    apps/web/src/components/ui/modal.tsx
    apps/web/src/components/ui/button.tsx
    apps/web/src/components/ui/input.tsx
    apps/web/src/components/ui/text-area.tsx
    apps/web/src/components/ui/select.tsx
    apps/web/src/components/ui/badge.tsx
    apps/web/src/components/ui/avatar.tsx
    apps/web/src/components/ui/profile-photo-viewer.tsx
  </files>
  <action>
For each file, import `glassSurface` and/or `glassSurfaceElevated` from `@/components/liquid-glass/shared` and replace legacy classes:

**card.tsx**: Replace `bg-dark-800 border-dark-700` → `${glassSurface} rounded-lg`. Replace hover
`bg-dark-750` → `hover:bg-white/[0.08]`. Preserve all CVA variants and the interactive prop
behavior.

**modal.tsx**: Replace `dark:bg-gray-900` → `${glassSurfaceElevated} rounded-xl`. Replace footer
`dark:bg-gray-800/50 dark:border-gray-700` → `bg-white/[0.04] border-white/[0.06]`. Replace cancel
button `dark:bg-gray-800 dark:border-gray-600` →
`bg-white/[0.06] border-white/[0.08] hover:bg-white/[0.10]`. Replace close button hover
`dark:hover:bg-gray-800` → `hover:bg-white/[0.08]`.

**button.tsx**: Do NOT replace existing CVA variants wholesale. Add a new `glass` variant:
`bg-white/[0.06] border border-white/[0.08] hover:bg-white/[0.12] text-white backdrop-blur-sm`.
Preserve all existing variant names and their current styles.

**input.tsx**: Replace `dark:bg-gray-800 dark:border-gray-600` →
`bg-white/[0.06] border-white/[0.08] focus:border-white/[0.16] focus:ring-1 focus:ring-white/[0.08]`.
Replace disabled `dark:disabled:bg-gray-900` → `disabled:bg-white/[0.02]`. Apply same pattern to
textarea variant.

**text-area.tsx**: Same glass input recipe as input.tsx.

**select.tsx**: Replace any `bg-dark-*` / `bg-gray-*` → glass input recipe for trigger,
`glassSurfaceElevated` for dropdown panel.

**badge.tsx**: Replace `bg-dark-*` → translucent glass pill:
`bg-white/[0.08] border border-white/[0.06] text-white/80`.

**avatar.tsx**: Replace `bg-dark-*` / `bg-gray-*` fallback backgrounds → `bg-white/[0.06]`.

**profile-photo-viewer.tsx**: Replace `bg-dark-*` overlays → `bg-black/60 backdrop-blur-md`.

MUST NOT change: Component prop interfaces, ref forwarding, CVA variant API signatures, any store
hooks. </action> <verify> Run
`cd apps/web && npx tsc --noEmit --pretty false 2>&1 | grep "components/ui/" | head -20` — zero type
errors in modified files. Run
`grep -rn "bg-dark-\|bg-gray-" apps/web/src/components/ui/card.tsx apps/web/src/components/ui/modal.tsx apps/web/src/components/ui/input.tsx apps/web/src/components/ui/badge.tsx`
— zero matches. </verify> <done>card, modal, button, input, text-area, select, badge, avatar,
profile-photo-viewer all use glass surfaces. Zero bg-dark/bg-gray in these 9 files.</done> </task>

<task type="auto">
  <name>Task 2: Upgrade display primitives (tooltip, popover, skeleton, empty-state, glass-card, animated-avatar)</name>
  <files>
    apps/web/src/components/ui/tooltip.tsx
    apps/web/src/components/ui/skeleton.tsx
    apps/web/src/components/ui/skeletons/admin-skeleton.tsx
    apps/web/src/components/ui/skeletons/channel-list-skeleton.tsx
    apps/web/src/components/ui/skeletons/conversation-skeleton.tsx
    apps/web/src/components/ui/skeletons/explore-skeleton.tsx
    apps/web/src/components/ui/skeletons/forum-skeleton.tsx
    apps/web/src/components/ui/skeletons/message-skeleton.tsx
    apps/web/src/components/ui/skeletons/settings-skeleton.tsx
    apps/web/src/components/ui/skeletons/user-card-skeleton.tsx
    apps/web/src/components/ui/empty-state.tsx
    apps/web/src/components/ui/glass-card.tsx
    apps/web/src/components/ui/animated-avatar/animated-avatar.tsx
    apps/web/src/components/ui/animated-avatar/avatar-style-picker.tsx
  </files>
  <action>
**tooltip.tsx**: Replace `bg-dark-*` / `bg-gray-*` → `glassSurfaceElevated` for tooltip container. Add `text-sm` if not present.

**skeleton.tsx + all skeletons/\*.tsx**: Replace every `bg-dark-600`, `bg-dark-700`, `bg-dark-800` →
`bg-white/[0.06]`. The animate-pulse class stays. Replace `border-dark-*` → `border-white/[0.04]`.
Apply this pattern to ALL 8 skeleton files in the skeletons/ directory.

**empty-state.tsx**: Replace `bg-dark-700/800` → `${glassSurface}`. Keep icon/text layout unchanged.

**glass-card.tsx**: This component is from a PRE-liquid-glass era. Align it with `LGCard` from
`components/liquid-glass/lg-card.tsx`. Import `glassSurface` from shared.ts and use it as the base
className. If glass-card.tsx has its own module CSS (`GlassCard.module.css`), keep it but update the
dark-mode values to match glass tokens. Alternatively, if glass-card.tsx is simple enough, re-export
`LGCard` as `GlassCard` for backward compatibility.

**animated-avatar.tsx**: Replace `bg-dark-600/700/800` backgrounds → `bg-white/[0.06]`. MUST
preserve `useAvatarStyle` store hook and all avatar animation logic.

**avatar-style-picker.tsx**: Replace `bg-dark-*` → `bg-white/[0.06]` for picker items,
`bg-white/[0.12]` for selected state.

MUST NOT change: skeleton animation timing, empty-state layout, avatar animation/store logic.
</action> <verify> Run
`grep -rn "bg-dark-" apps/web/src/components/ui/skeleton.tsx apps/web/src/components/ui/skeletons/ apps/web/src/components/ui/empty-state.tsx apps/web/src/components/ui/tooltip.tsx apps/web/src/components/ui/glass-card.tsx apps/web/src/components/ui/animated-avatar/`
— zero matches. Run
`cd apps/web && npx tsc --noEmit --pretty false 2>&1 | grep "components/ui/" | head -20` — zero type
errors. </verify> <done>All display primitives use translucent glass styling. Zero bg-dark/bg-gray
remaining in components/ui/ directory (excluding \*.stories.tsx test files).</done> </task>

</tasks>

<verification>
1. `cd apps/web && npx tsc --noEmit` — zero TypeScript errors
2. `grep -rn "bg-dark-" apps/web/src/components/ui/*.tsx apps/web/src/components/ui/animated-avatar/*.tsx apps/web/src/components/ui/skeletons/*.tsx | grep -v stories | grep -v test` — zero matches
3. `cd apps/web && npx vitest run --reporter=verbose 2>&1 | tail -5` — all existing tests pass
</verification>

<success_criteria>

- Zero `bg-dark-*` / `bg-gray-*` classes in any `components/ui/` source file (excluding
  stories/tests)
- `glassSurface` or `glassSurfaceElevated` imported from `liquid-glass/shared` in card.tsx,
  modal.tsx
- All component prop APIs unchanged (no breaking changes to consumers)
- TypeScript compiles with zero errors </success_criteria>

<output>
After completion, create `.gsd/phases/20-liquid-glass-ui/20-01-SUMMARY.md`
</output>
