---
phase: 24-landing-page-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/landing/package.json
  - apps/landing/src/data/landing-data.ts
  - apps/landing/src/data/pricing-data.ts
  - apps/landing/src/constants.ts
autonomous: true

must_haves:
  truths:
    - "Landing package.json version matches all other packages (1.0.0)"
    - "Feature descriptions in landing-data.ts reflect actual implemented capabilities"
    - "Pricing tiers match backend TierFeatures module"
    - "Footer links and external links point to correct, working URLs"
  artifacts:
    - path: "apps/landing/package.json"
      provides: "Synced version 1.0.0"
      contains: '"version": "1.0.0"'
    - path: "apps/landing/src/data/landing-data.ts"
      provides: "Accurate feature and security data"
    - path: "apps/landing/src/data/pricing-data.ts"
      provides: "Pricing tiers matching backend"
  key_links:
    - from: "apps/landing/src/data/landing-data.ts"
      to: "apps/landing/src/components/marketing/sections/Features.tsx"
      via: "features array import"
    - from: "apps/landing/src/data/pricing-data.ts"
      to: "apps/landing/src/components/marketing/sections/Pricing.tsx"
      via: "pricingTiers import"
---

<objective>
Sync the landing site version to 1.0.0 and update all core data files (landing-data.ts, pricing-data.ts, constants.ts) to reflect the actual current state of the CGraph platform at v1.0.0.

Purpose: The landing package.json is stuck at 0.9.47 while every other package is at 1.0.0. Feature descriptions, pricing tiers, and links must match what's actually shipped and working.
Output: Updated data files with accurate v1.0.0 information.
</objective>

<execution_context>
../.github/skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@apps/landing/package.json
@apps/landing/src/data/landing-data.ts
@apps/landing/src/data/pricing-data.ts
@apps/landing/src/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync landing package.json version to 1.0.0</name>
  <files>apps/landing/package.json</files>
  <action>
    Change `"version": "0.9.47"` to `"version": "1.0.0"` in apps/landing/package.json.
    
    This is the ONLY package in the monorepo still at 0.9.47 — all others (web, mobile, backend, root, all shared packages) are already at 1.0.0.
  </action>
  <verify>Run `grep '"version"' apps/landing/package.json` — should show "1.0.0".</verify>
  <done>"version" field in apps/landing/package.json reads "1.0.0".</done>
</task>

<task type="auto">
  <name>Task 2: Update landing-data.ts feature descriptions and footer links</name>
  <files>apps/landing/src/data/landing-data.ts</files>
  <action>
    Review and update the `features` array (12 items), `securityFeatures` array (10 items), `valueProps` array (4 items), and `footerLinks` object for accuracy:

    **features array — items to review/update:**
    - "Voice & Video Calls" description: Currently says "powered by LiveKit SFU". Verify whether LiveKit is the actual SFU in use or if it's WebRTC with Phoenix Channels for signaling. Based on PROJECT.md capabilities list in About.tsx, calls are "WebRTC-powered 1:1 and group calls with screen sharing. Built on Phoenix Channels for signaling." Update description to: "Group calls with screen sharing, E2EE indicators, and voice channels — powered by WebRTC with Phoenix Channels signaling."
    - "Premium Subscriptions" description: Shows "$14.99/mo" and "$29.99/mo" — verify these match pricing-data.ts (they do). Keep as-is.
    - All other feature descriptions appear accurate based on PROJECT.md validated requirements.

    **securityFeatures array — no changes needed.** All 10 items use careful, accurate language that avoids over-claiming.

    **valueProps array — no changes needed.** All 4 items are accurate.

    **footerLinks — review:**
    - "API Reference" under resources links to `/docs` (same as Documentation). Consider changing to `/docs#api` to deep-link to the API section. This is a minor UX improvement.
    - All other footer links point to correct pages.

    **IMPORTANT:** Do NOT change the exported types or the interface shapes. Only update string content within existing data structures.
  </action>
  <verify>Run `npx tsc --noEmit -p apps/landing/tsconfig.app.json 2>&1 | tail -5` — should show no errors related to landing-data.ts.</verify>
  <done>Feature descriptions accurately reflect v1.0.0 capabilities. No broken TypeScript.</done>
</task>

<task type="auto">
  <name>Task 3: Verify pricing-data.ts accuracy</name>
  <files>apps/landing/src/data/pricing-data.ts</files>
  <action>
    Review pricing-data.ts for accuracy against the platform:
    
    **Current state (likely accurate, verify):**
    - Free: $0, 5 forums, 25MB uploads, basic gamification, 3 themes
    - Premium: $14.99/mo ($12.49 annual), unlimited forums, 500MB, scheduled messages, 7+ themes, battle pass, animated borders
    - Enterprise: $29.99/mo ($24.99 annual), 2GB uploads, API access, SSO "coming soon", analytics, SLA

    **Updates needed:**
    - Enterprise tier `cta` is "Contact Us" with `ctaLink: "https://cgraph.org/contact"` — this should use the LANDING_URL constant or at minimum be a relative path `/contact`. Update to: `ctaLink: '/contact'` for consistency with internal routing.
    - SSO "coming soon" label — keep this, it's still planned.
    - All other pricing data appears to match the planned tier structure.
  </action>
  <verify>Run `npx tsc --noEmit -p apps/landing/tsconfig.app.json 2>&1 | tail -5` — no TS errors.</verify>
  <done>Pricing data is verified accurate. Enterprise CTA link uses relative path.</done>
</task>

</tasks>

<verification>
- `grep '"version"' apps/landing/package.json` shows "1.0.0"
- `npx tsc --noEmit -p apps/landing/tsconfig.app.json` passes
- Feature descriptions are factual and match PROJECT.md validated requirements
</verification>

<success_criteria>
- Landing package version synced to 1.0.0
- All feature, security, and value prop descriptions match actual v1.0.0 capabilities
- Pricing data verified accurate
- Footer links correctly point to existing pages
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.gsd/phases/24-landing-page-update/24-01-SUMMARY.md`
</output>
