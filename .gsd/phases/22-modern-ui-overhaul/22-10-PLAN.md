---
phase: 22-modern-ui-overhaul
plan: 10
type: execute
wave: 4
depends_on: ["22-03", "22-04", "22-05", "22-06", "22-07", "22-08", "22-09"]
files_modified:
  - apps/web/src/components/ui/loading-states.tsx
  - apps/web/src/components/ui/empty-state.tsx
  - apps/web/src/components/ui/micro-interactions.tsx
  - apps/web/src/components/ui/responsive-container.tsx
  - apps/web/src/shared/components/page-transition.tsx
  - apps/web/src/layouts/app-layout.tsx
  - apps/web/src/styles/responsive.css
  - apps/mobile/src/components/ui/loading-states.tsx
  - apps/mobile/src/components/ui/empty-state.tsx
  - apps/mobile/src/components/ui/haptic-feedback.tsx
  - apps/mobile/src/navigation/tab-bar.tsx
  - apps/mobile/src/navigation/transition-config.ts
autonomous: false

must_haves:
  truths:
    - "Loading states use skeleton screens (not spinners) for all major content areas"
    - "Empty states show contextual illustrations with clear CTAs"
    - "Micro-interactions: button press scales, toggle slides, badge bounces on increment, send button animates on click"
    - "Web layout is responsive across 3 breakpoints: compact (< 768px), normal (768-1200px), wide (> 1200px)"
    - "Page transitions fade/slide smoothly between routes"
    - "Mobile tab bar has animated active indicator that slides between tabs"
    - "Mobile navigation transitions match iOS/Android native feel"
  artifacts:
    - path: "apps/web/src/components/ui/loading-states.tsx"
      provides: "Skeleton loading compositions for all major content types"
    - path: "apps/web/src/components/ui/empty-state.tsx"
      provides: "Contextual empty state component with icon, title, description, CTA"
    - path: "apps/web/src/styles/responsive.css"
      provides: "Responsive breakpoint utilities and container queries"
  key_links:
    - from: "apps/web/src/components/ui/loading-states.tsx"
      to: "all pages using Suspense boundaries"
      via: "Suspense fallback={<LoadingState variant='...' />}"
      pattern: "LoadingState"
    - from: "apps/web/src/layouts/app-layout.tsx"
      to: "responsive.css"
      via: "responsive layout classes"
      pattern: "responsive-"
---

<objective>
Polish pass: skeleton loading states, contextual empty states, micro-interactions, responsive layout, page transitions, and mobile navigation polish.

Purpose: The final 10% that makes the app feel like a shipping product. Discord, Instagram, and Messenger all have meticulously crafted loading states, transitions, and responsive behavior. This plan brings that level of polish.
Output: 12 polish components across web + mobile, plus responsive layout system.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/phases/22-modern-ui-overhaul/22-01-SUMMARY.md
@.gsd/phases/22-modern-ui-overhaul/22-03-SUMMARY.md
@.gsd/phases/22-modern-ui-overhaul/22-07-SUMMARY.md
@apps/web/src/layouts/app-layout.tsx
@apps/web/src/shared/components/page-transition.tsx
@apps/mobile/src/navigation/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web loading states, empty states, micro-interactions, responsive layout</name>
  <files>
    apps/web/src/components/ui/loading-states.tsx
    apps/web/src/components/ui/empty-state.tsx
    apps/web/src/components/ui/micro-interactions.tsx
    apps/web/src/components/ui/responsive-container.tsx
    apps/web/src/shared/components/page-transition.tsx
    apps/web/src/layouts/app-layout.tsx
    apps/web/src/styles/responsive.css
  </files>
  <action>
    Create polish components bringing the app to shipping quality:

    **LoadingStates (new — composition of Skeleton primitive):**
    - Pre-built skeleton compositions for every major content area:
      - `ConversationListLoading`: 8 rows of avatar + 2 text lines
      - `MessageListLoading`: 5 message groups with avatar + text blocks of varying width
      - `ChannelListLoading`: 3 category headers + 4 channel items each
      - `ProfileLoading`: banner rectangle + avatar circle + stats row + tab bar
      - `ForumListLoading`: 4 thread cards in grid
      - `MemberListLoading`: 10 rows of avatar + name + role
      - `SearchResultsLoading`: 6 result rows with icon + text
    - Each uses the Skeleton primitive from Plan 01
    - Exported as named components and also as a `LoadingState` component with `variant` prop

    **EmptyState (upgrade):**
    - Props: `icon` (React node or built-in presets), `title`, `description`, `action` (button props), `secondaryAction`
    - Built-in presets: `no-messages`, `no-friends`, `no-channels`, `no-threads`, `no-results`, `no-notifications`, `no-media`, `error`, `offline`
    - Each preset has a relevant SVG illustration (simple geometric/abstract style, not complex art)
    - Centered vertically and horizontally
    - Animated entry (fade + translate-y)
    - Consistent use across all empty list/grid states in the app

    **MicroInteractions (new — utility hooks + components):**
    - `useHoverScale(scale = 1.02)`: returns motion props for subtle scale on hover
    - `useTapScale(scale = 0.97)`: returns motion props for scale-down on press
    - `AnimatedCounter`: number that animates when value changes (rolls up/down, like Discord member counts)
    - `PulseBadge`: badge component that pulses briefly when count increments (for unread, notifications)
    - `SendAnimation`: button morphs to checkmark on success (for message send, form submit)
    - `ToggleSwitch`: animated toggle with slide + color transition
    - All respect `useMotionSafe()` / `prefers-reduced-motion`

    **ResponsiveContainer (new):**
    - Wrapper component that provides responsive context
    - Breakpoints: compact (< 768px), normal (768-1200px), wide (> 1200px)
    - Uses container queries where supported, media queries as fallback
    - Hook: `useBreakpoint()` returns `'compact' | 'normal' | 'wide'`

    **responsive.css:**
    - Utility classes: `.hide-compact`, `.hide-wide`, `.show-compact-only`, etc.
    - Container query definitions for responsive panels
    - Sidebar auto-collapse on compact breakpoint
    - Member list auto-hide on compact + normal breakpoints
    - Stack layout on compact (sidebar overlays content)

    **PageTransition (upgrade):**
    - AnimatePresence wrapper for route transitions
    - Fade + subtle slide-x (10px) animation between pages
    - Exit animation before enter for clean transitions
    - Shared layout animations where appropriate (e.g., conversation list item → conversation header)
    - Duration: 150ms (fast enough to not feel slow)

    **AppLayout (update):**
    - Apply responsive classes: sidebar behavior changes at breakpoints
    - Compact: hamburger menu toggles sidebar as overlay
    - Normal: sidebar fixed, member list hidden
    - Wide: sidebar + content + member list all visible (3-column Discord layout)
    - Smooth animated transitions between layout modes
  </action>
  <verify>
    `cd apps/web && npx tsc --noEmit --pretty false -p tsconfig.app.json 2>&1 | head -5` — 0 errors.
    Files exist: loading-states.tsx, empty-state.tsx, micro-interactions.tsx, responsive-container.tsx, responsive.css.
  </verify>
  <done>Skeleton loading compositions, contextual empty states, micro-interaction utilities, and responsive 3-breakpoint layout system.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile loading states, empty states, haptics, tab bar, transitions</name>
  <files>
    apps/mobile/src/components/ui/loading-states.tsx
    apps/mobile/src/components/ui/empty-state.tsx
    apps/mobile/src/components/ui/haptic-feedback.tsx
    apps/mobile/src/navigation/tab-bar.tsx
    apps/mobile/src/navigation/transition-config.ts
  </files>
  <action>
    Create mobile polish components:

    **LoadingStates (mobile):**
    - Same variants as web, adapted for mobile dimensions
    - `ConversationListLoading`, `MessageListLoading`, `ChannelListLoading`, `ProfileLoading`, `ForumListLoading`
    - Uses mobile Skeleton primitive from Plan 02
    - Shimmer animation via Reanimated

    **EmptyState (mobile):**
    - Same API as web: icon/preset, title, description, action button
    - Same presets: no-messages, no-friends, no-channels, etc.
    - Centered in available space
    - Animated entry with Reanimated (fade + translateY)

    **HapticFeedback (mobile utility):**
    - `haptic.light()` — for button presses, toggles
    - `haptic.medium()` — for context menus, swipe actions completing
    - `haptic.heavy()` — for destructive actions (delete)
    - `haptic.success()` — for positive confirmations (message sent)
    - `haptic.warning()` — for alerts
    - `haptic.selection()` — for selection changes (picker scroll)
    - Wrapper around expo-haptics that checks if haptics are enabled in user settings
    - NoOp when haptics disabled or on Android without support

    **TabBar (upgrade):**
    - Custom tab bar component (not default React Navigation tab bar)
    - Animated active indicator: pill that slides between tabs using Reanimated (like Instagram)
    - Icons: filled when active, outline when inactive
    - Badge on icons for unread counts (messages, notifications)
    - Middle tab optionally larger (for FAB-style create button if desired)
    - Safe area aware (respects bottom inset)
    - Labels below icons (hide-able via setting)

    **TransitionConfig:**
    - Screen transition animations matching platform conventions:
      - iOS: horizontal slide (push from right) — default React Navigation but tuned
      - Android: fade + slide-up (Material motion)
    - Modal screens: slide up from bottom with backdrop dim
    - Shared element transitions where appropriate (conversation list item → chat header)
    - Duration calibrated: 200ms for navigations, 300ms for modals
    - Export as `screenOptions` preset objects for navigators
  </action>
  <verify>
    `cd apps/mobile && npx tsc --noEmit 2>&1 | grep "error" | head -5` — 0 new errors.
    Files exist: loading-states.tsx, empty-state.tsx, haptic-feedback.tsx, tab-bar.tsx, transition-config.ts.
  </verify>
  <done>Mobile loading states, empty states, haptic system, custom animated tab bar, and polished navigation transitions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 22 UI overhaul across 10 plans:
    - Design token system (web + mobile)
    - Discord-style chat messages with grouped rendering, floating actions, reactions
    - Messenger-style mobile chat with swipe-to-reply, long-press sheets
    - Instagram-style conversation list with stories row, online now
    - Instagram-style profiles with media grid, user card popovers
    - Discord-style server sidebar with channel categories, voice indicators
    - Modern forum cards with voting, rich composer, polls
    - Command palette, search overlay, notification center, call UI
    - Polish: skeleton loading, empty states, micro-interactions, responsive layout
  </what-built>
  <how-to-verify>
    1. Run `cd apps/web && npx vite build 2>&1 | tail -5` — should build cleanly
    2. Run `cd apps/web && npx tsc --noEmit 2>&1 | tail -5` — 0 errors
    3. Run `cd apps/mobile && npx tsc --noEmit 2>&1 | tail -5` — should compile
    4. Start the web dev server: `cd apps/web && npx vite --port 5173` and verify:
       - Chat messages group by author with floating action bar on hover
       - Ctrl+K opens command palette
       - Server sidebar shows channels with categories
       - Profile pages have tabbed layout with media grid
       - Conversation list has rich items with unread badges
       - Responsive: resize browser to see layout adapt at breakpoints
    5. Verify mobile builds: `cd apps/mobile && npx expo start --clear`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All loading states use skeletons (no bare spinners in main content areas)
- Empty states show relevant illustrations and CTAs
- Web layout adapts smoothly across 3 breakpoints
- Page transitions are smooth and fast (< 200ms)
- Mobile tab bar has animated indicator
- Mobile haptics fire on appropriate interactions
- Zero TypeScript errors on both web and mobile
</verification>

<success_criteria>
- 12 polish components across web + mobile
- 7 skeleton loading compositions per platform
- 9 empty state presets
- 6 micro-interaction utilities (web)
- 6 haptic feedback variants (mobile)
- Responsive layout with 3 breakpoints and animated transitions
- Custom animated mobile tab bar
- Platform-appropriate navigation transitions
- Full Phase 22 visual verification by user
- Zero TS errors on both platforms
</success_criteria>

<output>
After completion, create `.gsd/phases/22-modern-ui-overhaul/22-10-SUMMARY.md`
</output>
