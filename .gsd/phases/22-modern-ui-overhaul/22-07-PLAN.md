---
phase: 22-modern-ui-overhaul
plan: 07
type: execute
wave: 3
depends_on: ["22-01", "22-03"]
files_modified:
  - apps/web/src/modules/groups/components/server-sidebar.tsx
  - apps/web/src/modules/groups/components/channel-list.tsx
  - apps/web/src/modules/groups/components/channel-category.tsx
  - apps/web/src/modules/groups/components/channel-list-item.tsx
  - apps/web/src/modules/groups/components/server-header.tsx
  - apps/web/src/modules/groups/components/server-banner.tsx
  - apps/web/src/modules/groups/components/server-icon-bar.tsx
  - apps/web/src/modules/groups/components/voice-channel-users.tsx
  - apps/web/src/modules/groups/components/role-badge.tsx
  - apps/mobile/src/components/groups/server-sidebar.tsx
  - apps/mobile/src/components/groups/channel-list.tsx
  - apps/mobile/src/components/groups/channel-list-item.tsx
  - apps/mobile/src/components/groups/voice-channel-users.tsx
autonomous: true

must_haves:
  truths:
    - "Server sidebar shows channel categories with collapsible sections exactly like Discord"
    - "Channel list items show icon (# text, speaker voice, megaphone announce) + name + unread indicator"
    - "Server icon bar on far left shows circular server icons with unread pip like Discord"
    - "Voice channels show currently connected users with avatars and mute/deafen icons"
    - "Server header shows server name with dropdown menu (invite, settings, channels)"
    - "Server banner shows custom banner image at top of channel list"
    - "Mobile channel list is navigable with swipe-back gesture"
  artifacts:
    - path: "apps/web/src/modules/groups/components/server-sidebar.tsx"
      provides: "Full Discord-style server sidebar with categories and channels"
    - path: "apps/web/src/modules/groups/components/server-icon-bar.tsx"
      provides: "Vertical server icon bar (far left, 72px) with unread indicators"
    - path: "apps/web/src/modules/groups/components/voice-channel-users.tsx"
      provides: "Connected voice users shown under voice channel"
  key_links:
    - from: "apps/web/src/modules/groups/components/server-icon-bar.tsx"
      to: "app layout"
      via: "renders as far-left sidebar column"
      pattern: "ServerIconBar"
    - from: "apps/web/src/modules/groups/components/channel-list.tsx"
      to: "server-sidebar.tsx"
      via: "renders within server sidebar panel"
      pattern: "ChannelList"
---

<objective>
Rebuild group/server UI to match Discord's iconic server sidebar with channel categories, voice channel user lists, server icon bar, and server banners.

Purpose: Groups/servers are a differentiating feature. Discord's sidebar UX with collapsible categories, voice user indicators, and the server icon bar is the gold standard for community navigation.
Output: 13 group/server components across web + mobile.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/phases/22-modern-ui-overhaul/22-01-SUMMARY.md
@apps/web/src/modules/groups/components/
@apps/web/src/pages/groups/
@apps/mobile/src/components/groups/
@apps/mobile/src/screens/groups/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web server icon bar, server sidebar, channel list with categories</name>
  <files>
    apps/web/src/modules/groups/components/server-icon-bar.tsx
    apps/web/src/modules/groups/components/server-sidebar.tsx
    apps/web/src/modules/groups/components/channel-list.tsx
    apps/web/src/modules/groups/components/channel-category.tsx
    apps/web/src/modules/groups/components/channel-list-item.tsx
    apps/web/src/modules/groups/components/server-header.tsx
    apps/web/src/modules/groups/components/server-banner.tsx
  </files>
  <action>
    Create Discord-exact server navigation UI:

    **ServerIconBar (new — Discord far-left sidebar):**
    - Vertical bar, 72px wide, dark background (`bg-[#1e1f22]` equivalent via tokens)
    - "Home" button at top (DM icon) with separator below
    - Server icons: 48px circles, hover expands to rounded-lg (squircle transition)
    - Unread indicator: white pill on left edge (3px wide, 8px tall for unread, extends to 36px tall for active)
    - Active server: white border-radius transition from circle to squircle
    - "Add Server" button at bottom with green "+" icon
    - "Discover" button below that with compass icon
    - Tooltip on hover showing server name (right side)
    - Drag-to-reorder support (or placeholder for it)
    - Uses ScrollArea for overflow servers

    **ServerSidebar:**
    - 240px wide panel to the right of ServerIconBar
    - Contains: ServerHeader (top), optional ServerBanner, ChannelList (scrollable), user bar (bottom)
    - User bar at bottom: own avatar + name + status + mic/headphone/settings icons (exactly like Discord)

    **ServerHeader:**
    - Server name with dropdown chevron
    - Click opens dropdown menu: Invite People, Server Settings, Create Channel, Create Category, Notification Settings, Privacy Settings, Edit Server Profile, Hide Muted Channels, Leave Server (red, at bottom)
    - Animated dropdown entry (motion/react)
    - Shows boost level badge if applicable

    **ServerBanner:**
    - Optional banner image below server header
    - Invite splash screen style — gradient overlay at bottom for readability
    - Only shown if server has a banner set

    **ChannelList:**
    - Renders categories with channels, separated by ChannelCategory components
    - Uncategorized channels at top
    - Uses ScrollArea primitive

    **ChannelCategory:**
    - Collapsible section header: "CATEGORY NAME" (uppercase, small, muted) with arrow icon
    - Click to collapse/expand (persisted in local storage)
    - Shows channel count when collapsed
    - "+" button on hover to add channel to this category

    **ChannelListItem:**
    - Icon based on type: `#` for text, speaker icon for voice, megaphone for announcements, lock for private
    - Channel name (truncated if long)
    - Unread: bold channel name + white dot indicator
    - Mentions: brand-colored badge with count at right
    - Muted: dimmed text + mute icon
    - Hover: background change + action icons (invite, settings) slide in
    - Active: brighter background + bold name
    - Edit mode: inline rename on right-click > Edit
  </action>
  <verify>
    `cd apps/web && npx tsc --noEmit --pretty false -p tsconfig.app.json 2>&1 | head -5` — 0 errors.
    All 7 files export named components.
  </verify>
  <done>Discord-exact server icon bar, sidebar with categories, channels, server header, and banner.</done>
</task>

<task type="auto">
  <name>Task 2: Voice channel users, role badges, mobile server/channel UI</name>
  <files>
    apps/web/src/modules/groups/components/voice-channel-users.tsx
    apps/web/src/modules/groups/components/role-badge.tsx
    apps/mobile/src/components/groups/server-sidebar.tsx
    apps/mobile/src/components/groups/channel-list.tsx
    apps/mobile/src/components/groups/channel-list-item.tsx
    apps/mobile/src/components/groups/voice-channel-users.tsx
  </files>
  <action>
    Complete server UI with voice indicators and mobile adaptation:

    **VoiceChannelUsers (web):**
    - Renders below voice channel item when users are connected
    - Each user: avatar (24px) + name + mute/deafen icons (red slash if muted)
    - Connected count shown in channel item: "Voice — 3"
    - Self is highlighted
    - Animated enter/exit of users (motion/react)
    - Streaming indicator: red live badge if user is screen sharing

    **RoleBadge:**
    - Compact pill: role color dot + role name
    - Hover shows role permissions summary
    - Used in member list, profile header, user card
    - Sizes: sm (member list), md (profile)

    **Mobile ServerSidebar:**
    - Adapted for mobile: full-screen when open, slides in from left
    - Server icon bar as horizontal top row (scrollable) instead of vertical
    - Channel list below with same category structure
    - Swipe-right to go back to conversation list
    - No user bar at bottom (bottom tab bar serves this purpose)

    **Mobile ChannelList:**
    - Flat list of categories and channels
    - Tap to navigate to channel 
    - Category collapse/expand with animated Reanimated height
    - Pull-to-refresh to reload channel list

    **Mobile ChannelListItem:**
    - Icon + channel name + unread badge
    - Tap to navigate
    - Long-press for context menu (mute, notification settings, copy link)
    - Swipe left: mute toggle

    **Mobile VoiceChannelUsers:**
    - Shows connected user avatars in a horizontal row below voice channel
    - Tap to join voice channel
    - Tap user to see profile sheet
  </action>
  <verify>
    `cd apps/web && npx tsc --noEmit --pretty false -p tsconfig.app.json 2>&1 | head -5` — 0 errors.
    `cd apps/mobile && npx tsc --noEmit 2>&1 | grep "error" | head -5` — 0 new errors.
  </verify>
  <done>Voice channel user lists, role badges, and full mobile server/channel navigation.</done>
</task>

</tasks>

<verification>
- Server icon bar shows servers with unread indicators and squircle hover
- Channel categories collapse/expand with persistence
- Channel items show correct icons by type and unread state
- Voice channels show connected users
- Server header dropdown has full menu
- Mobile navigation works with horizontal server bar + swipe gestures
- Zero TypeScript errors
</verification>

<success_criteria>
- 13 group/server components across web + mobile
- Discord-exact server icon bar with unread pills and squircle transitions
- Channel categories are collapsible
- Channel items show type icons, unread/mention badges
- Voice channels show connected users with mute/deafen status
- Server header dropdown with full menu
- Mobile adaptation with horizontal icon bar and swipe navigation
- Zero TS errors
</success_criteria>

<output>
After completion, create `.gsd/phases/22-modern-ui-overhaul/22-07-SUMMARY.md`
</output>
