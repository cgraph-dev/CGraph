---
phase: 22-modern-ui-overhaul
plan: 06
type: execute
wave: 2
depends_on: ["22-01", "22-02"]
files_modified:
  - apps/web/src/modules/social/components/profile-header.tsx
  - apps/web/src/modules/social/components/profile-tabs.tsx
  - apps/web/src/modules/social/components/profile-media-grid.tsx
  - apps/web/src/modules/social/components/profile-about.tsx
  - apps/web/src/modules/social/components/profile-mutual-friends.tsx
  - apps/web/src/modules/social/components/user-card-popover.tsx
  - apps/web/src/modules/social/components/status-composer.tsx
  - apps/web/src/modules/social/components/friend-list-item.tsx
  - apps/mobile/src/screens/profile/profile-screen.tsx
  - apps/mobile/src/components/profile-header.tsx
  - apps/mobile/src/components/profile-media-grid.tsx
  - apps/mobile/src/components/user-card-sheet.tsx
autonomous: true

must_haves:
  truths:
    - "Profile header shows large avatar with gradient ring, cover photo/banner, name, bio, stats row"
    - "Profile has tabbed content sections like Instagram (Posts/Media/About/Mutual Friends)"
    - "Media grid shows 3-column thumbnail layout with tap to view"
    - "Hovering a username anywhere shows user card popover (Discord-style)"
    - "Status composer allows setting custom status with emoji + text + expiry"
    - "Friend list items show avatar, status, mutual friends count"
    - "Mobile profile matches web layout adapted for vertical scroll"
  artifacts:
    - path: "apps/web/src/modules/social/components/profile-header.tsx"
      provides: "Instagram-style profile header with banner, avatar, stats, action buttons"
    - path: "apps/web/src/modules/social/components/user-card-popover.tsx"
      provides: "Discord-style hover card showing user info on username hover"
    - path: "apps/web/src/modules/social/components/profile-media-grid.tsx"
      provides: "Instagram 3-column media grid"
  key_links:
    - from: "apps/web/src/modules/social/components/user-card-popover.tsx"
      to: "anywhere a username is rendered"
      via: "wraps username with Popover trigger"
      pattern: "UserCardPopover"
---

<objective>
Upgrade profile pages and social surfaces to Instagram/Discord quality with rich profile headers, media grids, user card popovers, and custom status system.

Purpose: Profiles are a key identity surface. Instagram's media grid, banner photos, and stats row combined with Discord's hover user cards create a rich social experience that makes users invest in their identity.
Output: 12 profile/social components across web + mobile.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/phases/22-modern-ui-overhaul/22-01-SUMMARY.md
@apps/web/src/pages/profile/
@apps/web/src/modules/social/components/
@apps/mobile/src/screens/profile/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web profile header, tabs, media grid, about section, mutual friends</name>
  <files>
    apps/web/src/modules/social/components/profile-header.tsx
    apps/web/src/modules/social/components/profile-tabs.tsx
    apps/web/src/modules/social/components/profile-media-grid.tsx
    apps/web/src/modules/social/components/profile-about.tsx
    apps/web/src/modules/social/components/profile-mutual-friends.tsx
  </files>
  <action>
    Create Instagram/Discord-quality profile surface:

    **ProfileHeader:**
    - Banner/cover image at top (full width, 200px height, with parallax on scroll if visible)
    - Large avatar (120px) overlapping banner bottom edge, centered or left-aligned
    - Avatar has status ring + border (like Discord)
    - Display name (bold, large), @username (muted), custom status with emoji
    - Stats row: N Messages · N Friends · N Groups · Member since Date
    - Action buttons: Add Friend / Message / Block (contextual based on relationship)
    - If own profile: Edit Profile button
    - Role badges rendered as colored pills
    - Gamification: level badge, XP bar, title/prestige if earned
    - Banner supports custom color fallback if no image uploaded

    **ProfileTabs (new — Instagram-style):**
    - Tab bar below header: Feed (if posts exist), Media, About, Mutual Friends, Badges/Achievements
    - Active tab indicator: animated underline that slides between tabs (motion/react layoutId)
    - Tab content lazy-loaded
    - Sticky tab bar when scrolling past header

    **ProfileMediaGrid (new — Instagram-style):**
    - 3-column grid of shared media (images + videos from messages/posts)
    - Square thumbnails with subtle gap (2px)
    - Hover shows play icon for videos, expand icon for images
    - Click opens lightbox with navigation
    - Infinite scroll / load more pagination
    - Empty state: "No media shared yet"

    **ProfileAbout:**
    - Bio text with full markdown support
    - Info rows: Location, Website (linked), Joined date, Last active
    - Connected accounts (Discord, GitHub, Twitter icons if linked)
    - Custom fields that user has set

    **ProfileMutualFriends:**
    - Grid of mutual friend cards (avatar + name + status)
    - Shows top 12, "View all N mutual friends" link
    - Each card clickable to navigate to that profile
  </action>
  <verify>
    `cd apps/web && npx tsc --noEmit --pretty false -p tsconfig.app.json 2>&1 | head -5` — 0 errors.
    All 5 files export named components.
  </verify>
  <done>Profile page has Instagram-quality header with banner, tabbed content, media grid, about section, and mutual friends.</done>
</task>

<task type="auto">
  <name>Task 2: User card popover, status composer, friend list, mobile profile</name>
  <files>
    apps/web/src/modules/social/components/user-card-popover.tsx
    apps/web/src/modules/social/components/status-composer.tsx
    apps/web/src/modules/social/components/friend-list-item.tsx
    apps/mobile/src/screens/profile/profile-screen.tsx
    apps/mobile/src/components/profile-header.tsx
    apps/mobile/src/components/profile-media-grid.tsx
    apps/mobile/src/components/user-card-sheet.tsx
  </files>
  <action>
    Create social interaction components:

    **UserCardPopover (new — Discord-style):**
    - Triggered on username hover anywhere in the app (with 300ms delay)
    - Card content: Banner color/image at top, avatar overlapping, name, username, custom status
    - Quick actions: Message, Add Friend
    - Connected to the user's profile data
    - Note field (private note visible only to you — like Discord)
    - Roles shown as colored pills
    - Animated entry: scale + fade from anchor point
    - Uses Radix Popover

    **StatusComposer:**
    - Modal/popover for setting custom status
    - Emoji picker for status emoji
    - Text input for status text (max 128 chars)
    - Duration picker: Don't clear, 30 min, 1 hour, 4 hours, Today, This week
    - Preset statuses: "In a meeting", "Focusing", "Gaming", "AFK"
    - Clear status button
    - Save button

    **FriendListItem (upgrade):**
    - Avatar (36px) with status indicator
    - Display name + @username
    - Custom status text (muted, truncated)
    - Mutual friends count badge
    - Action buttons on hover: Message, Call, Remove
    - Context menu on right-click

    **Mobile ProfileScreen:**
    - Vertical ScrollView with pull-to-refresh
    - Same sections as web: banner, avatar, stats, tabs (Media/About/Mutual)
    - But adapted for mobile: full-width banner, stacked layout
    - Tab content as horizontal swipeable pages (like Instagram)
    - Sticky header with name when scrolled past profile header (animated collapse)

    **Mobile ProfileHeader:**
    - Compact version of web header for mobile
    - Avatar 80px, banner full-width 150px
    - Stats row with counts
    - Action buttons: Message (primary), Add Friend (secondary)

    **Mobile ProfileMediaGrid:**
    - 3-column grid using FlatList with numColumns={3}
    - Square thumbnails
    - Tap opens image viewer

    **Mobile UserCardSheet:**
    - Bottom sheet instead of popover (mobile equivalent of UserCardPopover)
    - Triggered by long-press on username
    - Same content: banner, avatar, name, status, actions
    - Haptic feedback on open
  </action>
  <verify>
    `cd apps/web && npx tsc --noEmit --pretty false -p tsconfig.app.json 2>&1 | head -5` — 0 errors.
    `cd apps/mobile && npx tsc --noEmit 2>&1 | grep "error" | head -5` — 0 new errors.
  </verify>
  <done>User card popovers, status composer, friend list, and full mobile profile with Instagram-style layout.</done>
</task>

</tasks>

<verification>
- Profile header renders banner, avatar with status, stats row, action buttons
- Tab bar animates between sections
- Media grid shows 3-column thumbnails
- UserCardPopover appears on username hover
- StatusComposer allows setting custom status with emoji + expiry
- Mobile profile matches web content in mobile-native layout
- Zero TypeScript errors
</verification>

<success_criteria>
- 12 social/profile components across web + mobile
- Instagram-quality profile header with banner, avatar overlay, stats
- Media grid with lightbox
- Discord-style user card popover on hover
- Custom status system with emoji + text + expiry
- Mobile profile with swipeable tabs and collapsing header
- Zero TS errors
</success_criteria>

<output>
After completion, create `.gsd/phases/22-modern-ui-overhaul/22-06-SUMMARY.md`
</output>
