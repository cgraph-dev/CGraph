---
phase: 36-creator-economy
plan: 02
type: execute
wave: 1
depends_on:
  - 35-07
files_modified:
  - apps/backend/lib/cgraph/creators/premium_thread.ex (new — schema, placed in EXISTING creators/
    directory)
  - apps/backend/lib/cgraph/creators/subscription_tier.ex (new — schema for Node-based tiers)
  - apps/backend/lib/cgraph/creators/revenue_split.ex (new — schema)
  - apps/backend/lib/cgraph/creators/creators.ex (update — add delegates for new premium thread +
    tier functions)
  - apps/backend/lib/cgraph/creators/premium_content.ex (new — sub-module for premium thread + tier
    logic)
  - apps/backend/lib/cgraph_web/controllers/api/v1/creator_controller.ex (update — add premium
    thread + tier actions)
  - apps/backend/lib/cgraph_web/router/creator_routes.ex (update — add premium thread + tier routes
    to EXISTING macro)
  - apps/backend/lib/cgraph/workers/revenue_split_worker.ex (new)
  - apps/backend/priv/repo/migrations/<ts>_create_premium_content_tables.exs (new)
autonomous: true
must_haves:
  truths:
    - 'PremiumThread: thread_id, creator_id, price_nodes (integer), subscriber_only (boolean),
      preview_length (integer)'
    - 'SubscriptionTier: creator_id, forum_id, name, price_monthly_nodes, benefits (map/JSONB),
      max_subscribers, active (boolean)'
    - 'RevenueSplit: thread_id, creator_share (decimal), platform_share (decimal), referral_share
      (decimal) — must sum to 1.0'
    - 'Default split: 80% creator / 20% platform (matches existing platform_fee_percent in
      PaidSubscription)'
    - 'RevenueSplitWorker: processes splits on each premium content purchase via :payments Oban
      queue'
    - '⚠️ CreatorPayout schema ALREADY EXISTS at apps/backend/lib/cgraph/creators/creator_payout.ex
      — do NOT recreate'
    - '⚠️ CGraph.Creators facade ALREADY EXISTS with delegates for Connect, PaidSubscription,
      Earnings, Payout — EXTEND it'
    - '⚠️ CreatorRoutes macro ALREADY EXISTS at apps/backend/lib/cgraph_web/router/creator_routes.ex
      — add new routes there'
    - '⚠️ Existing PaidForumSubscription uses Stripe (real money, cents). New SubscriptionTier uses
      Nodes. Both coexist.'
    - 'Status fields use :string with validate_inclusion (NOT Ecto.Enum) — match existing creator
      schemas'
  artifacts:
    - apps/backend/lib/cgraph/creators/premium_thread.ex
    - apps/backend/lib/cgraph/creators/subscription_tier.ex
    - apps/backend/lib/cgraph/creators/revenue_split.ex
    - apps/backend/lib/cgraph/creators/premium_content.ex
    - apps/backend/lib/cgraph/workers/revenue_split_worker.ex
  key_links:
    - '⚠️ EXISTING creator monetization system — apps/backend/lib/cgraph/creators/ (9 files):
      creators.ex (facade), connect_onboarding.ex, content_gate.ex, creator_earning.ex,
      creator_payout.ex, earnings.ex, paid_forum_subscription.ex, paid_subscription.ex, payout.ex.
      ALL new code MUST extend this system, NOT create a parallel one.'
    - '⚠️ EXISTING creator routes — apps/backend/lib/cgraph_web/router/creator_routes.ex has:
      onboard, status, refresh_onboard, update_monetization, subscribe, unsubscribe, balance,
      request_payout, list_payouts, analytics. Add new routes to THIS macro, do NOT create a new
      route module.'
    - '⚠️ EXISTING CreatorPayout schema — apps/backend/lib/cgraph/creators/creator_payout.ex:
      amount_cents, currency, stripe_transfer_id, status (pending|processing|completed|failed). Do
      NOT recreate. Reuse for Node-based payouts too.'
    - '⚠️ EXISTING Earnings module — apps/backend/lib/cgraph/creators/earnings.ex: record_earning,
      get_balance, get_stats, list_earnings. Revenue splits should integrate with this, not replace
      it.'
    - 'Forum context: apps/backend/lib/cgraph/forums/forums.ex — thread references'
    - 'Forum schema: apps/backend/lib/cgraph/forums/forum.ex — has monetization_enabled,
      subscription_price_cents, subscription_currency, tier fields ALREADY'
    - 'Nodes context: apps/backend/lib/cgraph/nodes/nodes.ex — debit_nodes/credit_nodes for Node
      payments'
    - 'Oban config: apps/backend/config/config.exs — :payments queue (5 workers)'
    - 'router.ex: imports CGraphWeb.Router.CreatorRoutes at line 37, calls creator_routes() at line
      137. New routes go in the EXISTING creator_routes macro, NOT a new module.'
    - 'Schema pattern: @primary_key {:id, :binary_id, autogenerate: true}, @foreign_key_type
      :binary_id, @timestamps_opts [type: :utc_datetime]. Status fields use :string +
      validate_inclusion (see creator_payout.ex and paid_forum_subscription.ex for examples).'
---

<objective>
EXTEND the existing creator monetization system with Node-based premium threads, subscription tiers,
and revenue splits. The CGraph.Creators context, CreatorPayout schema, Earnings module, and
CreatorRoutes macro ALL already exist — this plan adds new schemas and functions to the existing
system, not a parallel one.

ATOMIC_PLAN tasks: 3.6 (Premium Thread Schema), 3.7 (Subscription Tier Schema), 3.8 (Creator Payout
— SKIP, already exists), 3.9 (Revenue Split), 3.10 (Premium Content Sub-module), 3.11 (Creator
Controller Extensions), 3.12 (Payout Worker — SKIP, HeldNodesReleaseWorker exists for nodes), 3.13
(Revenue Split Worker). </objective>

<tasks>

<task type="auto">
  <name>Task 1: New premium content schemas + migration (3.6, 3.7, 3.9)</name>
  <files>
    apps/backend/lib/cgraph/creators/premium_thread.ex
    apps/backend/lib/cgraph/creators/subscription_tier.ex
    apps/backend/lib/cgraph/creators/revenue_split.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>3 NEW schemas placed in the EXISTING creators/ directory. PremiumThread: belongs_to thread (CGraph.Forums.Thread) + creator (CGraph.Accounts.User), price_nodes (integer), subscriber_only (boolean), preview_length (integer, default: 200). SubscriptionTier: belongs_to creator + forum, name (:string), price_monthly_nodes (integer), benefits (:map), max_subscribers (integer), active (boolean, default: true). RevenueSplit: belongs_to thread, creator_share (decimal), platform_share (decimal), referral_share (decimal) — validate sum == 1.0. All use @primary_key {:id, :binary_id, autogenerate: true}, @foreign_key_type :binary_id, @timestamps_opts [type: :utc_datetime]. Status fields use :string with validate_inclusion (NOT Ecto.Enum) to match existing creator schemas. ⚠️ Do NOT create CreatorPayout — it already exists at creators/creator_payout.ex.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 2: Premium content sub-module + extend facade (3.10)</name>
  <files>
    apps/backend/lib/cgraph/creators/premium_content.ex
    apps/backend/lib/cgraph/creators/creators.ex
  </files>
  <action>NEW sub-module CGraph.Creators.PremiumContent with: create_premium_thread/3, purchase_thread_access/3 (uses CGraph.Nodes.debit_nodes + records earning via CGraph.Creators.Earnings.record_earning), create_subscription_tier/3, subscribe_to_tier/3, list_creator_tiers/1, calculate_revenue_split/2. EXTEND CGraph.Creators facade (creators.ex) — add defdelegate entries for new PremiumContent functions, following the existing pattern (see Connect, PaidSubscription, Earnings, Payout sections already in the file). Default revenue split: 80% creator / 20% platform (matches existing platform_fee_percent pattern).</action>
</task>

<task type="auto">
  <name>Task 3: Extend existing routes + controller + add worker (3.11, 3.13)</name>
  <files>
    apps/backend/lib/cgraph_web/router/creator_routes.ex
    apps/backend/lib/cgraph_web/controllers/api/v1/creator_controller.ex
    apps/backend/lib/cgraph/workers/revenue_split_worker.ex
  </files>
  <action>EXTEND the EXISTING creator_routes macro in creator_routes.ex — add routes: POST /creator/premium-threads, PUT /threads/:id/purchase, GET /creator/tiers, POST /creator/tiers, PUT /creator/tiers/:id, GET /creator/revenue-splits. EXTEND CreatorController with new actions: create_premium_thread, purchase_access, list_tiers, create_tier, update_tier, revenue_splits. RevenueSplitWorker: Oban worker on :payments queue, triggered on premium content purchase, atomically splits payment per RevenueSplit config using Ecto.Multi. ⚠️ Do NOT create new route module or new controller — extend the existing ones.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
