---
phase: 36-creator-economy
plan: 03
type: execute
wave: 1
depends_on:
  - 35-07
files_modified:
  - apps/backend/lib/cgraph/boosts/boosts.ex (new — context)
  - apps/backend/lib/cgraph/boosts/boost.ex (new — schema)
  - apps/backend/lib/cgraph/boosts/boost_effect.ex (new — schema)
  - apps/backend/lib/cgraph_web/controllers/api/v1/boost_controller.ex (new — under api/v1/ to match
    patterns)
  - apps/backend/lib/cgraph_web/router/boost_routes.ex (new — macro-based route module)
  - apps/backend/lib/cgraph_web/router.ex (update — add `import CGraphWeb.Router.BoostRoutes` +
    `boost_routes()` call)
  - apps/backend/lib/cgraph/compliance/tax_reporter.ex (new — ⚠️ compliance/ directory is NEW)
  - apps/backend/lib/cgraph/compliance/age_gate.ex (new)
  - apps/backend/lib/cgraph/data_export/processor.ex (update — extend EXISTING GDPR export with new
    data sources)
  - apps/backend/priv/repo/migrations/<ts>_create_boosts_tables.exs (new)
autonomous: true
must_haves:
  truths:
    - 'Boost schema: user_id, target_type (:string — "thread"|"post"|"forum"), target_id
      (:binary_id), boost_type (:string — "visibility"|"pinned"|"highlighted"), duration_hours,
      nodes_spent, started_at, expires_at, status (:string — "active"|"expired"|"cancelled")'
    - 'BoostEffect schema: boost_id, effect_type (:string), magnitude (:decimal), applied_at'
    - 'Boost pricing: visibility 50/hr, pinned 200/hr, highlighted 100/hr'
    - 'BoostController: create (POST), list_active (GET), cancel (DELETE)'
    - 'TaxReporter: generates 1099-K equivalent for creators earning > threshold'
    - 'AgeGate: verify age ≥ 13 (COPPA), ≥ 16 for purchases (EU), store consent timestamp'
    - '⚠️ GDPR export: CGraph.DataExport ALREADY EXISTS with 5 modules (Server, Processor,
      Formatter, Storage, Delivery). EXTEND Processor.ex @user_data_sources map with new sources —
      do NOT create a separate compliance/gdpr_export.ex.'
    - 'Status fields use :string with validate_inclusion (NOT Ecto.Enum) — match existing schema
      pattern'
    - '⚠️ router.ex MUST be updated: add `import CGraphWeb.Router.BoostRoutes` and `boost_routes()`
      call'
  artifacts:
    - apps/backend/lib/cgraph/boosts/boosts.ex
    - apps/backend/lib/cgraph/boosts/boost.ex
    - apps/backend/lib/cgraph/boosts/boost_effect.ex
    - apps/backend/lib/cgraph_web/controllers/api/v1/boost_controller.ex
    - apps/backend/lib/cgraph_web/router/boost_routes.ex
    - apps/backend/lib/cgraph/compliance/tax_reporter.ex
    - apps/backend/lib/cgraph/compliance/age_gate.ex
  key_links:
    - '⚠️ EXISTING GDPR export system — apps/backend/lib/cgraph/data_export/ has 5 modules:
      processor.ex (has @user_data_sources map with profile, settings, activity, messages,
      connections, notifications, etc.), formatter.ex, storage.ex, delivery.ex, server.ex. To add
      creator economy data: EXTEND @user_data_sources in processor.ex with new entries like
      cosmetics_inventory, nodes_transactions, paid_dm_files, boost_history. Do NOT create a new
      compliance/gdpr_export.ex — that would duplicate existing functionality.'
    - 'Nodes context: apps/backend/lib/cgraph/nodes/nodes.ex — debit_nodes/4 for boost purchases'
    - 'Forum threads: apps/backend/lib/cgraph/forums/ — thread/post references for boost targets'
    - 'Router pattern: apps/backend/lib/cgraph_web/router.ex imports route modules at lines 27-42
      and calls their macros at lines 128-143. New BoostRoutes MUST be: 1. Imported: `import
      CGraphWeb.Router.BoostRoutes` (add after line 42) 2. Called: `boost_routes()` (add after
      paid_dm_routes) See CGraphWeb.Router.NodesRoutes for the exact macro pattern.'
    - 'Schema conventions: @primary_key {:id, :binary_id, autogenerate: true}, @foreign_key_type
      :binary_id, @timestamps_opts [type: :utc_datetime]. Status fields use :string with
      validate_inclusion.'
---

<objective>
Build content boost system (visibility/pinned/highlighted boosts) and compliance modules
(tax reporting, age gating). EXTEND the existing CGraph.DataExport system for GDPR — do NOT
create a parallel GDPR export module.

ATOMIC_PLAN tasks: 3.14 (Boost Schema), 3.15 (Boost Effects), 3.16 (Boost Controller), 3.17 (Boost
Pricing), 3.18 (Boost Expiry Worker), 3.19 (Tax Reporter), 3.20 (GDPR Extension), 3.21 (Age Gate).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Boost schemas + migration (3.14, 3.15)</name>
  <files>
    apps/backend/lib/cgraph/boosts/boost.ex
    apps/backend/lib/cgraph/boosts/boost_effect.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>Boost: user_id (belongs_to CGraph.Accounts.User), target_type (:string — "thread"|"post"|"forum"), target_id (:binary_id), boost_type (:string — validate_inclusion "visibility"|"pinned"|"highlighted"), duration_hours (:integer), nodes_spent (:integer), started_at (:utc_datetime), expires_at (:utc_datetime), status (:string, default: "active" — validate_inclusion "active"|"expired"|"cancelled"). BoostEffect: belongs_to boost (CGraph.Boosts.Boost), effect_type (:string), magnitude (:decimal), applied_at (:utc_datetime). Use @primary_key {:id, :binary_id, autogenerate: true}, @foreign_key_type :binary_id, @timestamps_opts [type: :utc_datetime]. Migration with indexes on user_id, target_type+target_id, status, expires_at.</action>
</task>

<task type="auto">
  <name>Task 2: Boost context + controller + routes (3.16–3.18)</name>
  <files>
    apps/backend/lib/cgraph/boosts/boosts.ex
    apps/backend/lib/cgraph_web/controllers/api/v1/boost_controller.ex
    apps/backend/lib/cgraph_web/router/boost_routes.ex
    apps/backend/lib/cgraph_web/router.ex
  </files>
  <action>Context CGraph.Boosts: create_boost/3 (validate target exists, calculate price = rate * hours, use CGraph.Nodes.debit_nodes/4, create boost + effect records), list_active_boosts/1, cancel_boost/2 (refund prorated remaining time via CGraph.Nodes.credit_nodes/4, update status to "cancelled"), expire_boosts/0 (called by Oban cron, mark active boosts past expires_at as "expired"). Pricing module attrs: @visibility_rate 50, @pinned_rate 200, @highlighted_rate 100. Controller CGraphWeb.API.V1.BoostController: create (POST /api/v1/boosts), active (GET /api/v1/boosts), cancel (DELETE /api/v1/boosts/:id). Create CGraphWeb.Router.BoostRoutes defmacro boost_routes with scope "/api/v1", CGraphWeb.API.V1. ⚠️ ALSO update router.ex: add `import CGraphWeb.Router.BoostRoutes` after line 42 and `boost_routes()` call after line 143.</action>
</task>

<task type="auto">
  <name>Task 3: Compliance + GDPR extension (3.19–3.21)</name>
  <files>
    apps/backend/lib/cgraph/compliance/tax_reporter.ex
    apps/backend/lib/cgraph/compliance/age_gate.ex
    apps/backend/lib/cgraph/data_export/processor.ex
  </files>
  <action>TaxReporter (CGraph.Compliance.TaxReporter): generate_report/2 (creator_id, year) — aggregate earnings from CGraph.Creators.Earnings, calculate against threshold ($600 for US 1099-K), return report data. AgeGate (CGraph.Compliance.AgeGate): verify_age/2 (date_of_birth, country) — COPPA check (≥13, or ≥16 for EU purchases), record_consent/3 (user_id, consent_type, timestamp). ⚠️ GDPR: do NOT create compliance/gdpr_export.ex — instead EXTEND apps/backend/lib/cgraph/data_export/processor.ex: add new entries to the @user_data_sources map for cosmetics_inventory, nodes_transactions, paid_dm_files, boost_history, creator_earnings. Each entry follows the existing {Module, :export_function} pattern.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
