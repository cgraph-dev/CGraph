---
phase: 36-creator-economy
plan: 04
type: execute
wave: 2
depends_on:
  - 36-01
  - 36-02
  - 36-03
files_modified:
  - apps/web/src/modules/paid-dm/pages/paid-dm-settings-page.tsx (new)
  - apps/web/src/modules/paid-dm/components/paid-file-card.tsx (new)
  - apps/web/src/modules/paid-dm/components/file-unlock-modal.tsx (new)
  - apps/web/src/modules/creator/pages/creator-dashboard-page.tsx (new — ⚠️ module directory ALREADY
    EXISTS, add page to it)
  - apps/web/src/modules/creator/components/earnings-chart.tsx (new — add to EXISTING module)
  - apps/web/src/modules/creator/components/premium-thread-manager.tsx (new — add to EXISTING
    module)
  - apps/web/src/modules/creator/hooks/useCreatorDashboard.ts (update — ⚠️ ALREADY EXISTS, extend
    with premium content data)
  - apps/web/src/modules/creator/store/creatorStore.ts (update — ⚠️ ALREADY EXISTS, extend with
    premium content state)
  - apps/web/src/modules/creator/services/creatorService.ts (update — ⚠️ ALREADY EXISTS, extend with
    premium content API calls)
  - apps/web/src/modules/forums/components/boost-panel.tsx (new)
  - apps/web/src/modules/forums/components/premium-thread-gate.tsx (new)
autonomous: true
must_haves:
  truths:
    - 'Paid DM settings page: enable/disable, set price, accepted file types, auto-accept friends
      toggle'
    - 'PaidFileCard: file preview (blurred), price badge, unlock button'
    - 'FileUnlockModal: confirm payment, show balance, unlock → reveal animation'
    - 'Creator dashboard: earnings graph (daily/weekly/monthly), payout history, subscriber count'
    - 'Premium thread manager: create/edit premium threads, set price_nodes, toggle subscriber_only,
      preview_length'
    - 'Boost panel: select boost type, duration slider, price calculator, purchase flow'
    - 'Premium thread gate: preview content + "Unlock for X Nodes" CTA'
    - '⚠️ modules/creator/ ALREADY EXISTS with hooks/useCreator.ts, hooks/useCreatorDashboard.ts,
      services/creatorService.ts, store/creatorStore.ts, store/creatorStore.types.ts. EXTEND these
      files — do NOT create them from scratch.'
    - '⚠️ Subscription manager ALREADY EXISTS at modules/forums/components/subscription-manager/
      (has subscription-manager.tsx, useSubscriptions.ts, types.ts). This handles FORUM notification
      subscriptions. The new premium-thread-manager.tsx is DIFFERENT — it manages Node-priced
      premium thread tiers, NOT notification subscriptions. Name it premium-thread-manager.tsx to
      avoid confusion.'
  artifacts:
    - apps/web/src/modules/paid-dm/ (page + 2 components)
    - apps/web/src/modules/creator/pages/creator-dashboard-page.tsx
    - apps/web/src/modules/creator/components/earnings-chart.tsx
    - apps/web/src/modules/creator/components/premium-thread-manager.tsx
    - apps/web/src/modules/forums/components/boost-panel.tsx
    - apps/web/src/modules/forums/components/premium-thread-gate.tsx
  key_links:
    - '⚠️ EXISTING creator module — apps/web/src/modules/creator/ ALREADY has: hooks/useCreator.ts,
      hooks/useCreatorDashboard.ts (fetches earnings, subscribers, analytics),
      services/creatorService.ts (API calls for onboarding, monetization, subscriptions, payouts),
      store/creatorStore.ts + creatorStore.types.ts (Zustand store with CreatorProfile, earnings).
      New files go IN this existing structure. Extend existing hooks/services/store with premium
      content functionality — do NOT duplicate them.'
    - '⚠️ EXISTING subscription manager —
      apps/web/src/modules/forums/components/subscription-manager/ handles notification-level forum
      subscriptions. The new premium-thread-manager is for Node-based content monetization. These
      are SEPARATE concerns.'
    - 'DM module: apps/web/src/modules/chat/ — integrate paid file card into conversation view'
    - 'Forum thread view: apps/web/src/modules/forums/components/thread-view/'
    - 'Nodes store: apps/web/src/modules/nodes/ — has components (bundle-card,
      content-unlock-overlay, tip-button, tip-modal, transaction-row, withdrawal-modal), hooks,
      services, store, types. Use for balance checks and payment flows.'
---

<objective>
Build web frontend for creator economy: paid DM file UI (NEW module), EXTEND the existing creator
module with dashboard page + premium thread manager, add boost panel and premium thread gate to forums.

ATOMIC_PLAN tasks: 3.22 (Paid DM Settings UI), 3.23 (Paid File Card + Unlock Modal), 3.24 (Creator
Dashboard), 3.25 (Premium Thread Manager UI), 3.26 (Boost Panel), 3.27 (Premium Thread Gate UI).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Paid DM web UI (3.22, 3.23)</name>
  <files>
    apps/web/src/modules/paid-dm/pages/paid-dm-settings-page.tsx
    apps/web/src/modules/paid-dm/components/paid-file-card.tsx
    apps/web/src/modules/paid-dm/components/file-unlock-modal.tsx
  </files>
  <action>NEW module. Settings page: toggle, price input (min 10 nodes), file type checkboxes, auto-accept friends. PaidFileCard: blurred preview, file name, price badge, "Unlock" button. FileUnlockModal: confirm dialog, balance display from nodes store, debit → reveal with animation. Use existing useNodesStore for balance checks.</action>
</task>

<task type="auto">
  <name>Task 2: EXTEND creator dashboard + premium thread manager (3.24, 3.25)</name>
  <files>
    apps/web/src/modules/creator/pages/creator-dashboard-page.tsx
    apps/web/src/modules/creator/components/earnings-chart.tsx
    apps/web/src/modules/creator/components/premium-thread-manager.tsx
    apps/web/src/modules/creator/hooks/useCreatorDashboard.ts
    apps/web/src/modules/creator/store/creatorStore.ts
    apps/web/src/modules/creator/services/creatorService.ts
  </files>
  <action>NEW page creator-dashboard-page.tsx in EXISTING module: total earnings, active subscribers, revenue chart (recharts/visx), uses EXISTING useCreatorDashboard hook. NEW earnings-chart.tsx: time-series chart component. NEW premium-thread-manager.tsx: create/edit premium threads, set price_nodes, subscriber_only toggle, preview_length slider — this is DIFFERENT from the existing subscription-manager (which handles notification subscriptions). EXTEND existing useCreatorDashboard.ts with premium thread data fetching. EXTEND existing creatorService.ts with premium thread + tier API calls. EXTEND existing creatorStore.ts with premium thread state (premiumThreads, tiers arrays).</action>
</task>

<task type="auto">
  <name>Task 3: Boost panel + premium thread gate (3.26, 3.27)</name>
  <files>
    apps/web/src/modules/forums/components/boost-panel.tsx
    apps/web/src/modules/forums/components/premium-thread-gate.tsx
  </files>
  <action>BoostPanel: type selector (visibility/pinned/highlighted), duration slider, live price calculator (rate * hours), confirm + pay via nodes store debit. PremiumThreadGate: partial content preview (first N chars based on preview_length), blur overlay, "Unlock for X Nodes" CTA, subscribe tier alternative option. Both in existing forums/components/ directory.</action>
  <verify>cd apps/web && npx tsc --noEmit 2>&1 | tail -10</verify>
</task>

</tasks>
