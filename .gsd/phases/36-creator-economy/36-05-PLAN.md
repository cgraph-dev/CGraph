---
phase: 36-creator-economy
plan: 05
type: execute
wave: 2
depends_on:
  - 36-01
  - 36-02
  - 36-03
files_modified:
  - apps/mobile/src/screens/paid-dm/paid-dm-settings-screen.tsx (new)
  - apps/mobile/src/components/paid-dm/paid-file-card.tsx (new)
  - apps/mobile/src/components/paid-dm/file-unlock-sheet.tsx (new)
  - apps/mobile/src/screens/creator/creator-dashboard-screen.tsx (update — ⚠️ ALREADY EXISTS, extend
    with premium content)
  - apps/mobile/src/components/creator/earnings-summary.tsx (new)
  - apps/mobile/src/stores/creatorStore.ts (update — ⚠️ ALREADY EXISTS at
    apps/mobile/src/stores/creatorStore.ts, extend it)
  - apps/mobile/src/services/creatorService.ts (update — ⚠️ ALREADY EXISTS at
    apps/mobile/src/services/creatorService.ts, extend it)
  - apps/mobile/src/navigation/ (update — register paid DM screens in existing navigators)
autonomous: true
must_haves:
  truths:
    - 'Mobile paid DM settings: same toggles as web, native switches'
    - 'PaidFileCard: blurred thumbnail + price badge + unlock action'
    - 'FileUnlockSheet: bottom sheet confirm, balance check, haptic on success'
    - 'Creator dashboard: summary cards (earnings, subscribers), mini chart'
    - 'creatorStore uses create() with manual AsyncStorage'
    - 'creatorService imports from ../lib/api'
    - 'All new screens registered in navigation'
    - '⚠️ creator-dashboard-screen.tsx ALREADY EXISTS at
      apps/mobile/src/screens/creator/creator-dashboard-screen.tsx. EXTEND it with premium content
      sections (premium threads, Node-based tiers), do NOT overwrite.'
    - '⚠️ creatorStore.ts ALREADY EXISTS at apps/mobile/src/stores/creatorStore.ts. EXTEND with
      premium thread and tier state/actions.'
    - '⚠️ creatorService.ts ALREADY EXISTS at apps/mobile/src/services/creatorService.ts. EXTEND
      with premium thread and tier API calls.'
    - '⚠️ nodesStore.ts ALREADY EXISTS at apps/mobile/src/stores/nodesStore.ts. Use for balance
      checks in unlock flows — do NOT duplicate balance logic.'
    - '⚠️ Navigation: apps/mobile/src/navigation/ has 11 navigator files. Add paid DM screens to
      messages-navigator.tsx or settings-navigator.tsx as appropriate.'
  artifacts:
    - apps/mobile/src/screens/paid-dm/paid-dm-settings-screen.tsx
    - apps/mobile/src/components/paid-dm/paid-file-card.tsx
    - apps/mobile/src/components/paid-dm/file-unlock-sheet.tsx
    - apps/mobile/src/components/creator/earnings-summary.tsx
  key_links:
    - '⚠️ EXISTING mobile creator files: -
      apps/mobile/src/screens/creator/creator-dashboard-screen.tsx (EXTEND, not create) -
      apps/mobile/src/stores/creatorStore.ts (EXTEND, not create) -
      apps/mobile/src/services/creatorService.ts (EXTEND, not create) All exist and are functional —
      add premium content features to them.'
    - '⚠️ EXISTING mobile nodes files: - apps/mobile/src/stores/nodesStore.ts (balance,
      transactions, wallet) - apps/mobile/src/services/nodesService.ts (API calls for nodes) -
      apps/mobile/src/screens/nodes/ (wallet, shop, withdrawal screens) Use these for payment flows
      — do NOT create separate balance logic.'
    - 'Mobile store pattern: apps/mobile/src/stores/ — create() with manual AsyncStorage persist'
    - 'Mobile service pattern: apps/mobile/src/services/ — import api from ../lib/api'
    - 'Navigation: apps/mobile/src/navigation/ (11 navigator files: auth, main, tab-bar, forums,
      friends, groups, messages, notifications, search, settings, root)'
---

<objective>
Build mobile creator economy UI: paid DM settings/cards/unlock (NEW), EXTEND the existing creator
dashboard screen with premium content, EXTEND existing creator store and service.
Register new screens in navigation.

ATOMIC_PLAN tasks: 3.28 (Mobile Paid DM UI), 3.29 (Mobile Creator Dashboard Extension), 3.30
(Creator Store + Service Extension), 3.31 (Navigation Registration). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Mobile paid DM UI (3.28)</name>
  <files>
    apps/mobile/src/screens/paid-dm/paid-dm-settings-screen.tsx
    apps/mobile/src/components/paid-dm/paid-file-card.tsx
    apps/mobile/src/components/paid-dm/file-unlock-sheet.tsx
  </files>
  <action>NEW screens/components. Settings: native Switch toggles for enable, TextInput for price (min 10 nodes), file type chips, auto-accept friends toggle. PaidFileCard: blurred Image + LinearGradient overlay + price badge + "Tap to unlock" pressable. FileUnlockSheet: @gorhom/bottom-sheet with confirm, balance display from nodesStore, haptic feedback (expo-haptics) + confetti on success.</action>
</task>

<task type="auto">
  <name>Task 2: EXTEND creator dashboard + store/service (3.29, 3.30)</name>
  <files>
    apps/mobile/src/screens/creator/creator-dashboard-screen.tsx
    apps/mobile/src/components/creator/earnings-summary.tsx
    apps/mobile/src/stores/creatorStore.ts
    apps/mobile/src/services/creatorService.ts
  </files>
  <action>EXTEND existing creator-dashboard-screen.tsx: add premium threads section (list, create), Node-based tier management, integrate new earnings-summary component. NEW earnings-summary.tsx: breakdown cards by type (DM files, subscriptions, boosts, tips) with totals. EXTEND existing creatorStore.ts: add premium thread state (premiumThreads[], tiers[]), plus actions (fetchPremiumThreads, createPremiumThread, fetchTiers, createTier). EXTEND existing creatorService.ts: add API calls for premium threads and tiers (GET/POST /api/v1/creator/premium-threads, GET/POST /api/v1/creator/tiers). ⚠️ Do NOT recreate these files from scratch — read existing content first, then add new sections.</action>
</task>

<task type="auto">
  <name>Task 3: Navigation registration (3.31)</name>
  <files>apps/mobile/src/navigation/</files>
  <action>Register PaidDmSettingsScreen in settings-navigator.tsx (under account/privacy section). Ensure creator-dashboard-screen is accessible (may already be registered — verify before adding duplicate). Add deep link config entries for new screens to apps/mobile/src/lib/deepLinks.ts linking config. If messages-navigator.tsx exists, integrate paid file cards into conversation flow there.</action>
  <verify>cd apps/mobile && npx tsc --noEmit 2>&1 | tail -10</verify>
</task>

</tasks>
