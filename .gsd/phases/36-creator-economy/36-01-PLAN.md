---
phase: 36-creator-economy
plan: 01
type: execute
wave: 1
depends_on:
  - 35-07
files_modified:
  - apps/backend/lib/cgraph/paid_dm/paid_dm.ex (new — context module)
  - apps/backend/lib/cgraph/paid_dm/paid_dm_file.ex (new — schema)
  - apps/backend/lib/cgraph/paid_dm/paid_dm_setting.ex (new — schema)
  - apps/backend/lib/cgraph_web/controllers/api/v1/paid_dm_controller.ex (new — under api/v1/ to
    match CreatorController pattern)
  - apps/backend/priv/repo/migrations/<ts>_create_paid_dm_tables.exs (new)
  - apps/backend/lib/cgraph_web/router/paid_dm_routes.ex (new — macro-based route module)
  - apps/backend/lib/cgraph_web/router.ex (update — add `import CGraphWeb.Router.PaidDmRoutes` +
    `paid_dm_routes()` call)
autonomous: true
must_haves:
  truths:
    - 'PaidDmFile schema: sender_id, receiver_id, file_url, file_type, nodes_required, status
      (:string, validate_inclusion pending|paid|expired|refunded), expires_at'
    - 'PaidDmSetting schema: user_id, enabled (boolean), price_per_file (integer), accepted_types
      ({:array, :string}), auto_accept_friends (boolean)'
    - 'Context: send_paid_file/4, unlock_paid_file/2, list_pending_files/1, configure_settings/2'
    - 'Controller: send (POST), unlock (PUT), list_pending (GET), settings (GET/PUT)'
    - 'File upload to existing upload pipeline (CGraph.Uploads)'
    - 'Expiry: paid files expire after 72h if not unlocked (Oban worker on :payments queue)'
    - 'Payment: use CGraph.Nodes.debit_nodes/4 to charge receiver, CGraph.Nodes.credit_nodes/4 to
      pay sender (minus platform cut)'
    - 'Status fields use :string with validate_inclusion (NOT Ecto.Enum) — match existing creator
      schema pattern'
    - '⚠️ router.ex MUST be updated: add `import CGraphWeb.Router.PaidDmRoutes` (around line 42,
      after existing imports) and `paid_dm_routes()` macro call (around line 143, after
      nodes_routes). This is how ALL route modules work — see lines 27-42 for imports, lines 128-143
      for calls.'
  artifacts:
    - apps/backend/lib/cgraph/paid_dm/paid_dm.ex
    - apps/backend/lib/cgraph/paid_dm/paid_dm_file.ex
    - apps/backend/lib/cgraph/paid_dm/paid_dm_setting.ex
    - apps/backend/lib/cgraph_web/controllers/api/v1/paid_dm_controller.ex
  key_links:
    - 'Existing file upload: apps/backend/lib/cgraph/uploads/ — reuse upload pipeline'
    - 'Nodes context: apps/backend/lib/cgraph/nodes/nodes.ex — debit_nodes/4 and credit_nodes/4 for
      payment flow. Uses Ecto.Multi with SELECT FOR UPDATE for concurrency safety.'
    - 'Router pattern: apps/backend/lib/cgraph_web/router.ex imports 16 route modules at lines 27-42
      and calls their macros at lines 128-143. New PaidDmRoutes MUST be: 1. Imported: `import
      CGraphWeb.Router.PaidDmRoutes` (add after line 42) 2. Called: `paid_dm_routes()` (add after
      line 143, after cosmetics_routes) See CGraphWeb.Router.NodesRoutes or
      CGraphWeb.Router.CreatorRoutes for the exact macro pattern.'
    - 'Schema conventions: @primary_key {:id, :binary_id, autogenerate: true}, @foreign_key_type
      :binary_id, @timestamps_opts [type: :utc_datetime]. Use :string fields with validate_inclusion
      for status (see creators/creator_payout.ex and creators/paid_forum_subscription.ex for the
      pattern).'
    - 'Controller placement: Place under apps/backend/lib/cgraph_web/controllers/api/v1/ and scope
      routes as "/api/v1/paid-dm" in the macro, matching CreatorRoutes pattern (CGraphWeb.API.V1
      scope).'
---

<objective>
Build Paid DM Files backend: schemas, context module, controller, and migration. Enables users to
send files in DMs that require Nodes payment to unlock.

ATOMIC_PLAN tasks: 3.1 (Paid DM File Schema), 3.2 (Paid DM Settings), 3.3 (Paid DM Context), 3.4
(Paid DM Controller), 3.5 (Paid DM File Expiry Worker). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Paid DM schemas + migration (3.1, 3.2)</name>
  <files>
    apps/backend/lib/cgraph/paid_dm/paid_dm_file.ex
    apps/backend/lib/cgraph/paid_dm/paid_dm_setting.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>PaidDmFile: sender_id, receiver_id (both belongs_to CGraph.Accounts.User), file_url (:string), file_type (:string), nodes_required (:integer), status (:string, default: "pending"), expires_at (:utc_datetime). PaidDmSetting: user_id (unique belongs_to CGraph.Accounts.User), enabled (:boolean, default: false), price_per_file (:integer), accepted_types ({:array, :string}), auto_accept_friends (:boolean, default: false). Use @primary_key {:id, :binary_id, autogenerate: true}, @foreign_key_type :binary_id, @timestamps_opts [type: :utc_datetime]. Status uses :string with validate_inclusion (NOT Ecto.Enum). Migration with indexes on sender_id, receiver_id, status, user_id (unique for settings).</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 2: Context + controller + routes (3.3, 3.4, 3.5)</name>
  <files>
    apps/backend/lib/cgraph/paid_dm/paid_dm.ex
    apps/backend/lib/cgraph_web/controllers/api/v1/paid_dm_controller.ex
    apps/backend/lib/cgraph_web/router/paid_dm_routes.ex
    apps/backend/lib/cgraph_web/router.ex
  </files>
  <action>Context CGraph.PaidDm: send_paid_file/4 (create record + use CGraph.Uploads for file, set expires_at to now + 72h), unlock_paid_file/2 (use CGraph.Nodes.debit_nodes to charge receiver, CGraph.Nodes.credit_nodes to pay sender minus 20% cut, update status to "paid"), configure_settings/2, list_pending_files/1, expire_stale_files/0 (called by Oban worker, mark expired). Controller CGraphWeb.API.V1.PaidDmController: send (POST /api/v1/paid-dm/send), unlock (PUT /api/v1/paid-dm/:id/unlock), pending (GET /api/v1/paid-dm/pending), settings (GET/PUT /api/v1/paid-dm/settings). Create CGraphWeb.Router.PaidDmRoutes defmacro paid_dm_routes with scope "/api/v1/paid-dm", CGraphWeb.API.V1 do ... end. ⚠️ ALSO update router.ex: add `import CGraphWeb.Router.PaidDmRoutes` after line 42 and `paid_dm_routes()` call after line 143.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
