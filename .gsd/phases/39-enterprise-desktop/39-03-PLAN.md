---
phase: 39-enterprise-desktop
plan: 03
type: execute
wave: 2
depends_on:
  - 39-01
  - 39-02
files_modified:
  - apps/backend/lib/cgraph/enterprise/compliance_suite.ex (new)
  - apps/backend/lib/cgraph/enterprise/data_residency.ex (new)
  - apps/backend/lib/cgraph/enterprise/white_label.ex (new)
  - apps/backend/lib/cgraph/enterprise/analytics_dashboard.ex (new)
  - apps/backend/lib/cgraph_web/controllers/admin/compliance_controller.ex (new)
  - apps/backend/lib/cgraph_web/controllers/admin/analytics_controller.ex (new)
  - apps/web/src/modules/admin/ (ALREADY EXISTS — has api/, components/, hooks/, store/, types/,
    index.ts. Contains admin-dashboard.tsx, moderation-dashboard.tsx, feature-flags-panel.tsx,
    7 API modules, multiple hooks. EXTEND with compliance/analytics pages, do NOT recreate)
autonomous: true
must_haves:
  truths:
    - 'ComplianceSuite: SOC2 controls checklist, GDPR data map, HIPAA safeguards (configurable)'
    - 'DataResidency: route data to region-specific storage/DB based on tenant config'
    - 'WhiteLabel: custom branding (logo, colors, domain), tenant-specific theming'
    - 'AnalyticsDashboard: platform-wide metrics, per-tenant analytics, export to CSV'
    - 'Admin compliance controller: compliance_status (GET), run_audit (POST), export_report (GET)'
    - 'Admin analytics controller: overview (GET), tenant_metrics (GET), export (GET)'
    - 'Web admin SPA: React dashboard with charts, tables, forms for admin operations.
      ⚠️ apps/web/src/modules/admin/ ALREADY EXISTS with substantial content (79+ files):
      admin-dashboard.tsx, moderation-dashboard.tsx, feature-flags-panel.tsx, 7 API modules,
      5+ hooks. EXTEND with compliance + analytics pages, do not recreate.'
  artifacts:
    - apps/backend/lib/cgraph/enterprise/compliance_suite.ex
    - apps/backend/lib/cgraph/enterprise/data_residency.ex
    - apps/backend/lib/cgraph/enterprise/white_label.ex
    - apps/backend/lib/cgraph/enterprise/analytics_dashboard.ex
    - apps/backend/lib/cgraph_web/controllers/admin/compliance_controller.ex
    - apps/backend/lib/cgraph_web/controllers/admin/analytics_controller.ex
    - apps/web/src/modules/admin/
  key_links:
    - 'Compliance modules: apps/backend/lib/cgraph/compliance/ — ⚠️ This directory ALREADY
      EXISTS with age_gate.ex and tax_reporter.ex (no gdpr_export.ex). Phase 36-03 may add more
      files, but the directory is NOT new. Enterprise compliance modules go in enterprise/ not here.'
    - 'Admin controllers: apps/backend/lib/cgraph_web/controllers/admin/ — from 39-01.
      ⚠️ Only events_helpers.ex pre-exists. Main admin controllers are at api/v1/ and api/admin/.'
    - 'Monitoring: apps/backend/lib/cgraph/monitoring/ — metrics from Phase 38'
---

<objective>
Complete enterprise features: compliance suite (SOC2/GDPR/HIPAA), data residency, white-labeling,
analytics dashboard, and web admin SPA. Final phase of the ATOMIC_PLAN.

ATOMIC_PLAN tasks: 6.5 (Compliance Suite — 40h), 6.6 (Data Residency — 20h), 6.7 (White Label —
16h), 6.8 (Enterprise Analytics — 10h). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Compliance + data residency (6.5, 6.6)</name>
  <files>
    apps/backend/lib/cgraph/enterprise/compliance_suite.ex
    apps/backend/lib/cgraph/enterprise/data_residency.ex
    apps/backend/lib/cgraph_web/controllers/admin/compliance_controller.ex
  </files>
  <action>ComplianceSuite: checklist system (SOC2 controls, GDPR data processing map, HIPAA PHI safeguards), run_audit/1 checks all controls, generate_report/2 PDF-ready output. DataResidency: resolve_region/1 (tenant → region), route_storage/2, route_database/2, verify_residency/1. ComplianceController: status, run_audit, export_report endpoints.</action>
</task>

<task type="auto">
  <name>Task 2: White label + analytics (6.7, 6.8)</name>
  <files>
    apps/backend/lib/cgraph/enterprise/white_label.ex
    apps/backend/lib/cgraph/enterprise/analytics_dashboard.ex
    apps/backend/lib/cgraph_web/controllers/admin/analytics_controller.ex
  </files>
  <action>WhiteLabel: configure_branding/2 (logo_url, primary_color, accent_color, favicon, app_name, custom_domain), apply_theme/1 returns CSS variables. AnalyticsDashboard: platform_overview (DAU, MAU, revenue, growth), tenant_breakdown, time_series/3, export_csv/2. AnalyticsController: overview, tenant_metrics, export.</action>
</task>

<task type="auto">
  <name>Task 3: Web admin SPA (6.5–6.8 frontend)</name>
  <files>apps/web/src/modules/admin/</files>
  <action>Admin dashboard SPA: EXTEND existing apps/web/src/modules/admin/ (already has
    admin-dashboard, moderation-dashboard, feature-flags-panel, 7 API modules, 5+ hooks). Add
    pages for Compliance (status + audit), Analytics (charts + export), and Settings (platform
    config). Use existing component library. Route: /admin/* with admin auth guard.</action>
  <verify>cd apps/web && npx tsc --noEmit 2>&1 | tail -10</verify>
</task>

</tasks>
