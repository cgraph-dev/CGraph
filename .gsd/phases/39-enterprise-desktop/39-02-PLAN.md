---
phase: 39-enterprise-desktop
plan: 02
type: execute
wave: 1
depends_on:
  - 38-05
files_modified:
  - apps/backend/lib/cgraph/enterprise/self_hosting.ex (new — context)
  - apps/backend/lib/cgraph/enterprise/tenant.ex (new — schema)
  - apps/backend/lib/cgraph/enterprise/tenant_config.ex (new — schema)
  - infrastructure/docker/self-hosted-compose.yml (new)
  - infrastructure/docker/Dockerfile.self-hosted (new)
  - apps/desktop/ (new — Tauri/Electron desktop app scaffold)
  - apps/desktop/src-tauri/tauri.conf.json (new)
  - apps/desktop/src/ (shared with web build)
  - apps/backend/priv/repo/migrations/<ts>_create_tenant_tables.exs (new)
autonomous: true
must_haves:
  truths:
    - 'SelfHosting context: provision_instance/1, configure_tenant/2, migrate_tenant/1'
    - 'Tenant schema: name, domain, plan (free|premium|enterprise), config_id, provisioned_at,
      status. ⚠️ Canonical tiers are free|premium|enterprise (NOT "pro"). See Session 29.
      ⚠️ request_context.ex already has tenant_id type, get_tenant_id(), set_tenant(),
      and X-Tenant-Id header parsing — wire Tenant schema into this infrastructure.'
    - 'TenantConfig: tenant_id, database_url, redis_url, storage_provider, max_users, features
      (JSONB)'
    - 'Docker compose for self-hosted: postgres, redis, backend, web, reverse proxy.
      ⚠️ infrastructure/docker/ already exists (contains init-db.sql/ only).'
    - 'Single binary/Docker image deployment target'
    - 'Desktop app via Tauri 2.0 wrapping the web app'
    - 'Desktop: system tray, native notifications, auto-update, local file access'
    - 'Desktop shares web build output — no separate codebase'
  artifacts:
    - apps/backend/lib/cgraph/enterprise/self_hosting.ex
    - apps/backend/lib/cgraph/enterprise/tenant.ex
    - apps/backend/lib/cgraph/enterprise/tenant_config.ex
    - infrastructure/docker/self-hosted-compose.yml
    - infrastructure/docker/Dockerfile.self-hosted
    - apps/desktop/ (Tauri scaffold)
  key_links:
    - 'Existing Docker: apps/backend/Dockerfile — reference'
    - 'Web Dockerfile: apps/web/Dockerfile — reference'
    - 'Docker compose dev: docker-compose.dev.yml — reference'
---

<objective>
Build self-hosting infrastructure (multi-tenant, Docker packaging) and desktop application
scaffold using Tauri wrapping the web frontend.

ATOMIC_PLAN tasks: 6.3 (Self-Hosting Package — 60h), 6.4 (Desktop Application — 40h). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Multi-tenant schemas + context (6.3 part 1)</name>
  <files>
    apps/backend/lib/cgraph/enterprise/tenant.ex
    apps/backend/lib/cgraph/enterprise/tenant_config.ex
    apps/backend/lib/cgraph/enterprise/self_hosting.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>Tenant: name, domain (unique), plan (Ecto.Enum — free|premium|enterprise, NOT "pro"),
    status (provisioning|active|suspended|deprovisioned), provisioned_at. TenantConfig: belongs_to
    tenant, database_url (encrypted), redis_url (encrypted), storage_provider (s3|r2|local),
    max_users, features (JSONB feature flags). SelfHosting context: provision_instance (create DB,
    run migrations, seed defaults), configure_tenant, health_check_tenant. ⚠️ Wire into existing
    request_context.ex tenant_id infrastructure (get_tenant_id/set_tenant already exist).</action>
</task>

<task type="auto">
  <name>Task 2: Self-hosted Docker packaging (6.3 part 2)</name>
  <files>
    infrastructure/docker/self-hosted-compose.yml
    infrastructure/docker/Dockerfile.self-hosted
  </files>
  <action>Dockerfile.self-hosted: multi-stage, Elixir release + web static build, single image. Compose: postgres:16, redis:7, cgraph (single service), caddy reverse proxy, volumes for data persistence. Environment variables for all config. Health checks on all services. README with setup instructions.</action>
</task>

<task type="auto">
  <name>Task 3: Desktop application (6.4)</name>
  <files>apps/desktop/</files>
  <action>Tauri 2.0 scaffold: tauri.conf.json (window config, app name, icons), src-tauri/main.rs (minimal), uses web build output as frontend. Features: system tray icon with status, native OS notifications, auto-update via Tauri updater, local file downloads to system downloads folder. Package.json scripts: dev, build:mac, build:win, build:linux.</action>
  <verify>ls apps/desktop/src-tauri/tauri.conf.json 2>&1</verify>
</task>

</tasks>
