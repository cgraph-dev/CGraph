---
phase: 39-enterprise-desktop
plan: 02
type: execute
wave: 1
depends_on:
  - 38-05
files_modified:
  - apps/backend/lib/cgraph/enterprise/organization.ex (new — schema)
  - apps/backend/lib/cgraph/enterprise/org_settings.ex (new — schema)
  - apps/backend/lib/cgraph/enterprise/org_membership.ex (new — join table schema)
  - apps/backend/lib/cgraph/enterprise/organizations.ex (new — context)
  - apps/backend/lib/cgraph_web/controllers/api/v1/organization_controller.ex (new — ⚠️ all
    controllers are under controllers/api/v1/ with CGraphWeb.API.V1.* namespace)
  - apps/backend/lib/cgraph_web/router/admin_routes.ex (ALREADY EXISTS — 44+ routes, already
    imported in router.ex. EXTEND with org management routes)
  - apps/backend/priv/repo/migrations/<ts>_create_organizations.exs (new)
autonomous: true
must_haves:
  truths:
    - 'REMOVED: Self-hosting (Task 6.3) — CGraph hosts everything like Reddit/Discord. No
      self-hosted Docker packaging, no Dockerfile.self-hosted, no Helm charts, no
      Tenant/TenantConfig schemas, no SelfHosting context.'
    - 'DEFERRED: Desktop app (Task 6.4) — will be developed in a future phase. Enterprise and
      normal users use the SAME web/mobile app to login. No apps/desktop/ scaffold.'
    - 'Organization schema: name, slug (unique, citext), owner_id (ref users), subscription_tier
      (free|premium|enterprise), logo_url, max_members, created_at.
      ⚠️ Subscriptions are CURRENTLY per-user (user.subscription_tier). Enterprise orgs add
      org-level subscription that covers all org members.'
    - 'OrgSettings: org_id, sso_enabled (boolean), allowed_domains (JSONB — email domain whitelist),
      default_role, max_groups, features (JSONB — feature flags per org), branding (JSONB —
      logo_url, primary_color, accent_color, favicon)'
    - 'OrgMembership join table: org_id, user_id, role (owner|admin|member), joined_at'
    - 'Organizations context: create_org/2, update_org/2, add_member/3, remove_member/2,
      list_members/2 (cursor pagination), transfer_ownership/3'
    - 'Organization groups: Groups can optionally belong_to an Organization. Existing Group schema
      needs optional org_id FK added. ⚠️ Group schema is at apps/backend/lib/cgraph/groups/group.ex
      with fields: name, slug, owner_id, is_public, member_count, etc.'
    - 'Enterprise users login through the SAME web/mobile app — no separate admin app or portal.
      Enterprise features gated by org subscription tier.'
  artifacts:
    - apps/backend/lib/cgraph/enterprise/organization.ex
    - apps/backend/lib/cgraph/enterprise/org_settings.ex
    - apps/backend/lib/cgraph/enterprise/org_membership.ex
    - apps/backend/lib/cgraph/enterprise/organizations.ex
    - apps/backend/lib/cgraph_web/controllers/api/v1/organization_controller.ex
  key_links:
    - 'Groups: apps/backend/lib/cgraph/groups/group.ex — Discord-like servers with name, slug,
      owner_id, is_public, member_count, channels, roles, invites. Add optional org_id FK.'
    - 'Subscriptions: apps/backend/lib/cgraph/subscriptions/subscriptions.ex — per-user tiers
      (free|premium|enterprise). Org subscription extends this model.'
    - 'User schema: apps/backend/lib/cgraph/accounts/user.ex — has subscription_tier field'
    - 'Request context: apps/backend/lib/cgraph/request_context.ex — has tenant_id infrastructure
      (get_tenant_id/set_tenant). Can wire org_id through this for org-scoped API requests.'
    - 'Router: admin_routes.ex ALREADY EXISTS with 44+ routes. EXTEND it.'
---

<objective>
Build enterprise organization model — org-level subscriptions, settings, and group ownership.
Enterprise and normal users use the SAME app. No self-hosting, no desktop app.

ATOMIC_PLAN tasks: 6.3 (REMOVED — no self-hosting), 6.4 (DEFERRED — desktop app future).
Replaced with: Enterprise Organization model (~40h). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Organization schemas + migration</name>
  <files>
    apps/backend/lib/cgraph/enterprise/organization.ex
    apps/backend/lib/cgraph/enterprise/org_settings.ex
    apps/backend/lib/cgraph/enterprise/org_membership.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>Organization: name, slug (unique, citext), owner_id (ref users), subscription_tier
    (Ecto.Enum — free|premium|enterprise), logo_url, max_members, created_at. Has_one :settings,
    has_many :members (through org_memberships join table), has_many :groups. OrgSettings:
    belongs_to :organization, sso_enabled (boolean, default false), allowed_domains (JSONB array),
    default_role (string), max_groups (integer), features (JSONB), branding (JSONB — logo_url,
    primary_color, accent_color, favicon). OrgMembership join table: org_id, user_id, role
    (Ecto.Enum — owner|admin|member), joined_at. Migration for all 3 tables with indexes. Add
    optional org_id FK to existing groups table (nullable, no data migration needed).
    ⚠️ Use @primary_key {:id, :binary_id, autogenerate: true} and @foreign_key_type :binary_id
    per project conventions.</action>
</task>

<task type="auto">
  <name>Task 2: Organizations context + controller</name>
  <files>
    apps/backend/lib/cgraph/enterprise/organizations.ex
    apps/backend/lib/cgraph_web/controllers/api/v1/organization_controller.ex
    apps/backend/lib/cgraph_web/router/admin_routes.ex
  </files>
  <action>Organizations context: create_org/2 (owner creates), update_org/2, delete_org/1 (soft
    delete), get_org/1, get_org_by_slug/1, add_member/3 (with role), remove_member/2,
    list_members/2 (cursor pagination), transfer_ownership/3, update_settings/2.
    OrganizationController at controllers/api/v1/: CRUD + member management endpoints under
    /api/v1/organizations. EXTEND existing admin_routes.ex with admin org management routes
    (list all orgs, suspend org, org analytics). Wire org_id into request_context via
    X-Organization-Id header or JWT claim for org-scoped API requests.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
