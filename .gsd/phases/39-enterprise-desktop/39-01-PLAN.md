---
phase: 39-enterprise-desktop
plan: 01
type: execute
wave: 1
depends_on:
  - 38-05
files_modified:
  - apps/backend/lib/cgraph/enterprise/admin_console.ex (new — context)
  - apps/backend/lib/cgraph/enterprise/admin_user.ex (new — schema)
  - apps/backend/lib/cgraph/enterprise/admin_role.ex (new — schema)
  - apps/backend/lib/cgraph/enterprise/audit_log.ex (new — schema)
  - apps/backend/lib/cgraph_web/controllers/admin/ (extends — enterprise admin controllers.
    ⚠️ Only events_helpers.ex exists here currently. Existing admin controllers are at
    controllers/api/v1/admin_controller.ex (CGraphWeb.API.V1.AdminController — 15 actions) and
    controllers/api/admin/ (ModerationController, FeatureFlagController). Decide: extend existing
    AdminController or add new enterprise-specific controllers here)
  - apps/backend/lib/cgraph_web/router/admin_routes.ex (ALREADY EXISTS — 44+ routes already
    wired. Already imported in router.ex. EXTEND with new enterprise routes, do NOT recreate)
  - apps/backend/lib/cgraph/enterprise/sso.ex (new — SSO context)
  - apps/backend/lib/cgraph/enterprise/sso_provider.ex (new — schema)
  - apps/backend/lib/cgraph_web/controllers/api/v1/sso_controller.ex (new — ⚠️ there is NO
    controllers/auth/ directory. All auth controllers are under controllers/api/v1/:
    auth_controller.ex, oauth_controller.ex, qr_auth_controller.ex)
  - apps/backend/priv/repo/migrations/<ts>_create_enterprise_tables.exs (new)
autonomous: true
must_haves:
  truths:
    - 'AdminConsole context: manage platform settings, user management, content moderation'
    - 'AdminUser: email, role_id, permissions (JSONB), last_login, mfa_enabled'
    - 'AdminRole: name (super_admin|admin|moderator|analyst), permissions (JSONB)'
    - 'AuditLog: admin_id, action, resource_type, resource_id, changes (JSONB), ip_address.
      ⚠️ EXISTING audit modules: CGraph.Audit (GenServer with buffering),
      CGraph.Accounts.AuditLog, CGraph.Groups.AuditLog, CGraph.Moderation.AuditLog.
      New enterprise AuditLog must have distinct module name (e.g., Enterprise.AuditEntry)
      or explicitly extend the existing CGraph.Audit system.'
    - 'Admin controllers: DashboardController, UserManagementController, ContentModerationController.
      ⚠️ EXISTING admin: AdminController already has 15 actions (metrics, list_users, ban_user,
      list_reports, list_audit_log, get_config, update_config, maintenance, GDPR export/delete).
      New controllers should provide ENTERPRISE features not already covered.'
    - 'Admin routes scoped under /admin/ with admin auth plug. ⚠️ admin_routes.ex ALREADY
      EXISTS with 44+ routes. EXTEND the existing file, do NOT create a new one.'
    - 'SSO: SAML 2.0 + OIDC support — ⚠️ existing auth uses assent (~> 0.2), NOT samly/ueberauth.
      FIRST investigate if assent supports SAML/OIDC natively. If not, add samly + ueberauth deps to
      mix.exs (they do NOT exist in current deps).'
    - 'SSOProvider: name, type (saml|oidc), config (JSONB), org_id (ref
      organizations — SSO providers belong to an Organization, NOT a tenant), enabled'
    - 'SSO login flow: redirect → IdP → callback → create/link user → session'
  artifacts:
    - apps/backend/lib/cgraph/enterprise/ (admin_console.ex, admin_user.ex, admin_role.ex,
      audit_log.ex, sso.ex, sso_provider.ex)
    - apps/backend/lib/cgraph_web/controllers/admin/ (dashboard, user_management,
      content_moderation — ⚠️ only events_helpers.ex exists here. Existing admin controllers
      are at api/v1/admin_controller.ex and api/admin/)
    - apps/backend/lib/cgraph_web/controllers/api/v1/sso_controller.ex (⚠️ NOT
      controllers/auth/ — that directory does not exist)
  key_links:
    - 'Auth: apps/backend/lib/cgraph_web/controllers/api/v1/ — existing auth controllers
      (auth_controller.ex, oauth_controller.ex, qr_auth_controller.ex, wallet_auth_controller.ex).
      ⚠️ There is NO controllers/auth/ directory.'
    - '⚠️ Existing auth uses assent (~> 0.2) for OAuth — NOT samly/ueberauth. Investigate assent
      SAML/OIDC support before adding new deps.'
    - 'Router pattern: CGraphWeb.Router.AdminRoutes ALREADY EXISTS (44+ routes, already imported
      in router.ex at line 33). EXTEND it with new enterprise admin routes. It uses
      scope "/api/v1/admin", CGraphWeb.API.V1 with pipe_through [:api, :api_admin].'
    - 'Accounts: apps/backend/lib/cgraph/accounts/ — user management'
  pre_tasks:
    - 'MANDATORY: Audit assent docs to determine if SAML 2.0 + OIDC are supported. Only add
      samly/ueberauth if assent cannot handle these protocols.'
    - 'MANDATORY: If samly/ueberauth are needed, add them to mix.exs deps first.'
---

<objective>
Build enterprise admin console with role-based access, audit logging, and SSO integration
(SAML 2.0 + OIDC). This is the foundation for enterprise features.

ATOMIC_PLAN tasks: 6.1 (Admin Console — 80h), 6.2 (SSO Integration — 40h). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Admin schemas + migrations (6.1 part 1)</name>
  <files>
    apps/backend/lib/cgraph/enterprise/admin_user.ex
    apps/backend/lib/cgraph/enterprise/admin_role.ex
    apps/backend/lib/cgraph/enterprise/audit_log.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>AdminUser: email (unique), password_hash, role_id (ref admin_roles), permissions (JSONB override), last_login, mfa_enabled, mfa_secret. AdminRole: name (Ecto.Enum), permissions (JSONB matrix), description. AuditLog: admin_id, action, resource_type, resource_id, changes_before (JSONB), changes_after (JSONB), ip_address, user_agent. Migrations with indexes.</action>
</task>

<task type="auto">
  <name>Task 2: Admin console context + controllers (6.1 part 2)</name>
  <files>
    apps/backend/lib/cgraph/enterprise/admin_console.ex
    apps/backend/lib/cgraph_web/controllers/admin/
    apps/backend/lib/cgraph_web/router/admin_routes.ex
  </files>
  <action>AdminConsole context: platform_stats/0, manage_user/2, moderate_content/2, update_settings/2. Controllers: DashboardController (GET /admin/dashboard), UserManagementController (CRUD + ban/suspend/verify), ContentModerationController (review, approve, reject, escalate). Admin auth plug: verify admin JWT, check permissions. EXTEND existing CGraphWeb.Router.AdminRoutes with new enterprise routes (it already has 44+ routes and is imported in router.ex). ⚠️ Existing AdminController at api/v1/ already handles: metrics, list_users, ban_user, list_reports, audit_log, config, maintenance, GDPR. New controllers should add ENTERPRISE-specific functionality.</action>
</task>

<task type="auto">
  <name>Task 3: SSO integration (6.2)</name>
  <files>
    apps/backend/lib/cgraph/enterprise/sso.ex
    apps/backend/lib/cgraph/enterprise/sso_provider.ex
    apps/backend/lib/cgraph_web/controllers/api/v1/sso_controller.ex
  </files>
  <action>SSO context: initiate_sso/2, handle_callback/2, link_account/3, list_providers/1. SSOProvider schema: name, type (saml|oidc), config (JSONB — metadata_url, client_id, etc.), org_id (ref organizations — SSO providers belong to an Organization, NOT a tenant), enabled. SSOController at controllers/api/v1/ (NOT controllers/auth/ — that directory does not exist): initiate (GET /auth/sso/:provider), callback (POST /auth/sso/callback). ⚠️ Investigate existing assent dep first for SAML/OIDC. Fall back to Samly (SAML) + Ueberauth (OIDC) only if assent doesn't support these protocols. Add any new deps to mix.exs.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
