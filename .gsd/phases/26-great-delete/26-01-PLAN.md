---
phase: 26-great-delete
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/lib/cgraph/gamification/xp_transaction.ex
  - apps/backend/lib/cgraph/gamification/xp_config.ex
  - apps/backend/lib/cgraph/gamification/xp_event_handler.ex
  - apps/backend/lib/cgraph/gamification/daily_cap.ex
  - apps/backend/lib/cgraph/gamification/quest_system.ex
  - apps/backend/lib/cgraph/gamification/quest_templates.ex
  - apps/backend/lib/cgraph/gamification/quest_rotation_worker.ex
  - apps/backend/lib/cgraph/gamification/quest.ex
  - apps/backend/lib/cgraph/gamification/user_quest.ex
  - apps/backend/lib/cgraph/gamification/coin_transaction.ex
  - apps/backend/lib/cgraph/gamification/currency_system.ex
  - apps/backend/lib/cgraph/gamification/user_prestige.ex
  - apps/backend/lib/cgraph/gamification/prestige_reward.ex
  - apps/backend/lib/cgraph/gamification/seasonal_event.ex
  - apps/backend/lib/cgraph/gamification/user_event_progress.ex
  - apps/backend/lib/cgraph/gamification/event_system.ex
  - apps/backend/lib/cgraph/gamification/event_lifecycle_worker.ex
  - apps/backend/lib/cgraph/gamification/battle_pass_tier.ex
  - apps/backend/lib/cgraph/gamification/marketplace.ex
  - apps/backend/lib/cgraph/gamification/marketplace_item.ex
  - apps/backend/lib/cgraph/gamification/leaderboard_system.ex
  - apps/backend/lib/cgraph/gamification/leaderboard.ex
  - apps/backend/lib/cgraph/gamification/feature_gates.ex
  - apps/backend/lib/cgraph_web/plugs/level_gate_plug.ex
  - apps/backend/lib/cgraph_web/plugs/premium_gate_plug.ex
  - apps/backend/lib/cgraph_web/channels/gamification_channel.ex
  - apps/backend/lib/cgraph_web/controllers/gamification_controller.ex
  - apps/backend/lib/cgraph_web/controllers/gamification_json.ex
  - apps/backend/lib/cgraph_web/router/gamification_routes.ex
  - apps/backend/lib/cgraph_web/validation/gamification_params.ex
  - apps/backend/lib/cgraph_web/controllers/quest_controller.ex
  - apps/backend/lib/cgraph_web/controllers/quest_json.ex
  - apps/backend/lib/cgraph_web/controllers/prestige_controller.ex
  - apps/backend/lib/cgraph_web/controllers/marketplace_controller.ex
  - apps/backend/lib/cgraph_web/channels/marketplace_channel.ex
  - apps/backend/lib/cgraph_web/controllers/admin/marketplace_controller.ex
  - apps/backend/lib/cgraph/marketplace.ex
autonomous: true
must_haves:
  truths:
    - 'All gamification Elixir modules deleted except achievement-related ones'
    - 'GamificationRoutes removed from router, only achievement routes remain'
    - 'GamificationChannel deleted'
    - 'Feature gate plugs deleted'
    - 'Backend compiles clean after deletions'
  artifacts:
    - apps/backend/lib/cgraph_web/router/gamification_routes.ex (stripped to achievements only)
  key_links:
    - 'Must complete before frontend deletions (Plan 26-02) to avoid hanging API calls'
    - 'Achievement routes GET /achievements, GET /achievements/:id, POST /achievements/:id/unlock kept'
---

<objective>
Delete all deprecated gamification backend Elixir modules, controllers, channels, plugs, and routes.
Keep only simplified achievement routes. This is the backend half of "The Great Delete".

Purpose: Remove XP, quests, coins, prestige, seasonal events, marketplace, leaderboard, and feature
gate systems from the backend. The gamification pivot requires these to be gone before building
Pulse/Nodes/Discovery. Achievement system stays but is simplified.

Output:
- ~30 Elixir modules deleted
- gamification_routes.ex stripped to 3 achievement-only routes
- GamificationChannel deleted
- Backend compiles cleanly
</objective>

<context>
@.gsd/PROJECT.md
@docs/PrivateFolder/This will be the definitive.txt (Part 2 — Backend Delete List)
@apps/backend/lib/cgraph_web/router/gamification_routes.ex
@apps/backend/lib/cgraph_web/router.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete XP system modules</name>
  <files>
    apps/backend/lib/cgraph/gamification/xp_transaction.ex
    apps/backend/lib/cgraph/gamification/xp_config.ex
    apps/backend/lib/cgraph/gamification/xp_event_handler.ex
    apps/backend/lib/cgraph/gamification/daily_cap.ex
  </files>
  <action>
    Delete these 4 files. Then grep the entire `apps/backend/` for any remaining imports/references
    to `XpTransaction`, `XpConfig`, `XpEventHandler`, `DailyCap` modules. Comment out or remove
    any references found — do NOT break compilation.
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -20</verify>
  <done>XP system modules deleted, no dangling references</done>
</task>

<task type="auto">
  <name>Task 2: Delete quest system modules</name>
  <files>
    apps/backend/lib/cgraph/gamification/quest_system.ex
    apps/backend/lib/cgraph/gamification/quest_templates.ex
    apps/backend/lib/cgraph/gamification/quest_rotation_worker.ex
    apps/backend/lib/cgraph/gamification/quest.ex
    apps/backend/lib/cgraph/gamification/user_quest.ex
    apps/backend/lib/cgraph_web/controllers/quest_controller.ex
    apps/backend/lib/cgraph_web/controllers/quest_json.ex
  </files>
  <action>
    Delete these 7 files. Remove any references to Quest modules across the backend. Remove quest
    routes from the router if they exist outside gamification_routes.ex.
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -20</verify>
  <done>Quest system completely removed</done>
</task>

<task type="auto">
  <name>Task 3: Delete coin/currency, prestige, and seasonal event modules</name>
  <files>
    apps/backend/lib/cgraph/gamification/coin_transaction.ex
    apps/backend/lib/cgraph/gamification/currency_system.ex
    apps/backend/lib/cgraph/gamification/user_prestige.ex
    apps/backend/lib/cgraph/gamification/prestige_reward.ex
    apps/backend/lib/cgraph/gamification/seasonal_event.ex
    apps/backend/lib/cgraph/gamification/user_event_progress.ex
    apps/backend/lib/cgraph/gamification/event_system.ex
    apps/backend/lib/cgraph/gamification/event_lifecycle_worker.ex
    apps/backend/lib/cgraph/gamification/battle_pass_tier.ex
    apps/backend/lib/cgraph_web/controllers/prestige_controller.ex
  </files>
  <action>
    Delete these 10 files. Remove all references to CoinTransaction, CurrencySystem, UserPrestige,
    PrestigeReward, SeasonalEvent, UserEventProgress, EventSystem, EventLifecycleWorker,
    BattlePassTier across the backend codebase. Clean up any Oban worker registrations for
    EventLifecycleWorker and QuestRotationWorker.
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -20</verify>
  <done>Currency, prestige, and seasonal event systems removed</done>
</task>

<task type="auto">
  <name>Task 4: Delete marketplace, leaderboard, and feature gate modules</name>
  <files>
    apps/backend/lib/cgraph/gamification/marketplace.ex
    apps/backend/lib/cgraph/gamification/marketplace_item.ex
    apps/backend/lib/cgraph/marketplace.ex
    apps/backend/lib/cgraph/gamification/leaderboard_system.ex
    apps/backend/lib/cgraph/gamification/leaderboard.ex
    apps/backend/lib/cgraph/gamification/feature_gates.ex
    apps/backend/lib/cgraph_web/plugs/level_gate_plug.ex
    apps/backend/lib/cgraph_web/plugs/premium_gate_plug.ex
    apps/backend/lib/cgraph_web/controllers/marketplace_controller.ex
    apps/backend/lib/cgraph_web/channels/marketplace_channel.ex
    apps/backend/lib/cgraph_web/controllers/admin/marketplace_controller.ex
  </files>
  <action>
    Delete these 11 files. Remove any remaining plug/pipeline references to LevelGatePlug or
    PremiumGatePlug from router.ex. Remove marketplace channel registration from user_socket.ex.
    Remove leaderboard-related references. Clean up any remaining imports.
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -20</verify>
  <done>Marketplace, leaderboard, and feature gate systems removed</done>
</task>

<task type="auto">
  <name>Task 5: Strip gamification routes to achievement-only</name>
  <files>
    apps/backend/lib/cgraph_web/router/gamification_routes.ex
    apps/backend/lib/cgraph_web/controllers/gamification_controller.ex
    apps/backend/lib/cgraph_web/controllers/gamification_json.ex
    apps/backend/lib/cgraph_web/validation/gamification_params.ex
    apps/backend/lib/cgraph_web/channels/gamification_channel.ex
  </files>
  <action>
    1. Delete GamificationChannel entirely.
    2. Strip gamification_routes.ex to ONLY these 3 routes:
       - GET /api/v1/achievements (list)
       - GET /api/v1/achievements/:id (show)
       - POST /api/v1/achievements/:id/unlock
    3. Strip GamificationController to only the actions serving those 3 routes.
    4. Strip GamificationJson to only the achievement rendering functions.
    5. Strip GamificationParams to only achievement-related validations.
    6. Remove all other route definitions (XP, quests, coins, leaderboard, prestige,
       seasonal events, marketplace, referral routes).
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -20</verify>
  <done>Only 3 achievement routes remain, everything else stripped</done>
</task>

<task type="auto">
  <name>Task 6: Create database migration to drop defunct tables</name>
  <files>apps/backend/priv/repo/migrations/</files>
  <action>
    Create a new migration file. Before dropping, check which tables actually exist.
    Use conditional drops:
    ```elixir
    defmodule CGraph.Repo.Migrations.DropGamificationTables do
      use Ecto.Migration

      def change do
        # Confirmed tables with migrations:
        drop_if_exists table(:xp_transactions)
        drop_if_exists table(:coin_transactions)
        drop_if_exists table(:quests)
        drop_if_exists table(:user_quests)

        # Tables that may or may not exist (no confirmed migrations):
        drop_if_exists table(:xp_configs)
        drop_if_exists table(:daily_caps)
        drop_if_exists table(:user_prestige)
        drop_if_exists table(:prestige_rewards)
        drop_if_exists table(:seasonal_events)
        drop_if_exists table(:user_event_progress)
        drop_if_exists table(:battle_pass_tiers)
        drop_if_exists table(:marketplace_items)
        drop_if_exists table(:leaderboard_entries)
        drop_if_exists table(:feature_gate_configs)
      end
    end
    ```
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>Migration created with drop_if_exists for all defunct gamification tables</done>
</task>

<task type="auto">
  <name>Task 7: Final backend compilation check</name>
  <files>apps/backend/</files>
  <action>
    Run full compilation. Fix any remaining dangling references, unused aliases, or
    undefined function warnings. The backend must compile with zero errors.
    Also run: mix format to ensure code style consistency.
  </action>
  <verify>cd apps/backend && mix compile --warnings-as-errors 2>&1 | tail -20</verify>
  <done>Backend compiles clean with zero errors and zero warnings</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mix compile` passes with zero errors in apps/backend
- [ ] Only 3 gamification routes remain (GET achievements, GET achievements/:id, POST achievements/:id/unlock)
- [ ] No references to deleted modules remain
- [ ] Migration file created for table drops
- [ ] GamificationChannel removed from socket registration
- [ ] Feature gate plugs removed from router pipelines
</verification>

<success_criteria>
- All tasks completed
- Backend compiles cleanly
- ~30+ files deleted
- Only achievement system remains from gamification
</success_criteria>

<output>
After completion, create `.gsd/phases/26-great-delete/26-01-SUMMARY.md`
</output>
