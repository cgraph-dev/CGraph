---
phase: 26-great-delete
plan: 04
type: execute
wave: 2
depends_on:
  - 26-02
  - 26-03
files_modified:
  - apps/web/src/routes/app-routes.tsx
  - apps/web/src/routes/lazyPages.ts
  - apps/web/src/pages/customize/identity-customization/constants.ts
  - apps/web/src/pages/customize/identity-customization/useIdentityCustomization.ts
  - apps/web/src/pages/customize/identity-customization/index.ts
  - apps/mobile/src/navigation/ (route files)
autonomous: true
must_haves:
  truths:
    - 'All gamification routes removed from web router'
    - 'Lazy page imports for gamification pages removed'
    - 'LEGACY_BORDER_ID_TO_V2_TYPE duplicate removed'
    - 'No dead imports or references to deleted pages/components remain'
    - 'Web app compiles and routes work for remaining pages'
  artifacts:
    - apps/web/src/routes/app-routes.tsx (gamification routes removed)
    - apps/web/src/routes/lazyPages.ts (gamification lazy imports removed)
    - apps/web/src/pages/customize/identity-customization/constants.ts (LEGACY_BORDER_ID_TO_V2_TYPE removed)
  key_links:
    - 'Depends on Plan 26-03 (pages must be deleted before route cleanup)'
    - 'Depends on Plan 26-02 (stores must be deleted before import cleanup)'
---

<objective>
Clean up routing, lazy page imports, duplicate constants, and all remaining dangling references
after the Great Delete. This wave runs after stores and pages are deleted.

Purpose: Ensure the app is navigable, compiles cleanly, and has no dead code pointing
to deleted gamification modules.
</objective>

<context>
@apps/web/src/routes/app-routes.tsx
@apps/web/src/routes/lazyPages.ts
@apps/web/src/pages/customize/identity-customization/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove gamification routes from web router</name>
  <files>apps/web/src/routes/app-routes.tsx</files>
  <action>
    Remove these route definitions:
    - L28: import { UserLeaderboard }
    - L101: Route for "community/leaderboard"
    - L133: Route for "leaderboard" redirect
    - L134: Route for "gamification" redirect
    - L136-138: Route for "gamification/achievements" redirect
    - L140-141: Route for "gamification/quests" redirect
    - L143: Route for "gamification/titles" redirect
    - L144: Route for "achievements" redirect
    - L145: Route for "quests" redirect
    - L146: Route for "titles" redirect

    Replace ALL gamification-related redirects with a single catch-all:
    ```tsx
    <Route path="gamification/*" element={<Navigate to="/" replace />} />
    <Route path="leaderboard" element={<Navigate to="/" replace />} />
    ```
    This ensures old URLs don't 404 — they redirect to home.
  </action>
  <verify>grep -n "gamification\|leaderboard\|UserLeaderboard" apps/web/src/routes/app-routes.tsx</verify>
  <done>Only catch-all redirects remain, no imports to deleted pages</done>
</task>

<task type="auto">
  <name>Task 2: Clean lazy page imports</name>
  <files>apps/web/src/routes/lazyPages.ts</files>
  <action>
    Remove all lazy() imports for gamification pages:
    - L84: UserLeaderboard lazy import
    - L94 area: Premium & Gamification section comment and any lazy imports
    - Any other lazy imports pointing to deleted pages/gamification/ or pages/leaderboard/
    - Remove the progression-customization lazy import if it exists
  </action>
  <verify>grep -n "gamification\|leaderboard\|Leaderboard\|progression" apps/web/src/routes/lazyPages.ts</verify>
  <done>No lazy imports to deleted gamification pages remain</done>
</task>

<task type="auto">
  <name>Task 3: Remove LEGACY_BORDER_ID_TO_V2_TYPE</name>
  <files>
    apps/web/src/pages/customize/identity-customization/constants.ts
    apps/web/src/pages/customize/identity-customization/useIdentityCustomization.ts
    apps/web/src/pages/customize/identity-customization/index.ts
  </files>
  <action>
    1. Delete LEGACY_BORDER_ID_TO_V2_TYPE map from constants.ts (around line 128)
    2. Remove its import in useIdentityCustomization.ts (L29) and any usage (L196)
       — replace the usage with direct border type lookup from the canonical registry
    3. Remove re-export from index.ts (L22)
  </action>
  <verify>grep -rn "LEGACY_BORDER_ID_TO_V2_TYPE" apps/web/src/ 2>&1</verify>
  <done>Legacy border duplicate map completely removed</done>
</task>

<task type="auto">
  <name>Task 4: Clean mobile navigation/routes</name>
  <files>apps/mobile/src/navigation/</files>
  <action>
    Search mobile navigation files for gamification-related routes:
    - Remove screen registrations for deleted gamification screens
    - Remove leaderboard screen registration
    - Remove progression-customization screen registration
    - Clean up any tab bar or drawer entries referencing deleted screens
    - Add redirects or fallbacks for any deep links that pointed to deleted screens
  </action>
  <verify>grep -rn "gamification\|Gamification\|leaderboard\|Leaderboard\|progression" apps/mobile/src/navigation/ --include="*.ts" --include="*.tsx" | head -10</verify>
  <done>Mobile navigation cleaned of all gamification screen references</done>
</task>

<task type="auto">
  <name>Task 5: Global dangling reference sweep</name>
  <files>apps/web/src/ apps/mobile/src/</files>
  <action>
    Run a comprehensive search across both apps for any remaining references to:
    - "gamification" (case insensitive) in import statements
    - "useGamificationStore", "usePrestigeStore", "useSeasonalEventStore"
    - "useMarketplaceStore", "useCoinShopStore", "useReferralStore"
    - "useAvatarBorderStore", "useGamificationFacade"
    - "gamification-hub", "quests-page", "titles-page", "achievements-page"
    - "leaderboard-page", "leaderboard-widget"
    - "LevelGate", "XpToast", "level-up-modal", "StreakTracker"

    Fix all found references by removing dead imports and their usages.
    For any component that imported gamification components to render them,
    remove the JSX usage too.
  </action>
  <verify>grep -rn "from.*gamification\|useGamification\|usePrestige\|useSeasonal\|useMarketplace\|useCoinShop\|useReferral\|useAvatarBorder\|LevelGate\|XpToast\|StreakTracker" apps/web/src/ apps/mobile/src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v "__tests__" | head -20</verify>
  <done>No dangling references to any deleted gamification module</done>
</task>

<task type="auto">
  <name>Task 6: TypeScript compilation check</name>
  <files>apps/web/ apps/mobile/</files>
  <action>
    Run TypeScript compilation for the web app to verify no type errors:
    `cd apps/web && npx tsc --noEmit 2>&1 | head -50`
    Fix any remaining errors caused by the deletions.
    Focus on: missing imports, unused variables left behind, broken re-exports.
  </action>
  <verify>cd apps/web && npx tsc --noEmit --pretty false 2>&1 | grep "error" | wc -l</verify>
  <done>Zero TypeScript errors related to gamification deletions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] No gamification routes in app-routes.tsx (only catch-all redirect)
- [ ] No lazy imports for deleted pages
- [ ] LEGACY_BORDER_ID_TO_V2_TYPE removed
- [ ] Mobile navigation cleaned
- [ ] Global scan shows zero dangling imports
- [ ] TypeScript compiles with zero gamification-related errors
</verification>

<success_criteria>
- App compiles cleanly
- All routes for deleted pages redirect gracefully
- No dead code referencing deleted modules
</success_criteria>

<output>
After completion, create `.gsd/phases/26-great-delete/26-04-SUMMARY.md`
</output>
