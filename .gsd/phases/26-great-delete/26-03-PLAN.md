---
phase: 26-great-delete
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/pages/gamification/ (entire directory - 33 files)
  - apps/web/src/pages/leaderboard/ (entire directory - 19 files)
  - apps/web/src/pages/customize/progression-customization/ (entire directory - 10 files)
  - apps/web/src/pages/customize/progression-customization.tsx
  - apps/web/src/pages/community/user-leaderboard.tsx
  - apps/web/src/modules/gamification/components/ (entire directory - 214 files)
  - apps/web/src/modules/gamification/types/index.ts
  - apps/web/src/modules/gamification/index.ts
  - apps/mobile/src/screens/gamification/ (entire directory - 24 files)
  - apps/mobile/src/screens/community/leaderboard-screen.tsx
  - apps/mobile/src/screens/community/leaderboard/ (entire directory)
  - apps/mobile/src/screens/customize/progression-customization-screen.tsx
  - apps/mobile/src/features/gamification/ (entire directory)
  - apps/mobile/src/components/gamification/ (entire directory)
  - apps/mobile/src/modules/gamification/ (entire directory)
autonomous: true
must_haves:
  truths:
    - 'All gamification pages deleted (web gamification hub, achievements, quests, titles, leaderboard, progression customization)'
    - 'All 214 gamification components deleted (web modules/gamification/components/)'
    - 'All gamification types deleted (web modules/gamification/types/)'
    - 'Web gamification module barrel deleted'
    - 'All mobile gamification screens, features, and components deleted'
    - 'Standalone page wrappers for leaderboard and progression deleted'
  artifacts:
    - apps/web/src/modules/gamification/ (should be nearly empty — only keep if achievement types needed)
  key_links:
    - 'Run in parallel with Plans 26-01 and 26-02'
    - 'Plan 26-04 handles route cleanup after these pages are gone'
---

<objective>
Delete all gamification pages, components, screens, and UI modules from web and mobile.
This removes the entire frontend UI layer for the deprecated gamification system.

Scale: ~300+ files across web pages, components, mobile screens, features, and components.

Strategy: Delete entire directories rather than individual files wherever possible.
</objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Part 2 — Frontend Pages and Components)
@apps/web/src/pages/gamification/
@apps/web/src/pages/leaderboard/
@apps/web/src/modules/gamification/components/
@apps/mobile/src/screens/gamification/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete web gamification pages directory</name>
  <files>apps/web/src/pages/gamification/ (33 files)</files>
  <action>
    Delete the entire `apps/web/src/pages/gamification/` directory including:
    - gamification-hub-page.tsx, useGamificationHub.ts, index.ts
    - achievements-page.tsx and achievements-page/ subdirectory
    - quests-page.tsx and quests-page/ subdirectory
    - titles-page.tsx and titles-page/ subdirectory
    - components/ subdirectory (quick-link, stat-card, recent-achievements)
  </action>
  <verify>ls apps/web/src/pages/gamification/ 2>&1 | grep -c "No such"</verify>
  <done>Entire web gamification pages directory deleted</done>
</task>

<task type="auto">
  <name>Task 2: Delete web leaderboard pages directory</name>
  <files>apps/web/src/pages/leaderboard/ (19 files)</files>
  <action>
    Delete the entire `apps/web/src/pages/leaderboard/` directory including:
    - leaderboard-page.tsx, page.tsx, sections.tsx, constants.tsx
    - effects.tsx, types.ts, index.ts, utils.tsx
    - sections/ subdirectory (loading-state, pagination, headers, filters, tabs, podium, rankings)
  </action>
  <verify>ls apps/web/src/pages/leaderboard/ 2>&1 | grep -c "No such"</verify>
  <done>Entire web leaderboard pages directory deleted</done>
</task>

<task type="auto">
  <name>Task 3: Delete progression customization pages</name>
  <files>
    apps/web/src/pages/customize/progression-customization/ (10 files)
    apps/web/src/pages/customize/progression-customization.tsx
    apps/web/src/pages/community/user-leaderboard.tsx
  </files>
  <action>
    1. Delete entire `apps/web/src/pages/customize/progression-customization/` directory
    2. Delete standalone wrapper `apps/web/src/pages/customize/progression-customization.tsx`
    3. Delete `apps/web/src/pages/community/user-leaderboard.tsx`
    4. Check if the community/ directory or customize/ directory has other remaining files
       — do NOT delete the parent directories if they contain non-gamification pages
  </action>
  <verify>
    ls apps/web/src/pages/customize/progression-customization/ 2>&1;
    ls apps/web/src/pages/customize/progression-customization.tsx 2>&1;
    ls apps/web/src/pages/community/user-leaderboard.tsx 2>&1
  </verify>
  <done>Progression customization and user-leaderboard pages deleted</done>
</task>

<task type="auto">
  <name>Task 4: Delete web gamification components directory</name>
  <files>apps/web/src/modules/gamification/components/ (214 files)</files>
  <action>
    Delete the entire `apps/web/src/modules/gamification/components/` directory including
    all subdirectories: __tests__, xp-toast, streak-tracker, level-progress, level-up-modal,
    level-gate, achievement-notification, achievement-display, title-badge, user-stars, badges,
    daily-rewards, leaderboard-widget, price-history-chart, quest-panel, marketplace-page,
    referral-dashboard, events, avatar-border, and all root-level components.
  </action>
  <verify>ls apps/web/src/modules/gamification/components/ 2>&1 | grep -c "No such"</verify>
  <done>All 214 gamification components deleted</done>
</task>

<task type="auto">
  <name>Task 5: Delete web gamification types and module barrel</name>
  <files>
    apps/web/src/modules/gamification/types/index.ts
    apps/web/src/modules/gamification/index.ts
  </files>
  <action>
    1. Delete `apps/web/src/modules/gamification/types/` directory
    2. Delete `apps/web/src/modules/gamification/index.ts`
    3. Check what remains in `apps/web/src/modules/gamification/` — it should be empty
       (store/ and hooks/ deleted by Plan 26-02). If empty, delete the entire
       gamification module directory.
  </action>
  <verify>ls -la apps/web/src/modules/gamification/ 2>&1</verify>
  <done>Gamification types and module barrel deleted</done>
</task>

<task type="auto">
  <name>Task 6: Delete mobile gamification screens</name>
  <files>
    apps/mobile/src/screens/gamification/ (24 files)
    apps/mobile/src/screens/community/leaderboard-screen.tsx
    apps/mobile/src/screens/community/leaderboard/ (directory)
    apps/mobile/src/screens/customize/progression-customization-screen.tsx
  </files>
  <action>
    1. Delete entire `apps/mobile/src/screens/gamification/` directory
    2. Delete leaderboard-screen.tsx and leaderboard/ subdirectory from community/
    3. Delete progression-customization-screen.tsx from customize/
    4. Verify parent directories still have other (non-gamification) screens
  </action>
  <verify>ls apps/mobile/src/screens/gamification/ 2>&1 | grep -c "No such"</verify>
  <done>All mobile gamification screens deleted</done>
</task>

<task type="auto">
  <name>Task 7: Delete mobile gamification features, modules, and components</name>
  <files>
    apps/mobile/src/features/gamification/ (entire directory)
    apps/mobile/src/components/gamification/ (entire directory)
    apps/mobile/src/modules/gamification/ (entire directory)
  </files>
  <action>
    Delete all three directories entirely:
    1. apps/mobile/src/features/gamification/ (types, hooks, components, services, index)
    2. apps/mobile/src/components/gamification/ (title-badge, level-up-modal, achievement-notification, animated-border, streak-display, xp-toast, level-gate, level-progress, quest-panel, inline-title, index)
    3. apps/mobile/src/modules/gamification/ (AvatarBorder components - lottieBorderMap, BorderGlow, StaticRing, etc.)
  </action>
  <verify>
    ls apps/mobile/src/features/gamification/ 2>&1;
    ls apps/mobile/src/components/gamification/ 2>&1;
    ls apps/mobile/src/modules/gamification/ 2>&1
  </verify>
  <done>All mobile gamification feature directories deleted</done>
</task>

<task type="auto">
  <name>Task 8: Clean shared-types gamification file</name>
  <files>packages/shared-types/src/gamification.ts</files>
  <action>
    Evaluate `packages/shared-types/src/gamification.ts`:
    - If it contains ONLY gamification types (XP, Quest, Coin, Leaderboard, etc.) → delete it
    - If it contains Achievement types that are still needed → extract Achievement types
      to a new `packages/shared-types/src/achievements.ts`, then delete gamification.ts
    - Update the shared-types barrel export accordingly
    - Check consumers of the shared-types gamification module across the monorepo
  </action>
  <verify>grep -rn "shared-types.*gamification\|@cgraph/shared-types.*gamification" apps/ packages/ --include="*.ts" --include="*.tsx" | head -10</verify>
  <done>Shared gamification types handled — deleted or split</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] apps/web/src/pages/gamification/ deleted
- [ ] apps/web/src/pages/leaderboard/ deleted
- [ ] apps/web/src/pages/customize/progression-customization/ deleted
- [ ] apps/web/src/modules/gamification/ deleted (entire module)
- [ ] apps/mobile/src/screens/gamification/ deleted
- [ ] apps/mobile/src/features/gamification/ deleted
- [ ] apps/mobile/src/components/gamification/ deleted
- [ ] apps/mobile/src/modules/gamification/ deleted
- [ ] packages/shared-types gamification handled
- [ ] ~300 files removed total
</verification>

<success_criteria>
- All gamification UI removed from web and mobile
- No orphaned directories remain
- Shared types handled appropriately
</success_criteria>

<output>
After completion, create `.gsd/phases/26-great-delete/26-03-SUMMARY.md`
</output>
