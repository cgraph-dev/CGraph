---
phase: 31-forums-discovery
plan: 01
type: execute
wave: 1
depends_on:
  - 30-01
files_modified:
  - apps/backend/lib/cgraph/discovery/ (new context)
  - apps/backend/lib/cgraph/forums/ (new context)
  - apps/backend/lib/cgraph_web/controllers/feed_controller.ex (new)
  - apps/backend/lib/cgraph_web/controllers/forum_controller.ex (new)
  - apps/backend/priv/repo/migrations/ (discovery + forum tables)
autonomous: true
must_haves:
  truths:
    - 'user_frequencies and topics tables created'
    - 'Forums tables created (threads, replies, thread_metrics)'
    - 'Feed ranking implemented (Pulse, Fresh, Rising, Deep Cut, Frequency Surf modes)'
    - 'Community health score computed (backend only)'
    - 'Forums API endpoints functional'
    - 'Feed API endpoints functional'
  artifacts:
    - apps/backend/lib/cgraph/discovery/ (new context)
    - apps/backend/lib/cgraph/forums/ (new context)
    - apps/backend/lib/cgraph_web/controllers/feed_controller.ex (new)
    - apps/backend/lib/cgraph_web/controllers/forum_controller.ex (new)
  key_links:
    - 'Depends on Phase 30 Pulse backend (feed ranking uses pulse data)'
    - 'Plan 31-02 builds frontend'
---

<objective>
Build Forums and Discovery backend from scratch. 100% greenfield.
Create forums (communities, threads, reply trees), feed ranking with 5 modes,
user frequencies/topics system, and community health scoring.
</objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Part 8 — Discovery System)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Discovery database tables</name>
  <files>apps/backend/priv/repo/migrations/</files>
  <action>
    Create migration for:
    - topics (id, name, icon, category, created_at)
    - user_frequencies (user_id, topic_id, weight 0-100) — composite PK
    - post_metrics (post_id, weighted_resonates, reply_depth_avg,
      read_time_signal, cross_community_refs)
    - Seed initial topics (photography, gaming, finance, design, science, etc.)
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>Discovery tables created</done>
</task>

<task type="auto">
  <name>Task 2: Create Forums database tables</name>
  <files>apps/backend/priv/repo/migrations/</files>
  <action>
    Create migration for forum tables:
    - forum_threads (id, community_id, author_id, title, body, content_locked,
      preview_chars, unlock_cost_nodes, created_at, updated_at)
    - forum_replies (id, thread_id, parent_reply_id, author_id, body, created_at)
    - thread_metrics for feed ranking
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>Forum tables created</done>
</task>

<task type="auto">
  <name>Task 3: Build Feed ranking system</name>
  <files>apps/backend/lib/cgraph/discovery/</files>
  <action>
    Build feed ranking with 5 modes:
    - Pulse: weighted resonates (40%) + reply depth (25%) + read time (20%)
      + cross-community refs (10%) - age penalty (5%)
    - Fresh: chronological
    - Rising: recent posts with high engagement velocity
    - Deep Cut: older posts with high pulse but low total views
    - Frequency Surf: filter by specific community

    Use PostgreSQL computed scores as in the plan.
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>5 feed modes implemented</done>
</task>

<task type="auto">
  <name>Task 4: Build community health scoring</name>
  <files>apps/backend/lib/cgraph/discovery/community_health.ex</files>
  <action>
    Implement backend-only community health score:
    - positive_ratio * 0.30 + new_voice_ratio * 0.25 + reply_depth * 0.25
      - mod_penalty * 0.10 - dominance_penalty * 0.10
    - Cache results (ETS or Redis)
    - Never expose raw score to API (internal ranking only)
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>Community health scoring functional</done>
</task>

<task type="auto">
  <name>Task 5: Create API endpoints</name>
  <files>apps/backend/lib/cgraph_web/</files>
  <action>
    Feed endpoints:
    - GET /api/v1/feed?mode=pulse&page=1
    - GET /api/v1/topics (list available topics)
    - PUT /api/v1/frequencies (update user frequency weights)

    Forum endpoints:
    - POST /api/v1/communities/:id/threads (create thread)
    - GET /api/v1/communities/:id/threads (list threads)
    - GET /api/v1/threads/:id (thread with reply tree)
    - POST /api/v1/threads/:id/replies (reply to thread)
    - POST /api/v1/threads/:id/unlock (unlock with Nodes)
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>All forum and feed API endpoints created</done>
</task>

</tasks>

<verification>
- [ ] Database tables created
- [ ] Feed ranking works with 5 modes
- [ ] Community health computes
- [ ] Forum CRUD endpoints work
- [ ] Backend compiles cleanly
</verification>

<success_criteria>
- Complete Forums + Discovery backend
</success_criteria>

<output>
After completion, create `.gsd/phases/31-forums-discovery/31-01-SUMMARY.md`
</output>
