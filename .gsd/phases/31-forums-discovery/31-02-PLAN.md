---
phase: 31-forums-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
  - 30-02
files_modified:
  - apps/web/src/modules/discovery/ (new directory — greenfield)
  - apps/web/src/pages/feed/ (new page directory — greenfield)
  - apps/web/src/pages/settings/discovery/ (new settings page — greenfield)
  - apps/web/src/modules/forums/components/thread-card.tsx (extend with content gating UI)
  - apps/web/src/modules/forums/components/thread-view/ (extend with unlock flow)
  - apps/web/src/modules/forums/types/index.ts (add content gating fields)
  - apps/web/src/routes/lazyPages.ts (add Feed, DiscoverySettings lazy imports)
  - apps/web/src/routes/app-routes.tsx (add /feed + /settings/discovery routes)
autonomous: true
must_haves:
  truths:
    - 'Feed page at /feed with 5 mode tabs (Pulse, Fresh, Rising, Deep Cut, Frequency Surf)'
    - 'Feed calls GET /api/v1/feed?mode=X&cursor=Y with infinite scroll'
    - 'Frequency picker (topic cards + weight sliders) in onboarding + settings'
    - 'Settings → Discovery page for adjusting frequency weights'
    - 'Thread card shows content gating indicator (locked/preview)'
    - 'Thread view has unlock CTA for gated content (placeholder — actual payment in Phase 32)'
    - 'Pulse reaction buttons integrated into feed post cards'
    - 'Routes registered (/feed, /settings/discovery) and lazy loaded'
  artifacts:
    - apps/web/src/modules/discovery/ (new directory)
    - apps/web/src/modules/discovery/components/feed-mode-tabs.tsx
    - apps/web/src/modules/discovery/components/frequency-picker.tsx
    - apps/web/src/modules/discovery/components/topic-card.tsx
    - apps/web/src/modules/discovery/hooks/useFeed.ts
    - apps/web/src/modules/discovery/hooks/useFrequencies.ts
    - apps/web/src/modules/discovery/store/discoveryStore.ts
    - apps/web/src/pages/feed/feed-page.tsx
    - apps/web/src/pages/feed/feed-post-card.tsx
    - apps/web/src/pages/settings/discovery/discovery-settings.tsx
  key_links:
    - 'Depends on Plan 31-01 (FeedController, TopicController endpoints)'
    - 'Depends on Plan 30-02 (PulseReactions component — reused in feed cards)'
    - 'EXISTING: modules/forums/ has 316+ files — NOT rebuilt, only extended'
    - 'EXISTING: pages/forums/ has 109 files — NOT rebuilt'
    - 'EXISTING: ForumRoutes in route-groups/forum-routes.tsx — NOT modified'
    - 'EXISTING: lazyPages.ts has 12 forum lazy imports — we ADD new ones'
    - 'EXISTING: explore-page.tsx at /explore — remains as community browser, feed is new'
---

<objective>
Build Discovery frontend (100% greenfield) and extend existing forum components
with content gating UI.

CRITICAL: The forum UI ALREADY EXISTS extensively:

- apps/web/src/modules/forums/ — 316+ files (store, hooks, components, pages, types)
- apps/web/src/pages/forums/ — 109 files (forums, threads, boards, posts, moderation, etc.)
- apps/web/src/routes/route-groups/forum-routes.tsx — 15+ forum routes registered
- apps/web/src/routes/lazyPages.ts — 12 forum lazy imports
- Thread list, thread detail, reply tree, voting, polls — ALL EXIST

DO NOT recreate any existing forum UI. This plan creates:

1. NEW modules/discovery/ — feed hooks, frequency picker, topic cards, store
2. NEW pages/feed/ — feed page with 5 ranked modes
3. NEW pages/settings/discovery/ — frequency adjustment settings
4. EXTEND existing thread components with content gating indicators </objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Part 8 — Feed Architecture, User Frequencies)
Existing forum UI: apps/web/src/modules/forums/ (316+ files)
Existing forum pages: apps/web/src/pages/forums/ (109 files)
Existing Pulse UI: apps/web/src/modules/pulse/ (Phase 30)
Routes: app-routes.tsx (inline + route groups), lazyPages.ts (lazy imports)
API: import { api } from '@/lib/api'
State: Zustand stores (modules/**/store/*.ts pattern)
Styling: Tailwind + cn() utility
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Discovery module + Feed hooks</name>
  <files>apps/web/src/modules/discovery/</files>
  <action>
    Create discovery module (fully greenfield):

    1. hooks/useFeed.ts:
       - Fetch from GET /api/v1/feed?mode=X&cursor=Y
       - Use TanStack Query (useInfiniteQuery) with cursor pagination
       - Support mode switching without refetch flicker
       - Return { posts, fetchNextPage, hasNextPage, isLoading }
       - Query key: ['feed', mode, communityId?]

    2. hooks/useFrequencies.ts:
       - GET /api/v1/frequencies — current user's topic weights
       - PUT /api/v1/frequencies — update weights
       - GET /api/v1/topics — list all available topics
       - Use TanStack Query (useQuery + useMutation)

    3. store/discoveryStore.ts:
       - Zustand store with: activeMode (default 'pulse'),
         selectedCommunityId (for frequency_surf mode)
       - Actions: setMode, setCommunityId
       - Follow project pattern: store in modules/**/store/*.ts

    4. components/feed-mode-tabs.tsx:
       - Tab bar: [Pulse] [Fresh] [Rising] [Deep Cut] [Frequency Surf]
       - Active tab highlighted, click switches mode
       - Frequency Surf shows community selector dropdown
       - Use cn() for styling, Tailwind classes

    5. components/topic-card.tsx:
       - Topic display card with icon + name
       - Selectable state (for frequency picker)
       - Grid layout (multi-select)

    6. components/frequency-picker.tsx:
       - Multi-select grid of topic-cards
       - Weight slider (0-100) per selected topic
       - Save button → PUT /api/v1/frequencies
       - Used in both onboarding and settings

    7. index.ts barrel export

    Pattern: import { api } from '@/lib/api' for HTTP calls

  </action>
  <verify>cd apps/web && npx tsc --noEmit --pretty false -p tsconfig.app.json 2>&1 | grep 'discovery' | head -20</verify>
  <done>Discovery module with hooks, store, and components built</done>
</task>

<task type="auto">
  <name>Task 2: Build Feed page</name>
  <files>apps/web/src/pages/feed/</files>
  <action>
    Create feed page (fully greenfield):

    1. feed-page.tsx:
       - Import FeedModeTabs from modules/discovery
       - Import useFeed hook
       - Render mode tabs at top
       - Infinite scroll list of feed-post-cards below
       - Use IntersectionObserver for infinite scroll trigger
       - Empty state: "Follow topics to see your feed" with link to /settings/discovery
       - Loading state: skeleton cards

    2. feed-post-card.tsx:
       - Display: title, preview text, author info, community badge, timestamp
       - Metrics: resonates count, reply count, view count
       - Pulse reaction buttons (import PulseReactions from modules/pulse)
       - Content gating indicator: 🔒 icon if is_content_gated
       - Click navigates to existing thread detail page (/forums/thread/:id pattern)
       - Use cn() for styling, Tailwind
       - Match card style to existing thread-card.tsx density

    3. index.ts barrel export

    NOTE: Feed page is a NEW top-level page at /feed.
    It's separate from /explore (community browser) and /forums (forum listing).

  </action>
  <verify>ls apps/web/src/pages/feed/ 2>&1</verify>
  <done>Feed page with infinite scroll and post cards built</done>
</task>

<task type="auto">
  <name>Task 3: Build Discovery settings page + extend thread gating UI</name>
  <files>apps/web/src/pages/settings/discovery/ apps/web/src/modules/forums/</files>
  <action>
    1. Create pages/settings/discovery/discovery-settings.tsx:
       - Route: /settings/discovery
       - Import FrequencyPicker from modules/discovery
       - Show current frequency weights with edit capability
       - Save changes via PUT /api/v1/frequencies
       - Title: "Discovery Preferences" — "Adjust your interests"

    2. Extend existing modules/forums/types/index.ts:
       - ADD to Thread type: is_content_gated?: boolean, gate_price_nodes?: number,
         gate_preview_chars?: number
       - DO NOT remove or rename any existing type fields

    3. Extend existing thread display components:
       - In thread-card.tsx or thread-list.tsx: show 🔒 lock icon + "X Nodes to unlock"
         badge if thread.is_content_gated === true
       - In thread-view/ (thread detail): if content is gated and user hasn't unlocked,
         show truncated preview (gate_preview_chars) with unlock CTA button
       - Unlock CTA is a placeholder button ("Unlock for X Nodes") — actual payment
         flow comes in Phase 32 (Nodes Monetization)

    NOTE: Only EXTEND existing components. Add conditional rendering for gated content.
    Do not restructure, rename, or remove existing component code.

  </action>
  <verify>cd apps/web && npx tsc --noEmit --pretty false -p tsconfig.app.json 2>&1 | grep -E 'discovery|feed' | head -20</verify>
  <done>Discovery settings and thread gating UI built</done>
</task>

<task type="auto">
  <name>Task 4: Register routes + lazy imports</name>
  <files>apps/web/src/routes/app-routes.tsx apps/web/src/routes/lazyPages.ts</files>
  <action>
    1. In lazyPages.ts — ADD new lazy imports (do not modify existing):
       - FeedPage → '@/pages/feed/feed-page'
       - DiscoverySettings → '@/pages/settings/discovery/discovery-settings'

    2. In app-routes.tsx — ADD new routes:
       - { path: 'feed', element: <FeedPage /> } — inside protected routes
       - { path: 'settings/discovery', element: <DiscoverySettings /> }
         inside SettingsRoutes() group, OR inline if simpler

    3. Verify: /feed navigates to Feed page, /settings/discovery to frequency editor

    NOTE: Do NOT modify existing ForumRoutes, SettingsRoutes, or any other route groups.
    Only ADD new entries. Existing 12 forum lazy imports stay untouched.

  </action>
  <verify>grep -n "feed\|discovery" apps/web/src/routes/lazyPages.ts apps/web/src/routes/app-routes.tsx</verify>
  <done>Routes registered and lazy loaded for /feed and /settings/discovery</done>
</task>

</tasks>

<verification>
- [ ] Feed page renders at /feed with 5 mode tabs
- [ ] Mode tabs switch feed content (different API calls per mode)
- [ ] Infinite scroll loads more posts via cursor pagination
- [ ] Post cards show author, community, metrics, Pulse reactions
- [ ] Content-gated threads show lock icon in feed and forum listings
- [ ] Thread detail shows truncated preview for gated content with unlock CTA
- [ ] Discovery settings at /settings/discovery shows frequency picker
- [ ] Topic cards selectable with weight sliders
- [ ] All new components compile without TypeScript errors
- [ ] Routes lazy loaded (no impact on initial bundle size)
</verification>

<success_criteria>

- Complete Discovery frontend (feed page, frequency picker, settings)
- Thread content gating UI integrated with existing forum components
- Existing forum UI (316+ files) untouched except minimal gating extensions </success_criteria>

<output>
After completion, create `.gsd/phases/31-forums-discovery/31-02-SUMMARY.md`
</output>
