---
phase: 31-forums-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
  - 30-02
files_modified:
  - apps/web/src/modules/forums/ (new directory)
  - apps/web/src/modules/discovery/ (new directory)
  - apps/web/src/pages/feed/ (new feed page)
  - apps/web/src/pages/forums/ (new forum pages)
  - apps/web/src/routes/app-routes.tsx (new routes)
autonomous: true
must_haves:
  truths:
    - 'Feed page with 5 mode tabs (Pulse, Fresh, Rising, Deep Cut, Frequency Surf)'
    - 'Forum thread list page with community context'
    - 'Thread detail page with nested reply tree'
    - 'Thread creation with content gating option (lock with Nodes)'
    - 'Frequency onboarding flow (topic picker, weight sliders)'
    - 'Pulse reaction UI integrated into threads'
    - 'Routes registered for feed and forum pages'
  artifacts:
    - apps/web/src/modules/forums/ (new)
    - apps/web/src/modules/discovery/ (new)
    - apps/web/src/pages/feed/ (new)
    - apps/web/src/pages/forums/ (new)
  key_links:
    - 'Depends on Plans 31-01 (backend) and 30-02 (Pulse reaction UI)'
---

<objective>
Build Forums and Discovery frontend from scratch. Create feed page with 5 ranking modes,
forums UI with thread list, thread detail, reply tree, and frequency
onboarding/settings. Integrate Pulse reactions into threads.
</objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Part 8 — Feed Architecture, User Frequencies)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Feed page</name>
  <files>apps/web/src/pages/feed/</files>
  <action>
    Create feed page with:
    - 5 mode tabs: Pulse (default), Fresh, Rising, Deep Cut, Frequency Surf
    - Infinite scroll loading
    - Post cards with Resonate/Fade/Not-for-me buttons
    - Frequency Surf shows community-specific feed
    - Wire to GET /api/v1/feed?mode=X&page=N
  </action>
  <verify>ls apps/web/src/pages/feed/ 2>&1</verify>
  <done>Feed page with 5 modes built</done>
</task>

<task type="auto">
  <name>Task 2: Build Forum pages</name>
  <files>apps/web/src/pages/forums/</files>
  <action>
    Create forum pages:
    - Thread list page (within community context)
    - Thread detail page with nested reply tree (recursive component)
    - Thread creation form with content gating option
    - Reply composition inline
    - Content unlock flow (pay Nodes to read locked content)
  </action>
  <verify>ls apps/web/src/pages/forums/ 2>&1</verify>
  <done>Forum pages built</done>
</task>

<task type="auto">
  <name>Task 3: Build Frequency picker (onboarding + settings)</name>
  <files>apps/web/src/modules/discovery/</files>
  <action>
    Create frequency picker UI:
    - Topic cards with icons (multi-select grid)
    - Weight sliders (0-100) for fine-tuning
    - Onboarding step integration (Step 3/5: "What are you into?")
    - Settings page (Settings → Discovery) for adjusting frequencies
  </action>
  <verify>ls apps/web/src/modules/discovery/ 2>&1</verify>
  <done>Frequency picker built for onboarding and settings</done>
</task>

<task type="auto">
  <name>Task 4: Register routes</name>
  <files>apps/web/src/routes/app-routes.tsx apps/web/src/routes/lazyPages.ts</files>
  <action>
    Add routes:
    - /feed → Feed page (lazy loaded)
    - /community/:id/forums → Thread list (lazy loaded)
    - /threads/:id → Thread detail (lazy loaded)
    - /threads/new?community=:id → Thread creation
    Add lazy page imports for all new pages.
  </action>
  <verify>grep -n "feed\|forum\|thread" apps/web/src/routes/app-routes.tsx</verify>
  <done>Routes registered and lazy loaded</done>
</task>

</tasks>

<verification>
- [ ] Feed page shows posts in 5 modes
- [ ] Thread list and detail pages work
- [ ] Reply tree renders nested replies
- [ ] Content gating and unlock flow works
- [ ] Frequency picker in onboarding and settings
- [ ] Routes registered
</verification>

<success_criteria>
- Complete Forums + Discovery frontend
- Integrated with Pulse reactions
</success_criteria>

<output>
After completion, create `.gsd/phases/31-forums-discovery/31-02-SUMMARY.md`
</output>
