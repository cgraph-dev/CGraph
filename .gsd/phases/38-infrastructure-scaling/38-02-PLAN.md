---
phase: 38-infrastructure-scaling
plan: 02
type: execute
wave: 1
depends_on:
  - 37-06
files_modified:
  - apps/backend/lib/cgraph/cache/multi_tier_cache.ex (new — ⚠️ cache/ already has 10 files:
      l1.ex, l2.ex, l3.ex, tiered.ex, unified.ex, warming.ex, stats.ex, policy.ex, invalidation.ex,
      cache.ex. AUDIT before creating — may need to EXTEND tiered.ex or unified.ex instead)
  - apps/backend/lib/cgraph/cache/cache_warmer.ex (update — ⚠️ warming.ex already exists in cache/.
    EXTEND, do not recreate)
  - apps/backend/lib/cgraph/cache/cache_invalidator.ex (update — ⚠️ invalidation.ex already exists
    in cache/. EXTEND, do not recreate)
  - apps/backend/lib/cgraph/archival/archival.ex (new — context)
  - apps/backend/lib/cgraph/archival/archive_policy.ex (new)
  - apps/backend/lib/cgraph/workers/archival_worker.ex (update — ⚠️ message_archival_worker.ex
    already exists. EXTEND or rename, do not create duplicate)
  - apps/backend/lib/cgraph/workers/cache_warmer_worker.ex (new)
  - apps/backend/config/runtime.exs (update — cache config)
autonomous: true
must_haves:
  truths:
    - 'MultiTierCache: L1 (ETS, 1min TTL), L2 (Redis, 15min TTL), L3 (Postgres, permanent)'
    - 'Cache key convention: {module}:{id}:{version}'
    - 'CacheWarmer: preload hot data on startup + periodic refresh'
    - 'CacheInvalidator: PubSub subscriber, invalidates on write events'
    - 'Archival context: archive_old_messages/1, archive_inactive_threads/1, restore_archive/2'
    - 'ArchivePolicy: days_threshold (default 365), target_table, archive_table, conditions'
    - 'ArchivalWorker: monthly Oban cron, moves old data to archive tables'
    - 'Archive tables use TimescaleDB hypertables where available'
  artifacts:
    - apps/backend/lib/cgraph/cache/multi_tier_cache.ex
    - apps/backend/lib/cgraph/cache/cache_warmer.ex
    - apps/backend/lib/cgraph/cache/cache_invalidator.ex
    - apps/backend/lib/cgraph/archival/archival.ex
    - apps/backend/lib/cgraph/archival/archive_policy.ex
    - apps/backend/lib/cgraph/workers/archival_worker.ex
    - apps/backend/lib/cgraph/workers/cache_warmer_worker.ex
  key_links:
    - '⚠️ EXISTING CACHE SYSTEM (10 files): apps/backend/lib/cgraph/cache/ contains l1.ex, l2.ex,
      l3.ex, tiered.ex, unified.ex, warming.ex, stats.ex, policy.ex, invalidation.ex, cache.ex. This
      is NOT basic caching — it is a full multi-tier system. AUDIT ALL existing files before
      creating new ones to avoid duplication.'
    - '⚠️ EXISTING ARCHIVAL WORKER: message_archival_worker.ex already exists in workers/. Extend it
      for general archival or rename appropriately.'
    - 'Redis config: apps/backend/config/ — existing Redis setup'
    - 'PubSub: CGraphWeb.PubSub — for cache invalidation (invalidation.ex may already do this)'
  pre_tasks:
    - 'MANDATORY AUDIT: Read all 10 files in apps/backend/lib/cgraph/cache/ before creating any new
      cache modules. Document what each file does and identify genuine gaps.'
    - 'MANDATORY AUDIT: Read message_archival_worker.ex before creating archival_worker.ex'
---

<objective>
Build multi-tier caching (ETS → Redis → Postgres), cache warming/invalidation, and data archival
system with policies and Oban workers.

ATOMIC_PLAN tasks: 5.7 (Multi-Tier Cache), 5.8 (Cache Warmer), 5.9 (Cache Invalidation), 5.10
(Archival Context), 5.11 (Archive Policies), 5.12 (Archival Worker), 5.13 (Archive Tables), 5.14
(Cache Warmer Worker). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Audit + extend existing cache system (5.7)</name>
  <files>apps/backend/lib/cgraph/cache/</files>
  <action>⚠️ FIRST: Audit existing 10-file cache system (l1.ex, l2.ex, l3.ex, tiered.ex, unified.ex, warming.ex, stats.ex, policy.ex, invalidation.ex, cache.ex). THEN: identify gaps and extend existing modules. If multi_tier_cache.ex is genuinely needed (unified.ex or tiered.ex don't cover it), create it. Otherwise extend existing. Ensure: get/2 checks L1→L2→L3, TTLs configurable, telemetry on hit/miss per tier.</action>
</task>

<task type="auto">
  <name>Task 2: Cache warmer + invalidator (5.8, 5.9, 5.14)</name>
  <files>
    apps/backend/lib/cgraph/cache/cache_warmer.ex
    apps/backend/lib/cgraph/cache/cache_invalidator.ex
    apps/backend/lib/cgraph/workers/cache_warmer_worker.ex
  </files>
  <action>CacheWarmer: warm_on_boot/0 (top 1000 users, active conversations, hot threads). CacheInvalidator: GenServer subscribing to PubSub, pattern-matched invalidation rules. CacheWarmerWorker: Oban, hourly, refresh hot data.</action>
</task>

<task type="auto">
  <name>Task 3: Archival system (5.10–5.13)</name>
  <files>
    apps/backend/lib/cgraph/archival/archival.ex
    apps/backend/lib/cgraph/archival/archive_policy.ex
    apps/backend/lib/cgraph/workers/archival_worker.ex
  </files>
  <action>Archival context: archive_by_policy/1, restore/2, list_archives/1. ArchivePolicy: struct with days_threshold, target_table, archive_table, batch_size (1000). ArchivalWorker: monthly Oban cron, iterate policies, batch-move rows, verify counts. Create archive_messages + archive_forum_posts tables via migration.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
