---
phase: 38-infrastructure-scaling
plan: 01
type: execute
wave: 1
depends_on:
  - 37-06
files_modified:
  - apps/backend/lib/cgraph/sharding/shard_manager.ex (new)
  - apps/backend/lib/cgraph/sharding/shard_router.ex (new)
  - apps/backend/lib/cgraph/sharding/consistent_hash.ex (new)
  - apps/backend/lib/cgraph/sharding/shard_migration.ex (new)
  - apps/backend/config/runtime.exs (update — shard config)
  - apps/backend/priv/repo/migrations/<ts>_add_sharding_columns.exs (new)
autonomous: true
must_haves:
  truths:
    - 'ShardManager: GenServer managing shard topology, health checks, rebalancing'
    - 'ShardRouter: route queries to correct shard based on tenant/context key'
    - 'ConsistentHash: consistent hashing ring for shard distribution, virtual nodes'
    - 'ShardMigration: tools for live shard splitting/merging without downtime'
    - 'Messages table sharded by conversation_id hash (16 shards initial)'
    - 'Forum posts sharded by forum_id hash (8 shards initial)'
    - 'Shard topology configurable via runtime.exs'
  artifacts:
    - apps/backend/lib/cgraph/sharding/shard_manager.ex
    - apps/backend/lib/cgraph/sharding/shard_router.ex
    - apps/backend/lib/cgraph/sharding/consistent_hash.ex
    - apps/backend/lib/cgraph/sharding/shard_migration.ex
  key_links:
    - 'Repo: apps/backend/lib/cgraph/repo.ex — current single-repo setup'
    - 'Runtime config: apps/backend/config/runtime.exs'
    - 'Messages: apps/backend/lib/cgraph/messaging/message.ex — sharding target (schema has
      snowflake_id integer field for time-ordered IDs)'
    - '⚠️ Messages table already has monthly range partitioning by inserted_at — see migration
      20260213000001_partition_messages_table.exs. Audit before building shard infrastructure to
      understand existing partitioning vs. new sharding approach'
    - '⚠️ ReadRepo (apps/backend/lib/cgraph/read_repo.ex) exists for read replica routing — the
      shard router should integrate with this existing read/write split'
---

<objective>
Build database sharding infrastructure: shard manager, router, consistent hashing, and live
migration tools. Shard messages by conversation and forum posts by forum.

ATOMIC_PLAN tasks: 5.1 (Shard Manager), 5.2 (Shard Router), 5.3 (Consistent Hashing), 5.4 (Shard
Migration Tools), 5.5 (Message Sharding), 5.6 (Forum Post Sharding). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Consistent hashing + shard router (5.3, 5.2)</name>
  <files>
    apps/backend/lib/cgraph/sharding/consistent_hash.ex
    apps/backend/lib/cgraph/sharding/shard_router.ex
  </files>
  <action>ConsistentHash: ring with 256 virtual nodes per shard, add_node/2, remove_node/2, get_node/1. ShardRouter: route(table, key) returns shard Repo reference. Support read replicas.</action>
</task>

<task type="auto">
  <name>Task 2: Shard manager (5.1)</name>
  <files>
    apps/backend/lib/cgraph/sharding/shard_manager.ex
    apps/backend/config/runtime.exs
  </files>
  <action>GenServer: init with shard topology from config. Health checks every 30s. Auto-failover to read replica. Telemetry on shard events. Config in runtime.exs under :cgraph, :sharding.</action>
</task>

<task type="auto">
  <name>Task 3: Migration tools + schema changes (5.4–5.6)</name>
  <files>
    apps/backend/lib/cgraph/sharding/shard_migration.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>ShardMigration: split_shard/2 (live, dual-write then cutover), merge_shards/2, verify_integrity/1. Migration: add shard_key column to messages + forum_posts. Add hash index on shard_key. 16 shards for messages, 8 for forum posts.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
