---
phase: 38-infrastructure-scaling
plan: 05
type: execute
wave: 3
depends_on:
  - 38-03
  - 38-04
files_modified:
  - apps/backend/lib/cgraph/operations/runbook.ex (new — operational runbooks as code)
  - apps/backend/lib/cgraph/operations/capacity_planner.ex (new)
  - apps/backend/lib/cgraph/operations/disaster_recovery.ex (new)
  - apps/backend/lib/cgraph/operations/performance_profiler.ex (new)
  - infrastructure/scripts/dr_failover.sh (new)
  - docs/OPERATIONAL_RUNBOOKS.md (update — link to code runbooks)
autonomous: true
must_haves:
  truths:
    - 'Runbook module: executable runbook steps as functions with prereq checks'
    - 'CapacityPlanner: forecast_growth/1, recommend_scaling/1 based on metrics trends'
    - 'DisasterRecovery: initiate_failover/1, verify_replica/1, promote_replica/1,
      restore_from_backup/2'
    - 'PerformanceProfiler: flame_graph/1, slow_query_report/1, memory_analysis/1'
    - 'DR failover script with automated steps + manual verification gates'
    - 'Operational runbooks document updated with references to code modules'
  artifacts:
    - apps/backend/lib/cgraph/operations/runbook.ex
    - apps/backend/lib/cgraph/operations/capacity_planner.ex
    - apps/backend/lib/cgraph/operations/disaster_recovery.ex
    - apps/backend/lib/cgraph/operations/performance_profiler.ex
    - infrastructure/scripts/dr_failover.sh
  key_links:
    - 'Existing runbooks doc: docs/OPERATIONAL_RUNBOOKS.md'
    - 'Monitoring: apps/backend/lib/cgraph/monitoring/ — health + metrics from 38-04'
    - 'Fly config: apps/backend/fly.toml'
---

<objective>
Build operations toolkit: executable runbooks, capacity planning, disaster recovery procedures, and
performance profiling. Complete Phase 38 infrastructure work.

ATOMIC_PLAN tasks: 5.26 (Operational Runbooks), 5.27 (Capacity Planner), 5.28 (Disaster Recovery),
5.29 (Performance Profiler), 5.30 (DR Failover Script). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Runbooks + capacity planner (5.26, 5.27)</name>
  <files>
    apps/backend/lib/cgraph/operations/runbook.ex
    apps/backend/lib/cgraph/operations/capacity_planner.ex
  </files>
  <action>Runbook: define_runbook/2 macro, step/3 with prereq checks + rollback. Built-in runbooks: scale_up, scale_down, rotate_credentials, clear_cache. CapacityPlanner: forecast_growth (linear regression on last 30 days), recommend_scaling (CPU/memory/DB thresholds), generate_report/0.</action>
</task>

<task type="auto">
  <name>Task 2: Disaster recovery + profiler (5.28, 5.29)</name>
  <files>
    apps/backend/lib/cgraph/operations/disaster_recovery.ex
    apps/backend/lib/cgraph/operations/performance_profiler.ex
  </files>
  <action>DR: initiate_failover (check replica lag, promote, update DNS), verify_replica (SELECT comparison), restore_from_backup (pg_restore wrapper). PerformanceProfiler: flame_graph (eflame), slow_query_report (pg_stat_statements), memory_analysis (:erlang.memory + process inspection).</action>
</task>

<task type="auto">
  <name>Task 3: DR failover script + docs update (5.30)</name>
  <files>
    infrastructure/scripts/dr_failover.sh
    docs/OPERATIONAL_RUNBOOKS.md
  </files>
  <action>dr_failover.sh: detect primary failure, verify replica, promote, update Fly secrets, verify health. Update OPERATIONAL_RUNBOOKS.md with links to code modules in apps/backend/lib/cgraph/operations/.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
