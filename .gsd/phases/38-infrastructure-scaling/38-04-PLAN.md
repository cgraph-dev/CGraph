---
phase: 38-infrastructure-scaling
plan: 04
type: execute
wave: 2
depends_on:
  - 38-01
  - 38-02
files_modified:
  - apps/backend/lib/cgraph/cdn/cdn_manager.ex (new)
  - apps/backend/lib/cgraph/cdn/image_optimizer.ex (new — ⚠️ uploads/image_optimizer.ex already
    exists. Either extend existing one in-place or create cdn/ version that delegates to it. Do NOT
    create duplicate functionality.)
  - apps/backend/lib/cgraph/monitoring/health_dashboard.ex (new — ⚠️ health_check/ directory
    already exists with checks.ex (DB/cache/Redis/Oban/memory checks), reporter.ex (aggregated
    reports), and health_check.ex (GenServer). /health endpoint already served by
    health_controller.ex. AUDIT existing health_check/ before creating to avoid duplication —
    may need to EXTEND existing system instead)
  - apps/backend/lib/cgraph/monitoring/alerting.ex (new)
  - apps/backend/lib/cgraph/monitoring/metrics_collector.ex (new — ⚠️ CGraph.Metrics GenServer
    already exists at lib/cgraph/metrics.ex providing custom Prometheus metrics
    (counters/gauges/histograms). CGraphWeb.Telemetry + telemetry/handlers.ex + telemetry/metrics.ex
    also exist. AUDIT existing metrics infrastructure before creating to avoid duplication)
  - infrastructure/scripts/zero_downtime_migration.sh (new)
  - infrastructure/scripts/blue_green_deploy.sh (new)
autonomous: true
must_haves:
  truths:
    - 'CDN Manager: upload_to_cdn/2, purge/1, signed_url/2 with Cloudflare R2/S3 backend'
    - 'Image Optimizer: resize, WebP conversion, progressive JPEG, responsive srcset'
    - 'Health Dashboard: /health endpoint with component checks (DB, Redis, Oban, CDN)'
    - 'Alerting: threshold-based alerts via PagerDuty/Slack webhook integration'
    - 'MetricsCollector: Prometheus-compatible metrics for custom business metrics'
    - 'Zero-downtime migration script for Ecto migrations with safety checks'
    - 'Blue-green deploy script for Fly.io with health check verification'
  artifacts:
    - apps/backend/lib/cgraph/cdn/cdn_manager.ex
    - apps/backend/lib/cgraph/cdn/image_optimizer.ex
    - apps/backend/lib/cgraph/monitoring/health_dashboard.ex
    - apps/backend/lib/cgraph/monitoring/alerting.ex
    - apps/backend/lib/cgraph/monitoring/metrics_collector.ex
    - infrastructure/scripts/zero_downtime_migration.sh
    - infrastructure/scripts/blue_green_deploy.sh
  key_links:
    - 'Existing uploads: apps/backend/lib/cgraph/uploads/ — upload pipeline + image_optimizer.ex
      already exists here'
    - '⚠️ uploads/image_optimizer.ex ALREADY EXISTS — audit before creating cdn/image_optimizer.ex'
    - '⚠️ EXISTING HEALTH CHECK SYSTEM: apps/backend/lib/cgraph/health_check/ contains checks.ex
      (component checks for DB, Redis, Oban, cache, memory), reporter.ex (aggregated health
      reports), and health_check.ex (GenServer). health_controller.ex already serves /health
      endpoint. AUDIT before creating monitoring/health_dashboard.ex.'
    - '⚠️ EXISTING METRICS: CGraph.Metrics GenServer at lib/cgraph/metrics.ex already provides
      custom Prometheus counters/gauges/histograms. CGraphWeb.Telemetry supervisor +
      telemetry/handlers.ex + telemetry/metrics.ex handle Phoenix/Ecto/Oban/business telemetry.
      AUDIT before creating monitoring/metrics_collector.ex.'
    - 'Fly config: apps/backend/fly.toml — deployment config'
    - 'Prometheus config: infrastructure/prometheus/'
    - 'Grafana: infrastructure/grafana/'
  pre_tasks:
    - 'MANDATORY: Add mogrify or vix hex dep to mix.exs — neither exists in current deps. Verify
      which library fits best for image processing needs.'
    - 'Audit existing uploads/image_optimizer.ex before creating cdn/ version'
    - 'MANDATORY AUDIT: Read all files in apps/backend/lib/cgraph/health_check/ (checks.ex,
      reporter.ex, health_check.ex) and health_controller.ex before creating
      monitoring/health_dashboard.ex. Document what already exists vs genuine gaps.'
    - 'MANDATORY AUDIT: Read CGraph.Metrics (metrics.ex) and CGraphWeb.Telemetry
      (telemetry.ex + telemetry/handlers.ex + telemetry/metrics.ex) before creating
      monitoring/metrics_collector.ex. Document existing metrics vs genuine gaps.'
---

<objective>
Build CDN management with image optimization, production monitoring (health dashboard, alerting,
metrics), and deployment scripts for zero-downtime migration and blue-green deploys.

ATOMIC_PLAN tasks: 5.21 (CDN Manager), 5.22 (Image Optimizer), 5.23 (Health Dashboard), 5.24
(Alerting), 5.25 (Zero-Downtime Migration Script). </objective>

<tasks>

<task type="auto">
  <name>Task 1: CDN + image optimization (5.21, 5.22)</name>
  <files>
    apps/backend/lib/cgraph/cdn/cdn_manager.ex
    apps/backend/lib/cgraph/cdn/image_optimizer.ex
  </files>
  <action>CDN Manager: configurable backend (R2/S3), upload_to_cdn(path, opts), purge(url), signed_url(path, ttl). Image Optimizer: ⚠️ FIRST audit existing uploads/image_optimizer.ex. Then either extend it with resize/to_webp/progressive_jpeg/generate_srcset, or create cdn/image_optimizer.ex that delegates to it. Use Mogrify or Vix (⚠️ MUST add hex dep to mix.exs first — neither exists in current deps).</action>
</task>

<task type="auto">
  <name>Task 2: Monitoring stack (5.23, 5.24)</name>
  <files>
    apps/backend/lib/cgraph/monitoring/health_dashboard.ex
    apps/backend/lib/cgraph/monitoring/alerting.ex
    apps/backend/lib/cgraph/monitoring/metrics_collector.ex
  </files>
  <action>HealthDashboard: ⚠️ FIRST audit existing health_check/ directory (checks.ex, reporter.ex, health_check.ex) and health_controller.ex — /health endpoint already exists with DB, Redis, Oban, cache, memory checks. Either EXTEND existing system or create monitoring/health_dashboard.ex as a wrapper that delegates to existing checks + adds CDN check. Returns {status, components, latencies}. Alerting: define_alert/3, check_thresholds/0, send_alert/2 (Slack webhook + PagerDuty). MetricsCollector: ⚠️ FIRST audit existing CGraph.Metrics GenServer + CGraphWeb.Telemetry. Either EXTEND metrics.ex with business metrics (active_users, messages_per_min, purchases) or create monitoring/metrics_collector.ex that delegates to it.</action>
</task>

<task type="auto">
  <name>Task 3: Deployment scripts (5.25)</name>
  <files>
    infrastructure/scripts/zero_downtime_migration.sh
    infrastructure/scripts/blue_green_deploy.sh
  </files>
  <action>zero_downtime_migration.sh: pre-flight checks, run migration in transaction, rollback on failure, post-migration verification. blue_green_deploy.sh: deploy to staging, health check, traffic switch, rollback capability. Both for Fly.io.</action>
  <verify>shellcheck infrastructure/scripts/zero_downtime_migration.sh 2>&1 | tail -5 || echo "shellcheck not installed"</verify>
</task>

</tasks>
