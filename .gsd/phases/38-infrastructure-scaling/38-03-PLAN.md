---
phase: 38-infrastructure-scaling
plan: 03
type: execute
wave: 2
depends_on:
  - 38-01
  - 38-02
files_modified:
  - apps/backend/lib/cgraph/queue/priority_queue.ex (new)
  - apps/backend/lib/cgraph/queue/dead_letter_queue.ex (new — ⚠️ dead_letter_worker.ex already
    exists in workers/. Verify no overlap before creating separate DLQ module)
  - apps/backend/lib/cgraph/search/elastic_adapter.ex (new)
  - apps/backend/lib/cgraph/search/search_indexer.ex (update — ⚠️ indexer.ex already exists in
    search/ at ~100+ lines. EXTEND it for Elasticsearch, do not recreate)
  - apps/backend/lib/cgraph/presence/distributed_presence.ex (new)
  - apps/backend/lib/cgraph/workers/search_index_worker.ex (update — ⚠️ ALREADY EXISTS. EXTEND with
    Elasticsearch indexing support, do not recreate)
autonomous: true
must_haves:
  truths:
    - 'PriorityQueue: wraps Oban with priority levels (critical=0, high=1, normal=2, low=3)'
    - 'DeadLetterQueue: captures failed jobs after max retries, admin UI query support'
    - 'ElasticAdapter: search interface abstracted over Elasticsearch/OpenSearch'
    - 'SearchIndexer: index_document/3, remove_document/2, bulk_index/2'
    - 'DistributedPresence: tracks user online status across cluster nodes via CRDT/Phoenix.Presence'
    - 'SearchIndexWorker: triggered on content creation/update, indexes to search backend'
  artifacts:
    - apps/backend/lib/cgraph/queue/priority_queue.ex
    - apps/backend/lib/cgraph/queue/dead_letter_queue.ex
    - apps/backend/lib/cgraph/search/elastic_adapter.ex
    - apps/backend/lib/cgraph/search/search_indexer.ex
    - apps/backend/lib/cgraph/presence/distributed_presence.ex
    - apps/backend/lib/cgraph/workers/search_index_worker.ex
  key_links:
    - 'Oban config: apps/backend/config/config.exs — existing queue setup'
    - 'Phoenix Presence: existing in backend — extend for distributed'
    - 'Forum search: apps/backend/lib/cgraph/forums/advanced_search.ex — Phase 37 (already 277
      lines)'
    - '⚠️ EXISTING: search/indexer.ex (~100+ lines), search_index_worker.ex, dead_letter_worker.ex
      all already exist. Audit before extending.'
    - '⚠️ search/ directory already has 5+ files + search_queries/ subdirectory. Audit entire search
      module before adding elastic_adapter.'
  pre_tasks:
    - 'MANDATORY: Add elasticsearch or elastix hex dep to mix.exs before building adapter. Neither
      exists currently in deps.'
    - 'MANDATORY: Audit existing search/ directory files before creating/extending'
---

<objective>
Build priority queues with dead-letter handling, search infrastructure (Elasticsearch adapter +
indexer), and distributed presence tracking across cluster nodes.

ATOMIC_PLAN tasks: 5.15 (Priority Queue), 5.16 (Dead Letter Queue), 5.17 (Elasticsearch Adapter),
5.18 (Search Indexer), 5.19 (Distributed Presence), 5.20 (Search Index Worker). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Priority queue + DLQ (5.15, 5.16)</name>
  <files>
    apps/backend/lib/cgraph/queue/priority_queue.ex
    apps/backend/lib/cgraph/queue/dead_letter_queue.ex
  </files>
  <action>PriorityQueue: enqueue/3 with priority level, wraps Oban.insert with priority option. DeadLetterQueue: on_failure callback, stores failed job metadata (args, error, attempts, queue) in dedicated table. Query API for admin dashboard.</action>
</task>

<task type="auto">
  <name>Task 2: Search infrastructure (5.17, 5.18, 5.20)</name>
  <files>
    apps/backend/lib/cgraph/search/elastic_adapter.ex
    apps/backend/lib/cgraph/search/search_indexer.ex
    apps/backend/lib/cgraph/workers/search_index_worker.ex
  </files>
  <action>ElasticAdapter: behaviour-based, search/2, index/3, delete/2, bulk/2. Configurable backend (Elasticsearch, OpenSearch, or pg tsvector fallback). SearchIndexer: index_document(index, id, body), async via worker. SearchIndexWorker: Oban, indexes on content events, batch support.</action>
</task>

<task type="auto">
  <name>Task 3: Distributed presence (5.19)</name>
  <files>apps/backend/lib/cgraph/presence/distributed_presence.ex</files>
  <action>Extend Phoenix.Presence for multi-node: track user across all connected nodes, list_online/0, is_online?/1. CRDT-style conflict resolution. Telemetry on presence changes. Configure in endpoint.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
