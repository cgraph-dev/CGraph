---
phase: 25-cinematic-ui-parity
plan: 07
type: execute
wave: 2
depends_on: ['25-01']
files_modified:
  - apps/mobile/src/components/avatar.tsx
  - apps/mobile/src/components/lottie-avatar.tsx
  - apps/mobile/src/components/avatar-squircle-clip.tsx
autonomous: false
must_haves:
  truths:
    - 'Mobile avatar default shape is squircle (borderRadius: 43)'
    - 'Avatar supports lottieUrl prop for animated Lottie avatars'
    - 'Lottie avatars render via lottie-react-native within squircle mask'
    - 'All avatar sizes use squircle as default'
    - 'Status dot positions correctly on squircle shape'
    - 'Backward compatible — existing avatar consumers work without changes'
  artifacts:
    - apps/mobile/src/components/lottie-avatar.tsx
    - apps/mobile/src/components/avatar-squircle-clip.tsx
  key_links:
    - 'Avatar consumed by chat, contacts, profile, sidebar across mobile app'
    - 'Lottie rendering via lottie-react-native (already installed)'
---

<objective>
Redesign mobile app avatars to match web: squircle default shape (borderRadius: 43) and Lottie animated avatar support.

Purpose: Mobile avatars currently use `borderRadius: sizeValue / 2` (perfect circle). This plan
changes the default to a squircle (borderRadius: 43) matching the web app change in Plan 04, and
adds Lottie animated avatar rendering using the already-installed `lottie-react-native` package.

Output:

- Updated `avatar.tsx` with squircle default and shape prop
- `lottie-avatar.tsx` — Lottie rendering within squircle mask
- `avatar-squircle-clip.tsx` — shared squircle clip path component </objective>

<context>
@.gsd/PROJECT.md
@apps/mobile/src/components/avatar.tsx (current: borderRadius sizeValue/2, circular)
@apps/mobile/package.json (has lottie-react-native ^7.3)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create squircle clip component</name>
  <files>apps/mobile/src/components/avatar-squircle-clip.tsx</files>
  <action>
    Create a reusable squircle clip path component:

    1. Use `react-native-svg` to create a superellipse clip path
    2. Superellipse formula: `|x/a|^n + |y/b|^n = 1` with n≈4 for iOS-style squircle
    3. Accept `size` prop to generate appropriately scaled path
    4. Export as `SquircleClip` component wrapping children in `<ClipPath>` + `<View>`
    5. Fallback: if SVG clipping has issues, use `borderRadius: 43` with `overflow: 'hidden'`

  </action>
  <verify>pnpm turbo typecheck --filter=@cgraph/mobile</verify>
  <done>SquircleClip component renders squircle-masked children</done>
</task>

<task type="auto">
  <name>Task 2: Update avatar to squircle default</name>
  <files>apps/mobile/src/components/avatar.tsx</files>
  <action>
    Modify the mobile Avatar component:

    1. Add `shape` prop: `'squircle' | 'circle' | 'square'` with default `'squircle'`
    2. Map shapes:
       - `squircle` → `borderRadius: 43` (capped at `sizeValue / 2` for tiny avatars)
       - `circle` → `borderRadius: sizeValue / 2`
       - `square` → `borderRadius: 16`
    3. For sizes xs (24px) and sm (32px), cap borderRadius to avoid looking weird: `Math.min(43, sizeValue / 2)`
    4. Update status dot positioning for squircle (slightly different corner position)
    5. Update AnimatedBorder (gamification) integration for squircle shape
    6. Preserve all existing props

  </action>
  <verify>pnpm turbo typecheck --filter=@cgraph/mobile</verify>
  <done>Avatar defaults to squircle, supports shape prop</done>
</task>

<task type="auto">
  <name>Task 3: Add Lottie avatar rendering</name>
  <files>apps/mobile/src/components/lottie-avatar.tsx, apps/mobile/src/components/avatar.tsx</files>
  <action>
    Add Lottie animated avatar support:

    1. Create `LottieAvatar` component using `lottie-react-native`
    2. Accept `lottieUrl` prop — fetches and renders Lottie animation within avatar bounds
    3. Apply squircle clip mask via SquircleClip or borderRadius + overflow hidden
    4. Configuration:
       - `autoPlay: true`, `loop: true`
       - Pause when not visible (use `onLayout` + visibility detection)
       - Fallback to static image/initials while loading
    5. In `avatar.tsx`, add `lottieUrl?: string` prop
    6. When `lottieUrl` provided, render `LottieAvatar` instead of image
    7. Use `useReducedMotion()` from Reanimated — show first frame only when true

  </action>
  <verify>pnpm turbo typecheck --filter=@cgraph/mobile</verify>
  <done>Avatar supports lottieUrl, renders animated Lottie in squircle mask</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Squircle avatars with Lottie support on mobile</what-built>
  <how-to-verify>
    1. Open mobile app on device/simulator
    2. Check contact list — avatars should be squircle shape (not perfect circles)
    3. Open a chat — message avatars should be squircle
    4. Open profile — large avatar should clearly show squircle corners
    5. Verify status dots are positioned correctly on squircle
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm turbo typecheck --filter=@cgraph/mobile` passes
- [ ] Avatars render as squircle by default
- [ ] LottieAvatar renders animated content
- [ ] Status dots position correctly
- [ ] Reduced motion shows static frame
</verification>

<success_criteria>

- All mobile avatars display as squircles matching web app
- Lottie animated avatars work within squircle bounds
- No broken avatar rendering on any screen </success_criteria>

<output>
After completion, create `.gsd/phases/25-cinematic-ui-parity/25-07-SUMMARY.md`
</output>
