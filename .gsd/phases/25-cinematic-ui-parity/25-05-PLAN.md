---
phase: 25-cinematic-ui-parity
plan: 05
type: execute
wave: 3
depends_on: ['25-02', '25-03', '25-04']
files_modified:
  - apps/web/src/components/ui/glass-card.tsx
  - apps/web/src/components/ui/GlassCard.module.css
  - apps/web/src/components/ui/glow-text.tsx
  - apps/web/src/components/ui/animated-border.tsx
  - apps/web/src/components/ui/micro-interactions.tsx
  - apps/web/src/index.css
autonomous: false
must_haves:
  truths:
    - 'GlassCard has mouse-following spotlight border (matching landing GlassCard)'
    - 'GlowText has animated gradient flow (emerald→purple→cyan cycle)'
    - 'AnimatedBorder has smoother conic gradient with glow'
    - 'Micro-interactions include animated counters with spring physics'
    - 'Section headers in app use gradient text matching landing page style'
    - 'All effects respect useReducedMotion'
  artifacts:
    - apps/web/src/components/ui/glass-card.tsx (enhanced)
  key_links:
    - 'GlassCard used in settings panels, modals, feature cards'
    - 'GlowText used in section headings, empty states'
---

<objective>
Elevate web app component polish to match landing page visual quality — enhance GlassCard, GlowText, AnimatedBorder, and micro-interactions.

Purpose: While individual premium components exist in the web app (GlassCard, GlowText, TiltCard),
they're simpler versions of what the landing page uses. This plan brings them up to the same
fidelity: mouse-following spotlight borders, animated gradient text, smoother conic gradients, and
richer micro-interactions.

Output:

- Enhanced GlassCard with landing-page-quality spotlight effect
- Enhanced GlowText with gradient flow animation
- Enhanced AnimatedBorder with glow
- Richer micro-interaction utilities </objective>

<context>
@.gsd/PROJECT.md
@apps/web/src/components/ui/glass-card.tsx
@apps/web/src/components/ui/GlassCard.module.css
@apps/web/src/components/ui/glow-text.tsx
@apps/web/src/components/ui/animated-border.tsx
@apps/web/src/components/ui/micro-interactions.tsx
@apps/landing/src/components/marketing/ui/GlassCard.tsx (reference: spotlight + 3D tilt)
@apps/landing/src/components/marketing/ui/GradientText.tsx (reference: animated gradient)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance GlassCard with spotlight border</name>
  <files>apps/web/src/components/ui/glass-card.tsx, apps/web/src/components/ui/GlassCard.module.css</files>
  <action>
    Upgrade GlassCard to match landing page version:

    1. Add mouse-following spotlight border using `useMotionTemplate` and `useMotionValue` from `motion/react`
    2. Track mouse position relative to card → generate radial gradient at cursor position
    3. Use gradient as border-image: `radial-gradient(300px circle at ${mouseX}px ${mouseY}px, emerald-400, transparent 70%)`
    4. Keep existing variants (frosted, neon, solid, gradient) — spotlight is additive
    5. Enhance 3D tilt: smooth to max 8° rotation (vs current 15° — more subtle for app context)
    6. Add `spotlight?: boolean` prop (default true) to opt in/out
    7. Reduced motion: disable spotlight tracking + tilt, keep static frosted glass appearance

  </action>
  <verify>pnpm turbo typecheck --filter=@cgraph/web</verify>
  <done>GlassCard has mouse-following spotlight border with smooth tracking</done>
</task>

<task type="auto">
  <name>Task 2: Enhance GlowText with gradient animation</name>
  <files>apps/web/src/components/ui/glow-text.tsx</files>
  <action>
    Upgrade GlowText to match landing page GradientText:

    1. Add animated gradient flow: background-size 300%, position cycles left→right→left
    2. Default gradient: emerald (#10b981) → purple (#8b5cf6) → cyan (#06b6d4)
    3. Animation duration: 4s ease-in-out infinite
    4. Keep existing glow/shimmer options as additive
    5. Add `animated?: boolean` prop (default true)
    6. Reduced motion: show static gradient at center position

  </action>
  <verify>pnpm turbo typecheck --filter=@cgraph/web</verify>
  <done>GlowText has flowing gradient animation</done>
</task>

<task type="auto">
  <name>Task 3: Enhance micro-interactions</name>
  <files>apps/web/src/components/ui/micro-interactions.tsx</files>
  <action>
    Add richer micro-interaction utilities:

    1. `AnimatedCounter` — enhance with spring-based rolling numbers (per-digit animation)
    2. `RippleButton` — material-design ripple effect utility (click position → expanding circle)
    3. `StaggerChildren` — wrapper that staggers children entrance using framer-motion stagger
    4. `ParallaxScroll` — hook for scroll-linked parallax (different speed layers)
    5. All utilities respect useReducedMotion
    6. All utilities are tree-shakeable (named exports)

  </action>
  <verify>pnpm turbo build --filter=@cgraph/web</verify>
  <done>New micro-interaction utilities exported and tree-shakeable</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Enhanced component polish — dev server at http://localhost:3004</what-built>
  <how-to-verify>
    1. Visit http://localhost:3004 and log in
    2. Navigate to settings or any page with cards — hover over cards to see spotlight border following cursor
    3. Check section headers — text should have flowing gradient animation
    4. Verify cards have subtle 3D tilt on hover
    5. Check that effects are tasteful and not distracting during normal app use
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm turbo build --filter=@cgraph/web` passes
- [ ] GlassCard spotlight tracks mouse smoothly
- [ ] GlowText gradient animates
- [ ] All new micro-interactions work
- [ ] Reduced motion disables animations
- [ ] No visual regressions in existing UI
</verification>

<success_criteria>

- Glass cards feel premium with spotlight border tracking
- Text elements have landing-page-quality gradient animation
- Micro-interactions enhance the app feel without being distracting </success_criteria>

<output>
After completion, create `.gsd/phases/25-cinematic-ui-parity/25-05-SUMMARY.md`
</output>
