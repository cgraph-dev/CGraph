---
phase: 25-cinematic-ui-parity
plan: 08
type: execute
wave: 3
depends_on: ['25-06', '25-07']
files_modified:
  - apps/mobile/src/components/ui/glass-card.tsx
  - apps/mobile/src/components/ui/gradient-text.tsx
  - apps/mobile/src/components/ui/animated-background.tsx
  - apps/mobile/src/components/layouts/app-layout.tsx
autonomous: false
must_haves:
  truths:
    - 'Mobile app has glass card component with blur + transparency'
    - 'Mobile app has animated gradient text component'
    - 'Mobile app has subtle ambient background (animated gradient, not particles)'
    - 'All effects use Reanimated native thread animations'
    - 'All effects respect useReducedMotion'
  artifacts:
    - apps/mobile/src/components/ui/glass-card.tsx
    - apps/mobile/src/components/ui/gradient-text.tsx
    - apps/mobile/src/components/ui/animated-background.tsx
  key_links:
    - 'Components used across mobile app for premium feel'
    - 'Background renders in app layout wrapper'
---

<objective>
Add premium UI components to mobile app: glass cards, animated gradient text, and ambient background effects.

Purpose: The mobile app currently lacks the premium visual effects present in the web app and
landing page. While canvas-based particle fields aren't suitable for mobile (battery + performance),
the mobile app should have glassmorphism cards, animated gradient text, and a subtle animated
background that give it the same design language.

Output:

- `glass-card.tsx` — frosted glass card with Reanimated blur
- `gradient-text.tsx` — animated gradient text via react-native-linear-gradient
- `animated-background.tsx` — subtle animated gradient orbs for app background </objective>

<context>
@.gsd/PROJECT.md
@apps/mobile/src/components/ (current component inventory)
@apps/web/src/components/ui/glass-card.tsx (reference: web version)
@apps/web/src/components/ui/glow-text.tsx (reference: web version)
@apps/web/src/components/ui/gradient-background.tsx (reference: web version)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mobile glass card</name>
  <files>apps/mobile/src/components/ui/glass-card.tsx</files>
  <action>
    Create glassmorphism card for React Native:

    1. Use `expo-blur` BlurView for backdrop blur (iOS native blur, Android fallback)
    2. Semi-transparent background: `rgba(255,255,255,0.06)` on dark mode
    3. Border: `1px solid rgba(255,255,255,0.08)`
    4. borderRadius: 16 (matching Tailwind rounded-2xl)
    5. Accept `intensity` prop: 'light' | 'medium' | 'heavy' controlling blur amount
    6. Accept `tilt` prop (default false) — on true, use Reanimated `useAnimatedGestureHandler` for tilt on touch
    7. Reduced motion: disable tilt, keep static blur
    8. Export variants: default, frosted, neon (matching web variants)

  </action>
  <verify>pnpm turbo typecheck --filter=@cgraph/mobile</verify>
  <done>GlassCard renders with blur, transparency, and optional tilt</done>
</task>

<task type="auto">
  <name>Task 2: Create animated gradient text</name>
  <files>apps/mobile/src/components/ui/gradient-text.tsx</files>
  <action>
    Create animated gradient text for React Native:

    1. Use `expo-linear-gradient` or `react-native-linear-gradient` + MaskedView
    2. Animated gradient flow: emerald→purple→cyan, cycling position
    3. Use Reanimated `useSharedValue` + `useAnimatedStyle` for gradient animation
    4. Accept `animated` prop (default true) — static gradient when false
    5. Accept custom color props for different palettes
    6. Reduced motion: show static centered gradient

  </action>
  <verify>pnpm turbo typecheck --filter=@cgraph/mobile</verify>
  <done>GradientText renders with animated gradient flow</done>
</task>

<task type="auto">
  <name>Task 3: Create ambient background</name>
  <files>apps/mobile/src/components/ui/animated-background.tsx</files>
  <action>
    Create a lightweight animated background for mobile:

    1. NOT canvas particles (too heavy for mobile battery) — use animated gradient orbs
    2. Three `Animated.View` orbs: purple (top-left), emerald (bottom-right), cyan (center)
    3. Each orb: 200-300px, gaussian blur, very low opacity (0.15)
    4. Subtle Reanimated animation: slow drift (translateX/Y ±20px over 20s) + gentle scale pulse
    5. Position: absolute, behind all content
    6. Performance: all animations on native thread via `useAnimatedStyle`
    7. Accept `intensity` prop: 'full' | 'subtle' | 'off'
    8. Reduced motion: static orbs, no animation

  </action>
  <verify>pnpm turbo typecheck --filter=@cgraph/mobile</verify>
  <done>AnimatedBackground renders with drifting gradient orbs</done>
</task>

<task type="auto">
  <name>Task 4: Integrate background into app layout</name>
  <files>apps/mobile/src/components/layouts/app-layout.tsx</files>
  <action>
    Add AnimatedBackground to mobile app root layout:
    1. Render behind all navigation content
    2. Check if file exists at this path — if not, find the actual root layout/navigation wrapper
    3. Default to 'subtle' intensity for always-on use
  </action>
  <verify>pnpm turbo typecheck --filter=@cgraph/mobile</verify>
  <done>Mobile app has ambient animated background</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Premium mobile UI components — glass cards, gradient text, ambient background</what-built>
  <how-to-verify>
    1. Open mobile app on device/simulator
    2. Observe subtle ambient gradient orbs drifting behind content
    3. Navigate to screens with cards — should have glass/frosted effect
    4. Check section headers — gradient text should animate
    5. Verify performance — no battery drain, no frame drops
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm turbo typecheck --filter=@cgraph/mobile` passes
- [ ] Glass cards render with blur effect
- [ ] Gradient text animates
- [ ] Background drifts subtly
- [ ] No performance issues on device
- [ ] Reduced motion fallbacks work
</verification>

<success_criteria>

- Mobile app has the same design language as web/landing (glass, gradients, ambient effects)
- Performance is acceptable for always-on mobile use
- All effects respect reduced motion preferences </success_criteria>

<output>
After completion, create `.gsd/phases/25-cinematic-ui-parity/25-08-SUMMARY.md`
</output>
