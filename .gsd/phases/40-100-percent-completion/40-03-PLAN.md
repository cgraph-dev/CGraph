---
phase: 40-100-percent-completion
plan: 03
type: execute
wave: 2
depends_on:
  - 40-01
  - 40-02
files_modified:
  - apps/web/src/modules/chat/components/message-group.tsx (modify — add nameplate/identity rendering)
  - apps/web/src/modules/chat/components/chat-identity-card.tsx (new — lightweight identity card for chat)
  - apps/web/src/modules/chat/hooks/useChatIdentity.ts (new — hook to fetch user cosmetics)
  - apps/web/src/modules/forums/components/forum-monetization-panel.tsx (new — monetization settings)
  - apps/web/src/modules/forums/components/forum-tier-editor.tsx (new — tier CRUD)
  - apps/web/src/modules/boosts/components/profile-spotlight-card.tsx (new — profile boost UI)
  - packages/api-client/src/forum-monetization.ts (new — API client functions)
autonomous: true
must_haves:
  truths:
    - 'ChatIdentityCard: lightweight identity rendering for DM/group messages. Shows
      equipped avatar border (frame around avatar), equipped title (below name), and
      up to 3 equipped badges (small icons next to name). NOT the full forum identity
      card — a simplified "mini-nameplate" appropriate for chat message density.'
    - 'useChatIdentity hook: fetches user identity data (equipped border, title, badges)
      from existing /api/v1/users/{userId}/cosmetics/inventory endpoint (NOT the
      self-only Zustand customization store — that only has current user data).
      Uses @tanstack/react-query with 5min staleTime. Query key: ["user-identity", userId].
      Returns { border, title, badges, isLoading }.
      ⚠️ API client: cosmetics.getInventory(client, userId) from packages/api-client/src/cosmetics.ts'
    - 'message-group.tsx modification: wrap existing Avatar component with border frame.
      Show title text below author.name if user has equipped title. Show badge icons
      inline after username. ⚠️ message-group.tsx currently shows Avatar + author.name
      + author.roleColor. Keep existing rendering, ADD cosmetic enhancements on top.'
    - 'ForumMonetizationPanel: forum admin settings component. Shows current monetization
      mode (free/gated/hybrid) with radio buttons. Shows tier list with add/edit/delete.
      Calls PUT /forums/:id/monetization for mode, tier CRUD via new endpoints.
      ⚠️ Only visible to forum owner. Wire into existing forum admin/settings page.'
    - 'ForumTierEditor: inline form for creating/editing a tier. Fields: name, monthly
      price (Nodes), yearly price (Nodes), features checklist. Max 3 tiers enforced in UI.'
    - 'ProfileSpotlightCard: small component on boost panel showing "Boost your profile"
      option. Calls POST /api/v1/boosts with target_type="profile".'
    - 'forum-monetization.ts API client: getMonetizationSettings(forumId),
      updateMonetizationMode(forumId, type), createTier(forumId, data),
      updateTier(forumId, tierId, data), deleteTier(forumId, tierId)'
  artifacts:
    - apps/web/src/modules/chat/components/chat-identity-card.tsx
    - apps/web/src/modules/chat/hooks/useChatIdentity.ts
    - apps/web/src/modules/forums/components/forum-monetization-panel.tsx
    - apps/web/src/modules/forums/components/forum-tier-editor.tsx
    - apps/web/src/modules/boosts/components/profile-spotlight-card.tsx
    - packages/api-client/src/forum-monetization.ts
  key_links:
    - 'Message rendering: apps/web/src/modules/chat/components/message-group.tsx —
      Avatar + author.name + author.roleColor currently'
    - 'Identity card (forum): apps/web/src/modules/forums/components/identity-card.tsx —
      reference for cosmetic rendering patterns (border, title, badges)'
    - 'Cosmetics API: existing /api/v1/users/{userId}/cosmetics/inventory — provides equipped items'
    - 'Customization store: apps/web/src/modules/settings/store/customization/ —
      equippedBorder, equippedTitle, equippedBadges selectors'
    - 'Boost panel: apps/web/src/modules/forums/components/boost-panel.tsx —
      existing boost UI to extend with profile option'
    - 'Forum admin: apps/web/src/pages/forums/forum-admin-page.tsx or
      apps/web/src/modules/forums/pages/ — where to wire monetization panel'
    - 'API client pattern: packages/api-client/src/nodes.ts — existing pattern for
      typed API client functions'
  pre_tasks:
    - 'Read message-group.tsx to understand current Avatar/name rendering structure'
    - 'Read identity-card.tsx from forums module to understand border/badge rendering patterns'
---

<objective>
Build frontend components for identity cards in DMs, forum monetization settings, and profile
spotlight boost. This closes the remaining frontend gaps.

Gap IDs: F1 (identity cards in DMs), F2 (profile spotlight UI), B3 (forum monetization UI).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Chat identity card + hook</name>
  <files>
    apps/web/src/modules/chat/components/chat-identity-card.tsx
    apps/web/src/modules/chat/hooks/useChatIdentity.ts
    apps/web/src/modules/chat/components/message-group.tsx
  </files>
  <action>
    useChatIdentity hook:
    - Takes userId: string
    - Uses React Query (useQuery) to fetch from /api/v1/users/{userId}/cosmetics/inventory
    - Returns { border: Border | null, title: Title | null, badges: Badge[], isLoading }
    - Stale time: 5 minutes. Cache key: ['user-identity', userId]
    - Graceful fallback: if fetch fails, return nulls (no cosmetics shown)

    ChatIdentityCard component (lightweight):
    - Props: { userId, avatarUrl, avatarSize? }
    - If border exists: wrap avatar in a div with border.border_style CSS + border.primaryColor
    - Return just the wrapped avatar (used inline in message-group)

    message-group.tsx modifications:
    - Import useChatIdentity hook
    - Call useChatIdentity(author.id) for the message group author
    - Replace bare Avatar with ChatIdentityCard-wrapped avatar (only adds border frame)
    - After author.name span: if title exists, add small <span class="text-xs opacity-70">
      with title.display_name
    - After name+title: if badges.length > 0, render first 3 badges as tiny icons (16x16)
    - All cosmetic additions are optional — if no data, render exactly as before
    - ⚠️ Do NOT break existing message-group layout. Cosmetics are additive.
  </action>
</task>

<task type="auto">
  <name>Task 2: Forum monetization panel + tier editor</name>
  <files>
    apps/web/src/modules/forums/components/forum-monetization-panel.tsx
    apps/web/src/modules/forums/components/forum-tier-editor.tsx
    packages/api-client/src/forum-monetization.ts
  </files>
  <action>
    forum-monetization.ts API client:
    - getMonetizationSettings(forumId: string): GET /api/v1/forums/:id/monetization
    - updateMonetizationMode(forumId: string, type: 'free'|'gated'|'hybrid'): PUT /api/v1/forums/:id/monetization
    - createTier(forumId: string, data: TierInput): POST /api/v1/forums/:id/monetization/tiers
    - updateTier(forumId: string, tierId: string, data: TierInput): PUT /api/v1/forums/:id/monetization/tiers/:id
    - deleteTier(forumId: string, tierId: string): DELETE /api/v1/forums/:id/monetization/tiers/:id
    - Types: MonetizationSettings, MonetizationTier, TierInput

    ForumMonetizationPanel:
    - Fetches settings via getMonetizationSettings on mount
    - Shows monetization mode radio: Free / Gated / Hybrid
    - On mode change: updateMonetizationMode + refetch
    - Shows tier list (only when mode is gated or hybrid)
    - "Add Tier" button (disabled if already 3 tiers)
    - Each tier shows name, monthly/yearly price, edit/delete buttons
    - Uses glass-panel styling consistent with existing admin panels

    ForumTierEditor:
    - Inline form (within panel, not modal)
    - Fields: name (text), monthly_price_nodes (number), yearly_price_nodes (number)
    - Features checklist: private_boards, special_badges, increased_upload_limit,
      priority_thread, custom_nameplate
    - Save/Cancel buttons
    - On save: createTier or updateTier based on isEditing prop
  </action>
</task>

<task type="auto">
  <name>Task 3: Profile spotlight boost card</name>
  <files>
    apps/web/src/modules/boosts/components/profile-spotlight-card.tsx
  </files>
  <action>
    ProfileSpotlightCard component:
    - Shows "Boost Your Profile" card with user avatar preview
    - Duration selector: 1h, 6h, 24h (maps to different Nodes costs)
    - Cost display: "50 / 200 / 500 Nodes" for each duration
    - "Boost" button: POST /api/v1/boosts with { target_type: "profile", target_id: currentUserId,
      boost_type: "visibility", duration_hours: selected }
    - Result: "Your profile will appear in Featured Creators for X hours"
    - Wire into existing boost-panel.tsx as a new section/tab
    - Uses glass-panel styling
  </action>
</task>

</tasks>
