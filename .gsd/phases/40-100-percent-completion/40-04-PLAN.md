---
phase: 40-100-percent-completion
plan: 04
type: execute
wave: 3
depends_on:
  - 40-01
  - 40-02
files_modified:
  - apps/backend/priv/repo/seeds/profile_frame_seeds.exs (modify — add 48 new frames to reach 103)
  - apps/backend/priv/repo/seeds/profile_effect_seeds.exs (modify — add 12 new effects to reach 30)
  - apps/backend/priv/repo/seeds/seed_borders.exs (modify — add track field to all 42 borders)
  - apps/backend/priv/repo/migrations/TIMESTAMP_add_track_to_avatar_borders.exs (new)
autonomous: true
must_haves:
  truths:
    - '48 new profile frames: target distribution for 103 total:
      free=8 (+3), common=20 (+10), uncommon=20 (+10), rare=20 (+10),
      epic=15 (+7), legendary=12 (+5), mythic=8 (+3).
      Mix unlock_types: purchase, achievement, reputation, seasonal.
      New themes: celestial, nature, cyberpunk, steampunk, elemental, mythological, holiday.'
    - '12 new profile effects: target distribution for 30 total:
      free=3 (+1), common=6 (+2), uncommon=6 (+2), rare=5 (+2),
      epic=4 (+2), legendary=3 (+1), mythic=3 (+2).
      Three effect types: particle, aura, trail. New effects should add variety such as
      snowfall, fireflies, cosmic dust, thunderstorm, sakura petals, digital rain.'
    - 'Border track field: 7 valid tracks matching the unlock system:
      messaging, forum, group, social, security, creator, shop.
      Each of the 42 existing borders gets a track value based on its theme/unlock_type.
      ⚠️ track is a simple string column on avatar_borders table, nullable, default nil.'
    - 'All seed changes are additive and idempotent. on_conflict: :nothing ensures
      re-running seeds does not duplicate entries. Existing data is untouched.'
    - 'Migration for track column: alter table(:avatar_borders) do add :track, :string end
      Backfill existing borders with track values via execute() raw SQL in migration
      (matching the raw SQL pattern used by existing border seeds, NOT Repo.update_all).
      Example: execute("UPDATE avatar_borders SET track = \'forum\' WHERE theme IN (\'minimal\', \'gothic\')")
      ⚠️ Do NOT add :track to the Ecto schema since the schema already has DB/schema
      mismatches (tier, metadata columns missing from schema). Keep it DB-only for now.'
    - 'Frame fields: slug, name, rarity, unlock_type, unlock_condition, animated.
      Format must match existing entries exactly for insert_all compatibility.'
    - 'Effect fields: slug, name, type, rarity, config, preview_url.
      Format must match existing entries exactly for insert_all compatibility.'
  artifacts:
    - apps/backend/priv/repo/seeds/profile_frame_seeds.exs
    - apps/backend/priv/repo/seeds/profile_effect_seeds.exs
    - apps/backend/priv/repo/seeds/seed_borders.exs
    - apps/backend/priv/repo/migrations/TIMESTAMP_add_track_to_avatar_borders.exs
  key_links:
    - 'Existing frames (55): apps/backend/priv/repo/seeds/profile_frame_seeds.exs
      — fields: slug, name, rarity, unlock_type, unlock_condition, animated.
      Rarity dist: free=5, common=10, uncommon=10, rare=10, epic=8, legendary=7, mythic=5'
    - 'Existing effects (18): apps/backend/priv/repo/seeds/profile_effect_seeds.exs
      — fields: slug, name, type(particle|aura|trail), rarity, config, preview_url.
      Rarity dist: free=2, common=4, uncommon=4, rare=3, epic=2, legendary=2, mythic=1'
    - 'Existing borders (42): apps/backend/priv/repo/seeds/seed_borders.exs
      — fields: slug, name, description, theme, rarity, tier, border_style,
      animation_type, animation_config, lottie_url, lottie_config, unlock_type,
      coin_cost, is_purchasable, is_tradeable, sort_order, is_active, metadata.
      Themes: 8bit, kawaii, celestial, nature, cyberpunk, gothic, minimal, holographic
      ⚠️ IMPORTANT: Existing seeds use raw SQL (execute/1), NOT Repo.insert_all.
      The AvatarBorder Ecto schema at gamification/avatar_border.ex (NOT cosmetics/)
      is missing DB columns: tier, metadata, animation_config, deactivated_at.
      New seed entries and backfill MUST use raw SQL to match existing pattern.'
    - 'Avatar border schema: apps/backend/lib/cgraph/gamification/avatar_border.ex
      — Ecto schema for avatar_borders table (27 fields, missing :track, :tier from DB)
      ⚠️ Schema is at gamification/, NOT cosmetics/ as might be expected'
    - 'Cosmetics manifest: docs/COSMETICS_MANIFEST.json if exists — reference for
      rarity weights and distribution targets'
  pre_tasks:
    - 'Read full profile_frame_seeds.exs to list all 55 existing slugs (avoid duplicates)'
    - 'Read full profile_effect_seeds.exs to list all 18 existing slugs (avoid duplicates)'
    - 'Read full seed_borders.exs to understand all 42 border themes and map them to tracks'
    - 'Read avatar_border.ex schema to verify no track field exists yet'
---

<objective>
Fill cosmetic data gaps: add 48 profile frames (55→103), 12 profile effects (18→30),
and add the track field to all 42 avatar borders. Pure data/seed work, no API changes.

Gap IDs: D1 (profile frames), D2 (profile effects), D3 (border track metadata).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add 48 profile frames to seed file</name>
  <files>
    apps/backend/priv/repo/seeds/profile_frame_seeds.exs
  </files>
  <action>
    Append 48 new frame entries to the existing frames list in profile_frame_seeds.exs.
    
    Distribution of new frames (48 total to add):
      free: +3 (8 total) — simple geometric shapes, new users
      common: +10 (20 total) — purchase 40-80 Nodes
      uncommon: +10 (20 total) — purchase 100-200 Nodes, some animated
      rare: +10 (20 total) — purchase/achievement 250-500 Nodes
      epic: +7 (15 total) — purchase/achievement 500-1000 Nodes, mostly animated
      legendary: +5 (12 total) — achievement-only, all animated
      mythic: +3 (8 total) — achievement-only, all animated

    Theme categories for new frames:
    - Celestial: star-frame, moon-frame, constellation-ring, solar-flare-frame, comet-trail-frame
    - Nature: vine-frame, blossom-ring, coral-frame, mountain-frame, leaf-crown
    - Cyberpunk: glitch-frame, matrix-ring, hologram-frame, circuit-frame, data-stream-frame
    - Steampunk: gear-frame, clockwork-ring, brass-frame, cog-frame, steam-ring
    - Elemental: flame-frame-ii, ice-frame, lightning-frame, earth-frame, water-frame
    - Mythological: dragon-frame, phoenix-frame, kraken-ring, griffin-frame, unicorn-frame
    - Holiday/seasonal: snowflake-frame, jack-o-lantern, heartstring-frame, firework-frame
    - Achievement: streak-30-frame, streak-90-frame, veteran-frame, centurion-frame

    Unlock types distribution:
    - purchase: ~25 frames (common/uncommon mostly)
    - achievement: ~15 frames (rare/epic/legendary/mythic)
    - reputation: ~5 frames (tied to rep thresholds)
    - seasonal: ~3 frames (holiday themes)

    Each entry format (must match existing):
    %{slug: "xxx", name: "Xxx", rarity: "xxx", unlock_type: "xxx",
      unlock_condition: %{"description" => "...", "nodes_cost" => N},
      animated: true/false}

    Update file header comment to reflect new count: 103 profile frames.
    ⚠️ All slugs MUST be unique and not conflict with existing 55 slugs.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add 12 profile effects to seed file</name>
  <files>
    apps/backend/priv/repo/seeds/profile_effect_seeds.exs
  </files>
  <action>
    Append 12 new effect entries to the existing effects list in profile_effect_seeds.exs.

    Distribution of new effects (12 total to add):
      free: +1 (3 total) — simple passive effect
      common: +2 (6 total)
      uncommon: +2 (6 total)
      rare: +2 (5 total)
      epic: +2 (4 total)
      legendary: +1 (3 total)
      mythic: +2 (3 total)

    New effects to add:
    - free: "calm-breeze" (trail) — gentle wind effect
    - common: "snowfall" (particle), "ember-glow" (aura)
    - uncommon: "fireflies" (particle), "ocean-wave" (trail)
    - rare: "cosmic-dust" (particle), "thunderstorm" (particle)
    - epic: "sakura-petals" (particle), "digital-rain" (particle)
    - legendary: "northern-lights" (aura)
    - mythic: "time-rift" (aura), "genesis-burst" (particle)

    Each entry format (must match existing):
    %{slug: "xxx", name: "Xxx", type: "particle"|"aura"|"trail", rarity: "xxx",
      config: %{"color" => "#xxx", ...type-specific params...},
      preview_url: "/images/effects/xxx.webp"}

    Config params by type:
    - particle: color, count, speed, size + optional: secondary_color, glow, drift, opacity
    - aura: color, intensity, radius + optional: secondary_color, animated, pulse, morph
    - trail: color, length, fade, width + optional: secondary_color, glow

    Update file header comment to reflect new count: 30 profile effects.
    ⚠️ All slugs MUST be unique and not conflict with existing 18 slugs.
  </action>
</task>

<task type="auto">
  <name>Task 3: Add track column migration + update border seeds</name>
  <files>
    apps/backend/priv/repo/migrations/TIMESTAMP_add_track_to_avatar_borders.exs
    apps/backend/priv/repo/seeds/seed_borders.exs
  </files>
  <action>
    Migration (TIMESTAMP_add_track_to_avatar_borders.exs):
    - alter table(:avatar_borders) do add :track, :string end
    - Create index on [:track]
    - Backfill existing borders in migration using execute():
      Themes → tracks mapping:
      - 8bit → shop (purchase-oriented retro theme)
      - kawaii → social (community/friendship theme)
      - celestial → forum (celestial discussion theme)
      - nature → group (organic community theme)
      - cyberpunk → messaging (digital communication theme)
      - gothic → security (dark/protection theme)
      - minimal → shop (default purchases)
      - holographic → creator (premium creator theme)
      SQL: UPDATE avatar_borders SET track = CASE theme WHEN '8bit' THEN 'shop' ...

    seed_borders.exs modifications:
    - Add track: "xxx" field to each of the 42 border map entries
    - Track assignment matches the migration backfill mapping above
    - No other fields changed

    ⚠️ Migration timestamp format: YYYYMMDDHHMMSS
    ⚠️ Schema file (avatar_border.ex) needs field :track, :string added
  </action>
</task>

</tasks>
