---
phase: 40-100-percent-completion
plan: 02
type: execute
wave: 1
depends_on:
  - 39-03
files_modified:
  - apps/backend/lib/cgraph/compliance/kyc_enforcement.ex (new)
  - apps/backend/lib/cgraph/compliance/aml_monitor.ex (new)
  - apps/backend/lib/cgraph/workers/aml_scan_worker.ex (new)
  - apps/backend/lib/cgraph/forums/reputation_level.ex (new — schema)
  - apps/backend/lib/cgraph/forums/reputation.ex (modify — add level calculation)
  - apps/backend/lib/cgraph/forums/thread.ex (modify — add is_archived)
  - apps/backend/lib/cgraph/boosts/boost.ex (modify — add profile target_type)
  - apps/backend/priv/repo/migrations/<ts>_add_reputation_levels_and_thread_archive.exs (new)
  - apps/backend/lib/cgraph/nodes/nodes.ex (modify — add max_price + reputation gating)
autonomous: true
must_haves:
  truths:
    - 'KYCEnforcement: check_kyc_required/1 (user_id) — returns true if lifetime earnings
      exceed €500 threshold. enforce_kyc/1 — gates payouts until KYC complete.
      kyc_status/1 — returns :not_required | :pending | :verified | :rejected.
      ⚠️ Existing Stripe Connect onboarding handles identity collection — this module
      adds the enforcement gate (block withdrawals above threshold without KYC).'
    - 'AMLMonitor: scan_user/1 — checks for suspicious patterns. Patterns:
      (1) circular_tips: A tips B tips A within 7 days with similar amounts,
      (2) rapid_high_volume: >10 tips from same user in 1 hour,
      (3) structuring: multiple tips just below reporting threshold.
      flag_suspicious/2 — creates AML flag record. list_flags/1 — admin view.'
    - 'AMLScanWorker: Oban worker (daily at 5 AM UTC), scans users with >1000 Nodes
      transacted in last 7 days. Runs AMLMonitor.scan_user/1 for each.'
    - 'AML flag stored in aml_flags table: user_id, pattern_type (string), details (JSONB),
      severity (low|medium|high), status (open|reviewed|dismissed|escalated), reviewed_by,
      reviewed_at. Index on (user_id, status).'
    - 'ReputationLevel: 6 levels — iron (0-99), bronze (100-499), silver (500-1499),
      gold (1500-4999), platinum (5000-14999), diamond (15000+). Each level has name,
      min_score, color, icon slug. Stored as constants in module, NOT a DB table.'
    - 'reputation.ex extended: level_for_score/1 — returns level atom. format_level/1 —
      returns %{name: "Gold", color: "#FFD700", icon: "gold-shield", min_score: 1500}'
    - 'Thread.ex: add field :is_archived, :boolean, default: false.
      Add to changeset cast list. ⚠️ Existing fields: is_locked, is_pinned, is_hidden,
      is_approved. is_archived is the last missing status flag.'
    - 'Boost.ex: extend @target_types from ~w(thread post forum) to ~w(thread post forum profile).
      Profile spotlight boost: targets a user profile in discovery/featured carousel.'
    - 'Economic guardrails in nodes.ex: @max_paid_dm_price 10_000 (Nodes). Validate in
      paid_dm.ex send_paid_file/4. Add reputation_gate/2 — require min Pulse score of 10
      before allowing content sales (gated forum posts, paid DM files).'
  artifacts:
    - apps/backend/lib/cgraph/compliance/kyc_enforcement.ex
    - apps/backend/lib/cgraph/compliance/aml_monitor.ex
    - apps/backend/lib/cgraph/compliance/aml_flag.ex
    - apps/backend/lib/cgraph/workers/aml_scan_worker.ex
    - apps/backend/priv/repo/migrations/<ts>_create_aml_flags.exs
    - apps/backend/priv/repo/migrations/<ts>_add_thread_is_archived.exs
  key_links:
    - 'Existing compliance: apps/backend/lib/cgraph/compliance/tax_reporter.ex — $600 threshold
      pattern to follow'
    - 'Existing compliance: apps/backend/lib/cgraph/compliance/age_gate.ex — COPPA/GDPR pattern'
    - 'Stripe Connect: used for creator payouts — KYC should gate withdrawals'
    - 'Withdrawals: apps/backend/lib/cgraph/nodes/nodes.ex — request_withdrawal/2 function to add KYC check'
    - 'Paid DM: apps/backend/lib/cgraph/paid_dm/paid_dm.ex — send_paid_file/4 needs max price'
    - 'Reputation: apps/backend/lib/cgraph/forums/reputation.ex — calculate_score/1, add/deduct'
    - 'Thread: apps/backend/lib/cgraph/forums/thread.ex#L43-L46 — existing boolean flags'
    - 'Boost: apps/backend/lib/cgraph/boosts/boost.ex#L16 — @target_types ~w(thread post forum)'
    - 'Abuse detection: apps/backend/lib/cgraph/security/abuse_detection.ex — pattern to follow'
    - 'Oban config: apps/backend/config/config.exs — add aml_scan queue'
---

<objective>
Build compliance modules (KYC enforcement + AML monitoring), reputation levels, thread archiving,
profile spotlight boost, and economic guardrails. These are the remaining backend feature gaps.

Gap IDs: B4 (KYC), B5 (AML), B6 (reputation levels), B7 (thread archiving), F2 (profile boost).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: KYC enforcement module</name>
  <files>
    apps/backend/lib/cgraph/compliance/kyc_enforcement.ex
  </files>
  <action>
    KYCEnforcement module:
    - @kyc_threshold_eur 500
    - check_kyc_required/1 (user_id): query total lifetime earnings from NodeTransaction
      where type in [:tip_received, :content_unlock, :subscription_received]. If sum * exchange_rate
      >= @kyc_threshold_eur, return true.
    - kyc_status/1 (user_id): check Stripe Connect account status via existing Stripe integration.
      Return :not_required | :pending | :verified | :rejected based on account.requirements.
    - enforce_kyc!/1 (user_id): called before request_withdrawal/2. If check_kyc_required
      returns true and kyc_status is not :verified, return {:error, :kyc_required}.
    - Gate integration: Add enforce_kyc!/1 call at top of CGraph.Nodes.request_withdrawal/2
      (nodes.ex L436) pipeline (modify nodes.ex to add the check before processing withdrawal).
      ⚠️ CORRECTED: Function is request_withdrawal/2, NOT withdraw/3.
      ⚠️ KYC STATUS: Use existing ConnectOnboarding.check_account_status/1 which returns
      %{charges_enabled, payouts_enabled, requirements} — both charges_enabled AND
      payouts_enabled == true means KYC-verified.
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 2: AML monitoring + worker</name>
  <files>
    apps/backend/lib/cgraph/compliance/aml_monitor.ex
    apps/backend/lib/cgraph/compliance/aml_flag.ex
    apps/backend/lib/cgraph/workers/aml_scan_worker.ex
    apps/backend/priv/repo/migrations/
    apps/backend/config/config.exs
  </files>
  <action>
    AMLFlag schema on "aml_flags" table: user_id (ref users), pattern_type (string),
    details (map), severity (string — low|medium|high), status (string — open|reviewed|dismissed|escalated),
    reviewed_by (binary_id, nullable), reviewed_at (utc_datetime, nullable).
    Index on (user_id, status).

    AMLMonitor context:
    - scan_user/1 (user_id): runs all pattern checks, creates flags for matches
    - check_circular_tips/1: query tip_sent + tip_received between same user pairs in 7 days
      with similar amounts (within 20%). Flag if found.
    - check_rapid_volume/1: query tip_sent count from user in last 1 hour. Flag if >10.
    - check_structuring/1: query tips from user where amount is between 900-999 Nodes
      (just below 1000 reporting threshold). Flag if >3 such tips in 30 days.
    - flag_suspicious/3 (user_id, pattern_type, details): insert AMLFlag
    - list_flags/1 (opts): paginated list with status filter, for admin view
    - review_flag/3 (flag_id, admin_id, new_status): update flag status

    AMLScanWorker: Oban worker, :aml_scan queue, daily cron at 5 AM UTC.
    Queries users with sum(abs(amount)) > 1000 from node_transactions in last 7 days.
    Runs scan_user/1 for each. Batch size 50. Logger.info for results.

    Add queue: :aml_scan to Oban config (concurrency: 1).
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 3: Reputation levels + thread archiving + boost + guardrails</name>
  <files>
    apps/backend/lib/cgraph/forums/reputation_level.ex
    apps/backend/lib/cgraph/forums/reputation.ex
    apps/backend/lib/cgraph/forums/thread.ex
    apps/backend/lib/cgraph/boosts/boost.ex
    apps/backend/lib/cgraph/nodes/nodes.ex
    apps/backend/lib/cgraph/paid_dm/paid_dm.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>
    ReputationLevel module (pure constants, no DB table):
    - @levels [
        %{key: :iron, name: "Iron", min: 0, color: "#8B8B8B", icon: "iron-shield"},
        %{key: :bronze, name: "Bronze", min: 100, color: "#CD7F32", icon: "bronze-shield"},
        %{key: :silver, name: "Silver", min: 500, color: "#C0C0C0", icon: "silver-shield"},
        %{key: :gold, name: "Gold", min: 1500, color: "#FFD700", icon: "gold-shield"},
        %{key: :platinum, name: "Platinum", min: 5000, color: "#E5E4E2", icon: "platinum-shield"},
        %{key: :diamond, name: "Diamond", min: 15000, color: "#B9F2FF", icon: "diamond-shield"}
      ]
    - level_for_score/1: returns level map for given score
    - all_levels/0: returns @levels

    Extend reputation.ex: add level_for_score/1 delegating to ReputationLevel.

    Thread.ex migration: ADD COLUMN is_archived boolean DEFAULT false.
    Thread.ex schema: add `field :is_archived, :boolean, default: false`.
    Thread.ex changeset: add :is_archived to cast list.

    Boost.ex: change @target_types from ~w(thread post forum) to ~w(thread post forum profile).

    Economic guardrails:
    - nodes.ex: add @max_paid_dm_price 10_000. Export as function max_paid_dm_price/0.
    - paid_dm.ex send_paid_file/4: validate price <= Nodes.max_paid_dm_price()
      ⚠️ Current validation (paid_dm_setting.ex L33): validate_number(:price_per_file, greater_than: 0)
      Add: validate_number(:price_per_file, less_than_or_equal_to: 10_000)
    - nodes.ex: add reputation_gate/1 (user_id) — check CGraph.Pulse score >= 10.
      Return {:ok, :allowed} or {:error, :insufficient_reputation}.
    - Wire reputation_gate into unlock_content/2 (for sellers only, not buyers).

    ⚠️ NEW: Boost Expiration Worker
    - Create apps/backend/lib/cgraph/workers/boost_expiration_worker.ex
    - Oban worker in :maintenance queue (already exists, concurrency: 3)
    - Cron: every hour ("0 * * * *") — calls existing CGraph.Boosts.expire_boosts/0
    - expire_boosts/0 already exists at boosts.ex L128 but is NEVER called
    - Add crontab entry in prod.exs alongside existing cron jobs
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
