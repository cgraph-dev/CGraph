---
phase: 29-secret-chat
plan: 01
type: execute
wave: 1
depends_on:
  - 26-04
files_modified:
  - apps/backend/lib/cgraph/messaging/secret_chat.ex (existing context — extend)
  - apps/backend/lib/cgraph/messaging/secret_conversation.ex (existing schema — extend)
  - apps/backend/lib/cgraph_web/controllers/api/v1/secret_chat_controller.ex (extend)
  - apps/backend/lib/cgraph_web/channels/secret_chat_channel.ex (extend)
  - apps/backend/lib/cgraph_web/router.ex (wire secret chat routes)
  - apps/backend/lib/cgraph/presence/ghost_mode.ex (new)
autonomous: true
must_haves:
  truths:
    - 'Ghost mode implemented via Redis key with auto-expiry'
    - 'Presence API filters ghost users as offline'
    - 'Secret chat sessions support aliases, expiry, panic wipe flags'
    - 'Panic wipe API endpoint clears all sessions + Redis queue'
    - 'Secret chat messages NOT stored on server (relay only)'
  artifacts:
    - apps/backend/lib/cgraph/presence/ghost_mode.ex (new)
    - apps/backend/priv/repo/migrations/ (secret_conversations extension migration)
  key_links:
    - 'Depends only on Phase 26 cleanup being done'
    - 'Plan 29-02 builds the frontend UI for this backend'
---

<objective>
Extend the existing secret chat backend with Ghost Mode, Secret Identity (aliases),
Timed Conversations, and Panic Wipe. The basic SecretChat context, controller, and
channel already exist — this adds the privacy features.

Backend is Elixir/Phoenix. Messages are E2E encrypted using the custom Signal Protocol
in packages/crypto/ (pqxdh.ts, doubleRatchet.ts). Server relays encrypted blobs only.
</objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Part 7 — Secret Chat)
@apps/backend/lib/cgraph/messaging/secret_chat.ex (338 lines — context module with CRUD, send, read, self-destruct)
@apps/backend/lib/cgraph/messaging/secret_conversation.ex (104 lines — schema: status, self_destruct_seconds, device IDs, fingerprints)
@apps/backend/lib/cgraph/messaging/secret_message.ex (53 lines — ciphertext blob, nonce, ratchet_header)
@apps/backend/lib/cgraph_web/controllers/api/v1/secret_chat_controller.ex (180 lines — REST API, NOT WIRED in router.ex)
@apps/backend/lib/cgraph_web/channels/secret_chat_channel.ex (206 lines — join, new_message, typing, screenshot_detected)
@apps/backend/lib/cgraph/presence/store.ex (397 lines — Redis-backed presence storage)
@apps/backend/lib/cgraph/presence/queries.ex (278 lines — presence lookups, ghost filtering target)
@apps/backend/lib/cgraph/redis.ex (497 lines — GenServer wrapper with KeyValue, Hash, Set, SortedSet)
@apps/backend/lib/cgraph/workers/delete_expired_secret_messages.ex (39 lines — Oban cron cleanup)
@packages/crypto/ (4,482 lines — PQXDH, Triple Ratchet, AES-256-GCM, stores)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Ghost Mode</name>
  <files>apps/backend/lib/cgraph/presence/ghost_mode.ex</files>
  <action>
    Create GhostMode module with:
    - activate(user_id) — set Redis key "ghost:{user_id}" with 3600s TTL
    - deactivate(user_id) — delete Redis key
    - is_ghost?(user_id) — check Redis key existence
    - get_presence/2 — return fake "offline" for ghost users

    Integrate with existing presence system to filter ghost users.
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>Ghost mode functional via Redis</done>
</task>

<task type="auto">
  <name>Task 2: Extend secret_conversations schema</name>
  <files>apps/backend/lib/cgraph/messaging/secret_conversation.ex</files>
  <action>
    Extend the existing `secret_conversations` table/schema (SecretConversation).
    Current fields: id, status, self_destruct_seconds, initiator/recipient device_id,
    initiator/recipient fingerprint, terminated_at, terminated_by, initiator_id, recipient_id.

    Add via migration:
    - expires_at (TIMESTAMPTZ, nullable) — absolute session expiry
    - ghost_initiator (boolean, default false)
    - ghost_recipient (boolean, default false)
    - alias_initiator (varchar(50), nullable)
    - alias_recipient (varchar(50), nullable)
    - secret_theme_id (varchar(50), nullable)
    - panic_wipe_initiator (boolean, default false)
    - panic_wipe_recipient (boolean, default false)

    Update the Ecto schema in secret_conversation.ex to include new fields.
    NOTE: Existing naming convention uses initiator/recipient, NOT a/b.
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>Secret chat sessions schema extended</done>
</task>

<task type="auto">
  <name>Task 3: Wire routes + Implement Panic Wipe API</name>
  <files>apps/backend/lib/cgraph_web/controllers/api/v1/secret_chat_controller.ex, apps/backend/lib/cgraph_web/router.ex</files>
  <action>
    FIRST: Wire existing secret chat controller routes in router.ex.
    The controller at api/v1/secret_chat_controller.ex has 5 actions
    (create, index, show, delete, update_timer) but NONE are routed.
    Add to the authenticated API v1 scope:
      resources "/secret-chats", SecretChatController, only: [:create, :index, :show, :delete]
      put "/secret-chats/:id/timer", SecretChatController, :update_timer
      post "/secret-chats/panic-wipe", SecretChatController, :panic_wipe

    THEN: Add panic_wipe action to the controller:
    - Mark all user's sessions with panic_wipe flag
    - Clear Redis message queue for user
    - Return success
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>Panic wipe API functional</done>
</task>

<task type="auto">
  <name>Task 4: Implement timed conversation expiry</name>
  <files>apps/backend/lib/cgraph/messaging/secret_chat.ex, apps/backend/lib/cgraph/workers/</files>
  <action>
    1. Add Oban worker to check and expire sessions where expires_at < now()
    2. On expiry: mark session deleted, notify both participants via channel
    3. Add API to set/update expires_at on a session
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>Timed conversations expire automatically</done>
</task>

</tasks>

<verification>
- [ ] Ghost mode works via Redis
- [ ] Secret chat sessions have all required fields
- [ ] Panic wipe clears sessions + queue
- [ ] Timed conversations auto-expire
- [ ] Backend compiles cleanly
</verification>

<success_criteria>
- All secret chat backend features functional
</success_criteria>

<output>
After completion, create `.gsd/phases/29-secret-chat/29-01-SUMMARY.md`
</output>
