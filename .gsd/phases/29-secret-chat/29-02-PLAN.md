---
phase: 29-secret-chat
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - apps/web/src/modules/secret-chat/ (new directory)
  - apps/web/src/pages/chat/ (secret chat integration)
  - apps/web/src/pages/customize/ (secret theme slot)
autonomous: true
must_haves:
  truths:
    - 'Secret Chat UI built: header toggle, ghost indicator, timer, panic wipe button'
    - 'E2E encryption uses packages/crypto/ (pqxdh, doubleRatchet, tripleRatchet)'
    - '12 secret chat themes implemented (CSS + WebP textures)'
    - 'Separate cosmetic equip slot for secret themes in customize page'
    - 'Secret Identity UI shows alias + deterministic SVG avatar'
  artifacts:
    - apps/web/src/modules/secret-chat/ (new module)
    - apps/web/src/modules/secret-chat/components/ (header, ghost, timer, themes)
    - apps/web/src/modules/secret-chat/store/ (secret chat state)
  key_links:
    - 'Depends on Plan 29-01 (backend features must exist first)'
    - 'Uses packages/crypto/ for encryption'
---

<objective>
Build the entire Secret Chat frontend UI from scratch. Zero secret chat UI components
exist currently. Build header toggle, ghost mode indicator, timed conversation countdown,
panic wipe button, 12 secret themes, and secret identity display.
</objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Part 7 — Secret Chat UI)
@packages/crypto/
@apps/web/src/pages/chat/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build secret chat module structure</name>
  <files>apps/web/src/modules/secret-chat/</files>
  <action>
    Create module structure:
    - store/secretChatStore.ts — Zustand store for secret chat state
    - store/types.ts — SecretChatSession, SecretTheme, etc.
    - components/SecretChatHeader.tsx — header with ghost, timer, panic wipe
    - components/GhostModeIndicator.tsx — ghost status display
    - components/TimerCountdown.tsx — conversation expiry countdown
    - components/PanicWipeButton.tsx — long-press destructive action
    - components/SecretIdentity.tsx — alias + deterministic SVG avatar
    - hooks/useSecretChat.ts — hook combining store + encryption
    - index.ts — barrel export
  </action>
  <verify>ls apps/web/src/modules/secret-chat/ 2>&1</verify>
  <done>Secret chat module structure created</done>
</task>

<task type="auto">
  <name>Task 2: Build SecretChatHeader component</name>
  <files>apps/web/src/modules/secret-chat/components/</files>
  <action>
    Build header replacing regular DM header when secret mode is active:
    ← 🔒 Secret  [👻 Ghost]  [⏱ 24h]  [💣]  [⋮]
    - Toggle between regular and secret mode
    - Ghost button activates/deactivates ghost mode via API
    - Timer shows countdown to session expiry
    - Panic wipe button with long-press confirmation
  </action>
  <verify>ls apps/web/src/modules/secret-chat/components/SecretChatHeader.tsx</verify>
  <done>SecretChatHeader component built</done>
</task>

<task type="auto">
  <name>Task 3: Build 12 secret chat themes</name>
  <files>apps/web/src/modules/secret-chat/themes/</files>
  <action>
    Create 12 themes as CSS + WebP texture files (NOT Lottie — ultra-lightweight):
    Void, Redacted, Midnight, Signal, Ghost, Cipher,
    Onyx, Eclipse, Static, Shadow, Obsidian, Abyss

    Each theme defines: background, text colors, bubble styles, accent colors.
    Create theme registry mapping theme IDs to CSS classes and texture URLs.
  </action>
  <verify>ls apps/web/src/modules/secret-chat/themes/ 2>&1</verify>
  <done>12 secret chat themes created</done>
</task>

<task type="auto">
  <name>Task 4: Add secret theme equip slot to customization</name>
  <files>apps/web/src/pages/customize/</files>
  <action>
    Add a separate cosmetic equip slot for secret chat themes in the customization page.
    This is distinct from the regular chat theme — users equip their preferred secret
    chat visual separately.
  </action>
  <verify>grep -n "secretTheme\|secret.*theme" apps/web/src/pages/customize/ -r --include="*.tsx" | head -5</verify>
  <done>Secret theme equip slot in customize page</done>
</task>

<task type="auto">
  <name>Task 5: Integrate E2E encryption</name>
  <files>apps/web/src/modules/secret-chat/hooks/</files>
  <action>
    Wire the secret chat to use packages/crypto/:
    - pqxdh.ts for key agreement (Post-Quantum X3DH)
    - doubleRatchet.ts for message encryption
    - Client-side key generation and storage
    - Encrypt messages before sending via WebSocket
    - Decrypt messages on receipt
  </action>
  <verify>grep -n "pqxdh\|doubleRatchet\|tripleRatchet" apps/web/src/modules/secret-chat/ -r | head -5</verify>
  <done>E2E encryption wired to secret chat</done>
</task>

</tasks>

<verification>
- [ ] Secret chat module exists with all components
- [ ] Header toggle works between regular and secret mode
- [ ] Ghost mode activates/deactivates
- [ ] Timer countdown displays
- [ ] Panic wipe with confirmation
- [ ] 12 themes available
- [ ] E2E encryption via packages/crypto/
</verification>

<success_criteria>
- Complete secret chat UI functional
- E2E encryption integrated
- 12 themes selectable
</success_criteria>

<output>
After completion, create `.gsd/phases/29-secret-chat/29-02-SUMMARY.md`
</output>
