---
phase: 28-complete-cosmetics
plan: 01
type: execute
wave: 1
depends_on:
  - 27-02
files_modified:
  - apps/web/src/components/nameplate/NameplateBar.tsx (new)
  - apps/web/src/pages/customize/identity-customization/ (nameplate wiring)
  - apps/web/src/components/profile/ProfileContent.tsx
autonomous: true
must_haves:
  truths:
    - 'NameplateBar component built (web + native sharing design tokens)'
    - 'Full rendering: Lottie background OR CSS gradient, border, emblem, text effect, particles'
    - 'Wired to ProfileContent replacing partial stub'
    - 'All 24 nameplates render correctly in live preview'
  artifacts:
    - apps/web/src/components/nameplate/NameplateBar.tsx (new)
    - apps/web/src/components/nameplate/index.ts (new)
  key_links:
    - 'Depends on Phase 27 (achievement/types consolidated)'
    - 'Must be done before Phase 30 (Pulse profile card needs nameplate)'
---

<objective>
Build the NameplateBar component — the most impactful missing cosmetic feature.
Implement full rendering with Lottie backgrounds, CSS gradients, border, emblem,
text effects, and particles. Wire to profile display. Test all 24 nameplates.
</objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Sprint 3 — Nameplate)
@apps/web/src/components/profile/ProfileContent.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build NameplateBar component</name>
  <files>apps/web/src/components/nameplate/</files>
  <action>
    Create NameplateBar component with:
    - Lottie background support (using existing Lottie infrastructure)
    - CSS gradient fallback
    - Border frame rendering
    - Emblem icon rendering
    - Text effect layer
    - Particle integration via ParticleEngine
    - Design tokens shared with mobile (via packages/animation-constants or shared-types)
  </action>
  <verify>ls apps/web/src/components/nameplate/NameplateBar.tsx</verify>
  <done>NameplateBar component created with full rendering</done>
</task>

<task type="auto">
  <name>Task 2: Wire NameplateBar to ProfileContent</name>
  <files>apps/web/src/components/profile/ProfileContent.tsx</files>
  <action>
    Replace the "partially wired" nameplate stub in ProfileContent with the actual
    NameplateBar component. Wire equippedNameplate from customization store to the component.
  </action>
  <verify>grep -n "NameplateBar\|nameplate" apps/web/src/components/profile/ProfileContent.tsx</verify>
  <done>NameplateBar renders in profile</done>
</task>

<task type="auto">
  <name>Task 3: Test all 24 nameplates in live preview</name>
  <files>apps/web/src/pages/customize/</files>
  <action>
    Ensure the nameplate picker in customization page correctly previews all 24 nameplates.
    Verify each one renders without errors. Fix any missing Lottie files or broken references
    in the nameplate registry.
  </action>
  <verify>cd apps/web && npx tsc --noEmit --pretty false 2>&1 | grep "nameplate\|Nameplate" | head -10</verify>
  <done>All 24 nameplates render correctly</done>
</task>

</tasks>

<verification>
- [ ] NameplateBar component exists and renders
- [ ] Wired to ProfileContent
- [ ] All 24 nameplates render in customization preview
</verification>

<success_criteria>
- NameplateBar fully functional
- Profile displays equipped nameplate
</success_criteria>

<output>
After completion, create `.gsd/phases/28-complete-cosmetics/28-01-SUMMARY.md`
</output>
