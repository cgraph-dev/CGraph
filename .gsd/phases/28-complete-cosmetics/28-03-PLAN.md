---
phase: 28-complete-cosmetics
plan: 03
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - apps/web/src/components/avatar-border/ (border rendering)
  - apps/backend/lib/cgraph/cosmetics/ (border seed sync)
autonomous: true
must_haves:
  truths:
    - 'All CSS-particle borders replaced with Lottie equivalents'
    - 'Single rendering path per border (Lottie or CSS, never both)'
    - 'All 42 canonical borders render correctly on web'
    - 'Backend seed synced: 42 borders (up from 18)'
  artifacts:
    - apps/backend/priv/repo/seeds/ (updated border seed)
  key_links:
    - 'Depends on Plan 28-01 (nameplate must work first for full profile rendering)'
---

<objective>
Unify border rendering: replace CSS-particle borders with Lottie, ensure single rendering
path, verify all 42 canonical borders, and sync backend seed data.
</objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Sprint 3 — Border unification)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CSS-particle borders with Lottie</name>
  <files>apps/web/src/components/avatar-border/</files>
  <action>
    Find borders using CSS particles (fire, ice, electric at minimum).
    Replace with Lottie animation equivalents. Ensure each border uses EITHER
    Lottie OR CSS rendering, never both simultaneously.
  </action>
  <verify>grep -rn "particle.*border\|border.*particle" apps/web/src/ --include="*.ts" --include="*.tsx" | head -10</verify>
  <done>All particle borders use Lottie</done>
</task>

<task type="auto">
  <name>Task 2: Verify all 42 canonical borders</name>
  <files>apps/web/src/</files>
  <action>
    Check the border registry has 42 entries. Verify each has a valid rendering source
    (lottie file or CSS class). Fix any broken references.
  </action>
  <verify>grep -rn "BORDER_REGISTRY\|borderRegistry\|AVATAR_BORDER" apps/web/src/ --include="*.ts" --include="*.tsx" | head -10</verify>
  <done>42 borders verified</done>
</task>

<task type="auto">
  <name>Task 3: Sync backend border seed to 42</name>
  <files>apps/backend/priv/repo/seeds/</files>
  <action>
    Find the border seed file. Update it to include all 42 canonical borders.
    Currently only 18 are seeded. Add the missing 24 with proper metadata.
  </action>
  <verify>grep -c "border" apps/backend/priv/repo/seeds/*.exs 2>&1 || echo "Check seed files"</verify>
  <done>Backend serves all 42 borders</done>
</task>

</tasks>

<verification>
- [ ] No CSS-particle borders remain (all Lottie)
- [ ] Single rendering path per border
- [ ] 42 borders in frontend registry
- [ ] 42 borders in backend seed
</verification>

<success_criteria>
- Border system unified
- Backend and frontend in sync
</success_criteria>

<output>
After completion, create `.gsd/phases/28-complete-cosmetics/28-03-SUMMARY.md`
</output>
