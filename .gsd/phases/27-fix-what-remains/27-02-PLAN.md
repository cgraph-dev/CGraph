---
phase: 27-fix-what-remains
plan: 02
type: execute
wave: 1
depends_on:
  - 26-04
files_modified:
  - apps/web/src/modules/gamification/types/ (achievement types)
  - apps/web/src/modules/settings/store/customization/ (types)
  - packages/shared-types/src/ (achievement types)
autonomous: true
must_haves:
  truths:
    - 'Achievement interface stripped of xpReward, coinReward, unlockLevel, progress'
    - 'cosmeticReward field added to Achievement interface'
    - '38 orphaned titleReward strings handled (15 new titles created, 23 removed)'
    - 'free rarity added to titles constants'
    - 'uncommon/unique rarity references removed'
    - 'RARITY_COLORS/RARITY_GRADIENTS have mythic entry'
  artifacts:
    - packages/shared-types/src/achievements.ts (consolidated)
    - apps/web/src/pages/customize/identity-customization/constants.ts (RARITY updated)
  key_links:
    - 'Depends on Phase 26 completion'
    - 'Achievement types used by Phase 28 cosmetics work'
---

<objective>
Fix the achievement system to work without gamification. Strip gamification fields,
add cosmetic reward type, fix orphaned title rewards, and clean up rarity constants.
</objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Sprint 2 — Achievement system fix)
@packages/shared-types/src/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip gamification fields from Achievement interface</name>
  <files>packages/shared-types/src/ apps/web/src/</files>
  <action>
    Find the Achievement interface/type definition.
    Remove these fields:
    - xpReward
    - coinReward
    - unlockLevel
    - progress (if it's a gamification progress tracker)

    Add new field:
    - cosmeticReward: { type: 'title' | 'badge' | 'border' | 'nameplate' | 'effect'; itemId: string }

    Update all consumers of these removed fields.
  </action>
  <verify>grep -rn "xpReward\|coinReward\|unlockLevel" apps/web/src/ packages/ --include="*.ts" --include="*.tsx" | head -10</verify>
  <done>Achievement interface cleaned and updated</done>
</task>

<task type="auto">
  <name>Task 2: Fix orphaned titleReward strings</name>
  <files>apps/web/src/ apps/backend/</files>
  <action>
    Find all 38 achievements with titleReward strings.
    - For 15 that reference valid cosmetic titles: create those title entries
      in the titles registry if they don't exist
    - For 23 that reference non-existent titles: remove the titleReward field
    - Convert remaining titleRewards to use the new cosmeticReward format
  </action>
  <verify>grep -rn "titleReward" apps/web/src/ apps/backend/ --include="*.ts" --include="*.tsx" --include="*.ex" | head -10</verify>
  <done>All titleReward strings resolved</done>
</task>

<task type="auto">
  <name>Task 3: Fix rarity constants</name>
  <files>apps/web/src/</files>
  <action>
    1. Add 'free' rarity to titles constants (for default/starting titles)
    2. Remove 'uncommon' and 'unique' rarity references if they exist
    3. Add 'mythic' entry to RARITY_COLORS and RARITY_GRADIENTS
    4. Ensure rarity constants are consistent: common, rare, epic, legendary, mythic, free
  </action>
  <verify>grep -rn "RARITY_COLORS\|RARITY_GRADIENTS\|uncommon\|unique" apps/web/src/ --include="*.ts" --include="*.tsx" | head -10</verify>
  <done>Rarity constants consistent with mythic and free, no uncommon/unique</done>
</task>

<task type="auto">
  <name>Task 4: Collapse achievement type files</name>
  <files>apps/web/src/ packages/shared-types/src/</files>
  <action>
    If multiple achievement type definitions exist across different files,
    collapse them into a single canonical location:
    - Primary: packages/shared-types/src/achievements.ts (or cosmetics.types.ts)
    - Re-export from wherever convenience imports are needed
    - Delete duplicate type definitions
  </action>
  <verify>grep -rn "interface Achievement\|type Achievement" apps/web/src/ packages/ --include="*.ts" | head -10</verify>
  <done>Single canonical achievement type location</done>
</task>

</tasks>

<verification>
- [ ] Achievement interface has no gamification fields
- [ ] cosmeticReward field exists on Achievement
- [ ] All 38 titleReward strings resolved
- [ ] free rarity exists, uncommon/unique removed
- [ ] mythic in RARITY_COLORS/RARITY_GRADIENTS
- [ ] TypeScript compiles cleanly
</verification>

<success_criteria>
- Achievement system works standalone without gamification
- Rarity system consistent
- No orphaned references
</success_criteria>

<output>
After completion, create `.gsd/phases/27-fix-what-remains/27-02-SUMMARY.md`
</output>
