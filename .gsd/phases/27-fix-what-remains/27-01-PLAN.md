---
phase: 27-fix-what-remains
plan: 01
type: execute
wave: 1
depends_on:
  - 26-04
files_modified:
  - apps/web/src/modules/settings/store/customization/customizationStore.types.ts
  - apps/web/src/modules/settings/store/customization/customizationStore.ts
  - apps/web/src/pages/customize/ (theme and effects components)
autonomous: true
must_haves:
  truths:
    - 'Legacy Theme[] grid deleted, only single theme system remains'
    - 'useNewProfileThemes hook deleted'
    - 'Background Effects picker replaced with 3-state toggle (none/static/animated)'
    - 'ParticleEngine component built with priority stack'
    - 'PARTICLE_ID_TO_EFFECT lossy mapping removed'
    - 'Reaction Styles section removed from chat customization'
    - 'Locked layouts "Professional" and "Artistic" removed'
    - '12 legacy aliases deleted from useCustomizationStore'
    - '10 dead store props deleted'
  artifacts:
    - apps/web/src/modules/settings/store/customization/customizationStore.types.ts (cleaned)
    - apps/web/src/components/particles/ParticleEngine.tsx (new)
  key_links:
    - 'Depends on Phase 26 completion (Great Delete)'
---

<objective>
Fix broken customization systems left after the Great Delete. Consolidate themes, simplify
background effects, build ParticleEngine, clean dead props from customization store, and
remove unused UI sections.

This is Sprint 2 of the definitive plan — the "Fix What Remains Broken" phase.
</objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Sprint 2 — Customization fixes)
@apps/web/src/modules/settings/store/customization/customizationStore.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate dual profile theme system</name>
  <files>apps/web/src/pages/customize/ apps/web/src/modules/settings/</files>
  <action>
    1. Find and delete the legacy `Theme[]` grid component — search for "useNewProfileThemes" hook
    2. Delete the useNewProfileThemes hook
    3. Ensure only one theme system remains (the current ThemePreset-based system)
    4. Clean up any duplicated theme references
  </action>
  <verify>grep -rn "useNewProfileThemes\|Theme\[\]" apps/web/src/ --include="*.ts" --include="*.tsx" | head -10</verify>
  <done>Single theme system only</done>
</task>

<task type="auto">
  <name>Task 2: Replace Background Effects picker with 3-state toggle</name>
  <files>apps/web/src/pages/customize/</files>
  <action>
    1. Find the Background Effects picker (10-option selector)
    2. Replace with a simple 3-state toggle: none / static / animated
    3. Map existing effects to the 3 states
    4. Update customization store to reflect new state shape
  </action>
  <verify>grep -rn "BackgroundEffect\|backgroundEffect" apps/web/src/ --include="*.ts" --include="*.tsx" | head -10</verify>
  <done>3-state toggle replaces complex picker</done>
</task>

<task type="auto">
  <name>Task 3: Build ParticleEngine component</name>
  <files>apps/web/src/components/particles/</files>
  <action>
    Build a single `<ParticleEngine>` component that consolidates 4 particle sources:
    1. Profile background particles
    2. Border particles
    3. Nameplate particles
    4. Ambient effect particles

    Implement priority stack: border > nameplate > profile bg > ambient.
    Only one source active at a time based on context.
    Use canvas rendering for performance.
  </action>
  <verify>ls apps/web/src/components/particles/ParticleEngine.tsx 2>&1</verify>
  <done>ParticleEngine component exists and handles all particle sources</done>
</task>

<task type="auto">
  <name>Task 4: Fix PARTICLE_ID_TO_EFFECT mapping</name>
  <files>apps/web/src/</files>
  <action>
    1. Find PARTICLE_ID_TO_EFFECT constant
    2. Remove the lossy mapping — use particleEffect ID directly instead
    3. Update all consumers to use the direct ID lookup
  </action>
  <verify>grep -rn "PARTICLE_ID_TO_EFFECT" apps/web/src/ --include="*.ts" --include="*.tsx"</verify>
  <done>PARTICLE_ID_TO_EFFECT removed, direct ID used</done>
</task>

<task type="auto">
  <name>Task 5: Remove Reaction Styles and locked layouts</name>
  <files>apps/web/src/pages/customize/</files>
  <action>
    1. Remove Reaction Styles section from chat customization UI
    2. Remove locked "Professional" and "Artistic" layouts from profile layouts
    3. Clean up any related constants, types, or mock data
  </action>
  <verify>grep -rn "Reaction Style\|Professional\|Artistic" apps/web/src/pages/customize/ --include="*.ts" --include="*.tsx" | head -10</verify>
  <done>Dead UI sections removed</done>
</task>

<task type="auto">
  <name>Task 6: Delete legacy aliases and dead store props</name>
  <files>apps/web/src/modules/settings/store/customization/customizationStore.types.ts</files>
  <action>
    1. Delete 12 legacy aliases from useCustomizationStore (identify them by searching for
       deprecated or aliased properties)
    2. Delete 10 dead store props that no longer have consumers
    3. Run a consumer check: grep for each prop across the codebase, delete if zero consumers
  </action>
  <verify>cd apps/web && npx tsc --noEmit --pretty false 2>&1 | grep "error" | wc -l</verify>
  <done>Customization store type file cleaned, no dead props</done>
</task>

</tasks>

<verification>
- [ ] Single theme system remains (no dual themes)
- [ ] 3-state background effects toggle works
- [ ] ParticleEngine component renders correctly
- [ ] PARTICLE_ID_TO_EFFECT removed
- [ ] Reaction Styles section gone
- [ ] Locked layouts removed
- [ ] Customization store types cleaned
- [ ] TypeScript compiles cleanly
</verification>

<success_criteria>
- All customization fixes complete
- No regression in working features
- Store is cleaner with no dead props
</success_criteria>

<output>
After completion, create `.gsd/phases/27-fix-what-remains/27-01-SUMMARY.md`
</output>
