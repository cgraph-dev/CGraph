---
phase: 08-social-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/modules/social/components/user-search.tsx
  - apps/web/src/modules/social/hooks/useUserSearch.ts
  - apps/mobile/src/screens/social/user-search-screen.tsx
autonomous: true

must_haves:
  truths:
    - "User types a name/username in the search bar and sees matching results within 500ms"
    - "Search results show avatar, display name, and username for each match"
    - "User can send a friend request from search results"
    - "Blocked users do not appear in search results (already enforced backend-side)"
  artifacts:
    - path: "apps/web/src/modules/social/components/user-search.tsx"
      provides: "Web user search UI with debounced input and result list"
      min_lines: 50
    - path: "apps/web/src/modules/social/hooks/useUserSearch.ts"
      provides: "Debounced search hook calling GET /api/v1/search/users"
      min_lines: 25
    - path: "apps/mobile/src/screens/social/user-search-screen.tsx"
      provides: "Mobile user search screen with FlatList results"
      min_lines: 50
  key_links:
    - from: "apps/web/src/modules/social/hooks/useUserSearch.ts"
      to: "/api/v1/search/users"
      via: "api.get with debounced query param"
      pattern: "api\\.get.*search/users"
    - from: "apps/mobile/src/screens/social/user-search-screen.tsx"
      to: "/api/v1/search/users"
      via: "api.get with debounced query param"
      pattern: "api\\.get.*search/users"
---

<objective>
Wire user search UI on web and mobile to the existing Meilisearch + PostgreSQL backend.

Purpose: SEARCH-02 — users can find other users by name or username. The backend (`CGraph.Search.Users.search_users/2`) and API endpoint (`GET /api/v1/search/users`) already exist with Meilisearch primary + PostgreSQL ILIKE fallback, including blocked-user exclusion. This plan builds the frontend search experiences.
Output: Web search component + hook, mobile search screen — both calling the existing API.
</objective>

<execution_context>
@.gsd/skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md

@apps/backend/lib/cgraph/search/users.ex
@apps/backend/lib/cgraph_web/controllers/api/v1/search_controller.ex
@apps/backend/lib/cgraph_web/router/user_routes.ex
@apps/web/src/modules/social/hooks/useProfileData.ts
@apps/web/src/modules/social/components/user-profile-card/user-profile-card.tsx
@apps/mobile/src/components/chat/QRCodeScanner.tsx
@packages/shared-types/src/models.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create web user search hook and component</name>
  <files>apps/web/src/modules/social/hooks/useUserSearch.ts, apps/web/src/modules/social/components/user-search.tsx</files>
  <action>
1. Create `useUserSearch` hook in `apps/web/src/modules/social/hooks/useUserSearch.ts`:
   - Accept `query: string` parameter
   - Debounce input by 300ms (use existing pattern from codebase — check for lodash debounce or custom)
   - Call `api.get('/api/v1/search/users', { params: { q: query } })` when query.length >= 2
   - Return `{ results: User[], isLoading: boolean, error: string | null }`
   - Handle empty query (clear results)
   - Support cursor-based pagination via `cursor` param from the API response

2. Create `user-search.tsx` component in `apps/web/src/modules/social/components/`:
   - Search input with magnifying glass icon and clear button
   - Debounced text input wired to `useUserSearch`
   - Results list showing avatar (40px circle), display_name (bold), username (muted @-prefixed)
   - Each result row has "Add Friend" button (calls existing friend request API: `POST /api/v1/friends`)
   - Show "Add Friend" only if not already friends (check friendship status from result data)
   - Empty state: "No users found" when query is entered but no results
   - Loading state: skeleton/spinner while searching
   - Use existing Tailwind design system classes consistent with the codebase
   - Export the component from `apps/web/src/modules/social/components/index.ts`
  </action>
  <verify>Run `cd apps/web && npx tsc --noEmit` — no type errors in new files</verify>
  <done>Web search component renders, debounces input, calls search API, displays results with avatar/name/username, and offers friend request action</done>
</task>

<task type="auto">
  <name>Task 2: Create mobile user search screen</name>
  <files>apps/mobile/src/screens/social/user-search-screen.tsx, apps/mobile/src/screens/social/index.ts</files>
  <action>
1. Create `apps/mobile/src/screens/social/user-search-screen.tsx`:
   - TextInput at the top with search icon, placeholder "Search users..."
   - Debounce search by 300ms
   - Call `api.get('/api/v1/search/users', { params: { q: query } })` when query.length >= 2
   - Display results in a FlatList with avatar (Image, 44px circle), display_name, @username
   - Each row has a "Add Friend" TouchableOpacity button
   - Empty state component when no results
   - ActivityIndicator while loading
   - Use existing React Native styling patterns from the codebase (check apps/mobile/src/screens/ for StyleSheet patterns)
   - Handle keyboard dismiss on scroll (keyboardDismissMode="on-drag" on FlatList)

2. Create barrel export at `apps/mobile/src/screens/social/index.ts`

3. Add the screen to mobile navigation if a social/search navigator exists. If not, just export it — navigation wiring can be done when used in onboarding (Plan 05).
  </action>
  <verify>Run `cd apps/mobile && npx tsc --noEmit` — no type errors in new files</verify>
  <done>Mobile search screen renders with input, calls API, shows results in FlatList with avatars, handles loading/empty states</done>
</task>

</tasks>

<verification>
- [ ] `cd apps/web && npx tsc --noEmit` passes
- [ ] `cd apps/mobile && npx tsc --noEmit` passes
- [ ] Web search hook calls GET /api/v1/search/users with debounced query
- [ ] Mobile search screen calls same API endpoint
- [ ] Both platforms show avatar, display_name, username in results
</verification>

<success_criteria>
- Web and mobile search UIs wired to existing backend search API
- Debounced input prevents excessive API calls
- Results display user info with friend request action
- No type errors introduced
</success_criteria>

<output>
After completion, create `.gsd/phases/08-social-profiles/08-01-SUMMARY.md`
</output>
