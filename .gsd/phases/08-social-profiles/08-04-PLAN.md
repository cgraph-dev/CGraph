---
phase: 08-social-profiles
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/modules/social/components/profile-card/profile-card.tsx
  - apps/web/src/modules/social/components/index.ts
  - apps/web/src/modules/social/hooks/useProfileEdit.ts
  - apps/mobile/src/screens/social/profile-edit-screen.tsx
  - apps/mobile/src/screens/social/index.ts
autonomous: true

must_haves:
  truths:
    - "User can edit display name, avatar, and bio from a profile edit screen"
    - "Avatar upload works on both web and mobile with image cropping"
    - "Profile changes are immediately reflected in the user's profile card"
    - "Other users see the updated profile when viewing the user's profile"
  artifacts:
    - path: "apps/web/src/modules/social/hooks/useProfileEdit.ts"
      provides: "Profile edit hook with avatar upload + field updates"
      contains: "upload_avatar|updateProfile"
    - path: "apps/mobile/src/screens/social/profile-edit-screen.tsx"
      provides: "Mobile profile edit screen with avatar picker and form fields"
      min_lines: 60
  key_links:
    - from: "apps/web/src/modules/social/hooks/useProfileEdit.ts"
      to: "/api/v1/me"
      via: "PUT request with profile fields"
      pattern: "api\\.put.*me"
    - from: "apps/mobile/src/screens/social/profile-edit-screen.tsx"
      to: "/api/v1/me/avatar"
      via: "POST multipart form data for avatar upload"
      pattern: "upload.*avatar|FormData"
---

<objective>
Complete the profile setup and edit experience on both platforms.

Purpose: AUTH-10 — users can set up and edit their display name, avatar, and bio. The backend already has `PUT /api/v1/me` for field updates and `POST /me/avatar` for avatar upload. The web has `useProfileEdit` hook and mobile has an onboarding screen with avatar + name fields. This plan ensures the full profile edit experience is complete, connected, and works on both platforms with proper avatar cropping.
Output: Enhanced profile edit UIs on web and mobile, verified end-to-end with existing backend.
</objective>

<execution_context>
@.gsd/skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md

@apps/backend/lib/cgraph_web/controllers/api/v1/user_controller.ex
@apps/backend/lib/cgraph/accounts/profile.ex
@apps/web/src/modules/social/hooks/useProfileEdit.ts
@apps/web/src/modules/social/components/profile-card/profile-card.tsx
@apps/mobile/src/screens/auth/onboarding/onboarding-screen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete web profile edit with avatar cropping</name>
  <files>apps/web/src/modules/social/components/profile-edit-form.tsx, apps/web/src/modules/social/hooks/useProfileEdit.ts</files>
  <action>
1. Verify the existing `useProfileEdit.ts` hook covers:
   - `updateProfile({ display_name, bio, signature })` via `PUT /api/v1/me`
   - `uploadAvatar(file: File)` via `POST /api/v1/me/avatar` with multipart FormData
   - If any of these are missing, add them. If they exist, leave as-is.

2. Create `profile-edit-form.tsx` in `apps/web/src/modules/social/components/`:
   - Full profile edit form component with:
     - Avatar section: current avatar preview (80px circle) + "Change Avatar" button
     - On "Change Avatar": open file picker (accept="image/*"), then show react-easy-crop modal for square crop (aspect: 1)
     - After crop: convert cropped area to Blob, upload via `uploadAvatar()`
     - Display name input (text, max 50 chars, required)
     - Bio textarea (max 500 chars, with character count)
     - Signature input (max 100 chars, optional)
     - Save button that calls `updateProfile()` with changed fields only
     - Loading state on save button
     - Success toast on save (use existing toast pattern from codebase)
   - Install react-easy-crop if not present: check package.json first. If missing, add `react-easy-crop` to apps/web dependencies.
   - Use existing Tailwind styling patterns from the codebase
   - Export from `apps/web/src/modules/social/components/index.ts`

3. Verify the existing profile-card component (`profile-card.tsx`) displays the user's display_name, avatar, and bio correctly. If it's a skeleton/stub, wire it to use real data from `useProfileData` hook.
  </action>
  <verify>Run `cd apps/web && npx tsc --noEmit` — no type errors</verify>
  <done>Web profile edit form allows changing display name, bio, signature, and avatar with crop UI. Changes persist via existing API endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Create mobile profile edit screen</name>
  <files>apps/mobile/src/screens/social/profile-edit-screen.tsx</files>
  <action>
1. Create `profile-edit-screen.tsx` in `apps/mobile/src/screens/social/`:
   - Header with "Edit Profile" title and Save button (right side)
   - Avatar section at top: current avatar (Image, 100px circle), "Change Photo" text button below
   - "Change Photo" opens `expo-image-picker` with `launchImageLibraryAsync({ mediaTypes: 'images', allowsEditing: true, aspect: [1, 1], quality: 0.8 })` — this gives built-in square crop
   - Upload cropped image via `POST /api/v1/me/avatar` with FormData (use existing mobile API patterns)
   - TextInput for display_name (max 50 chars)
   - TextInput for bio (multiline, max 500 chars) with character count
   - TextInput for signature (max 100 chars, optional)
   - Save calls `PUT /api/v1/me` with `{ display_name, bio, signature }`
   - Show loading indicator on save, success feedback (Alert or toast)
   - Pre-populate fields from current user data (fetch from `GET /api/v1/me` or use auth store)
   - Use existing StyleSheet patterns from the mobile app
   - Export from `apps/mobile/src/screens/social/index.ts`
  </action>
  <verify>Run `cd apps/mobile && npx tsc --noEmit` — no type errors</verify>
  <done>Mobile profile edit screen allows changing avatar (with crop), display name, bio, and signature. Changes persist via existing API endpoints.</done>
</task>

</tasks>

<verification>
- [ ] `cd apps/web && npx tsc --noEmit` passes
- [ ] `cd apps/mobile && npx tsc --noEmit` passes
- [ ] Web avatar upload with crop works via react-easy-crop
- [ ] Mobile avatar upload with crop works via expo-image-picker
- [ ] Profile fields save to backend via PUT /api/v1/me
</verification>

<success_criteria>
- Both platforms have complete profile edit experiences
- Avatar cropping works natively on each platform
- Profile changes persist via existing backend API
- No type errors introduced
</success_criteria>

<output>
After completion, create `.gsd/phases/08-social-profiles/08-04-SUMMARY.md`
</output>
