---
phase: 32-nodes-monetization
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - apps/web/src/modules/nodes/ (new directory)
  - apps/web/src/pages/wallet/ (new page)
  - apps/web/src/routes/app-routes.tsx (wallet route)
autonomous: true
must_haves:
  truths:
    - 'Node wallet page showing balance, transactions, withdrawal'
    - 'Get Nodes button with bundle selection'
    - 'Tipping UI on user profiles and posts'
    - 'Content unlock flow in forums (pay Nodes to read locked threads)'
    - 'Transaction history with filtering'
    - 'Withdrawal request form'
  artifacts:
    - apps/web/src/modules/nodes/ (new module)
    - apps/web/src/pages/wallet/ (new page)
  key_links:
    - 'Depends on Plan 32-01 (backend must exist)'
    - 'Content unlock integrates with forum threads (Phase 31)'
---

<objective>
Build the Nodes frontend: wallet page, bundle purchase flow, tipping UI,
content unlock, transaction history, and withdrawal requests.
</objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Part 6 — Currency Setup, Bundles)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build wallet page</name>
  <files>apps/web/src/pages/wallet/</files>
  <action>
    Create wallet page with:
    - Balance display (available ℕ + pending ℕ)
    - "Get Nodes" button → bundle selection overlay
    - Transaction history list with type filters
    - Withdrawal section (for creators with earned nodes)
    - Lifetime stats (earned, spent)
  </action>
  <verify>ls apps/web/src/pages/wallet/ 2>&1</verify>
  <done>Wallet page built</done>
</task>

<task type="auto">
  <name>Task 2: Build bundle purchase flow</name>
  <files>apps/web/src/modules/nodes/</files>
  <action>
    Build purchase UI:
    - Bundle cards: Starter(500/€4.99), Popular(1200/€9.99, +20%🔥),
      Creator(2800/€19.99, +40%), Pro(6500/€39.99, +63%), Ultimate(17000/€99.99, +70%)
    - Integrate with existing Stripe checkout (NOT Paddle — Paddle is future epic)
    - Success toast: "ℕ 1,200 added to your wallet"
    - Wire to POST /api/v1/nodes/credit via webhook
  </action>
  <verify>ls apps/web/src/modules/nodes/ 2>&1</verify>
  <done>Bundle purchase flow working via Stripe</done>
</task>

<task type="auto">
  <name>Task 3: Build tipping UI</name>
  <files>apps/web/src/modules/nodes/components/</files>
  <action>
    Build tip button/modal:
    - Tip button on user profiles and posts
    - Amount selector (preset amounts + custom)
    - Confirmation with platform cut disclosure
    - Success animation
    - Wire to POST /api/v1/nodes/tip
  </action>
  <verify>grep -n "TipButton\|tip" apps/web/src/modules/nodes/ -r | head -5</verify>
  <done>Tipping UI functional</done>
</task>

<task type="auto">
  <name>Task 4: Build content unlock flow</name>
  <files>apps/web/src/modules/nodes/components/</files>
  <action>
    Build content unlock for forum threads:
    - Locked content preview (shows first N chars, blurred rest)
    - "Unlock with ℕ X" button
    - Confirmation modal
    - On unlock: POST /api/v1/nodes/unlock, then reveal content
    - Wire into forum thread detail page
  </action>
  <verify>grep -n "unlock\|Unlock" apps/web/src/modules/nodes/ -r | head -5</verify>
  <done>Content unlock flow integrated with forums</done>
</task>

<task type="auto">
  <name>Task 5: Register routes and wire navigation</name>
  <files>apps/web/src/routes/app-routes.tsx apps/web/src/routes/lazyPages.ts</files>
  <action>
    - Add /wallet route → Wallet page (lazy loaded)
    - Add "Get Nodes" quick access in header/nav
    - Add wallet balance display in user menu
  </action>
  <verify>grep -n "wallet\|Wallet\|nodes\|Nodes" apps/web/src/routes/app-routes.tsx</verify>
  <done>Wallet route registered, navigation wired</done>
</task>

</tasks>

<verification>
- [ ] Wallet page displays balance and history
- [ ] Bundle purchase works via Stripe
- [ ] Tipping works between users
- [ ] Content unlock works in forums
- [ ] Routes registered
</verification>

<success_criteria>
- Complete Nodes frontend
- End-to-end flow: purchase → wallet → tip/unlock → withdrawal
</success_criteria>

<output>
After completion, create `.gsd/phases/32-nodes-monetization/32-02-SUMMARY.md`
</output>
