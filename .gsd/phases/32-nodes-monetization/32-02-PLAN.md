---
phase: 32-nodes-monetization
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - apps/web/src/modules/nodes/ (new module — store, hooks, components, services)
  - apps/web/src/pages/nodes/ (new — wallet page, shop page)
  - apps/web/src/routes/app-routes.tsx (nodes routes)
  - apps/web/src/routes/lazyPages.ts (lazy imports)
  - apps/web/src/modules/forums/components/thread-view/thread-view.tsx (wire unlock onClick)
  - apps/web/src/modules/forums/components/thread-card.tsx (unlock indicator)
  - DELETES: entire pages/premium/coin-shop/ directory, pages/premium/coin-shop.tsx,
    modules/premium/components/coin-package-card.tsx, coin-shop-widget.tsx, coin-shop-data.tsx,
    useCoinShop.ts; update modules/premium/components/index.ts to remove CoinShopWidget exports;
    delete modules/premium/components/__tests__/premiumConstants.test.tsx (imports coin-shop-data)
autonomous: true
must_haves:
  truths:
    - 'Nodes wallet page at /nodes showing available + pending balance, transaction history, withdrawal'
    - 'Nodes shop page at /nodes/shop with 5 bundle cards (Starter through Ultimate)'
    - 'Bundle purchase via Stripe Checkout (reuse existing stripe.tsx provider)'
    - 'Tip button on forum posts + user profiles with preset amounts (10, 50, 100, 500) + custom'
    - 'Content unlock: preview + blur + floating "Unlock for ℕ X" button on gated threads'
    - 'Thread-view.tsx L143 empty onClick WIRED to actual unlock flow (POST /api/v1/nodes/unlock)'
    - 'Transaction history with type filtering (purchases, tips sent/received, unlocks, withdrawals)'
    - 'Withdrawal request form (minimum 1000 nodes, shows EUR conversion at €0.80/100 nodes)'
    - 'Existing coin-shop files DELETED: entire coin-shop/ directory (11 files) + coin-shop.tsx
      entry point + 4 premium module coin components + 1 test file + useCoinShop.ts = 18 files total'
    - 'modules/premium/components/index.ts UPDATED: remove CoinShopWidget exports (L17-18)'
    - 'Existing stripe.tsx (Stripe Elements provider) KEPT and REUSED'
    - 'Existing premium-page/ KEPT (premium subscriptions are separate from Nodes)'
    - 'Existing lib/wallet/ (WalletAuth) NOT TOUCHED (crypto auth, not currency)'
    - 'Toast notification on purchase success: "ℕ X added to your wallet"'
  artifacts:
    - apps/web/src/modules/nodes/ (new module)
    - apps/web/src/pages/nodes/ (new page directory)
  key_links:
    - 'Thread gating UI: thread-view.tsx L131-147 (empty onClick at L143 — WIRE THIS)'
    - 'Thread card gating: thread-card.tsx L58 (isContentGated prop), L195 (conditional render)'
    - 'Feed post card gating: feed-post-card.tsx L73'
    - 'Stripe Elements: lib/stripe.tsx (KEEP, reuse getStripe() + theme)'
    - 'DELETE: pages/premium/coin-shop/ (entire directory), pages/premium/coin-shop.tsx'
    - 'DELETE: modules/premium/components/coin-package-card.tsx, coin-shop-widget.tsx,
      coin-shop-data.tsx, useCoinShop.ts'
    - 'UPDATE: modules/premium/components/index.ts L17-18 (remove CoinShopWidget exports)'
    - 'DELETE: modules/premium/components/__tests__/premiumConstants.test.tsx (imports coin-shop-data)'
    - 'REMOVE: lazyPages.ts L100 (CoinShop lazy import)'
    - 'REMOVE: app-routes.tsx L36 (CoinShop import) + L122 (premium/coins route)'
    - 'KEEP: pages/premium/premium-page/ (separate premium subscription feature)'
    - 'KEEP: modules/premium/store/premiumStore.ts (separate premium state)'
    - 'KEEP: lib/wallet/ (WalletAuth for crypto identity, NOT currency wallet)'
    - 'Plan 32-01 provides backend API endpoints'
---

<objective>
Build the Nodes frontend and wire Phase 31 thread gating to actual payment flow.
Delete all coin shop frontend files. Build new Nodes wallet page, shop page, tipping UI,
and content unlock experience. Reuse existing Stripe provider.

⚠️ CORRECTIONS APPLIED (10 corrections vs original plan):

1. NOT greenfield — ~15 existing frontend coin/premium files exist
2. DELETE entire coin-shop/ directory (11 files) + coin-shop.tsx entry point
3. DELETE 3 premium module coin components (coin-package-card.tsx, coin-shop-widget.tsx,
   coin-shop-data.tsx)
4. KEEP stripe.tsx (lib/stripe.tsx) — reuse getStripe() and theme configuration
5. KEEP premium-page/ — premium subscriptions are a separate feature from Nodes
6. KEEP premiumStore.ts — premium subscription state is separate
7. KEEP lib/wallet/ — WalletAuth is crypto identity auth, NOT currency wallet
8. Thread-view.tsx L143 has EMPTY onClick with comment "// Phase 32: Nodes payment flow" — MUST WIRE
9. Thread-card.tsx L195 already conditionally renders gating indicator — enhance with unlock prompt
10. Use /nodes and /nodes/shop routes (NOT /wallet to avoid WalletAuth naming conflict) </objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Part 6 — Currency Setup, Bundles)

Key existing files (from codebase audit):

- apps/web/src/pages/premium/coin-shop/ (11 files) — DELETE entire directory
- apps/web/src/pages/premium/coin-shop.tsx — DELETE entry point
- apps/web/src/modules/premium/components/coin-package-card.tsx — DELETE
- apps/web/src/modules/premium/components/coin-shop-widget.tsx — DELETE
- apps/web/src/modules/premium/components/coin-shop-data.tsx — DELETE
- apps/web/src/lib/stripe.tsx — KEEP (Stripe Elements provider)
- apps/web/src/pages/premium/premium-page/ — KEEP (premium subs)
- apps/web/src/modules/premium/store/premiumStore.ts — KEEP (premium state)
- apps/web/src/lib/wallet/ — KEEP (WalletAuth, NOT currency)
- apps/web/src/modules/forums/components/thread-view/thread-view.tsx — MODIFY (wire unlock L143)
- apps/web/src/modules/forums/components/thread-card.tsx — MODIFY (enhance gating UX) </context>

<tasks>

<task type="auto">
  <name>Task 1: Delete coin shop files</name>
  <files>
    apps/web/src/pages/premium/coin-shop/
    apps/web/src/pages/premium/coin-shop.tsx
    apps/web/src/modules/premium/components/coin-package-card.tsx
    apps/web/src/modules/premium/components/coin-shop-widget.tsx
    apps/web/src/modules/premium/components/coin-shop-data.tsx
  </files>
  <action>
    DELETE the following:
    - Entire directory: apps/web/src/pages/premium/coin-shop/ (11 files including
      coin-shop.tsx, coin-bundle-card.tsx, coin-balance-card.tsx, useCoinShop.ts,
      category-filter.tsx, shop-item-card.tsx, constants.tsx, daily-bonus-banner.tsx,
      ambient-particles.tsx, types.ts, index.ts)
    - Entry point: apps/web/src/pages/premium/coin-shop.tsx
    - Module components: coin-package-card.tsx, coin-shop-widget.tsx, coin-shop-data.tsx
    - Module hook: modules/premium/components/useCoinShop.ts
    - Test file: modules/premium/components/__tests__/premiumConstants.test.tsx (imports from coin-shop-data)

    UPDATE modules/premium/components/index.ts:
    - Remove L17: export { CoinShopWidget } from './coin-shop-widget';
    - Remove L18: export type { CoinShopWidgetProps } from './coin-shop-widget';

    Fix any imports/references that break:
    - REMOVE lazyPages.ts L100: export const CoinShop = lazyRetry(() => import('@/pages/premium/coin-shop'));
    - REMOVE app-routes.tsx L36: CoinShop import from lazyPages
    - REMOVE app-routes.tsx L122: <Route path="premium/coins" element={<CoinShop />} />
    - Grep for "coin-shop", "CoinShop", "coinShop", "CoinShopWidget" and clean up all references

    DO NOT DELETE:
    - pages/premium/premium-page/ (premium subscriptions — different feature)
    - modules/premium/store/premiumStore.ts (premium state — different feature)
    - lib/stripe.tsx (Stripe Elements provider — we REUSE this)
    - lib/wallet/ (WalletAuth — different feature entirely)

  </action>
  <verify>ls apps/web/src/pages/premium/coin-shop/ 2>&1 | head -3; echo "---"; ls apps/web/src/pages/premium/premium-page/ 2>&1 | head -3</verify>
  <done>Coin shop files deleted, premium page preserved</done>
</task>

<task type="auto">
  <name>Task 2: Build Nodes module (store, hooks, services)</name>
  <files>apps/web/src/modules/nodes/</files>
  <action>
    Create new module at apps/web/src/modules/nodes/:

    nodes/store/nodesStore.ts — Zustand store:
    - wallet: { available: number, pending: number, lifetimeEarned: number, lifetimeSpent: number } | null
    - transactions: NodeTransaction[]
    - bundles: NodeBundle[]
    - isLoading: boolean
    - Actions: fetchWallet, fetchTransactions, fetchBundles

    nodes/types/index.ts — TypeScript types:
    - NodeWallet { available_balance, pending_balance, lifetime_earned, lifetime_spent }
    - NodeTransaction { id, amount, type, reference_id, reference_type, platform_cut, net_amount, hold_until, metadata, inserted_at }
    - TransactionType = 'purchase' | 'tip_received' | 'tip_sent' | 'content_unlock' | 'subscription_received' | 'subscription_sent' | 'withdrawal' | 'cosmetic_purchase'
    - NodeBundle { id, name, nodes, price_eur, bonus_percent, popular: boolean }
    - WithdrawalRequest { id, nodes_amount, fiat_amount, currency, status, inserted_at, completed_at }

    nodes/services/nodesApi.ts — API service (use existing api client pattern):
    - getWallet() → GET /api/v1/nodes/wallet
    - getTransactions(params) → GET /api/v1/nodes/transactions
    - getBundles() → GET /api/v1/nodes/bundles
    - createCheckout(bundleId) → POST /api/v1/nodes/checkout
    - sendTip(recipientId, amount) → POST /api/v1/nodes/tip
    - unlockContent(threadId) → POST /api/v1/nodes/unlock
    - requestWithdrawal(nodesAmount) → POST /api/v1/nodes/withdraw

    nodes/hooks/useNodes.ts — TanStack Query hooks:
    - useNodeWallet() — query for wallet data
    - useNodeTransactions(type?) — query with type filter
    - useNodeBundles() — query for bundles
    - useSendTip() — mutation
    - useUnlockContent() — mutation (invalidates thread query on success)
    - useCreateCheckout() — mutation (redirects to Stripe Checkout URL)
    - useRequestWithdrawal() — mutation

    nodes/index.ts — barrel export

  </action>
  <verify>ls apps/web/src/modules/nodes/ -R 2>&1</verify>
  <done>Nodes module with store, hooks, services, types</done>
</task>

<task type="auto">
  <name>Task 3: Build Nodes wallet + shop pages</name>
  <files>apps/web/src/pages/nodes/</files>
  <action>
    Create pages at apps/web/src/pages/nodes/:

    nodes/nodes-wallet.tsx — Wallet page (/nodes):
    - Balance card: large "ℕ {available}" display + "Pending: ℕ {pending}" subtitle
    - "Get Nodes" button → navigates to /nodes/shop
    - Transaction history list with type filter tabs:
      All | Purchases | Tips | Unlocks | Withdrawals
    - Each transaction row: icon + description + amount (green/red) + date
    - Withdrawal section (shown if pending_balance > 0 or lifetime_earned > 0):
      - Available for withdrawal: ℕ {available}
      - Conversion preview: "ℕ 1000 = €8.00"
      - "Request Withdrawal" button → confirmation modal
      - Minimum: 1000 nodes
    - Lifetime stats footer: "Earned: ℕ X | Spent: ℕ X"

    nodes/nodes-shop.tsx — Shop page (/nodes/shop):
    - 5 bundle cards in a responsive grid:
      | Starter | 500 nodes | €4.99 | |
      | Popular | 1,200 nodes | €9.99 | +20% 🔥 | ← highlighted/recommended
      | Creator | 2,800 nodes | €19.99 | +40% |
      | Pro | 6,500 nodes | €39.99 | +63% |
      | Ultimate | 17,000 nodes | €99.99 | +70% |
    - Each card: name, node amount, price, bonus badge (if applicable)
    - Popular bundle gets a "Most Popular" badge or border highlight
    - "Buy" button → createCheckout mutation → redirects to Stripe Checkout
    - Current balance shown at top: "Your balance: ℕ {available}"
    - Back link to /nodes (wallet)

    nodes/components/bundle-card.tsx — Individual bundle card component
    nodes/components/transaction-row.tsx — Individual transaction row
    nodes/components/withdrawal-modal.tsx — Withdrawal confirmation modal

    Use cn() for className merging (clsx + tailwind-merge).
    Use lazyRetry() for code splitting.

  </action>
  <verify>ls apps/web/src/pages/nodes/ 2>&1</verify>
  <done>Wallet and shop pages built</done>
</task>

<task type="auto">
  <name>Task 4: Build tipping UI + wire content unlock</name>
  <files>
    apps/web/src/modules/nodes/components/
    apps/web/src/modules/forums/components/thread-view/thread-view.tsx
    apps/web/src/modules/forums/components/thread-card.tsx
  </files>
  <action>
    CREATE apps/web/src/modules/nodes/components/tip-button.tsx:
    - Small "Tip" button with ℕ icon
    - On click: opens tip modal
    - Preset amounts: 10, 50, 100, 500
    - Custom amount input
    - Shows platform cut disclosure: "Creator receives 80%"
    - Confirmation step before sending
    - Success toast: "Tipped ℕ X to @username"
    - Uses useSendTip() mutation

    CREATE apps/web/src/modules/nodes/components/tip-modal.tsx:
    - Amount selector (presets + custom)
    - Recipient info (avatar + username)
    - Balance check (disable if insufficient)
    - "Send Tip" button with loading state

    INTEGRATE tip button:
    - Import TipButton into forum thread-view for post actions area
    - Import TipButton into user profile page actions area
    - Do NOT add to DMs or group messages (forums + profiles only per user decision)

    WIRE content unlock in thread-view.tsx (L131-147):
    - Replace the empty onClick at L143 (`// Phase 32: Nodes payment flow`) with:
      1. Import useUnlockContent from nodes hooks
      2. On click: show confirmation modal ("Unlock this thread for ℕ {gate_price_nodes}?")
      3. On confirm: call unlock mutation (POST /api/v1/nodes/unlock with thread_id)
      4. On success: refetch thread data to reveal full content, show success toast
      5. On error (insufficient balance): show "Not enough Nodes" with link to /nodes/shop

    ENHANCE content gate UX (preview + blur per user decision):
    - Show first gate_preview_chars characters readable
    - Apply CSS blur effect (backdrop-filter: blur(8px)) to remaining content
    - Float "Unlock for ℕ {gate_price_nodes}" button over the blurred section
    - If user has already unlocked (check via is_unlocked field in thread response), show full content

    MODIFY thread-card.tsx L195:
    - If isContentGated, show small lock icon + "ℕ {gatePriceNodes}" badge on the card
    - Clicking leads to thread detail where unlock flow is available

  </action>
  <verify>grep -n "unlock\|Unlock\|tip\|Tip" apps/web/src/modules/nodes/components/ -r 2>&1 | head -10</verify>
  <done>Tipping UI on posts + profiles, content unlock wired in thread-view</done>
</task>

<task type="auto">
  <name>Task 5: Register routes + wire navigation</name>
  <files>
    apps/web/src/routes/app-routes.tsx
    apps/web/src/routes/lazyPages.ts
  </files>
  <action>
    MODIFY apps/web/src/routes/lazyPages.ts:
    - Add lazy imports for NodesWallet and NodesShop using lazyRetry():
      export const NodesWalletPage = lazyRetry(() => import('@/pages/nodes/nodes-wallet'))
      export const NodesShopPage = lazyRetry(() => import('@/pages/nodes/nodes-shop'))

    MODIFY apps/web/src/routes/app-routes.tsx:
    - Add routes:
      /nodes → NodesWalletPage (authenticated)
      /nodes/shop → NodesShopPage (authenticated)
    - Remove any existing coin-shop routes if present

    WIRE navigation:
    - Add "Nodes" or "ℕ" balance display in the user menu / sidebar (wherever coin balance was shown)
    - Add "Get Nodes" quick action in header area (if coin shop link existed, replace it)
    - Remove any navigation links to old coin shop
    - Grep for references to old coin-shop routes and update

  </action>
  <verify>grep -n "nodes\|Nodes\|coin-shop\|coinShop" apps/web/src/routes/app-routes.tsx 2>&1; echo "---"; grep -n "nodes\|Nodes" apps/web/src/routes/lazyPages.ts 2>&1</verify>
  <done>Nodes routes registered, navigation wired, old coin routes removed</done>
</task>

</tasks>

<verification>
- [ ] Coin shop files deleted (11 + 1 + 4 + 1 + 1 = 18 files)
- [ ] modules/premium/components/index.ts updated (CoinShopWidget exports removed)
- [ ] lazyPages.ts CoinShop import removed (L100)
- [ ] app-routes.tsx CoinShop import + route removed (L36, L122)
- [ ] Premium page preserved (premium-page/ intact)
- [ ] Stripe provider preserved (lib/stripe.tsx intact)
- [ ] WalletAuth preserved (lib/wallet/ intact)
- [ ] Wallet page shows balance + transactions + withdrawal
- [ ] Shop page shows 5 bundles with correct pricing
- [ ] Bundle purchase creates Stripe Checkout session
- [ ] Tip button on forum posts + user profiles
- [ ] Tip presets: 10, 50, 100, 500 + custom
- [ ] Content unlock: preview + blur + floating unlock button
- [ ] thread-view.tsx onClick wired (no more empty handler)
- [ ] Routes registered (/nodes, /nodes/shop)
- [ ] TypeScript compiles cleanly
</verification>

<success_criteria>

- Complete Nodes frontend with coin shop replaced
- End-to-end flow: shop → purchase → wallet → tip/unlock → withdrawal
- Thread gating from Phase 31 fully wired to payment flow </success_criteria>

<output>
After completion, create `.gsd/phases/32-nodes-monetization/32-02-SUMMARY.md`
</output>
