---
phase: 32-nodes-monetization
plan: 01
type: execute
wave: 1
depends_on:
  - 31-01
files_modified:
  - apps/backend/lib/cgraph/nodes/ (new context)
  - apps/backend/lib/cgraph_web/controllers/nodes_controller.ex (new)
  - apps/backend/priv/repo/migrations/ (nodes tables)
autonomous: true
must_haves:
  truths:
    - 'node_wallets, node_transactions, withdrawal_requests tables created'
    - 'Node wallet system with available + pending balances'
    - 'Transaction types: purchase, tip, content_unlock, subscription, withdrawal'
    - '20% platform cut on earned transactions'
    - '21-day hold on earned nodes before withdrawal'
    - 'Withdrawal request system (pending → processing → completed)'
    - 'API endpoints for wallet, transactions, tipping, content unlock'
  artifacts:
    - apps/backend/lib/cgraph/nodes/ (new context)
    - apps/backend/lib/cgraph_web/controllers/nodes_controller.ex (new)
  key_links:
    - 'Depends on Phase 31 (Forums need content unlock via Nodes)'
    - 'Plan 32-02 builds frontend wallet UI'
    - 'Stripe already integrated (234 file refs) — Paddle migration is separate epic'
---

<objective>
Build the Nodes virtual currency backend from scratch. 100% greenfield.
Create wallet system, transaction processing, tipping, content unlock,
and withdrawal requests. Use existing Stripe integration for now —
Paddle migration is a separate multi-sprint epic as noted in the corrections.

NOTE: Do NOT migrate Stripe→Paddle in this phase. Focus on Node wallet logic only.
</objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Part 6 — Nodes Monetization)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Nodes database tables</name>
  <files>apps/backend/priv/repo/migrations/</files>
  <action>
    Create migration for:
    - node_wallets (user_id PK, available_balance, pending_balance,
      lifetime_earned, lifetime_spent, stripe_account_id, updated_at)
    - node_transactions (id, user_id, amount, type, reference_id, reference_type,
      platform_cut, net_amount, hold_until, metadata JSONB, created_at)
    - withdrawal_requests (id, user_id, nodes_amount, currency, fiat_amount,
      status, payoneer_payout_id, created_at, completed_at)
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>Nodes tables created</done>
</task>

<task type="auto">
  <name>Task 2: Build Nodes context</name>
  <files>apps/backend/lib/cgraph/nodes/</files>
  <action>
    Build Elixir context with:
    - Wallet operations: credit, debit, get_balance, transfer
    - Transaction logging with all types
    - Platform cut calculation (20% on earned transactions)
    - Hold management (21-day hold on earned nodes)
    - Tipping: user A tips user B, platform takes 20%, B gets 80% in pending
    - Content unlock: debit N nodes, unlock content_id
    - Subscription payment: recurring node deduction
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>Nodes context with all operations</done>
</task>

<task type="auto">
  <name>Task 3: Build withdrawal system</name>
  <files>apps/backend/lib/cgraph/nodes/</files>
  <action>
    Implement withdrawal requests:
    - Create withdrawal request (validates available balance minus holds)
    - Fiat conversion: nodes * 0.008 = EUR per node (€0.80 per 100 nodes)
    - Status progression: pending → processing → completed/failed
    - Oban worker to process withdrawals
    - Minimum withdrawal threshold
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>Withdrawal system functional</done>
</task>

<task type="auto">
  <name>Task 4: Create Nodes API endpoints</name>
  <files>apps/backend/lib/cgraph_web/controllers/nodes_controller.ex</files>
  <action>
    Create endpoints:
    - GET /api/v1/nodes/wallet — get user's wallet
    - POST /api/v1/nodes/credit — credit nodes (webhook from purchase)
    - POST /api/v1/nodes/tip — send tip to another user
    - POST /api/v1/nodes/unlock — unlock gated content
    - GET /api/v1/nodes/transactions — transaction history
    - POST /api/v1/nodes/withdraw — request withdrawal
    - GET /api/v1/nodes/bundles — list purchasable node bundles

    Add routes to router.
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
  <done>Nodes API endpoints functional</done>
</task>

</tasks>

<verification>
- [ ] Database tables created
- [ ] Wallet operations work
- [ ] Platform cut calculated
- [ ] 21-day hold enforced
- [ ] Withdrawal requests process
- [ ] API endpoints functional
- [ ] Backend compiles cleanly
</verification>

<success_criteria>
- Complete Nodes backend
</success_criteria>

<output>
After completion, create `.gsd/phases/32-nodes-monetization/32-01-SUMMARY.md`
</output>
