---
phase: 35-cosmetics-unlock-engine
plan: 07
type: execute
wave: 3
depends_on:
  - 35-03
  - 35-04
  - 35-05
  - 35-06
files_modified:
  - packages/shared-types/src/cosmetics.ts (update — CosmeticType union already has: border, title,
    badge, nameplate, profile_effect, chat_bubble, emoji_pack, sound_pack, theme. ADD: profile_frame,
    name_style. ADD new interfaces: Badge, Nameplate, ProfileEffect, ProfileFrame, NameStyle, InventoryItem)
  - packages/api-client/src/cosmetics.ts (NEW — ⚠️ this file does NOT exist yet. Must CREATE it.
    The api-client package currently has: client.ts, endpoints.ts, index.ts, nodes.ts, resilience.ts)
  - apps/backend/priv/repo/migrations/<ts>_add_cosmetics_join_tables.exs (new)
  - apps/backend/lib/cgraph/cosmetics/cosmetics.ex (⚠️ CREATE — this file does NOT exist.
    Only rarity.ex exists in the cosmetics/ directory. See Plan 35-01 and 35-03 which also create this.)
autonomous: true
must_haves:
  truths:
    - 'shared-types cosmetics.ts exports: Badge, Nameplate, ProfileEffect, ProfileFrame, NameStyle, InventoryItem, CosmeticType union'
    - 'api-client cosmetics.ts exports: getInventory, equipItem, unequipItem, getBadges, getNameplates, getUserBadges, etc.'
    - 'Join table migration for user↔cosmetic relationships not covered by inventory table'
    - 'Types match backend JSON:API response shapes exactly'
    - 'Barrel re-exports from package index'
  artifacts: []
  key_links:
    - 'Existing shared-types: packages/shared-types/src/cosmetics.ts — already has CosmeticItem,
      CosmeticType (border|title|badge|nameplate|profile_effect|chat_bubble|emoji_pack|sound_pack|theme),
      UserCosmeticInventory, CosmeticSurface, UnlockCondition, CosmeticVisibilityRule.
      The CosmeticType union is MISSING: profile_frame, name_style. These must be added.'
    - '⚠️ api-client: packages/api-client/src/ has NO cosmetics.ts file. It must be CREATED.
      Existing files: client.ts, endpoints.ts, index.ts, nodes.ts, resilience.ts.
      New cosmetics.ts must import the client from client.ts and add cosmetics API methods.'
    - 'Barrel re-export: update packages/api-client/src/index.ts to export from cosmetics.ts'
    - 'Barrel re-export: update packages/shared-types/src/index.ts to export from cosmetics.ts
      (verify if already re-exported)'
---

<objective>
Complete Phase 35: extend shared types + API client for all new cosmetic types, create any needed
join table migrations, and ensure end-to-end type safety from backend to frontend.

ATOMIC_PLAN tasks: 2.34 (Cosmetics API Client Extension), 2.35 (Join Table Migration). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared cosmetics types (2.34 partial)</name>
  <files>packages/shared-types/src/cosmetics.ts</files>
  <action>Add types: Badge (id, name, description, iconUrl, rarity, category, unlockCondition), Nameplate (id, name, backgroundUrl, textColor, borderStyle, rarity, animated), ProfileEffect (id, name, type, config, rarity, previewUrl), ProfileFrame (id, name, frameUrl, animated, rarity), NameStyle (id, name, fontFamily, colorScheme, animation, rarity), InventoryItem (id, userId, itemType: CosmeticType, itemId, equippedAt, obtainedAt, obtainedVia), CosmeticType union type. Export from barrel.</action>
</task>

<task type="auto">
  <name>Task 2: CREATE API client cosmetics module (2.34)</name>
  <files>packages/api-client/src/cosmetics.ts</files>
  <action>CREATE NEW FILE (this does not exist). Import client from ./client.ts. Add typed methods: getInventory(userId?), equipItem(itemType, itemId), unequipItem(itemType, itemId), getBadges(params?), getUserBadges(userId), getNameplates(params?), getUserNameplates(userId), getProfileEffects, getProfileFrames, getNameStyles. All with proper types from @cgraph/shared-types. Export from barrel (update index.ts).</action>
</task>

<task type="auto">
  <name>Task 3: Join table migration (2.35)</name>
  <files>apps/backend/priv/repo/migrations/</files>
  <action>Create cosmetics_unlock_log table (tracks unlock events): user_id, item_type, item_id, unlocked_at, via (evaluator type). Index on user_id + item_type. This supplements the inventory table by providing unlock history.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
