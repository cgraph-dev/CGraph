---
phase: 35-cosmetics-unlock-engine
plan: 02
type: execute
wave: 1
depends_on:
  - 34-06
files_modified:
  - apps/backend/lib/cgraph/cosmetics/inventory.ex (new)
  - apps/backend/lib/cgraph/cosmetics/nameplate_setting.ex (new)
  - apps/backend/lib/cgraph/cosmetics/name_style.ex (new)
  - apps/backend/priv/repo/migrations/<ts>_create_user_inventory.exs (new)
  - apps/backend/priv/repo/migrations/<ts>_create_nameplate_settings.exs (new)
  - apps/backend/priv/repo/migrations/<ts>_create_name_styles.exs (new)
autonomous: true
must_haves:
  truths:
    - 'Unified inventory: user_id (binary_id) + item_type (string, validated) + item_id
      (binary_id/UUID, NOT integer) + equipped_at + obtained_at + obtained_via'
    - 'item_type validated values: border, title, badge, nameplate, profile_effect, profile_frame,
      chat_effect, profile_theme, name_style — use :string + validate_inclusion (NOT Ecto.Enum) to
      match existing pattern'
    - 'Unique index on [user_id, item_type, item_id] — prevents duplicates'
    - 'NameplateSetting: user_id, nameplate_id, custom_text_color, custom_border_color, layout'
    - 'NameStyle: slug (unique), name, font_family, color_scheme (JSONB), animation, rarity
      (:string), previewable, sort_order'
    - 'All schemas use @primary_key {:id, :binary_id, autogenerate: true}, @foreign_key_type
      :binary_id'
    - 'All schemas use @timestamps_opts [type: :utc_datetime_usec]'
    - 'Rarity validated via Rarity.string_values() — same pattern as avatar_border.ex'
    - '⚠️ item_id is binary_id (UUID) because all cosmetic tables use binary_id PKs'
  artifacts:
    - apps/backend/lib/cgraph/cosmetics/inventory.ex
    - apps/backend/lib/cgraph/cosmetics/nameplate_setting.ex
    - apps/backend/lib/cgraph/cosmetics/name_style.ex
  key_links:
    - 'Existing user cosmetics join tables (4 tables to eventually replace): - user_avatar_borders
      (CGraph.Gamification.UserAvatarBorder) — has is_equipped, unlock_source, expires_at,
      custom_colors - user_titles (CGraph.Gamification.UserTitle) — has unlocked_at -
      user_chat_effects (CGraph.Gamification.UserChatEffect) — has is_active, unlock_source,
      expires_at, custom_config - user_profile_themes (CGraph.Gamification.UserProfileTheme) — has
      is_active, unlock_source, expires_at, custom overrides Migration path: unified inventory
      supplements these (dual-read), drop old tables in Phase 37'
    - 'Rarity module: apps/backend/lib/cgraph/cosmetics/rarity.ex — only file in cosmetics/ dir'
    - '⚠️ All existing schemas use :binary_id (UUID) for PKs and FKs — inventory.item_id must also
      be :binary_id'
---

<objective>
Create unified cosmetics inventory (polymorphic), nameplate customization settings, and name style
schema with migrations.

ATOMIC_PLAN tasks: 2.5 (Unified Inventory), 2.6 (Nameplate Settings), 2.7 (Name Style Schema).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Unified inventory schema (2.5)</name>
  <files>
    apps/backend/lib/cgraph/cosmetics/inventory.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>Polymorphic inventory: user_id (references users, type: :binary_id), item_type (:string, validated with @valid_item_types), item_id (:binary_id — UUID, NOT integer), equipped_at (utc_datetime_usec, nullable), obtained_at (utc_datetime_usec), obtained_via (string — purchase, unlock, reward, gift, default, achievement, event, level). Use @primary_key {:id, :binary_id, autogenerate: true}. Unique composite index on [user_id, item_type, item_id]. Migration with proper foreign key on user_id.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 2: Nameplate settings + name style (2.6, 2.7)</name>
  <files>
    apps/backend/lib/cgraph/cosmetics/nameplate_setting.ex
    apps/backend/lib/cgraph/cosmetics/name_style.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>
    NameplateSetting: belongs_to user + nameplate (both :binary_id FKs). Custom overrides for text_color, border_color, layout (string). Use binary_id PKs.
    NameStyle: slug (unique), name, font_family, color_scheme (JSONB — gradient stops, primary, secondary), animation (string), rarity (:string + Rarity.string_values()), previewable (boolean), sort_order (integer), is_active (boolean). NOTE: manifest has 50 name_styles (not 25 as in context doc).
    Both with migrations, binary_id PKs, utc_datetime_usec timestamps.
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
