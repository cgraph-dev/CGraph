---
phase: 35-cosmetics-unlock-engine
plan: 01
type: execute
wave: 1
depends_on:
  - 34-06
files_modified:
  - apps/backend/lib/cgraph/cosmetics/badge.ex (new)
  - apps/backend/lib/cgraph/cosmetics/nameplate.ex (new)
  - apps/backend/lib/cgraph/cosmetics/profile_effect.ex (new)
  - apps/backend/lib/cgraph/cosmetics/profile_frame.ex (new)
  - apps/backend/priv/repo/migrations/<ts>_create_badges_table.exs (new)
  - apps/backend/priv/repo/migrations/<ts>_create_nameplates_table.exs (new)
  - apps/backend/priv/repo/migrations/<ts>_create_profile_effects_table.exs (new)
  - apps/backend/priv/repo/migrations/<ts>_create_profile_frames_table.exs (new)
autonomous: true
must_haves:
  truths:
    - 'Badge schema: id, name, description, icon_url, rarity (7-tier), category, unlock_condition
      (JSONB), created_at'
    - 'Nameplate schema: id, name, background_url, text_color, border_style, rarity,
      unlock_condition, animated (bool)'
    - 'ProfileEffect schema: id, name, type (particle|aura|trail), config (JSONB), rarity,
      preview_url — NOTE: profile_effects is a NEW cosmetic type not in COSMETICS_MANIFEST.json
      (manifest has profile_themes, not profile_effects)'
    - 'ProfileFrame schema: id, name, frame_url, animated (bool), rarity, unlock_condition — NOTE:
      manifest has 55 profile_frames'
    - 'All schemas use 7-tier rarity from CGraph.Cosmetics.Rarity (rarity.ex is the ONLY file
      currently in cgraph/cosmetics/)'
    - 'unlock_condition JSONB stores criteria: {type, threshold, metadata}'
    - 'All tables have proper indexes on rarity + category columns'
    - 'All schemas use @primary_key {:id, :binary_id, autogenerate: true} and @foreign_key_type
      :binary_id (CGraph UUID convention)'
    - 'All schemas use @timestamps_opts [type: :utc_datetime_usec] (CGraph microsecond convention)'
  artifacts:
    - apps/backend/lib/cgraph/cosmetics/badge.ex
    - apps/backend/lib/cgraph/cosmetics/nameplate.ex
    - apps/backend/lib/cgraph/cosmetics/profile_effect.ex
    - apps/backend/lib/cgraph/cosmetics/profile_frame.ex
  key_links:
    - 'Rarity module: apps/backend/lib/cgraph/cosmetics/rarity.ex — 7-tier (Phase 33)'
    - 'Existing border schema: apps/backend/lib/cgraph/gamification/avatar_border.ex — uses :string
      rarity + Rarity.string_values() validation pattern. Has slug, name, description, theme,
      rarity, border_style (map), animation_type, colors (array), unlock_type, unlock_requirement
      (map), coin_cost, gem_cost, season_id, available_from/until, sort_order, is_active,
      is_tradeable, preview_url. Reference this pattern for ALL new schemas.'
    - 'Existing title schema: apps/backend/lib/cgraph/gamification/title.ex — uses :string rarity +
      Rarity.string_values() validation. Has slug, name, description, color, rarity, unlock_type,
      unlock_requirement (:string NOT :map), coin_cost, sort_order. Titles live under gamification/.'
    - '⚠️ cgraph/cosmetics/ currently contains ONLY rarity.ex. All NEW schemas (badge, nameplate,
      profile_effect, profile_frame) go here. Existing schemas (avatar_border, title, chat_effect,
      profile_theme) remain under cgraph/gamification/ — do NOT move them.'
    - '⚠️ Rarity field should be :string type with validate_inclusion(:rarity,
      Rarity.string_values()) to match existing avatar_border.ex and title.ex pattern — NOT
      Ecto.Enum'
---

<objective>
Create 4 new cosmetic schemas (Badge, Nameplate, ProfileEffect, ProfileFrame) with migrations and
proper rarity integration. These schemas extend the existing cosmetics system.

ATOMIC_PLAN tasks: 2.1 (Badge Schema), 2.2 (Nameplate Schema), 2.3 (Profile Effect Schema), 2.4
(Profile Frame Schema). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Badge schema + migration (2.1)</name>
  <files>
    apps/backend/lib/cgraph/cosmetics/badge.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>Ecto schema with fields: slug (string, unique), name, description, icon_url, rarity (:string + validate_inclusion with Rarity.string_values()), category (string), track (string), unlock_type (string), unlock_condition (map/JSONB), nodes_cost (integer), stackable (boolean). Use @primary_key {:id, :binary_id, autogenerate: true}, @foreign_key_type :binary_id, @timestamps_opts [type: :utc_datetime_usec]. Migration: create_if_not_exists :badges, add indexes on [:rarity, :category], unique index on :slug. Follow avatar_border.ex pattern exactly.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 2: Nameplate schema + migration (2.2)</name>
  <files>
    apps/backend/lib/cgraph/cosmetics/nameplate.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>Schema: slug (unique), name, background_url, text_color (string), border_style (string), rarity (:string + Rarity.string_values()), unlock_type (string), unlock_condition (map), animated (boolean), sort_order (integer), is_active (boolean). Use binary_id PKs and utc_datetime_usec timestamps. Create migration with rarity index + unique slug index.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 3: Profile effect + frame schemas (2.3, 2.4)</name>
  <files>
    apps/backend/lib/cgraph/cosmetics/profile_effect.ex
    apps/backend/lib/cgraph/cosmetics/profile_frame.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>
    ProfileEffect: slug (unique), name, type (:string with validate_inclusion ~w(particle aura trail)), config (map/JSONB), rarity (:string + Rarity.string_values()), preview_url, sort_order, is_active. Use binary_id PKs, utc_datetime_usec timestamps.
    ProfileFrame: slug (unique), name, frame_url, animated (boolean), rarity (:string + Rarity.string_values()), unlock_type, unlock_condition (map), sort_order, is_active. NOTE: manifest has 55 frames.
    Both with migrations + rarity indexes + unique slug indexes. Use :string for type field (NOT Ecto.Enum) to match existing patterns.
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
