---
phase: 35-cosmetics-unlock-engine
plan: 03
type: execute
wave: 2
depends_on:
  - 35-01
  - 35-02
files_modified:
  - apps/backend/lib/cgraph/cosmetics/cosmetics.ex (update — extend context module; only rarity.ex
    currently exists in cosmetics/)
  - apps/backend/lib/cgraph_web/controllers/cosmetics_controller.ex (update — extend existing; has
    borders/themes/effects actions)
  - apps/backend/lib/cgraph_web/controllers/api/v1/badge_controller.ex (new — under api/v1/ matching
    existing controller_test patterns)
  - apps/backend/lib/cgraph_web/controllers/api/v1/nameplate_controller.ex (new — under api/v1/)
  - apps/backend/lib/cgraph_web/router/cosmetics_routes.ex (new — macro-based route module)
  - apps/backend/lib/cgraph_web/router.ex (update — MUST add import + macro call for
    cosmetics_routes)
autonomous: true
must_haves:
  truths:
    - 'Cosmetics context module CREATED (not extended — only rarity.ex exists in cosmetics/ dir
      currently): list_user_inventory/2, equip_item/3, unequip_item/3, grant_item/4'
    - 'grant_item checks rarity_check against manifest counts'
    - 'equip_item validates item ownership before equipping'
    - 'CosmeticsController extended with inventory endpoints: GET /inventory, PUT /equip, DELETE
      /unequip'
    - 'BadgeController under api/v1/: index, show, user_badges (GET /users/:id/badges)'
    - 'NameplateController under api/v1/: index, show, user_nameplates, update_settings'
    - 'All controllers use render_data/render_error from CGraphWeb.ControllerHelpers'
    - 'Routes registered via CGraphWeb.Router.CosmeticsRoutes macro module'
    - '⚠️ router.ex MUST be updated: add `import CGraphWeb.Router.CosmeticsRoutes` and call
      `cosmetics_routes()` after gamification_routes(). This is required — macro modules are
      imported, not auto-discovered.'
  artifacts:
    - apps/backend/lib/cgraph_web/controllers/api/v1/badge_controller.ex
    - apps/backend/lib/cgraph_web/controllers/api/v1/nameplate_controller.ex
  key_links:
    - 'Cosmetics context: apps/backend/lib/cgraph/cosmetics/ — currently contains ONLY rarity.ex.
      Create cosmetics.ex as a NEW context module here (do NOT look for an existing one to extend).'
    - 'CosmeticsController: apps/backend/lib/cgraph_web/controllers/cosmetics_controller.ex —
      EXISTING file with border/theme/effect actions + serializers subdirectory. Extend this file
      with inventory endpoints.'
    - 'Router pattern: See apps/backend/lib/cgraph_web/router/gamification_routes.ex for the exact
      macro pattern. Create CGraphWeb.Router.CosmeticsRoutes following the same pattern. Then add
      `import CGraphWeb.Router.CosmeticsRoutes` at the top of router.ex (line ~40) and call
      `cosmetics_routes()` after `gamification_routes()` (line ~135).'
    - '⚠️ Existing gamification_routes.ex already has cosmetics routes for borders/themes/effects.
      The NEW cosmetics_routes.ex should handle ONLY new endpoints: inventory, badges, nameplates,
      profile_effects, profile_frames, name_styles. Do NOT duplicate existing border/theme/effect
      routes.'
    - '⚠️ Controllers under api/v1/ use `scope "/api/v1", CGraphWeb` pattern in the route macro —
      the module reference is just the controller name without namespace prefix.'
---

<objective>
Extend the Cosmetics context module with inventory operations, extend CosmeticsController with
inventory endpoints, create dedicated Badge and Nameplate controllers with routes.

ATOMIC_PLAN tasks: 2.8 (Extend Context), 2.9 (Extend CosmeticsController), 2.10 (Badge Controller),
2.11 (Nameplate Controller). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Create cosmetics context module (2.8)</name>
  <files>apps/backend/lib/cgraph/cosmetics/cosmetics.ex</files>
  <action>CREATE new context module CGraph.Cosmetics (file does not exist yet — only rarity.ex is in this directory). Add functions: list_user_inventory(user_id, opts \\ []), equip_item(user_id, item_type, item_id), unequip_item(user_id, item_type, item_id), grant_item(user_id, item_type, item_id, obtained_via). Remove duplicates in inventory. Validate ownership before equip. PubSub broadcast on equip/unequip. Import Ecto.Query. Follow gamification.ex context pattern.</action>
</task>

<task type="auto">
  <name>Task 2: Extend CosmeticsController + inventory endpoints (2.9)</name>
  <files>apps/backend/lib/cgraph_web/controllers/cosmetics_controller.ex</files>
  <action>Add actions: inventory (GET, returns all user items), equip (PUT), unequip (DELETE). Follow existing controller pattern. Use render_data/render_error from CGraphWeb.ControllerHelpers for JSON responses (NOT raw json/2).</action>
</task>

<task type="auto">
  <name>Task 3: Badge + Nameplate controllers + route module (2.10, 2.11)</name>
  <files>
    apps/backend/lib/cgraph_web/controllers/api/v1/badge_controller.ex
    apps/backend/lib/cgraph_web/controllers/api/v1/nameplate_controller.ex
    apps/backend/lib/cgraph_web/router/cosmetics_routes.ex
    apps/backend/lib/cgraph_web/router.ex
  </files>
  <action>BadgeController (under api/v1/): index (list all), show (by id), user_badges (GET /users/:id/badges). NameplateController (under api/v1/): index, show, user_nameplates, update_settings (PUT). Create CGraphWeb.Router.CosmeticsRoutes macro module following GamificationRoutes pattern. Then update router.ex: add `import CGraphWeb.Router.CosmeticsRoutes` after line 40 and call `cosmetics_routes()` after `gamification_routes()`. Do NOT duplicate existing border/theme/effect routes from gamification_routes.ex.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
