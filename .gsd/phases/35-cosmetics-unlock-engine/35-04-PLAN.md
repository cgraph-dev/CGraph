---
phase: 35-cosmetics-unlock-engine
plan: 04
type: execute
wave: 2
depends_on:
  - 35-01
  - 35-02
files_modified:
  - apps/backend/lib/cgraph/cosmetics/unlock_engine.ex (new — core engine)
  - apps/backend/lib/cgraph/cosmetics/unlock_evaluator.ex (new — behaviour)
  - apps/backend/lib/cgraph/cosmetics/evaluators/level_evaluator.ex (new)
  - apps/backend/lib/cgraph/cosmetics/evaluators/achievement_evaluator.ex (new)
  - apps/backend/lib/cgraph/cosmetics/evaluators/collection_evaluator.ex (new)
  - apps/backend/lib/cgraph/cosmetics/evaluators/event_evaluator.ex (new)
  - apps/backend/lib/cgraph/cosmetics/evaluators/purchase_evaluator.ex (new)
  - apps/backend/lib/cgraph/workers/unlock_check_worker.ex (new)
  - apps/backend/lib/cgraph/workers/seasonal_rotation_worker.ex (new)
  - apps/backend/lib/cgraph/cosmetics/visibility_rules.ex (new)
autonomous: true
must_haves:
  truths:
    - 'UnlockEngine dispatches to registered evaluators by unlock_condition.type'
    - 'UnlockEvaluator behaviour: @callback evaluate(user, condition) :: {:ok, boolean} | {:error,
      term}'
    - '5 evaluators: level (reputation-based), achievement, collection, event (time-limited),
      purchase (Nodes)'
    - 'UnlockCheckWorker (Oban, :unlocks queue — already configured in config.exs with 10 workers)
      triggered on achievement earned'
    - 'SeasonalRotationWorker (Oban, :cosmetics queue — already configured in config.exs with 10
      workers) monthly cron rotates limited items'
    - 'VisibilityRules: items with rarity >= legendary show preview but greyed out when locked'
    - 'All evaluators are pure functions — no side effects, easy to test'
    - 'Engine logs unlock events to telemetry [:cgraph, :cosmetics, :unlocked]'
    - '⚠️ LevelEvaluator uses reputation_score or post_count (NOT user.level — XP tables were
      dropped in migration 20260723120000). Check users table for reputation_score field or compute
      from forum post counts.'
  artifacts:
    - apps/backend/lib/cgraph/cosmetics/unlock_engine.ex
    - apps/backend/lib/cgraph/cosmetics/unlock_evaluator.ex
    - apps/backend/lib/cgraph/cosmetics/evaluators/ (5 evaluator files)
    - apps/backend/lib/cgraph/workers/unlock_check_worker.ex
    - apps/backend/lib/cgraph/workers/seasonal_rotation_worker.ex
    - apps/backend/lib/cgraph/cosmetics/visibility_rules.ex
  key_links:
    - 'Oban queues: :cosmetics (10 workers) and :unlocks (10 workers) — both already in config.exs.
      Use :unlocks for UnlockCheckWorker, :cosmetics for SeasonalRotationWorker.'
    - 'Rarity module: apps/backend/lib/cgraph/cosmetics/rarity.ex — use Rarity.rank/1 for tier
      ordering in visibility'
    - 'Telemetry: follow existing patterns in apps/backend/lib/cgraph_web/telemetry.ex'
    - '⚠️ XP/level system was DROPPED (migration 20260723120000_drop_gamification_tables.exs).
      LevelEvaluator MUST use alternative metrics: Option A: User.reputation_score field (check if
      exists in users table) Option B: COUNT of user posts/messages as proxy Option C: Achievement
      count as proxy leveling Verify which fields exist on the User schema before implementing.'
    - 'Achievement triggers: apps/backend/lib/cgraph/gamification/achievement_triggers.ex — has
      @action_achievement_map with action-to-achievement slug mappings. UnlockCheckWorker should
      subscribe to same PubSub events.'
---

<objective>
Build the Unlock Engine: core dispatcher, evaluator behaviour with 5 implementations, Oban workers
for unlock checking and seasonal rotation, and visibility rules for locked/unlocked states.

ATOMIC_PLAN tasks: 2.12 (Unlock Engine Core), 2.13 (Level Evaluator), 2.14 (Achievement Evaluator),
2.15 (Collection Evaluator), 2.16 (Event/Seasonal Evaluator), 2.17 (Purchase Evaluator), 2.18
(Unlock Check Worker), 2.19 (Seasonal Rotation Worker), 2.20 (Visibility Rules). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Unlock engine core + behaviour (2.12)</name>
  <files>
    apps/backend/lib/cgraph/cosmetics/unlock_engine.ex
    apps/backend/lib/cgraph/cosmetics/unlock_evaluator.ex
  </files>
  <action>
    UnlockEvaluator behaviour: @callback evaluate(user :: User.t(), condition :: map()) :: {:ok, boolean()} | {:error, term()}.
    UnlockEngine: evaluate(user, item) dispatches to correct evaluator based on item.unlock_condition["type"]. Registry pattern for evaluator lookup. Returns :unlocked | :locked | {:progress, float}.
  </action>
</task>

<task type="auto">
  <name>Task 2: Five evaluator implementations (2.13–2.17)</name>
  <files>apps/backend/lib/cgraph/cosmetics/evaluators/</files>
  <action>
    LevelEvaluator: check reputation score OR post count >= threshold (NOT user.level — XP tables dropped). Verify User schema fields before implementing. Returns progress percentage.
    AchievementEvaluator: check user has earned specific achievement(s) via CGraph.Gamification.AchievementSystem.
    CollectionEvaluator: check user owns N items of target type via CGraph.Cosmetics.Inventory.
    EventEvaluator: check current time within event window + additional criteria.
    PurchaseEvaluator: check user has enough Nodes (via CGraph.Nodes context), debit on unlock.
    All implement UnlockEvaluator behaviour. Pure functions, no side effects.
  </action>
</task>

<task type="auto">
  <name>Task 3: Oban workers + visibility rules (2.18–2.20)</name>
  <files>
    apps/backend/lib/cgraph/workers/unlock_check_worker.ex
    apps/backend/lib/cgraph/workers/seasonal_rotation_worker.ex
    apps/backend/lib/cgraph/cosmetics/visibility_rules.ex
  </files>
  <action>
    UnlockCheckWorker: Oban, :unlocks queue (NOT :cosmetics — :unlocks queue already exists with 10 workers), triggered on achievement events via PubSub subscriber. Checks all locked items for user, grants newly unlocked.
    SeasonalRotationWorker: Oban, :cosmetics queue, monthly cron (1st of month), rotates seasonal/limited items, marks expired items as unavailable.
    VisibilityRules: filter_visible(items, user) — all items visible but locked ones show preview only. rarity >= legendary gets special locked treatment. Use Rarity.rank/1 for comparison.
  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
