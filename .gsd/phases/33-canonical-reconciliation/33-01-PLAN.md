---
phase: 33-canonical-reconciliation
plan: 01
type: execute
wave: 1
depends_on:
  - 32-02
files_modified:
  - docs/COSMETICS_MANIFEST.md (new — human-readable canonical reference)
  - apps/backend/priv/repo/seeds/cosmetics_manifest.json (new — machine-readable seed source)
  - apps/backend/lib/cgraph/cosmetics/rarity.ex (new — unified rarity module)
  - apps/backend/priv/repo/migrations/YYYYMMDD_unify_rarity_tiers.exs (new)
  - apps/backend/lib/cgraph/gamification/avatar_border.ex (update rarity validation)
  - apps/backend/lib/cgraph/gamification/chat_effect.ex (update rarity validation)
  - apps/backend/lib/cgraph/gamification/profile_theme.ex (update rarity validation)
  - apps/backend/lib/cgraph/gamification/title.ex (update rarity validation)
autonomous: true
must_haves:
  truths:
    - 'COSMETICS_MANIFEST.md created with definitive counts: 70 badges, 70 titles, 45 nameplates, 25
      profile themes, 50 name styles, 50+ profile frames, 10 forum themes'
    - 'cosmetics_manifest.json contains every cosmetic item with id, slug, name, rarity, category,
      track, unlock_type, unlock_condition, nodes_cost'
    - 'All 7 rarity tiers represented across all cosmetic types'
    - 'Zero numeric conflicts between section counts and item lists'
    - 'CGraph.Cosmetics.Rarity module created with tiers/0, string_values/0, color/1, rank/1,
      compare/2 functions'
    - 'Existing schemas (avatar_border.ex, chat_effect.ex, profile_theme.ex, title.ex) updated to
      use Rarity.string_values() in validate_inclusion/3'
    - 'Migration adds free tier to schemas missing it, adds source field for seasonal/event tracking'
    - 'Migration has down/0 rollback'
  artifacts:
    - docs/COSMETICS_MANIFEST.md
    - apps/backend/priv/repo/seeds/cosmetics_manifest.json
    - apps/backend/lib/cgraph/cosmetics/rarity.ex
    - apps/backend/priv/repo/migrations/YYYYMMDD_unify_rarity_tiers.exs
  key_links:
    - 'Source docs: docs/PrivateFolder/ — 14 files (guides, design docs, migration notes); cosmetic
      counts conflict across multiple files (resolution required)'
    - 'Existing rarity in avatar_border.ex — @rarities module attribute with validate_inclusion'
    - 'Existing rarity in chat_effect.ex — same pattern'
    - 'Existing rarity in profile_theme.ex — same pattern'
    - 'Existing rarity in title.ex — same pattern'
    - '⚠️ Rarity values DIVERGE across schemas: avatar_border has 9 values (common uncommon rare
      epic legendary mythic unique seasonal event), chat_effect/title have 7 (no seasonal/ event),
      profile_theme has 8 (no event). Migration must normalize ALL to the canonical 7.'
    - '⚠️ cgraph/cosmetics/ is a NEW directory — does not exist yet. Existing cosmetic schemas live
      under cgraph/gamification/. This plan creates the first Cosmetics-namespaced module.'
    - '⚠️ Migration 20260723120000_drop_gamification_tables.exs dropped XP/quest/prestige/event
      tables but KEPT cosmetic tables (avatar_borders, titles, profile_themes, chat_effects). Do NOT
      reference dropped tables.'
  pre_tasks:
    - 'Audit existing rarity string values: grep -r "@rarities\|validate_inclusion.*rarity"
      apps/backend/lib/cgraph/gamification/ to capture current enum values before migration'
---

<objective>
Create the canonical cosmetics manifest resolving all source document conflicts, build the unified
Rarity module, and run the rarity migration to normalize all existing cosmetics to the 7-tier
system.

ATOMIC_PLAN tasks covered: P0.1 (Canonical Cosmetics Manifest), P0.2 (Unified Rarity Module), P0.3
(Rarity Migration). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical cosmetics manifest (P0.1)</name>
  <files>
    docs/COSMETICS_MANIFEST.md
    apps/backend/priv/repo/seeds/cosmetics_manifest.json
  </files>
  <action>
    Resolve all 5 source document conflicts:
    - Badges: 70 (Cosmetics doc 60 + Forums doc 10 extras)
    - Titles: 70 (Cosmetics 55 + Forums 15 rank/tier/event)
    - Nameplates: 45 (Cosmetics 30 + Forums 15 extras)
    - Profile Themes: 25 (5 free + 5 earned + 15 shop)
    - Name Styles: 50 (8 fonts + 12 effects + 15 colors + 10 prefixes + 5 suffixes)
    - Profile Frames: 50+ (Forums doc specification)
    - Forum Themes: 10 (Neon Cyber → Zen Garden)

    Create COSMETICS_MANIFEST.md with:
    - Summary table of all cosmetic types with counts
    - Per-type sections with every item listed
    - Each item: id, slug, name, rarity, category, track, unlock_type, unlock_condition, nodes_cost

    Create cosmetics_manifest.json with identical data in machine-readable format.

  </action>
  <verify>wc -l docs/COSMETICS_MANIFEST.md; cat apps/backend/priv/repo/seeds/cosmetics_manifest.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(f'Total items: {sum(len(v) for v in d.values())}')"</verify>
  <done>Canonical manifest with zero conflicts</done>
</task>

<task type="auto">
  <name>Task 2: Create unified rarity module (P0.2)</name>
  <files>
    apps/backend/lib/cgraph/cosmetics/rarity.ex
    apps/backend/lib/cgraph/gamification/avatar_border.ex
    apps/backend/lib/cgraph/gamification/chat_effect.ex
    apps/backend/lib/cgraph/gamification/profile_theme.ex
    apps/backend/lib/cgraph/gamification/title.ex
  </files>
  <action>
    Create CGraph.Cosmetics.Rarity with:
    - @tiers [:free, :common, :uncommon, :rare, :epic, :legendary, :mythic]
    - tiers/0, string_values/0, atom_values/0, color/1, rank/1, compare/2
    - Full @moduledoc and @doc on every function
    - @spec on every function

    Update existing schemas to import from this module:
    - Replace @rarities module attributes with Rarity.string_values() in validate_inclusion/3

  </action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -10</verify>
  <done>Rarity module created, existing schemas updated</done>
</task>

<task type="auto">
  <name>Task 3: Rarity migration (P0.3)</name>
  <files>apps/backend/priv/repo/migrations/</files>
  <action>
    Create migration to:
    - Add `free` tier to schemas missing it (uncommon already exists)
    - Remove `unique`, `seasonal`, `event` from rarity lists (these are source categories)
    - Add `source` field (Ecto.Enum: [:earned, :purchased, :seasonal, :event, :admin_granted, :gifted])
    - Backfill: items marked `seasonal` → rarity = intended rarity, source = :seasonal
    - Include down/0 rollback
  </action>
  <verify>cd apps/backend && mix ecto.migrate --dry-run 2>&1 | tail -10</verify>
  <done>Rarity migration with rollback</done>
</task>

</tasks>
