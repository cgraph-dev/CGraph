---
phase: 33-canonical-reconciliation
plan: 02
type: execute
wave: 1
depends_on:
  - 32-02
files_modified:
  - packages/shared-types/src/rarity.ts (new — canonical RarityTier type)
  - packages/shared-types/src/cosmetics.ts (new — all cosmetics type definitions)
  - packages/shared-types/src/index.ts (update — re-export new files)
  - packages/animation-constants/src/borders.ts (update — UPPERCASE → lowercase rarity)
  - packages/animation-constants/src/registries/nameplates.ts (update — UPPERCASE → lowercase)
  - packages/animation-constants/src/registries/profileEffects.ts (update — UPPERCASE → lowercase)
  - apps/web/src/pages/customize/identity-customization/constants.ts (update — add UNCOMMON)
  - apps/mobile/src/stores/themeStore.ts (verify 7-tier alignment)
autonomous: true
must_haves:
  truths:
    - 'shared-types exports RarityTier, RARITY_TIERS, RARITY_COLORS, rarityRank, compareRarity'
    - 'shared-types exports all cosmetics types: CosmeticItem, CosmeticType, UnlockType,
      UnlockCondition, UserCosmeticInventory, CosmeticSurface, CosmeticVisibilityRule'
    - 'animation-constants uses lowercase rarity values throughout: free, common, uncommon, rare,
      epic, legendary, mythic'
    - 'MYTHICAL unified to mythic across all registries'
    - 'Web constants.ts includes UNCOMMON tier'
    - 'Mobile themeStore.ts rarity colors match shared definition'
    - 'TypeScript compiles with zero errors across web + mobile'
    - 'shared-types package.json exports map updated with ./rarity and ./cosmetics entries'
  artifacts:
    - packages/shared-types/src/rarity.ts
    - packages/shared-types/src/cosmetics.ts
  key_links:
    - 'animation-constants borders.ts (531 lines) — ALL rarity values currently UPPERCASE. Uses
      MYTHIC (not MYTHICAL). File is at src/borders.ts (NOT src/registries/borders.ts).'
    - 'animation-constants nameplates.ts — UPPERCASE rarity values, uses MYTHICAL (differs from
      borders.ts which uses MYTHIC)'
    - 'animation-constants profileEffects.ts — UPPERCASE rarity values, MYTHICAL in type definition
      but no data entries actually use it'
    - 'Web constants.ts — identity-customization, missing UNCOMMON tier'
    - 'Mobile themeStore.ts — uses 7 tiers: common, uncommon, rare, epic, legendary, mythic, divine.
      Has divine (not free); has uncommon that web constants.ts lacks. Must align to canonical
      7-tier set.'
    - '⚠️ UNCOMMON is absent from ALL animation-constants registries (borders, nameplates,
      profileEffects), not just web constants.ts. Conversion must ADD uncommon tier entries.'
    - '⚠️ Rarity naming divergence: borders.ts uses MYTHIC, nameplates.ts/profileEffects.ts use
      MYTHICAL. Both must unify to lowercase mythic.'
    - 'Plan 33-03 handles package.json exports map comprehensively'
---

<objective>
Unify rarity across frontend packages and create shared cosmetics types. Convert all
animation-constants rarity values from UPPERCASE to lowercase to match the backend canonical
system. Create the RarityTier type and full cosmetics type definitions in shared-types.

ATOMIC_PLAN tasks covered: P0.4 (Rarity Unification Frontend), P0.8 (Shared Cosmetics Types).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create rarity.ts in shared-types (P0.4 partial)</name>
  <files>packages/shared-types/src/rarity.ts</files>
  <action>
    Create canonical rarity type mirroring CGraph.Cosmetics.Rarity:
    - RARITY_TIERS const array
    - RarityTier type
    - RARITY_COLORS record
    - RARITY_LABELS record
    - rarityRank() and compareRarity() functions
  </action>
</task>

<task type="auto">
  <name>Task 2: Create cosmetics.ts in shared-types (P0.8)</name>
  <files>packages/shared-types/src/cosmetics.ts</files>
  <action>
    Define all cosmetic types:
    - CosmeticItem (base interface with all fields)
    - CosmeticType union (border, title, badge, nameplate, etc.)
    - UnlockType, AnimationType unions
    - UnlockCondition interface with 14 condition types
    - UserCosmeticInventory interface
    - CosmeticSurface (6 surfaces) and CosmeticVisibilityRule
    Import RarityTier from ./rarity
  </action>
</task>

<task type="auto">
  <name>Task 3: Convert animation-constants to lowercase rarity (P0.4)</name>
  <files>
    packages/animation-constants/src/borders.ts
    packages/animation-constants/src/registries/nameplates.ts
    packages/animation-constants/src/registries/profileEffects.ts
  </files>
  <action>
    Convert ALL rarity values from UPPERCASE to lowercase:
    - FREE → free, COMMON → common, UNCOMMON → uncommon, RARE → rare
    - EPIC → epic, LEGENDARY → legendary, MYTHIC → mythic, MYTHICAL → mythic
    Update all consumers of these values.
    Update web constants.ts to include UNCOMMON tier.
    Verify mobile themeStore.ts alignment.
  </action>
  <verify>grep -c "MYTHICAL\|'FREE'\|'COMMON'\|'RARE'\|'EPIC'\|'LEGENDARY'\|'MYTHIC'" packages/animation-constants/src/borders.ts packages/animation-constants/src/registries/*.ts</verify>
</task>

<task type="auto">
  <name>Task 4: Update shared-types barrel + re-export</name>
  <files>
    packages/shared-types/src/index.ts
    packages/shared-types/package.json
  </files>
  <action>
    Re-export new files from index.ts:
    - export * from './rarity'
    - export * from './cosmetics'
    Add exports map entries in package.json for ./rarity and ./cosmetics
  </action>
  <verify>cd packages/shared-types && npx tsc --noEmit 2>&1 | tail -5</verify>
</task>

</tasks>
