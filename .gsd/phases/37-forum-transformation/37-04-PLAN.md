---
phase: 37-forum-transformation
plan: 04
type: execute
wave: 2
depends_on:
  - 37-01
  - 37-02
files_modified:
  - apps/backend/lib/cgraph/forums/forum_permission.ex (update — extend. ⚠️ singular, NOT
    forum_permissions.ex)
  - apps/backend/lib/cgraph/forums/custom_forum.ex (new — schema)
  - apps/backend/lib/cgraph/forums/moderation_log.ex (new — schema)
  - apps/backend/lib/cgraph_web/controllers/api/v1/forum_admin_controller.ex (new — under api/v1/,
    namespace CGraphWeb.API.V1.ForumAdminController)
  - apps/backend/lib/cgraph_web/router/forum_admin_routes.ex (new — macro-based route module)
  - apps/backend/lib/cgraph_web/router.ex (update — add import + macro call for ForumAdminRoutes)
  - apps/backend/priv/repo/migrations/<ts>_create_custom_forum_tables.exs (new)
autonomous: true
must_haves:
  truths:
    - 'ForumPermission extended: add new permission fields for Phase 37 capabilities (manage_tags,
      manage_templates, manage_scheduled_posts, view_analytics, manage_identity). Permission system
      is GROUP-BASED with tri-state (inherit/allow/deny) via ForumUserGroup — NOT role-based.
      ForumMember already has @roles ["member", "moderator", "admin", "owner"] — do NOT conflate
      roles with permissions.'
    - 'Permission matrix: new permission fields added to existing tri-state system. Each resolves
      via can?/3 → get_effective_permission → check_permission_with_inheritance chain.'
    - 'CustomForum: owner_id, name, slug, description, theme (JSONB), rules (text), icon_url,
      banner_url, is_private, invite_only'
    - 'ModerationLog: moderator_id, action (string), target_type, target_id, reason, metadata
      (JSONB), created_at'
    - 'ForumAdminController: create_forum (POST), update_settings (PUT), manage_members (POST),
      moderation_log (GET)'
  artifacts:
    - apps/backend/lib/cgraph/forums/custom_forum.ex
    - apps/backend/lib/cgraph/forums/moderation_log.ex
    - apps/backend/lib/cgraph_web/controllers/api/v1/forum_admin_controller.ex
    - apps/backend/lib/cgraph_web/router/forum_admin_routes.ex
  key_links:
    - 'Forum permissions: apps/backend/lib/cgraph/forums/forum_permission.ex (⚠️ singular, not
      plural)'
    - '⚠️ forum_permission.ex uses GROUP-BASED permissions with tri-state (inherit/allow/deny), NOT
      role-based. Works via ForumUserGroup. can?/3 resolves through get_effective_permission/4 →
      check_permission_with_inheritance. Extend by adding new permission fields to schema +
      changeset, not by adding roles.'
    - '⚠️ ForumMember already has @roles ["member", "moderator", "admin", "owner"] in forum_member.ex
      — roles and permissions are SEPARATE systems. Do NOT add roles to ForumPermission.'
    - '⚠️ moderation.ex already exists in forums/ — handles mod queue, hide_post, soft delete.
      moderation_log.ex is different (audit trail of actions). No overlap.'
    - 'Forums facade: apps/backend/lib/cgraph/forums/core.ex — delegates to Core.{Authorization,
      Listing, Membership, Stats}. There is NO forums.ex facade.'
    - 'Router pattern: create macro-based route module, THEN update router.ex — add import at ~line
      45 and macro call at ~line 149.'
    - 'Controller namespace: ALL controllers are CGraphWeb.API.V1.* under
      apps/backend/lib/cgraph_web/controllers/api/v1/'
---

<objective>
Extend forum permissions with role-based access control, create custom forums with owner management,
and build moderation logging with admin controller.

ATOMIC_PLAN tasks: 4.16 (Permission Extension), 4.17 (Custom Forums), 4.18 (Moderation Logging),
4.19 (Forum Admin Controller). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Permission extension + custom forums (4.16, 4.17)</name>
  <files>
    apps/backend/lib/cgraph/forums/forum_permission.ex
    apps/backend/lib/cgraph/forums/custom_forum.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>Extend permissions: add new permission FIELDS to existing tri-state system (manage_tags, manage_templates, manage_scheduled_posts, view_analytics, manage_identity as :string fields with values "inherit"/"allow"/"deny"). Do NOT add roles to ForumPermission — roles already exist on ForumMember (@roles ["member", "moderator", "admin", "owner"]). Update changeset to include new fields. CustomForum schema: owner_id (:binary_id), name, slug (unique), description, theme (JSONB), rules, icon_url, banner_url, is_private, invite_only. Migration with slug unique index. Use @primary_key {:id, :binary_id, autogenerate: true}, @foreign_key_type :binary_id.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 2: Moderation log + admin controller (4.18, 4.19)</name>
  <files>
    apps/backend/lib/cgraph/forums/moderation_log.ex
    apps/backend/lib/cgraph_web/controllers/api/v1/forum_admin_controller.ex
    apps/backend/lib/cgraph_web/router/forum_admin_routes.ex
    apps/backend/lib/cgraph_web/router.ex
  </files>
  <action>ModerationLog schema: moderator_id (:binary_id), action (string), target_type (thread|post|user), target_id (:binary_id), reason (text), metadata (JSONB), timestamps. ForumAdminController (CGraphWeb.API.V1.ForumAdminController): create_forum (POST), update_settings (PUT), manage_members (POST — add/remove/change role), moderation_log (GET with cursor pagination + filters). Use `import CGraphWeb.ControllerHelpers` for render_data/render_error. Create CGraphWeb.Router.ForumAdminRoutes macro module, THEN update router.ex: add `import CGraphWeb.Router.ForumAdminRoutes` after line 44 and `forum_admin_routes()` after line 148.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
