---
phase: 37-forum-transformation
plan: 04
type: execute
wave: 2
depends_on:
  - 37-01
  - 37-02
files_modified:
  - apps/backend/lib/cgraph/forums/forum_permission.ex (update — extend. ⚠️ singular, NOT
    forum_permissions.ex)
  - apps/backend/lib/cgraph/forums/custom_forum.ex (new — schema)
  - apps/backend/lib/cgraph/forums/moderation_log.ex (new — schema)
  - apps/backend/lib/cgraph_web/controllers/forum_admin_controller.ex (new — top-level, NOT under
    api/v1/)
  - apps/backend/lib/cgraph_web/router/forum_admin_routes.ex (new — macro-based route module)
  - apps/backend/priv/repo/migrations/<ts>_create_custom_forum_tables.exs (new)
autonomous: true
must_haves:
  truths:
    - 'ForumPermissions extended: role-based (owner, admin, moderator, member, guest)'
    - 'Permission matrix: create_thread, reply, pin, delete, ban, manage_tags, manage_templates per
      role'
    - 'CustomForum: owner_id, name, slug, description, theme (JSONB), rules (text), icon_url,
      banner_url, is_private, invite_only'
    - 'ModerationLog: moderator_id, action (string), target_type, target_id, reason, metadata
      (JSONB), created_at'
    - 'ForumAdminController: create_forum (POST), update_settings (PUT), manage_members (POST),
      moderation_log (GET)'
  artifacts:
    - apps/backend/lib/cgraph/forums/custom_forum.ex
    - apps/backend/lib/cgraph/forums/moderation_log.ex
    - apps/backend/lib/cgraph_web/controllers/forum_admin_controller.ex
    - apps/backend/lib/cgraph_web/router/forum_admin_routes.ex
  key_links:
    - 'Forum permissions: apps/backend/lib/cgraph/forums/forum_permission.ex (⚠️ singular, not
      plural)'
    - '⚠️ forum_permission.ex already has full RBAC with can?/3 — extend roles, do not replace'
    - '⚠️ moderation.ex already exists in forums/ — check for overlap with moderation_log.ex'
    - 'Forums context: apps/backend/lib/cgraph/forums/forums.ex'
---

<objective>
Extend forum permissions with role-based access control, create custom forums with owner management,
and build moderation logging with admin controller.

ATOMIC_PLAN tasks: 4.16 (Permission Extension), 4.17 (Custom Forums), 4.18 (Moderation Logging),
4.19 (Forum Admin Controller). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Permission extension + custom forums (4.16, 4.17)</name>
  <files>
    apps/backend/lib/cgraph/forums/forum_permission.ex
    apps/backend/lib/cgraph/forums/custom_forum.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>Extend permissions: add roles (owner|admin|moderator|member|guest), permission matrix as module attribute. CustomForum schema: owner_id, name, slug (unique), description, theme (JSONB), rules, icon_url, banner_url, is_private, invite_only. Migration with slug unique index.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 2: Moderation log + admin controller (4.18, 4.19)</name>
  <files>
    apps/backend/lib/cgraph/forums/moderation_log.ex
    apps/backend/lib/cgraph_web/controllers/forum_admin_controller.ex
    apps/backend/lib/cgraph_web/router/forum_admin_routes.ex
  </files>
  <action>ModerationLog schema: moderator_id, action, target_type (thread|post|user), target_id, reason, metadata (JSONB), timestamp. ForumAdminController: create_forum (POST), update_settings (PUT), manage_members (POST — add/remove/change role), moderation_log (GET with pagination + filters). Create CGraphWeb.Router.ForumAdminRoutes macro module (do NOT edit router.ex directly).</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
