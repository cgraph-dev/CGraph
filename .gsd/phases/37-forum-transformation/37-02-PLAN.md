---
phase: 37-forum-transformation
plan: 02
type: execute
wave: 1
depends_on:
  - 36-05
files_modified:
  - apps/backend/lib/cgraph/forums/thread_tag.ex (new — schema)
  - apps/backend/lib/cgraph/forums/tag_category.ex (new — schema)
  - apps/backend/lib/cgraph/forums/reputation.ex (new — context, consolidates existing reputation logic)
  - apps/backend/lib/cgraph_web/controllers/api/v1/tag_controller.ex (new — under api/v1/,
    namespace CGraphWeb.API.V1.TagController)
  - apps/backend/lib/cgraph_web/router/forum_tags_routes.ex (new — macro-based route module)
  - apps/backend/lib/cgraph_web/router.ex (update — add import + macro call for ForumTagsRoutes)
  - apps/backend/priv/repo/migrations/<ts>_create_tags_tables.exs (new)
  - apps/backend/lib/cgraph/forums/reputation_recalc_worker.ex (new — in forums/ dir, not workers/)
autonomous: true
must_haves:
  truths:
    - 'ThreadTag schema: thread_id, tag_name, tag_category_id, applied_by, applied_at'
    - 'TagCategory: name, color, icon, max_per_thread, required (boolean), forum_id scope'
    - 'Tag CRUD with permission checks (thread author + moderators can tag)'
    - 'Reputation context: calculate_score/1, add_reputation/3, deduct_reputation/3'
    - 'Reputation factors: posts (1pt), replies (0.5pt), upvotes received (2pt), best answers (5pt),
      reports against (-3pt)'
    - 'ReputationRecalcWorker: nightly full recalc, :default queue'
  artifacts:
    - apps/backend/lib/cgraph/forums/thread_tag.ex
    - apps/backend/lib/cgraph/forums/tag_category.ex
    - apps/backend/lib/cgraph/forums/reputation.ex
    - apps/backend/lib/cgraph_web/controllers/api/v1/tag_controller.ex
    - apps/backend/lib/cgraph_web/router/forum_tags_routes.ex
    - apps/backend/lib/cgraph/forums/reputation_recalc_worker.ex
  key_links:
    - 'Forums facade: apps/backend/lib/cgraph/forums/core.ex — delegates to Core.{Authorization,
      Listing, Membership, Stats}. There is NO forums.ex facade.'
    - 'Thread schema: apps/backend/lib/cgraph/forums/thread.ex'
    - '⚠️ EXISTING reputation infrastructure — DO NOT recreate from scratch:
      - members.ex has update_reputation/3 (increments reputation, reputation_positive, reputation_negative on ForumMember)
      - voting.ex has propagate_post_reputation (calls update_reputation on upvote/downvote)
      - ranking_engine.ex exists for ranking calculations
      - forum_rank.ex exists as rank schema
      - ForumMember already has reputation (integer), reputation_positive, reputation_negative fields'
    - 'ForumMember: apps/backend/lib/cgraph/forums/forum_member.ex — @roles ["member", "moderator",
      "admin", "owner"], reputation fields already exist'
    - 'Router pattern: create macro-based route module, THEN update router.ex — add import at ~line
      45 and macro call at ~line 149. See ForumRoutes for pattern.'
    - 'Controller namespace: ALL controllers are CGraphWeb.API.V1.* under
      apps/backend/lib/cgraph_web/controllers/api/v1/'
    - 'Forum workers pattern: digest_worker.ex is in forums/ dir, not workers/ — place
      reputation_recalc_worker.ex in forums/ too'
---

<objective>
Build thread tagging system with categories and forum reputation scoring with automated recalculation.

ATOMIC_PLAN tasks: 4.5 (Thread Tag Schema), 4.6 (Tag Categories + Controller), 4.7 (Reputation
System + Worker). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Tag schemas + migration (4.5, 4.6)</name>
  <files>
    apps/backend/lib/cgraph/forums/thread_tag.ex
    apps/backend/lib/cgraph/forums/tag_category.ex
    apps/backend/lib/cgraph_web/controllers/tag_controller.ex
    apps/backend/lib/cgraph_web/router/forum_tags_routes.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>ThreadTag: belongs_to thread + tag_category, tag_name (string), applied_by (ref :binary_id users), applied_at. TagCategory: name, color (hex string), icon (string), max_per_thread (integer), required (boolean), belongs_to forum. Unique index on [thread_id, tag_name]. TagController (CGraphWeb.API.V1.TagController): index, create, delete (with permission check via ForumPermission.can?/3). Routes in ForumTagsRoutes macro module, THEN update router.ex: add `import CGraphWeb.Router.ForumTagsRoutes` after line 44 and `forum_tags_routes()` after line 148. Use @primary_key {:id, :binary_id, autogenerate: true}, @foreign_key_type :binary_id.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 2: Reputation system (4.7)</name>
  <files>
    apps/backend/lib/cgraph/forums/reputation.ex
    apps/backend/lib/cgraph/forums/reputation_recalc_worker.ex
  </files>
  <action>Reputation context: CONSOLIDATES existing reputation logic from members.ex (update_reputation/3), voting.ex (propagate_post_reputation), and ranking_engine.ex into a single module. calculate_score/1 (posts×1 + replies×0.5 + upvotes×2 + best_answers×5 - reports×3), add_reputation/3 (type, user_id, amount), deduct_reputation/3. PubSub subscribe to post/upvote/report events. Delegates to existing members.ex update_reputation/3 for actual DB writes. ReputationRecalcWorker: nightly Oban cron (:default queue), full recalc all users, update identity_card.reputation_score. Place worker in forums/ dir (same as digest_worker.ex).</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
