---
phase: 37-forum-transformation
plan: 01
type: execute
wave: 1
depends_on:
  - 36-05
files_modified:
  - apps/backend/lib/cgraph/forums/identity_card.ex (new — schema)
  - apps/backend/lib/cgraph/forums/post_creation_flow.ex (new — context)
  - apps/backend/lib/cgraph_web/controllers/identity_card_controller.ex (new — top-level, NOT under
    api/v1/)
  - apps/backend/priv/repo/migrations/<ts>_create_identity_cards.exs (new)
  - apps/backend/lib/cgraph_web/router/forum_identity_routes.ex (new — macro-based route module)
autonomous: true
must_haves:
  truths:
    - 'IdentityCard schema: user_id, display_name, avatar_frame_id, badge_ids (array), title_id,
      bio_snippet, reputation_score, custom_css (JSONB)'
    - 'PostCreationFlow context: validates identity card on post, enforces forum rules, checks
      permissions'
    - 'IdentityCardController: show (GET /users/:id/identity-card), update (PUT)'
    - 'Identity card caches in ETS with 5-minute TTL'
    - 'Forum posts reference identity card snapshot at time of posting'
  artifacts:
    - apps/backend/lib/cgraph/forums/identity_card.ex
    - apps/backend/lib/cgraph/forums/post_creation_flow.ex
    - apps/backend/lib/cgraph_web/controllers/identity_card_controller.ex
  key_links:
    - 'Forums context: apps/backend/lib/cgraph/forums/forums.ex — 68+ files, extensive module'
    - 'User schema: apps/backend/lib/cgraph/accounts/user.ex'
    - 'Router pattern: create macro-based route module — do NOT edit router.ex directly. See
      CGraphWeb.Router.GamificationRoutes for pattern.'
---

<objective>
Build identity card system for forums and enhance post creation flow with identity integration
and rule enforcement.

ATOMIC_PLAN tasks: 4.1 (Identity Card Schema), 4.2 (Identity Card Controller), 4.3 (PostCreationFlow
Context), 4.4 (Identity ↔ Post Integration). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Identity card schema + migration (4.1)</name>
  <files>
    apps/backend/lib/cgraph/forums/identity_card.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>Schema: user_id (references :users, unique), display_name, avatar_frame_id (nullable ref), badge_ids (array of integers), title_id (nullable ref), bio_snippet (string, max 140), reputation_score (decimal), custom_css (map/JSONB). Migration with user_id unique index.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 2: Controller + post creation flow (4.2, 4.3, 4.4)</name>
  <files>
    apps/backend/lib/cgraph_web/controllers/identity_card_controller.ex
    apps/backend/lib/cgraph/forums/post_creation_flow.ex
    apps/backend/lib/cgraph_web/router/forum_identity_routes.ex
  </files>
  <action>Controller: show (GET, with ETS cache 5min TTL), update (PUT, invalidate cache). PostCreationFlow: create_post_with_identity/3 — snapshots current identity card onto post metadata, validates forum-level rules (min reputation, required badges), checks permissions (banned, muted, rate-limited). Create CGraphWeb.Router.ForumIdentityRoutes macro module (do NOT edit router.ex directly).</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
