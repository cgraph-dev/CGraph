---
phase: 37-forum-transformation
plan: 01
type: execute
wave: 1
depends_on:
  - 36-05
files_modified:
  - apps/backend/lib/cgraph/forums/identity_card.ex (new — schema)
  - apps/backend/lib/cgraph/forums/post_creation_flow.ex (new — context)
  - apps/backend/lib/cgraph_web/controllers/api/v1/identity_card_controller.ex (new — under api/v1/,
    namespace CGraphWeb.API.V1.IdentityCardController)
  - apps/backend/priv/repo/migrations/<ts>_create_identity_cards.exs (new)
  - apps/backend/lib/cgraph_web/router/forum_identity_routes.ex (new — macro-based route module)
  - apps/backend/lib/cgraph_web/router.ex (update — add import + macro call for ForumIdentityRoutes)
autonomous: true
must_haves:
  truths:
    - 'IdentityCard schema: user_id, display_name, avatar_frame_id, badge_ids ({:array, :binary_id}),
      title_id, bio_snippet, reputation_score (integer — matches ForumMember.reputation), custom_css
      (JSONB)'
    - 'PostCreationFlow context: validates identity card on post, enforces forum rules, checks
      permissions'
    - 'IdentityCardController: show (GET /users/:id/identity-card), update (PUT)'
    - 'Identity card caches in ETS with 5-minute TTL'
    - 'Forum posts reference identity card snapshot at time of posting'
  artifacts:
    - apps/backend/lib/cgraph/forums/identity_card.ex
    - apps/backend/lib/cgraph/forums/post_creation_flow.ex
    - apps/backend/lib/cgraph_web/controllers/api/v1/identity_card_controller.ex
  key_links:
    - 'Forums facade: apps/backend/lib/cgraph/forums/core.ex — delegates to Core.{Authorization,
      Listing, Membership, Stats}. There is NO forums.ex facade.'
    - 'User schema: apps/backend/lib/cgraph/accounts/user.ex'
    - 'ForumMember schema: apps/backend/lib/cgraph/forums/forum_member.ex — already has display_name,
      title, reputation (integer), reputation_positive, reputation_negative, avatar_url, post_count'
    - 'Router pattern: create macro-based route module, THEN update router.ex — add
      `import CGraphWeb.Router.ForumIdentityRoutes` at ~line 45 and `forum_identity_routes()` macro
      call at ~line 149. See CGraphWeb.Router.ForumRoutes for pattern (same domain).'
    - 'Router.ex: apps/backend/lib/cgraph_web/router.ex — 18 imports at lines 27-44, 18 macro calls
      at lines 131-148. New modules go after existing ones.'
    - 'Controller namespace: ALL controllers are CGraphWeb.API.V1.* — see 140+ existing controllers
      at apps/backend/lib/cgraph_web/controllers/api/v1/'
---

<objective>
Build identity card system for forums and enhance post creation flow with identity integration
and rule enforcement.

ATOMIC_PLAN tasks: 4.1 (Identity Card Schema), 4.2 (Identity Card Controller), 4.3 (PostCreationFlow
Context), 4.4 (Identity ↔ Post Integration). </objective>

<tasks>

<task type="auto">
  <name>Task 1: Identity card schema + migration (4.1)</name>
  <files>
    apps/backend/lib/cgraph/forums/identity_card.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>Schema: user_id (references :users, :binary_id, unique), display_name, avatar_frame_id (nullable :binary_id ref), badge_ids ({:array, :binary_id} — CGraph uses UUID PKs), title_id (nullable :binary_id ref), bio_snippet (string, max 140), reputation_score (integer — matches ForumMember.reputation field type), custom_css (map/JSONB). Migration with user_id unique index. Use @primary_key {:id, :binary_id, autogenerate: true}, @foreign_key_type :binary_id, @timestamps_opts [type: :utc_datetime_usec].</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 2: Controller + post creation flow (4.2, 4.3, 4.4)</name>
  <files>
    apps/backend/lib/cgraph_web/controllers/api/v1/identity_card_controller.ex
    apps/backend/lib/cgraph/forums/post_creation_flow.ex
    apps/backend/lib/cgraph_web/router/forum_identity_routes.ex
    apps/backend/lib/cgraph_web/router.ex
  </files>
  <action>Controller (CGraphWeb.API.V1.IdentityCardController): show (GET, with ETS cache 5min TTL), update (PUT, invalidate cache). Use `use CGraphWeb, :controller` and `import CGraphWeb.ControllerHelpers`. PostCreationFlow: create_post_with_identity/3 — snapshots current identity card onto post metadata (user_nameplate_snapshot on ThreadPost), validates forum-level rules (min reputation, required badges), checks permissions via ForumPermission.can?/3. Create CGraphWeb.Router.ForumIdentityRoutes macro module, THEN update router.ex: add `import CGraphWeb.Router.ForumIdentityRoutes` after line 44 and `forum_identity_routes()` after line 148.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
