---
phase: 37-forum-transformation
plan: 03
type: execute
wave: 2
depends_on:
  - 37-01
  - 37-02
files_modified:
  - apps/backend/lib/cgraph/forums/at_mention.ex (new — @mention parser)
  - apps/backend/lib/cgraph/forums/thread_template.ex (new — schema)
  - apps/backend/lib/cgraph/forums/forum_analytics.ex (new — context)
  - apps/backend/lib/cgraph/forums/search.ex (update — ⚠️ ALREADY EXISTS as CGraph.Forums.Search
    at ~280 lines. Has tsvector/tsquery, forum_id, board_id, author_id, date_from, date_to, sort.
    EXTEND with tag-based filtering only — existing filters are production-ready)
  - apps/backend/lib/cgraph/forums/thread_poll.ex (update — ⚠️ ALREADY COMPLETE at 61 lines.
    Already has question, options ({:array, :map} JSONB), is_multiple_choice, max_options, is_public,
    closes_at, total_votes, PollVote association, full changeset. Only add anonymous voting if
    needed — do NOT recreate existing fields)
  - apps/backend/lib/cgraph/forums/scheduled_post.ex (new — schema)
  - apps/backend/lib/cgraph/forums/scheduled_post_worker.ex (new — in forums/ dir)
  - apps/backend/lib/cgraph/forums/forum_analytics_worker.ex (new — in forums/ dir)
  - apps/backend/priv/repo/migrations/<ts>_create_forum_features_tables.exs (new)
autonomous: true
must_haves:
  truths:
    - '@mention parser: extracts @username from post content, creates notifications'
    - 'ThreadTemplate: forum_id, name, structure (JSONB with sections), required_fields'
    - 'ForumAnalytics: top_threads/1, engagement_metrics/2, growth_stats/1'
    - 'AdvancedSearch: EXTEND existing CGraph.Forums.Search (search.ex) with tag-based filtering.
      Forum/author/date filters already exist and are production-ready — do NOT reimplement'
    - 'ThreadPoll: ALREADY COMPLETE — has question, options (JSONB :array of :map),
      is_multiple_choice, max_options, is_public, closes_at, total_votes, PollVote. Only add
      anonymous voting support if needed'
    - 'ScheduledPost: author_id, forum_id, content, scheduled_for, status'
    - 'ScheduledPostWorker: per-minute check, publishes due posts'
    - 'Analytics worker: hourly aggregation of forum metrics'
  artifacts:
    - apps/backend/lib/cgraph/forums/at_mention.ex
    - apps/backend/lib/cgraph/forums/thread_template.ex
    - apps/backend/lib/cgraph/forums/forum_analytics.ex
    - apps/backend/lib/cgraph/forums/search.ex
    - apps/backend/lib/cgraph/forums/thread_poll.ex
    - apps/backend/lib/cgraph/forums/scheduled_post.ex
    - apps/backend/lib/cgraph/forums/scheduled_post_worker.ex
    - apps/backend/lib/cgraph/forums/forum_analytics_worker.ex
  key_links:
    - 'Notifications: apps/backend/lib/cgraph/notifications/ — for @mention notifications'
    - 'Forums facade: apps/backend/lib/cgraph/forums/core.ex — delegates to Core.{Authorization,
      Listing, Membership, Stats}. There is NO forums.ex facade.'
    - 'PubSub: CGraphWeb.PubSub'
    - '⚠️ search.ex ALREADY EXISTS as CGraph.Forums.Search (~280 lines) with tsvector, ts_rank,
      search/2. Has filters: forum_id, board_id, author_id, date_from, date_to, sort (relevance/
      new/top), cursor pagination. ONLY extend with tag-based filtering — do NOT reimplement
      existing filters.'
    - '⚠️ thread_poll.ex is ALREADY COMPLETE (61 lines). Has: question, options ({:array, :map}
      JSONB), is_multiple_choice, max_options, is_public, closes_at, total_votes, belongs_to thread,
      has_many :votes (PollVote), full changeset with validation. Do NOT recreate these fields.'
    - '⚠️ polls.ex context ALREADY EXISTS (CGraph.Forums.Polls) — wraps poll operations:
      create_thread_poll/2, get_thread_poll/1. Extend this context, do not create a new one.'
    - '⚠️ poll_vote.ex ALREADY EXISTS (CGraph.Forums.PollVote) — handles vote deduplication.'
    - 'Forum workers pattern: digest_worker.ex is in forums/ dir, not workers/ — place new workers
      in forums/ dir too'
    - 'Controller namespace: ALL controllers are CGraphWeb.API.V1.* under
      apps/backend/lib/cgraph_web/controllers/api/v1/'
    - 'Existing poll controller routes in ForumRoutes: GET/POST/PUT polls, POST vote, POST close'
---

<objective>
Build advanced forum features: @mention parsing, thread templates, forum analytics, advanced
full-text search, thread polls, and scheduled posts with Oban workers.

ATOMIC_PLAN tasks: 4.8 (@Mentions), 4.9 (Thread Templates), 4.10 (Forum Analytics), 4.11 (Advanced
Search), 4.12 (Thread Polls), 4.13 (Scheduled Posts), 4.14 (Analytics Worker), 4.15 (Scheduled Post
Worker). </objective>

<tasks>

<task type="auto">
  <name>Task 1: @Mentions + thread templates (4.8, 4.9)</name>
  <files>
    apps/backend/lib/cgraph/forums/at_mention.ex
    apps/backend/lib/cgraph/forums/thread_template.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>AtMention: parse/1 extracts @username via regex, resolve_mentions/1 returns user_ids, notify_mentioned/2 creates notification for each. ThreadTemplate: forum_id, name, structure (JSONB with title/sections/required), migration.</action>
</task>

<task type="auto">
  <name>Task 2: Analytics + advanced search (4.10, 4.11)</name>
  <files>
    apps/backend/lib/cgraph/forums/forum_analytics.ex
    apps/backend/lib/cgraph/forums/advanced_search.ex
    apps/backend/lib/cgraph/workers/forum_analytics_worker.ex
  </files>
  <action>ForumAnalytics: top_threads (by views/replies/upvotes), engagement_metrics (DAU, posts/day, avg replies), growth_stats (new users, new threads trend). Search (CGraph.Forums.Search, NOT AdvancedSearch): EXTEND existing search.ex with tag-based filter only — forum_id, board_id, author_id, date_from, date_to, sort already exist and work. Do NOT recreate tsvector setup. AnalyticsWorker: hourly Oban, aggregate to analytics table. Place in forums/ dir.</action>
</task>

<task type="auto">
  <name>Task 3: Polls + scheduled posts (4.12, 4.13, 4.14, 4.15)</name>
  <files>
    apps/backend/lib/cgraph/forums/thread_poll.ex
    apps/backend/lib/cgraph/forums/scheduled_post.ex
    apps/backend/lib/cgraph/forums/scheduled_post_worker.ex
    apps/backend/priv/repo/migrations/
  </files>
  <action>ThreadPoll: ALREADY COMPLETE — has question, options (JSONB {:array, :map}), is_multiple_choice, max_options, is_public, closes_at, total_votes, PollVote for vote dedup. Only add anonymous voting support if is_public is insufficient. Extend existing Polls context (polls.ex) with any new functions. ScheduledPost: author_id (:binary_id), forum_id (:binary_id), content (text), scheduled_for (utc_datetime_usec), status (pending|published|cancelled). Worker: per-minute cron, query pending + due, publish atomically. Place worker in forums/ dir.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

</tasks>
