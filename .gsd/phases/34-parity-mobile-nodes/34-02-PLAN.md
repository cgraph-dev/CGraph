---
phase: 34-parity-mobile-nodes
plan: 02
type: execute
wave: 1
depends_on:
  - 33-03
files_modified:
  - apps/web/src/modules/chat/components/message-bubble/message-action-menu.tsx (update — add tip
    action)
  - apps/web/src/modules/chat/components/message-actions-bar.tsx (update — add tip icon)
  - apps/web/src/modules/nodes/components/tip-modal.tsx (update — accept context prop)
  - apps/web/src/modules/social/components/profile-card/profile-card.tsx (update — add tip button)
  - apps/web/src/modules/nodes/components/content-unlock-overlay.tsx (new)
  - apps/web/src/modules/forums/components/thread-view/thread-view.tsx (update — wire unlock)
autonomous: true
must_haves:
  truths:
    - 'Tip button visible on hover of received DM messages (not own messages)'
    - 'Tip button on user profile cards (not own profile)'
    - 'TipModal opens with correct recipient, add new context prop (dm | profile | forum)'
    - 'Minimum tip validation: disable Send when amount < 10, show error'
    - 'Content unlock overlay: blurred content + centered unlock button'
    - 'Unlock flow: confirm → POST /api/v1/nodes/unlock → reveal content'
    - 'Error state: "Insufficient Nodes" with link to shop'
    - 'Feature-flagged: nodes.dm_tipping'
    - '⚠️ Post type has isContentGated + gatePriceNodes but NO userHasAccess field — derive from
      unlock state or add field'
  artifacts:
    - apps/web/src/modules/nodes/components/content-unlock-overlay.tsx
  key_links:
    - 'Message actions: apps/web/src/modules/chat/components/message-bubble/message-action-menu.tsx
      (NOT message-actions.tsx — that file does not exist. Also see message-actions-bar.tsx)'
    - 'Profile card: apps/web/src/modules/social/components/profile-card/profile-card.tsx (⚠️ has 7
      layout variants: minimal, compact, detailed, gaming, social, creator, custom — verify tip
      button placement in each)'
    - 'Thread view: apps/web/src/modules/forums/components/thread-view/thread-view.tsx (⚠️ Already
      has inline unlock logic with useUnlockContent() at L148–184 — extract into
      ContentUnlockOverlay)'
    - 'Existing tip-modal: apps/web/src/modules/nodes/components/tip-modal.tsx (⚠️ Currently has NO
      context/source prop — must ADD context: dm|profile|forum prop to TipModalProps)'
    - 'TipButton wrapper: apps/web/src/modules/nodes/components/tip-button.tsx (props: recipientId,
      recipientName, className)'
    - 'Message types: apps/web/src/modules/chat/components/message-bubble/types.ts — extend with
      onTip callback'
    - 'Post type: apps/web/src/modules/forums/store/forumStore.types.ts — has isContentGated (L97) +
      gatePriceNodes (L98) but NO userHasAccess field'
---

<objective>
Add web tip button (DM messages + profile cards) and content unlock overlay for gated forum posts.
Wire the thread-view unlock onClick from Phase 31/32 to actual payment flow.

ATOMIC_PLAN tasks: 1.4 (TipButton DM), 1.5 (TipButton Profile), 1.6 (Tip Minimum Validation), 1.7
(Content Unlock Overlay), 1.8 (Integrate Unlock in Thread View). </objective>

<tasks>

<task type="auto">
  <name>Task 1: TipButton in DM message actions (1.4)</name>
  <files>
    apps/web/src/modules/chat/components/message-bubble/message-action-menu.tsx
    apps/web/src/modules/chat/components/message-actions-bar.tsx
  </files>
  <action>Add "Tip" icon button to message action bar. On click: open TipModal with context 'dm' and recipientId. Show on received messages only. Feature-flagged.</action>
</task>

<task type="auto">
  <name>Task 2: TipButton on profile card (1.5)</name>
  <files>apps/web/src/modules/social/components/profile-card/profile-card.tsx</files>
  <action>Add "Tip" button next to "Add Friend" / "Message". Hidden on own profile. Opens TipModal with context 'profile'.</action>
</task>

<task type="auto">
  <name>Task 3: Tip minimum validation in modal (1.6)</name>
  <files>apps/web/src/modules/nodes/components/tip-modal.tsx</files>
  <action>Disable "Send Tip" when amount < 10. Show "Minimum tip is 10 Nodes". Import MIN_TIP from @cgraph/shared-types/nodes (⚠️ added by 34-01 Task 4 — must execute first). Also ADD a `context: 'dm' | 'profile' | 'forum'` prop to TipModalProps for analytics/display differentiation.</action>
</task>

<task type="auto">
  <name>Task 4: Content unlock overlay + thread integration (1.7, 1.8)</name>
  <files>
    apps/web/src/modules/nodes/components/content-unlock-overlay.tsx
    apps/web/src/modules/forums/components/thread-view/thread-view.tsx
  </files>
  <action>Create ContentUnlockOverlay: blurred preview + "🔒 Unlock for X Nodes" button. Extract existing unlock logic from thread-view.tsx (lines 148–184 GlassCard block with useUnlockContent() + insufficient_balance handling + shop navigation). Integrate in thread-view: replace inline unlock block with <ContentUnlockOverlay> component. ⚠️ Post type has isContentGated but NO userHasAccess — derive unlock state from local store/query (e.g., check if user has matching unlock record).</action>
  <verify>cd apps/web && npx tsc --noEmit 2>&1 | tail -5</verify>
</task>

</tasks>
