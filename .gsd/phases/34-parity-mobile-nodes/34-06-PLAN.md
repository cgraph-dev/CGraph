---
phase: 34-parity-mobile-nodes
plan: 06
type: execute
wave: 2
depends_on:
  - 34-01
  - 34-02
  - 34-03
  - 34-04
  - 34-05
files_modified:
  - apps/mobile/src/screens/settings/privacy-screen.tsx (update — expand toggles)
  - apps/mobile/src/stores/chatEffectsStore.ts (new)
  - apps/mobile/src/navigation/ (update — register all new Phase 34 screens)
  - apps/web/src/modules/social/components/ (update — friend favorites + nicknames)
  - apps/web/src/modules/social/components/profile-card/ (update — mutual friends/groups)
  - packages/shared-types/src/nodes.ts (update — Nodes types)
  - packages/api-client/src/nodes.ts (new — Nodes API methods)
autonomous: true
must_haves:
  truths:
    - 'Mobile privacy settings expanded from 6 to 15 toggles matching web'
    - 'chatEffectsStore created for mobile chat effects'
    - 'All new Phase 34 screens registered in navigation + ParamList types updated'
    - 'Web friend favorites UI + nicknames editor (types already exist on Friend interface)'
    - '⚠️ Web profile card social-layout.tsx ALREADY shows mutual friends + forumsInCommon — extend
      to other layouts (compact, gaming, creator) or skip if already sufficient'
    - 'shared-types nodes.ts has TipContext type added (⚠️ NodeWallet, NodeTransaction, NodeBundle
      ALREADY EXIST — do NOT re-declare, only add TipContext)'
    - 'api-client nodes.ts has typed methods: tipUser, unlockContent, getWallet, getBundles,
      withdraw (⚠️ NODES endpoint constants already in endpoints.ts — methods should reference
      those)'
  artifacts:
    - apps/mobile/src/stores/chatEffectsStore.ts
    - packages/api-client/src/nodes.ts
  key_links:
    - 'Privacy screen: apps/mobile/src/screens/settings/privacy-screen.tsx — currently 6 boolean
      toggles (BooleanSettingKey at L26): showOnlineStatus, showTypingIndicators, showReadReceipts,
      allowMessageRequests, allowFriendRequests, showInSearch'
    - 'Navigation: apps/mobile/src/navigation/ — root-navigator.tsx, main-navigator.tsx (7 tabs),
      settings-navigator.tsx (~25 screens), messages-navigator.tsx (5 screens) ⚠️ Must update
      ParamList types when registering new screens'
    - 'Friend list: apps/web/src/modules/social/ — Friend interface already has nickname + favorited
      fields but NO UI components for favorites/nicknames yet'
    - 'Profile card: apps/web/src/modules/social/components/profile-card/ — social-layout.tsx
      ALREADY shows mutualFriends + forumsInCommon'
    - '@cgraph/socket: consider NodesChannel for real-time balance (evaluate during implementation)'
    - '⚠️ api-client has NO nodes.ts file yet — only endpoint constants in endpoints.ts (NODES at
      L196)'
    - '⚠️ shared-types/nodes.ts ALREADY has NodeWallet, NodeTransaction, NodeBundle types — only add
      TipContext, do NOT duplicate existing types'
---

<objective>
Complete Phase 34: expand privacy settings, create chat effects store, register all new screens in
navigation, add web social parity features (friend favorites, nicknames, mutual friends/groups),
and create shared Nodes types + API client.

ATOMIC_PLAN tasks: 1.25 (Privacy Expansion), 1.26 (Chat Effects Store), 1.27 (Navigator Updates),
1.28 (Friend Favorites + Nicknames), 1.29 (Mutual Friends/Groups), 1.30 (Shared Nodes Types + API).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Privacy settings expansion (1.25)</name>
  <files>apps/mobile/src/screens/settings/privacy-screen.tsx</files>
  <action>Add 9 missing toggles: online status visibility, read receipts, typing indicators, profile visibility, friend request filtering, message request filtering, block list management, data sharing, activity status.</action>
</task>

<task type="auto">
  <name>Task 2: Chat effects store + navigation (1.26, 1.27)</name>
  <files>
    apps/mobile/src/stores/chatEffectsStore.ts
    apps/mobile/src/navigation/
  </files>
  <action>Create chatEffectsStore (create() + manual AsyncStorage). Register all Phase 34 screens: NodesWallet, NodesShop, Withdrawal, SecretChat, SecretChatSettings, ThemeBrowser, 5 customize screens, Discovery updates. ⚠️ Must also update TypeScript ParamList types (SettingsStackParamList, MessagesStackParamList) — not just the navigator JSX.</action>
</task>

<task type="auto">
  <name>Task 3: Web social parity (1.28, 1.29)</name>
  <files>
    apps/web/src/modules/social/components/
    apps/web/src/modules/social/components/profile-card/
  </files>
  <action>Add friend favorites (star/priority, reorder) and nicknames (custom display name per friend) — types already exist on Friend interface (nickname, favorited fields). Create UI: FavoriteButton, NicknameEditor components. ⚠️ Mutual friends/groups on profile card ALREADY done in social-layout.tsx — extend to compact/gaming/creator layouts only if needed.</action>
</task>

<task type="auto">
  <name>Task 4: Shared Nodes types + API client (1.30)</name>
  <files>
    packages/shared-types/src/nodes.ts
    packages/api-client/src/nodes.ts
  </files>
  <action>Add TipContext type to shared-types/nodes.ts (⚠️ NodeWallet, NodeTransaction, NodeBundle ALREADY EXIST — do NOT re-declare). Create typed API methods file in api-client/src/nodes.ts using existing NODES endpoint constants from endpoints.ts: tipUser, unlockContent, getWallet, getBundles, withdraw. Re-export from barrels.</action>
</task>

</tasks>
