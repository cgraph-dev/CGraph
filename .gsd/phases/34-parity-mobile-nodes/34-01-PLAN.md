---
phase: 34-parity-mobile-nodes
plan: 01
type: execute
wave: 1
depends_on:
  - 33-03
files_modified:
  - apps/backend/lib/cgraph/nodes/nodes.ex (update — add @min_tip, tip validation)
  - apps/backend/lib/cgraph_web/controllers/nodes_controller.ex (update — add controller-level rate
    limit plug)
  - apps/backend/lib/cgraph/workers/held_nodes_release_worker.ex (new)
  - apps/backend/config/config.exs (update — Oban cron for HeldNodesReleaseWorker)
  - packages/shared-types/src/nodes.ts (update — add MIN_TIP constant)
autonomous: true
must_haves:
  truths:
    - '@min_tip 10 constant in nodes.ex with validation in tip/3'
    - 'tip(sender, receiver, 5) returns {:error, :tip_below_minimum}'
    - 'Tip rate limiting via existing RateLimiterV2 with :strict tier on NodesController :tip action'
    - 'Controller-level plug: plug RateLimiterV2, tier: :strict when action in [:tip]'
    - 'MIN_TIP = 10 exported from packages/shared-types/src/nodes.ts'
    - 'HeldNodesReleaseWorker runs daily at 03:00 UTC, :payments queue'
    - 'Held transactions (21+ days) released and wallet balance updated atomically'
    - 'Worker is idempotent — re-running does not double-credit'
  artifacts:
    - apps/backend/lib/cgraph/workers/held_nodes_release_worker.ex
  key_links:
    - 'Existing nodes.ex: apps/backend/lib/cgraph/nodes/nodes.ex — tip/3 at L175, @exchange_rate_eur
      at L35'
    - 'Oban config: apps/backend/config/config.exs — :payments queue (concurrency 5) at L142'
    - 'Oban crontab: apps/backend/config/config.exs — lines 91–117, add HeldNodesReleaseWorker entry'
    - 'Existing release logic: CGraph.Nodes.release_held_nodes/0 at nodes.ex L378 — worker wraps
      this'
    - 'Existing rate limiter: CGraphWeb.Plugs.RateLimiterV2 — :standard (100/60s) :strict (20/60s)
      :relaxed tiers'
    - 'Tip route: apps/backend/lib/cgraph_web/router/nodes_routes.ex — post "/tip", NodesController,
      :tip'
    - 'NodesController: apps/backend/lib/cgraph_web/controllers/nodes_controller.ex'
    - '⚠️ Do NOT create a new tip_rate_limiter.ex — reuse RateLimiterV2 at controller level'
---

<objective>
Backend hardening for Nodes: add tip minimum validation, tip rate limiter plug, and held nodes
release worker (Oban cron). These backend improvements support the mobile Nodes and web tip
features built in subsequent plans.

ATOMIC_PLAN tasks: 1.1 (Tip Minimum), 1.2 (Tip Rate Limiter), 1.3 (Held Nodes Release Worker).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Tip minimum constant + validation (1.1)</name>
  <files>apps/backend/lib/cgraph/nodes/nodes.ex</files>
  <action>Add @min_tip 10, validate amount >= @min_tip in tip/3, return {:error, :tip_below_minimum}. Add @doc and @spec.</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 2: Tip rate limiting via RateLimiterV2 (1.2)</name>
  <files>apps/backend/lib/cgraph_web/controllers/nodes_controller.ex</files>
  <action>Add controller-level plug: `plug CGraphWeb.Plugs.RateLimiterV2, tier: :strict when action in [:tip]`. The :strict tier already provides 20 req/60s with 429 + Retry-After. Do NOT create a new rate limiter plug — RateLimiterV2 already handles ETS counters, 429 responses, and Retry-After headers.</action>
</task>

<task type="auto">
  <name>Task 3: HeldNodesReleaseWorker (1.3)</name>
  <files>apps/backend/lib/cgraph/workers/held_nodes_release_worker.ex</files>
  <action>Oban.Worker, queue: :payments, max_attempts: 5. perform/1 calls CGraph.Nodes.release_held_nodes/0 (already implemented at nodes.ex L378 — handles Multi, row-level locks, batching). Add cron entry to config.exs crontab: {"0 3 * * *", CGraph.Workers.HeldNodesReleaseWorker}. Telemetry on [:cgraph, :nodes, :hold_released]. Follow existing worker pattern (see PulseDecayWorker).</action>
  <verify>cd apps/backend && mix compile 2>&1 | tail -5</verify>
</task>

<task type="auto">
  <name>Task 4: Add MIN_TIP to shared-types (1.1b)</name>
  <files>packages/shared-types/src/nodes.ts</files>
  <action>Add `export const MIN_TIP = 10 as const;` alongside existing MIN_WITHDRAWAL. Plan 34-02 imports this — must exist before web tip validation can compile.</action>
  <verify>cd packages/shared-types && npx tsc --noEmit 2>&1 | tail -5</verify>
</task>

</tasks>
