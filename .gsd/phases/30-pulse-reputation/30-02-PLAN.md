---
phase: 30-pulse-reputation
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - apps/web/src/modules/pulse/ (new directory)
  - apps/web/src/modules/pulse/components/PulseDots.tsx (new)
  - apps/web/src/modules/social/components/profile-card/ (pulse section)
autonomous: true
must_haves:
  truths:
    - 'PulseDots component built (●●●●○ display with tier label)'
    - 'Top-3 Pulse communities shown on profile card'
    - 'Pulse wired to achievement triggers'
    - 'Resonate/Fade/Not-for-me reaction buttons on forum threads'
  artifacts:
    - apps/web/src/modules/pulse/components/PulseDots.tsx (new)
    - apps/web/src/modules/pulse/ (new module)
  key_links:
    - 'Depends on Plan 30-01 (backend must exist)'
    - 'Reaction UI reused in Phase 31 (Forums)'
---

<objective>
Build the Pulse frontend: PulseDots display component, profile card integration,
and Resonate/Fade/Not-for-me reaction UI. Pulse is displayed qualitatively (dots)
not quantitatively (numbers) to prevent karma farming.
</objective>

<context>
@docs/PrivateFolder/This will be the definitive.txt (Part 5 — Pulse on Profile Card)
@apps/web/src/modules/social/components/profile-card/profile-card.tsx
@apps/web/src/modules/social/components/profile-card/types.ts
(Profile card has 7 layout variants: compact, minimal, social, detailed, gaming, creator + main.
 Pulse section goes into the main profile-card.tsx and relevant layouts.)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PulseDots component</name>
  <files>apps/web/src/modules/pulse/components/</files>
  <action>
    Create PulseDots component at `modules/pulse/components/PulseDots.tsx`
    (follows project convention — features live in `modules/`, not `components/`).
    Display pulse as filled dots:
    - ●●●●● = 5 dots, filled based on tier
    - Tier label displayed alongside (Newcomer, Active, Trusted, Expert, Authority, Legend)
    - Hover/tap shows exact score as tooltip
    - Color based on tier (gradient from grey→gold)
  </action>
  <verify>ls apps/web/src/modules/pulse/components/PulseDots.tsx</verify>
  <done>PulseDots component created</done>
</task>

<task type="auto">
  <name>Task 2: Add Pulse to profile card</name>
  <files>apps/web/src/modules/social/components/profile-card/</files>
  <action>
    Add "Top Communities" section to the profile card.
    The profile card lives at `modules/social/components/profile-card/` with
    7 layout variants (compact, minimal, social, detailed, gaming, creator + main).
    Add pulse info to the main `profile-card.tsx` and the `social-layout.tsx`
    and `detailed-layout.tsx` variants:
    - Show top-3 communities by pulse score
    - Display community icon + name + PulseDots + tier label
    - "See all →" link to full pulse view
    - Only show if user has pulse > 0 in any community
  </action>
  <verify>grep -rn "PulseDots\|TopCommunit" apps/web/src/modules/social/components/profile-card/ | head -5</verify>
  <done>Profile card shows pulse communities</done>
</task>

<task type="auto">
  <name>Task 3: Build reaction UI for Resonate/Fade/Not-for-me</name>
  <files>apps/web/src/modules/pulse/</files>
  <action>
    Build reaction component for use on forum posts/threads:
    - Resonate (upvote equivalent, always available)
    - Fade (downvote equivalent, gated at pulse >= 50 in community)
    - Not-for-me (algorithmic signal only, no pulse change)
    - Wire to POST /api/v1/pulse/vote endpoint
      (named "vote" not "react" — existing ReactionController handles emoji reactions)
    - Show disabled state + tooltip for Fade when pulse < 50
  </action>
  <verify>ls apps/web/src/modules/pulse/ 2>&1</verify>
  <done>Reaction UI built and wired to API</done>
</task>

<task type="auto">
  <name>Task 4: Wire Pulse achievement triggers</name>
  <files>apps/backend/lib/cgraph/gamification/achievement_triggers.ex</files>
  <action>
    Add pulse tier achievements to the existing `@action_achievement_map` in
    `CGraph.Gamification.AchievementTriggers` (same pattern as message_sent,
    forum_thread, etc.).

    Add new action type `pulse_tier_reached` with achievements:
    - "trusted_voice" — reach Trusted tier in any community
    - "expert_contributor" — reach Expert tier
    - "authority_figure" — reach Authority tier

    Call `AchievementTriggers.check_all(user_id, :pulse_tier_reached)` from
    PulseSystem context when a tier change is detected after vote processing.
    Wire through the existing `AchievementSystem` — do NOT create a separate system.

  </action>
  <verify>grep -rn "pulse.*achievement\|achievement.*pulse" apps/ --include="*.ts" --include="*.tsx" --include="*.ex" | head -5</verify>
  <done>Pulse achievement triggers wired</done>
</task>

</tasks>

<verification>
- [ ] PulseDots renders correctly
- [ ] Profile card shows top-3 communities
- [ ] Reaction UI works (resonate/fade/not-for-me)
- [ ] Fade properly gated at pulse >= 50
- [ ] Achievement triggers fire
</verification>

<success_criteria>

- Complete Pulse frontend functional
- Integrated with profile and achievements </success_criteria>

<output>
After completion, create `.gsd/phases/30-pulse-reputation/30-02-SUMMARY.md`
</output>
